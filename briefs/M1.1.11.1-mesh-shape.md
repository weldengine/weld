# M1.1.11.1 — Forge 3D shapes: the static triangle mesh

> **Status:** CLOSED
> **Phase:** 1
> **Branch:** `phase-1/forge/mesh-shape`
> **Planned tag:** `v0.11.11-mesh-shape`
> **Base:** `main` at `03157b7` (one commit past `v0.11.11-plane-halfspace`, the `chore/spec-reference-reconciliation` merge)
> **Dependencies:** M1.1.0 (shape store, `BodyManager`), M1.1.1 (broadphase BVH, `Aabb.rayInterval`, `Aabb.inflate`, pair generation), M1.1.2 (support maps, GJK cores), M1.1.3 (EPA, manifold, `feature_id` producer classes), M1.1.6 (contact cache keyed `(pair, subshape_id, feature_id)`), M1.1.7 (NGS), M1.1.9 (query family frozen, ray traversal), M1.1.10 (the eight entries, the ordering key, `queryCast`), M1.1.11 (`ShapeClass`, `error.ShapeMustBeStatic`, §1.11.15–§1.11.16)
> **Opened:** 2026-08-02
> **Closed:** 2026-08-02 (code-complete, PR open; merge + tag are Guy's)

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

Thirteenth core sub-milestone of the M1.1 rigid arc, and the **twelfth and last shape** of the C1.1 list. The M1.1.11 plan row originally grouped the infinite plane with the static mesh; it was split, and this is the second half. The plane half shipped a taxonomy enum and an unbounded-shape path; none of that carries over except the enum and one reused error.

The normative model was authored for this milestone and is `engine-physics-forge.md` **§1.11.17**, appended to the §1.11 series so that nothing renumbers. Read it before anything else. `engine-tier-interfaces.md` is at **version 0.5** and carries the descriptor payload and the back-face field.

**This milestone is not split, and the size rule was checked rather than invoked.** `engine-development-workflow.md` §2.2 targets 500–2000 delivered lines including tests. Measured on the four preceding sub-milestones: M1.1.8 delivered 3237 Zig lines, M1.1.9 3968, M1.1.10 5095, M1.1.11 4607. All four exceed the target by 1.6× to 2.5×, all four were reviewed gate by gate and merged. This milestone is estimated at 3400–3900, i.e. **below M1.1.10 and comparable to M1.1.9 and M1.1.11**. It is a normal-sized sub-milestone for this arc, not an outlier, and it ships whole. There is no M1.1.11.2 and none is to be proposed.

Four things are settled here that are larger than one shape.

First, **a mesh is a surface, not a solid, and that is categorical.** Membership is false everywhere; `pointQuery` never returns a body carrying a mesh; `closestPoint` measures to the surface and is never zero by interiority. There is no mesh counterpart to `treat_convex_as_solid`. The reference has one — `Shape::sCollidePointUsingRayCast`, hit-count parity along a `+Y` ray — and Weld refuses it: parity presumes a **closed** mesh, nothing validates closure, on an open mesh the answer then depends on an arbitrary ray direction, and a membership test would cost a full raycast.

Second, **the triangle enters as a fourth `Core` variant, not as a family of kernels.** A triangle is a *bounded* convex whose support map is the max of three dot products — the only property GJK, EPA, the manifold generator and the M1.1.10 cast kernel require. Those four then serve the mesh **unchanged**. Only the ray kernel gains an analytic arm, for the reason §1.11.3 already gives ray kernels their existence: a configuration-space ray march against a *flat* core is ill-conditioned where ray↔triangle has a closed form. This is one variant against four reused families, and it is the reason this milestone is 3900 lines and not 8000.

Third, **the compile-error safety net advertised at M1.1.11 has four holes, and this milestone is where they bite.** `shape.zig` states that every `switch` on `ShapeClass` is exhaustive with no `else` arm, so that adding the third variant is a compile error at each site owing a decision. Verified on `main`: eight sites are real exhaustive switches and will break correctly; **four are not** — `addBody` (`if (class == .half_space and body_type != .static)`), `bodyAabb` (`assert(class == .convex)`), `closestPointBody` (`if (class == .half_space)` then falling through to `supportShape`), `worldAabb` (`assert(class == .convex)`). The fall-through in `closestPointBody` is the dangerous one: it panics in Debug and ReleaseSafe and is undefined behaviour in ReleaseFast. These four are fixed in this milestone, and the docstring is corrected to match what the code does. By contrast the six `switch` sites on `Core` **are** all exhaustive — verified — so the triangle variant is greeted correctly everywhere.

Fourth, **the back-face field is the last thing that can still land on the frozen surface.** §1.11.4's row was amended to say so: the bound is this shape, not the end of Phase 1, the mesh being itself Phase 1. After the M1.1.15 freeze the field cannot land at all. It ships here or never.

## Scope

- **`src/modules/forge/api/types.zig`** — the `ShapeDescriptor.triangle_mesh` payload `{ vertices: []const Vec3, indices: []const u32 }`, borrowed for the duration of the call; and `BackFaceMode { ignore, collide }` with `back_face_mode: BackFaceMode = .ignore` on `RaycastQuery`, `ShapeCastQuery` and `OverlapQuery`. Both are pre-freeze extensions, permitted by `engine-tier-interfaces.md` §1. The three entries that do **not** carry the field carry it for a stated reason (§1.11.17); do not add it to them for symmetry.
- **`ShapeClass` gains `.triangle_soup`** — the category name §1.11.3 and §1.11.15 already use, not `.mesh`. HeightField joins the same category at its own sub-milestone. Every switch stays exhaustive with no `else` arm.
- **The four non-exhaustive class sites become dispatches.** `addBody`, `bodyAabb`, `closestPointBody`, `worldAabb`, plus the two class asserts in `body.zig`. A mesh **has** a valid `local_aabb` — unlike a half-space — so these are not "extend the assert", they are "the mesh has an answer and it differs".
- **Owned memory in the shape store.** A mesh owns its vertices, its indices, its acceleration nodes and its edge flags. `createShape` becomes **transactional**: reserve every allocation before mutating any slot, so an OOM never leaves a half-built mesh in a live slot. `destroyShape` frees; `ShapeStore.deinit` walks live slots. The public signature `destroyShape: fn (*Impl, ShapeId) void` is **unchanged** — the interface tier holds the allocator and supplies it.
- **Descriptor domain, refused by typed error and never sanitised.** Rejected at creation: an index count not a multiple of three, an index outside the vertex array, a non-finite vertex, a mesh with no triangle, and a triangle whose cross product `(v₁−v₀) × (v₂−v₀)` is **exactly** zero — the true-zero guard of §1.11.4 on its largest absolute component, applied verbatim. A sliver of tiny but non-zero area normalises exactly and **must be served**; no area threshold appears anywhere. The reference sanitises (`MeshShapeSettings::Sanitize`); Weld refuses, because removal **renumbers** and that number **is** the `subshape_id`.
- **`forge_3d/mesh.zig`** (create) — `MeshData`: owned arrays, the static binned-SAH builder into a flat node array, the adjacency/active-edge build, and the explicit-stack traversals (ray, swept, AABB, bounded-distance). **Traversal is by explicit fixed-depth stack, never recursion**: the `Bvh` recursion is safe only because its rotation balancing bounds its height, and a static build offers no such guarantee. The depth is a build-time invariant the builder knows and asserts, and the stack is sized on it.
- **Adjacency is built at creation, inside the same transaction.** Pairing is by **sorting** `(min_index, max_index)` keys and pairing adjacent entries — **no hashed container**, the `computePairs` pattern verbatim and the same reason (§1.5). Three flags per triangle, bit 0 for edge `v₀v₁`, bit 1 for `v₁v₂`, bit 2 for `v₂v₀`. An unpaired edge — the boundary of an open mesh — is **active**. Building it in a later gate would reopen the OOM transaction a second time; it belongs to storage.
- **`pipeline/narrowphase/triangle.zig`** (create) — the analytic ray↔triangle kernel and the back-face predicate. Dependency discipline identical to `plane.zig`: imports `foundation` and the sibling `support.zig` only — never `gjk.zig`, `epa.zig`, `manifold.zig`, `weld_forge`, `body*.zig`, `config.zig` or `broadphase.zig`. A triangle core with a non-zero inflation radius is an **asserted precondition** of the ray arm, the same form as the rounded box.
- **`SupportShape.Core` gains `triangle`** in `support.zig`, and the six exhaustive switches on it each gain their arm.
- **Back-face semantics, written once and shared by three entries.** Front face is the side the outward normal points to. For a ray and for a sweep, the hit is a back-face hit when `n · direction > 0`. For an overlap, the triangle is back-facing when the probe lies **entirely** in the rear half-space of its plane: `n · support_probe(n) − r_probe < n · v₀`, one support call, §1.11.15's construction applied to the triangle's plane. A probe straddling the plane touches from the front and counts in both modes.
- **A back-face hit returns a flipped normal.** §1.11.4 declares `normal · direction <= 0` on **all** hits, and the `−direction` choice at distance zero draws its justification from it. Returning the unflipped outward normal would puncture it. The returned normal is `−n`, for the ray family and for the shape cast. Assumed divergence from the reference, which returns it unflipped.
- **`subshape_id` is threaded end to end.** The mesh is the first shape carrying sub-shapes; being root, its path **is** the triangle index. Today the field is written nowhere: `LocalHit` carries only `distance` and `normal`, and `evaluate` in `query/ray.zig` never sets it. `LocalHit`, `BodyCastHit` and `BodyClosestPoint` gain it, and the adapters and collectors carry it to `RaycastHit`, `ShapeCastHit` and `ClosestPointResult`.
- **A mesh returns one hit per body, not one per triangle.** The nearest accepted triangle wins, deduplicated at the collector. The reason is not performance: §1.11.14's key `(distance, entity, BodyId)` does not discriminate two triangles of one body, so two hits would be neither ordered nor invariant and their truncation arbitrary.
- **Contact generation, `collidePair` from 2×2 to 3×3.** Mesh × convex produces **several manifolds per body pair**, one per contacting triangle — the only shape change the mesh imposes on the rigid solver. The contact cache key has carried an unused `subshape_id` at 0 since M1.1.6; this milestone fills it, and warm-starting becomes per triangle and survives a re-traversal. The constraint build loop consumes the collection.
- **Active edges are consumed at contact time.** A contact whose normal comes from an inactive edge has its normal corrected to the face normal. The threshold is a **named, configurable physical parameter** with default `cos(5°)`, the class of `restitution_threshold` and `penetration_slop` — it selects a modelling behaviour, not a numerical tolerance, and §1.11.2's `k · floatEps(T) · coordScale` discipline does **not** apply to it. Say so in the code, at the declaration.
- **Mesh × mesh and mesh × half-space are asserted preconditions of `collidePair`.** Not a structural property: no `BodyType → BroadphaseLayer` mapping exists on `main` — the layer is an insertion argument, `gjk_test.zig` inserts static bodies into `.dynamic`, and the wiring arrives with `PhysicsWorld` at M1.1.15. The motive is that the adapter presumes its pair came from `computePairs` under a correct layer assignment, and a static↔static pair is a programming error at the call site. Write **that** motive, not the other one.
- **A mesh is an inadmissible probe.** `probeAdmissible` returns false for `.triangle_soup`, `error.UnsupportedShape` on `shapeCast` and `overlapShape`. §1.11.7's fourth principle already states it.
- **Normals are not precomputed.** Precomputing does not improve the conditioning — the same cancellation happens at build time — it only costs memory on the heaviest shape in the store. Determinism is satisfied by the **fixed evaluation order** `(v₁−v₀) × (v₂−v₀)`.
- **`shape.zig`'s docstring on `ShapeClass` is corrected** to state what the code does after this milestone, not what M1.1.11 hoped it did.

## Out of scope

- **HeightField** — joins `.triangle_soup` at its own sub-milestone (M1.1.20), with an *implicit* acceleration structure instead of a built tree. Add no arm for it.
- **Per-triangle material and per-triangle user data.** §1.11.7 has the material derive from `subshape_id` through an accessor once per-shape materials exist; this milestone gives that accessor its first real argument. The material table is §4 and is not built here.
- **Quantised or compressed triangle storage.** Purely additive memory optimisation, no effect on any answer.
- **A fourth term on §1.11.14's ordering key.** Deferred at M1.1.11 and deferred again, with the reason now written down: no consumer wants two hits on one wall.
- **A bit-packed `SubShapeID` path creator.** §1.11.16 settles the contract; the mechanism waits for compounds (M1.1.20). A mesh is root, so its path is its triangle index and no packing is needed.
- **Runtime deformation or mutation of a mesh.**
- **The ECS authoring surface** — `CollisionShape` / `ShapeParams` gain no mesh variant. The variant needs an asset handle that does not exist before M1.6. Deferred with the plane variant, deliberately, as recorded at M1.1.11.
- **`step()` / `PhysicsWorld` / `PhysicsModule` instantiation / ECS `Transform` sync / the public `f32` wrapper / widening `Real` on the public surface** (M1.1.15, one grouped decision). Nothing in production inserts a body into the broadphase today; this milestone proves its behaviour through the test harness and must say so rather than imply a wiring that does not exist.
- **The 2D symmetry.** Do not touch `forge_2d`. `PhysicsModule2D` freezes at M1.8.11.
- **The 2D `collision_layer` bound** and `CharacterDescriptor`'s — registered open signals in `engine-audit-checklist.md` §5, another domain, another milestone.
- Far-field conditioning beyond what §1.11.4 bis and §1.11.17 mandate: characterised, **not** fixed.
- Character controller (M1.1.12), sensors and triggers (M1.1.13), cross-platform determinism (M1.1.14), joints (M1.1.16–18), Cylinder/TaperedCylinder/ConvexHull (M1.1.19), Compound/MutableCompound/Empty (M1.1.20), vehicle (M1.1.21), the deformable branch (M1.1.22–23).
- The Tier 1 `physics_query` service, its Etch wrappers, editor overlays, `src/c-api/`.

## Specs to read first

1. `engine-physics-forge.md` — **§1.11.17 first**, in full. Then §1.11 in full, with attention to the amended §1.11.4 (back-face row, and the `normal · direction <= 0` invariant on the row above it), §1.11.4 bis, the amended §1.11.3 (three categories, and the clause distinguishing the soup from the triangle), §1.11.7 (four principles), §1.11.10, §1.11.11, §1.11.12, §1.11.14, §1.11.15, §1.11.16. Then §1.1, §1.2, §1.5, §2 (the shape table), §3 in full, §4, §13, §17.
2. `engine-tier-interfaces.md` — §1 at **version 0.5**: the `triangle_mesh` payload, `BackFaceMode` and the three fields, the owned-memory note on the Shapes block, the `RaycastHit` doc, `BodyDescriptor.collision_layer`.
3. `engine-phase-1-criteria.md` — C1.1.
4. `engine-phase-1-plan.md` — the M1.1.11.1 row, and M1.1.12, M1.1.19, M1.1.20.
5. `engine-zig-conventions.md` — naming, unmanaged-first allocation, the `root.zig` convention, the comptime interface-check pattern, the file-length guideline.
6. `engine-physics-forge-2d.md` — §7, for symmetry only. Nothing 2D is touched.

## Files to create or modify

- `src/modules/forge/api/types.zig` — modify — the `triangle_mesh` payload, `BackFaceMode`, the three `back_face_mode` fields.
- `src/modules/forge/forge_3d/shape.zig` — modify — `.triangle_soup`, the mesh record, owned memory, the transactional `createShape`, `destroyShape`, `deinit`, the corrected `ShapeClass` docstring, the `supportShape` precondition.
- `src/modules/forge/forge_3d/mesh.zig` — create — `MeshData`: owned arrays, static SAH builder, adjacency build, explicit-stack traversals. If the import graph forces a different placement, that is a Recorded deviation, not a silent move.
- `src/modules/forge/forge_3d/pipeline/narrowphase/triangle.zig` — create — the analytic ray↔triangle kernel and the back-face predicate.
- `src/modules/forge/forge_3d/pipeline/narrowphase/support.zig` — modify — the `triangle` core variant, and `LocalHit` / `CastHit` gaining `subshape_id`.
- `src/modules/forge/forge_3d/pipeline/narrowphase/gjk.zig`, `.../epa.zig`, `.../manifold.zig`, `.../fast_paths.zig`, `.../raycast.zig`, `.../shapecast.zig` — modify — the arms the new core variant demands at each exhaustive switch.
- `src/modules/forge/forge_3d/pipeline/narrowphase/root.zig` — modify — re-export.
- `src/modules/forge/forge_3d/body.zig` — modify — the two class asserts.
- `src/modules/forge/forge_3d/body_manager.zig` — modify — the mesh arms of the six adapters, the four non-exhaustive class sites converted, `addBody`'s static rejection, `collidePair` 3×3 and its multi-manifold return.
- `src/modules/forge/forge_3d/query/root.zig` — modify — `probeAdmissible`, the mesh arms, the back-face field threaded to the kernels.
- `src/modules/forge/forge_3d/query/ray.zig`, `.../cast.zig`, `.../overlap.zig` — modify — `subshape_id` propagation, per-body deduplication, the back-face field.
- `src/modules/forge/forge_3d/rigid/contact_constraint.zig` — modify — several constraints per body pair, the cache key's `subshape_id` filled.
- `src/modules/forge/forge_3d/root.zig` — modify — re-exports and the comptime pin for the new suite.
- `src/modules/forge/forge_3d/tests/mesh_test.zig` — create — the mesh acceptance suite.
- `src/modules/forge/forge_3d/tests/raycast_test.zig`, `.../shapecast_test.zig`, `.../overlap_test.zig`, `.../body_manager_test.zig`, `.../manifold_test.zig`, `.../solver_test.zig` — modify — the frozen-signature pins extended field by field (**extended, never weakened**), the mesh cases.
- `bench/` — modify — see Benchmarks.
- `CLAUDE.md` — modify — the `Current state` header block and the §3.4 patch, in the closing gate, content supplied by Claude.ai, **inside the PR**.

## Acceptance criteria

### Tests

RED-first on every pinnable behaviour. Every expectation is a **closed form computed in the comment above it**, never a value read back from the implementation.

**`tests/mesh_test.zig`** (create)

*Descriptor domain*
- `test "an index count not a multiple of three is refused"`, `test "an out-of-range index is refused"`, `test "a non-finite vertex is refused"`, `test "an empty mesh is refused"` — typed errors, one per condition, each distinguishable from the others.
- `test "an exactly degenerate triangle is refused and a sliver is served"` — the discriminating pair. The sliver's area is tiny but non-zero, its normal normalises exactly, and the test asserts a **unit** normal on it. A suite that only rejects degenerates would pass with an area threshold in the code; this one will not.

*Owned memory*
- `test "createShape is transactional under OOM"` — a failing allocator at each successive allocation point in turn; after every failure the store holds no live mesh slot and leaks nothing.
- `test "destroyShape frees and deinit walks live slots"` — proven with a leak-checking allocator whose `safety` is **forced true**, and proven in both directions by reintroducing a leak deliberately, once, and reporting that it fired.

*Static acceleration structure*
- `test "the built tree is a partition of the triangle set"` — every triangle in exactly one leaf, leaf boxes contained in their parents, root box equal to the tight bound of all vertices.
- `test "traversal agrees with brute force"` — the load-bearing test of the whole structure. On a pseudo-random mesh of a few hundred triangles, every ray / swept / AABB / bounded-distance traversal returns exactly the brute-force answer over all triangles. Run it over enough seeds that a wrong slab test or a wrong descent order cannot survive.
- `test "the stack depth invariant holds on an adversarial mesh"` — a triangle set chosen to degrade the SAH split; the asserted depth bound holds and the traversal does not overflow.

*Adjacency*
- `test "edge pairing is exact on a closed mesh"` — every edge paired exactly once.
- `test "a boundary edge of an open mesh is active"`.
- `test "a concave edge is inactive and a convex edge past the threshold is active"` — closed forms on a two-triangle fold, at the threshold and one step either side.
- `test "pairing is order-independent"` — the same mesh with permuted triangle order gives bit-identical flags.

*The eight entries*
- `test "raycast hits the nearest triangle and reports its index"` — `subshape_id` equal to the triangle index, verified against the closed form, not read back.
- `test "raycastAll returns one entry per body, not one per triangle"` — a ray crossing several triangles of one mesh yields exactly one entry.
- `test "shapeCast against a mesh"` — closed-form time of impact for sphere, box and capsule probes; a receding direction that misses; a grazing direction whose guard is at true zero.
- `test "overlapShape against a mesh"`, `test "overlapAabb stops at AABB granularity"` — the latter asserting the body **is** returned for a box that overlaps the mesh's AABB but no triangle, so the documented approximation is pinned rather than accidentally tightened.
- `test "pointQuery never returns a mesh body"` — including a point inside the volume a closed mesh encloses. This is the categorical rule, and it is the test that fails if someone adds solidity later.
- `test "closestPoint measures to the surface"` — outside and inside a closed mesh both give the distance to the nearest triangle, never zero by interiority.
- `test "a mesh probe is refused, not silently empty"` — `shapeCast` and `overlapShape`.

*Back faces*
- `test "a ray from behind is ignored by default and hit under collide"` — both verdicts, same geometry.
- `test "a back-face hit returns a flipped normal"` — `normal · direction <= 0` asserted on **every** hit of the suite, both modes, ray and cast.
- `test "a shape cast from behind honours the mode"`.
- `test "an overlap probe entirely behind is discarded, straddling is not"` — the two-sided case is the one that fails if the predicate is written as a sign test on the closest point instead of on the support.

*Contacts and active edges*
- `test "a mesh and a convex produce one manifold per contacting triangle"` — count and identity, closed form.
- `test "warm starting is per triangle"` — the cache key carries the triangle index; a re-traversal that reorders the candidates still warm-starts each contact from its own history.
- `test "a slider does not catch on an internal edge"` — the plan row's own criterion. A box or capsule driven across a flat mesh seam over many steps: the velocity loss across the seam is bounded, and the test asserts that bound rather than eyeballing a trajectory. Assert it **fails** with active-edge correction disabled, so the test proves the mechanism and not the geometry.
- `test "mesh versus mesh and mesh versus half-space are unreachable"` — the precondition asserted, exercised in the debug build.

*Shared, in the suite that owns each entry*
- `test "the object mask filters"` and `test "exclusions are honoured"` — including a mesh body, for each entry.
- `test "a sleeping body answers and stays asleep"` — with a mesh in the scene.
- `test "the result is invariant under creation-order permutation"` — including the identity of the bodies returned.
- `test "two identical runs are bit-identical"`.
- `test "an oblique far-field configuration keeps a unit normal"` — the §1.11.4 bis obligation. The norm is asserted **tight and everywhere**; the distance-dependent bound applies to orientation only, and per §1.11.17 the relevant ratio is the triangle's edge length against its distance from the shape's local origin — not the distance to the world origin. Build the case accordingly.

The whole suite green at `f32` **and** under `-Dphysics_f64=true`, Debug and ReleaseSafe.

### Benchmarks

- **One new bench file** for the mesh: build time and traversal time against triangle count, on a synthetic mesh at three sizes. **Reported, not gated.** No numeric envelope is pre-registered.
- Re-run every existing forge bench against `main` in both precisions and report the movement digit for digit. The `Core` variant touches the hot switch in `support()`; if that costs, it must be seen and reported, not discovered later.
- Any leak-checking allocator used must have `safety` **forced true** — its default is `std.debug.runtime_safety`, false in ReleaseFast.

### Observable behavior

- `zig build test-forge-3d` green, and green again with `-Dphysics_f64=true`.
- The benches run and print their measurements; the figures go in the closing notes.

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (Debug + ReleaseSafe)
- `zig fmt --check` green
- `zig build lint` green
- `commit-msg` hook green on every commit of the branch
- The full forge suite green under `-Dphysics_f64=true`
- **Every check on the PR verified individually.** Read each one; do not read the summary badge. Four closing items were missed at M1.1.11 for want of this.

## Conventions

- **Branch:** `phase-1/forge/mesh-shape`
- **Final tag:** `v0.11.11-mesh-shape` — same patch number as `v0.11.11-plane-halfspace`, distinguished by slug, because both are halves of the split M1.1.11 row and consuming patch 12 would desynchronise the tag↔sub-milestone mapping for the thirteen remaining rows. `vMAJOR.MINOR.PATCH-slug` per `engine-development-workflow.md` §2.3.
- **PR title:** `Phase 1 / Forge / Shapes: static triangle mesh`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

**The inflation radius is a failure mode again, in a new place.** `SupportShape.support` returns the support of the **core**. A triangle's core is the triangle and its radius is zero, so the triangle side is easy — but the *probe* side is not, and the back-face predicate `n · support_probe(n) − r_probe < n · v₀` carries the probe's radius explicitly. Written without `−r_probe` it is correct for a box and silently wrong for a sphere and a capsule, by exactly the radius. The M1.1.11 brief said the same thing about the plane's separation formula, and it was right then.

**Threshold discipline, and the one exception.** Every numerical guard on this shape is at **true zero**: the degenerate-triangle test, the ray's parallel test, the cast's grazing test. No geometric epsilon appears in `triangle.zig` or `mesh.zig`. The active-edge threshold is **not** a numerical guard — it is a named physical parameter selecting a modelling behaviour, defaulting to `cos(5°)`, and it lives with `restitution_threshold` and `penetration_slop`. Declare it there and say why, because a reviewer applying §1.11.2's `k · floatEps(T) · coordScale` rule to it will call it a violation and be wrong.

**Recursion is the trap.** The `Bvh` traverses recursively and is safe doing so because rotation balancing bounds its height. Copying that shape into the mesh traversal is the single most likely way this milestone ships a stack overflow that no test on a well-formed mesh will find. Explicit stack, fixed depth, asserted at build.

**Adjacency belongs to gate one, not to the contacts gate.** It is tempting to build the edge flags where they are first consumed. Doing so reopens `createShape`'s OOM transaction a second time and reorders the ownership set of `MeshData` after it has been tested. Build it with the storage; consume it later.

**One hit per body is a decision, not an optimisation.** If the implementation finds itself wanting to return two, the ordering key is the thing that would have to change, and it is not changing here.

**Reference lineage, verified on source at `jrouwe/JoltPhysics@master`.** `Jolt/Physics/Collision/Shape/MeshShape.h`: `MustBeStatic()` returns true and the class comment gives the reason — a mesh need not form a closed hull, so it has no well-defined volume; `GetInnerRadius()` returns 0; triangles are counter-clockwise and single-sided for simulation; `mMaxTrianglesPerLeaf` defaults to 8 with sensible values documented between 4 and 8; `mActiveEdgeCosThresholdAngle` defaults to `0.996195f`, i.e. `cos(5°)`; `MeshShapeSettings::Sanitize` removes duplicate and degenerate triangles. `Jolt/Physics/Collision/BackFaceMode.h`: `EBackFaceMode { IgnoreBackFaces, CollideWithBackFaces }`. `Jolt/Physics/Collision/RayCast.h`: `RayCastSettings` carries `mBackFaceModeTriangles` and `mBackFaceModeConvex`, both `IgnoreBackFaces`, and `mTreatConvexAsSolid` true. `Jolt/Physics/Collision/ShapeCast.h`: the type is `ShapeCastSettings`, not `CastShapeSettings`, and carries the same two fields at the same defaults. `Jolt/Physics/Collision/CollideShape.h`: `CollideShapeSettings` carries `mBackFaceMode` at `IgnoreBackFaces` and `mActiveEdgeMode` at `CollideOnlyWithActive`. `Jolt/Physics/Collision/ActiveEdges.h`: `IsEdgeActive` treats opposite normals as active, concave edges as inactive, and convex edges as active below the threshold; the flag bit layout is `v0..v1`, `v1..v2`, `v2..v0`. `Jolt/Physics/Collision/Shape/Shape.cpp`: `sCollidePointUsingRayCast` exists and works by hit-count parity. Weld keeps the static-only invariant, the winding, the flag layout and the threshold default; it refuses the sanitiser, the parity membership test, and the unflipped back-face normal. Verify any further claim about the reference **on the reference**, never on a comment in this repository.

**Tooling facts established at M1.1.11, not to be rediagnosed.** The leak checker's `safety` flag is false by default in ReleaseFast. The pre-commit hook lints only staged files. The build runner's `failed command:` line appears on tests that pass while writing to stderr, and is not a failure.

**File length.** `mesh.zig` is a new file and will be large; the 500-line review guideline is a guideline and an overage here is expected, but it is declared in a Recorded deviation with its shape, not absorbed in silence. `body_manager.zig` and `broadphase.zig` are already conscious overages; if either grows further, say so.

---

# LIVING SECTION

*Filled by Claude Code during execution. Recorded deviations, blockers, closing notes, execution log.*

## Recorded deviations

**RD-1 — the active-edge threshold is declared in `mesh.zig`, not beside
`restitution_threshold` and `penetration_slop` in `rigid/solver_config.zig`** (gate B).
The brief and §1.11.17 place it in that family, and it belongs to it: it is a named
physical parameter selecting a modelling behaviour, and §1.11.2's
`k · floatEps(T) · coordScale` discipline does not apply to it. But the flags are BAKED
AT CREATION, so a field on `SolverConfig` would be read long after the decision it
governs had been taken and changing it at runtime would silently rebuild nothing; and
`mesh.zig` is shape storage at the `forge_3d/` level, so importing the rigid branch's
config from it would invert the dependency. It is therefore declared as
`mesh.default_active_edge_cos_threshold`, taken as a per-mesh parameter by
`MeshData.init` (which is what makes it configurable), and `solver_config.zig` carries a
doc cross-reference naming it as the third member of the family — so the family stays
readable from one place, which is what "declared with them" is for.

**RD-1 amended at Guy's finding (gate B fix).** The "configurable" claim was FALSE as
first delivered: the only caller path to `MeshData.init` is `createShape(gpa, desc)`, and
the descriptor carried no threshold, so the store passed the default and nothing could
override it — a window that closes for good at the M1.1.15 freeze. Corrected:
`ShapeDescriptor.triangle_mesh` gains `active_edge_cos_threshold: f32` defaulted to the
`f32` rendering of `cos(5°)`, `createShape` passes the descriptor's field and never the
constant, and `default_active_edge_cos_threshold` becomes `f32` — widened once at `init` —
so a build configuration is not an input to the geometry.

**RD-6 — the internal-edge artefact does not arise where the brief's slider test assumed it
would, and the test geometry is what changed** (gate F). The brief asks for a BOX or capsule
driven across a flat mesh seam. Measured, over sixty ticks on a twelve-quad flat strip: a box
produces **zero** edge contacts, its maximum normal tilt from the face normal being `6e-8`, i.e.
float noise. The reason is structural — a box resting flat contacts a triangle FACE against FACE,
so the supporting plane IS the face and the normal is the face normal, with nothing to correct.
The artefact belongs to a probe whose closest feature can be the triangle's EDGE: a SPHERE whose
centre has passed a seam projects OUTSIDE the triangle behind it, so that triangle answers from
its seam edge with a tilted normal. Measured at 5 cm penetration: 8 edge contacts and a real
`4.2e-3` tilt. A second measurement fixed the rest of the setup: the default `linear_damping` of
0.05 alone costs `5 · (1 − 0.05/60)⁶⁰ = 4.756049` m/s over sixty ticks — the figure matched the
first probe's loss to the digit — so the slider is frictionless AND undamped, and the retained
velocity is then the catch and nothing else.

**RD-5 — the creation-order invariance of a SIMULATION is a physical bound, not a bit one, and
the figure is measured** (gate E). Swapping the two bodies' creation order swaps which is A, so
`collideOrdered` runs with its arguments the other way round — and §3 guarantees only GEOMETRIC
EQUIVALENCE between the two orders, never bit-exactness, the two computing in different frames
and the manifold's point count being free to differ at a topological contact boundary. C1.1 says
the rest: a contact stack is chaotic, so a bound on continuous deviation after hundreds of frames
is ill-posed. MEASURED over 300 steps on a box settling on the mesh floor: `Δy = 1.34e-4` m, and
IDENTICALLY at f32 and f64 — which is what shows it is the geometric difference and not float
noise. The test asserts 1 mm, some seven times that, as a physical claim about where a box comes
to rest, and says so.

**RD-3 — the overlap back-face predicate's radius term is ADDED, not subtracted, and the
frozen text contradicts itself on it** (gate D). §1.11.17 and the brief both write the
predicate as `n · support_probe(n) − r_probe < n · v₀`. That contradicts their own normative
sentence one line later — "a probe straddling the plane touches from the front and counts in
both modes" — and the contradiction is decidable on the case they name: a unit sphere centred
EXACTLY on the plane has a point core, so `n · support_core = n · v₀`, and the subtracted form
gives `n·v₀ − 1 < n·v₀`, TRUE, discarding a probe the same paragraph requires to count. The
added form gives `n·v₀ + 1 < n·v₀`, FALSE. The implementation adds, the straddling case is the
test that discriminates, and the reasoning sits at `triangle.probeIsBehind`.

**RD-4 — `back_face_mode` on `OverlapQuery` is observable only inside GJK's contact margin**
(gate D), measured rather than argued. A probe ENTIRELY in the rear half-space of a triangle's
plane cannot intersect the triangle, which lies IN that plane, so every triangle the predicate
discards is one GJK already classifies `separated`; and any probe that does overlap a triangle
crosses its plane, hence STRADDLES, hence counts in both modes. What remains is a band a few
ULPs wide — a probe whose core sits just behind the plane can be `.shallow` to GJK, and there
`.ignore` drops it where `.collide` keeps it. The field is implemented faithfully because the
spec places it there and it costs nothing, and the acceptance test asserts the two modes AGREE
at the entry grain while discriminating at the predicate grain, which is where the effect
lives. Contrast the SWEEP, where the mode is fully observable: a cast can meet a back face
from a distance, and the suite measures both verdicts on one geometry.

**RD-2 — the parity half of that finding is REFUTED, measured in the repo.** The finding
held that an `f32` field widened at `init` "bakes the same flags in both precisions from
the same descriptor". It does not, and the reason is not the threshold. Measured on a 5°
fold, the same descriptor, `-Dphysics_f64` off then on: the computed fold cosine is
`0.9961947` at `f32` against `0.9961946981138378` at `f64`, and the flags come out
`0b110` at `f32` and `0b111` at `f64` WITH the fixed `f32` threshold. The compared
quantity is `normalize((v₁−v₀) × (v₂−v₀)) · n_neighbour`, whose own build-dependence is
`2.3e-8` — the same order as the `2.2e-8` between the two renderings of the constant — and
no typing of the threshold touches it. Two further measurements bound the claim: NO `f32`
value lies strictly inside that `2.2e-8` band, the `f32` ULP near 1 being `5.96e-8`, so
with `f32` inputs the old typing could never flip a verdict on its own; and a search over
200 000 folds around 5° found the divergence driven by the geometric term in every case,
with the old and new forms diverging identically. The requested band test is therefore
NOT WRITABLE at the descriptor grain, and was not written — what is written instead is the
type and bit-pattern pin of the constant, the equality pin against the descriptor default,
and the round-trip proof that the widening is exact. The fix stands entirely on the
finding's FIRST reason, configurability, which was correct.

**RD-7 — twelve files outside the FROZEN "Files to create or modify" list, and three inside it
left untouched** (closure). Counted rather than asserted: 39 files changed, 27 of them in the
list or covered by its `bench/` and brief entries, 12 outside. Each is a consequence of a
decision the brief itself asks for, and each carries its reason at the site rather than only
here. `src/foundation/math/vec.zig` and `src/foundation/math/math.zig` — closure findings F2 and
F5: the shared `normalizeScaled`, then `maxAbsComponent`, `scalePow2`, `pow2ReductionExponent` and
`triangleCross` with their two re-exports. The brief forbids a geometric epsilon in `mesh.zig`, and
the overflow-safe forms are `foundation` math that the ray kernel and `faceNormal` must SHARE —
`pipeline/` may not import `mesh.zig`, so a form shared by the degeneracy guard and both normal
consumers has nowhere else to live — exactly as M1.1.11 put `overlapsHalfSpace` there under its own
RD-1. **This count therefore rose from twelve to thirteen at the second closing round**, which is
recorded rather than quietly absorbed. `rigid/contact_cache.zig` and `rigid/velocity_solver.zig` — the two
SITES of the warm-start key whose `subshape_id` term the brief requires filled; the list names
`contact_constraint.zig` alone, and the term cannot be filled in one place. `rigid/island_manager.zig`
and `rigid/root.zig` — finding F4's second total order and the two comparators it had to expose,
totality being a claim only a caller can check. `rigid/solver_config.zig` — nine comment lines
placing the active-edge threshold in the `restitution_threshold` / `penetration_slop` family and
saying why it is declared in `mesh.zig` instead (baked at creation), which is the brief's own
Notes instruction discharged where a reviewer will look. `api/root.zig` — the `BackFaceMode`
re-export, without which the type the brief adds to three query structs is unreachable from the
public namespace. `tests/epa_robustness_test.zig`, `tests/fast_paths_test.zig`, `tests/gjk_test.zig`
— three of the eleven exhaustive `Core` switches, which live in suites the list does not
enumerate. `tests/plane_test.zig` — the `ShapeClass` variant-count pin, EXTENDED 2 → 3 with the
three variants named; it is M1.1.11's own pin and it fired when the mesh landed, which is why it
exists. `build.zig` — the `bench-forge-mesh` step, inseparable from the `bench/` entry.
Three listed files were not touched and none needed to be: `narrowphase/epa.zig` and
`narrowphase/shapecast.zig` reach the triangle through `support.zig`'s support map and needed no
arm of their own, and `tests/manifold_test.zig` holds no `Core` switch — the mesh manifold tests
belong beside the mesh, as M1.1.11 concluded for the plane. `CLAUDE.md` is the fourth and is
pending Guy's amended patch, so the PR is not opened before it lands.

## Closing-review findings, all four fixed in-milestone

**F1 [P1] — the mesh candidate filter was STRICTER than the kernel it feeds.** §1.11.12's exact
predicate is "the GJK regime is not `separated`", and that regime's boundary sits at
`conv_k · floatEps(T) · coordScale` beyond touching. Both mesh paths bounded their candidates by
the probe's box, so a triangle inside that band but outside the box was dropped BEFORE the kernel
could classify it — and the entry answered `false` where the same probe against a bounded convex,
which no box filters at all, answered `true`. Fixed by inflating the candidate box by the
NORMATIVE margin, reused and not reinvented: `contact_margin_conv_k`, `contactMargin` and
`coordScale` were promoted out of `gjk`'s locals so the filter evaluates the very expression the
classification does. The mesh side of the scale is an O(1) upper bound over all candidates
(`MeshData.maxVertexMagnitude`), the box having to be fixed before the traversal chooses. Both
paths fixed — shape overlap and contact generation. Counter-factual run: removing the inflation
takes the test down.

**F2 [P1] — a legal descriptor produced a normal of length ZERO.** `faceCross` on `(0,0,0)`,
`(1e10,0,0)`, `(0,1e10,0)` is `(0,0,1e20)`, whose squared length is infinity at `f32`; `normalize`
then divides by infinity and returns the zero vector, puncturing §1.11.17's unit-length invariant
on an input the domain admits. The comment above `faceNormal` claimed the degeneracy refusal made
`normalize` safe — true about NaN and irrelevant here, the failure being an OVERFLOW at the other
end of the range; it is rewritten on the real failure domain. Fixed with a SHARED
`Vec.normalizeScaled` in `foundation`, which reduces by the largest absolute component before
squaring anything: the form `query/root.zig`'s `unitDirection` introduced at M1.1.9 for exactly
this overflow, which the mesh path had not inherited. Three consumers now — `unitDirection`
delegates to it bit-identically, `MeshData.faceNormal` and the ray↔triangle kernel. Tested at both
ends of the range, `2⁻⁷⁰` vertices included, with the length asserted EXACT. Counter-factual run.

**F3 [P1] — `gjkPair` was the FIFTH hole in the `ShapeClass` net, open since M1.1.11.** It passed
either shape straight to `supportShape` and inherited that precondition in silence, so a
half-space — and now a mesh — panicked in a safe build and was undefined behaviour in ReleaseFast.
Fixed by asserting "both bodies carry bounded convexes" AT THAT SITE, which is the only place a
caller can read the requirement against the handles it holds. A precondition and not an error
channel: a mesh has no single GJK result and a half-space no bounded support map. The `ShapeClass`
docstring now enumerates FIVE closed holes, so the advertised net stays checkable against the code.

**F4 [P2] — the constraint order rested on `std.sort.block`'s tie-handling, annulling an invariant
M1.1.8 had written down.** `lessByPairKey` compared `pair_key` alone; with several constraints per
pair that is not a total order, and the sort is UNSTABLE, so their relative order was the
algorithm's internal behaviour on the Gauss-Seidel path. Fixed with a TOTAL key at both sort
sites — `(pair_key, subshape_id)` and `(rank, pair_key, subshape_id)` — total because two
constraints of one pair cannot share a triangle index. **The first version of this test did not
discriminate**, and that was measured: an assertion on the OUTPUT order still passed with the
partial key, block sort happening to leave equal keys where it found them. The test now calls the
COMPARATORS, which had to be exposed for it, and asserts that exactly one of `less(x,y)` and
`less(y,x)` holds for every distinct couple. Counter-factual run: it takes both down.

## Closing-review findings, second round — F5 and the second finding it uncovered

**F5 [P1] — the cross product overflows BEFORE `normalizeScaled` sees it, and the edge overflows
before that. F2 closed only the square of the norm.** Three failures on one path, measured on the
branch before the fix, not deduced: `f32` legs of `1e20` give a cross of `(0, 0, 1e40)` = `(0, 0,
inf)`, and `normalizeScaled` then divides that infinity by its own largest component, also
infinity, and answers `(NaN, NaN, NaN)` — WORSE than F2's zero vector, a NaN propagating where the
zero sat still; legs of `3e38` the same; and vertices spanning `±0.9 · floatMax` overflow one step
earlier still, in the SUBTRACTION, `e0 = (inf, 0, 0)`, which is precision-INDEPENDENT and
reproduces at `f64`. §1.11.17's unit-length invariant was punctured on descriptors `MeshData.init`
admits, at both precisions.

Fixed on the VERTICES rather than the edges, as the finding requires: the three points are reduced
by ONE common power of two derived from `frexp` of the largest absolute component of all nine,
then subtracted, then crossed. Every component then lies below 1, every difference below 2, every
cross component below 4, and no step can leave the range at either end. `Vec.scalePow2` applies the
factor in TWO halves because the exponent itself can be unrepresentable while the result is not —
reducing `3.4e38` asks for `2^-128` and lifting a denormal asks for `2^148` — and the doc says so
where the code is. The power of two is load-bearing and not convenience: it rewrites only the
exponent field, so exactly-collinear points stay exactly collinear and the true-zero degeneracy
guard keeps its verdict, and because `normalizeScaled` divides by a component of its own input the
common factor cancels and the unit vector is BIT-IDENTICAL whatever exponent was chosen. That last
property is what makes "the shared form" a claim rather than a hope, and it is asserted directly:
the same normal comes back for exponents `−37` through `+37`. The form is shared by all three
sites — `MeshData.init`'s degeneracy guard, `faceNormal`, and the ray kernel — through
`math.triangleCross`, whose placement follows M1.1.9's `normalizeScaled` and M1.1.11's
`overlapsHalfSpace`.

The 415 pre-existing tests did not move, which is the evidence for bit-identity on everything that
already worked. Two COUNTER-FACTUALS were run rather than argued: reverting the shared cross alone
fails exactly the two F5 tests and no other (415/417), and removing the kernel's reduced retry
fails exactly the F5 ray test and no other (416/417). A third counter-factual is inside the test
itself, on the power-of-two requirement: a non-power-of-two divisor is SEARCHED for until the
collinear triple loses its exactly-zero cross, and the search matters — a hard-coded `7` breaks
the property at `f32` and preserves it at `f64`, so a fixed divisor would have made that
counter-factual silently vacuous at one precision.

**The second finding, measured as the finding demanded: Möller–Trumbore's own arithmetic returned
`t = NaN` on the same legal descriptors, and it is closed here.** At `f32`, legs of `1e20` give
`det = 1e40` = infinity; `u` and `v` reach infinity too and PASS their bounds, `inf > inf` being
false; and `t_num` then multiplies an infinity by an exact zero, so the NaN the file's own doc
comment credits the `|det|` form with preventing arrives anyway, from the other side, and leaves as
the returned DISTANCE. The spanning case fails at `f64` as well. Note the shape of the defect: the
comment was right about the mechanism (`0 · inf`) and wrong that its form alone closed it.

Closed with the same two exact scales, and the ORDER between them was measured rather than
assumed — the first ordering I implemented was the wrong one and the measurement is what said so.
Reducing unconditionally is correct but strictly worse twice over. On COST, the mesh bench measured
`+38.7 % / +22.8 % / +37.8 %` on the raycast column at 1 000 / 4 000 / 16 000 triangles. On
ACCURACY, a very negative exponent pushes the origin's small components into the SUBNORMAL range
where the mantissa is truly truncated, so reducing loses low bits it had no need to touch: over a
leg × height sweep it answered `9.99979261261345e19` where the input's own scale answers
`1.0000000200408773e20` for a true `1e20`, with two further rows the same way. And a THIRD ordering
— reduce by the triangle alone, form `qvec` from the unreduced difference — produced NaNs on twelve
sweep rows, while reducing by the origin as well never did but lost eleven hits the unreduced form
had found, among them a UNIT triangle with the ray starting `1e30` away. So the kernel attempts the
input's own scale FIRST and retries reduced on a STRUCTURAL signal — an exactly zero determinant or
an intermediate outside the float range, never a tolerance — which is why `Attempt` reports
`.degenerate` and `.unrepresentable` apart from `.miss`: conflating either with a miss is exactly
what let the NaN out. The two orderings were compared on every extreme case at both precisions and
agree exactly, same verdicts and same distances, so nothing is given away in the regime the
reduction exists for. Re-measured fairly, three runs of each form in one session, best of three:
`740 → 729`, `1651 → 1698.5`, `2989.5 → 2964` ns — both signs, under ±3 %, hence below this bench's
noise floor, and the first run of every series is a cache-warming outlier, which is what the
earlier single-run comparison had mistaken for a cost.

**One residual, measured and asserted rather than left implicit.** An ordinary-magnitude ray origin
against a triangle whose legs are `4 · floatTrueMin` is a MISS at `f32` in every form measured —
unreduced, reduced, and the composition. The configuration spans some forty orders of magnitude:
`det` is the leg squared, zero at the input's own scale, while at the reduced scale the offset no
longer fits. The information is not in the inputs, exactly as §1.11.4 bis records for the convex
kernels, and the failure DIRECTION is safe — a miss, never a NaN and never a false hit. The test
asserts the miss at `f32` and the exact distance at `f64`, where the same descriptor is
unremarkable, so the contrast itself is the evidence that the limit is one of precision and not of
design. The reduction also GAINS a case the unreduced form missed: a denormal triangle with a
commensurate origin, whose cross product used to underflow.

## Closing-review findings, third round — F6, and the completeness claim REFUTED by measurement

**F6 [P1] — my F5 fix regressed a class of its own, and with the worst symptom of the three
rounds.** One factor common to the three vertices cannot serve a triangle whose components span
more than the format's exponent range. Reproduced before touching anything: the entirely ordinary
right triangle `(0,0,0)`, `(2e38,0,0)`, `(0,2⁻⁶⁰,0)` reduces by `2⁻¹²⁸`, which sends the second leg
to `2⁻¹⁸⁸`, below the subnormal floor, so that edge becomes exactly zero, the cross with it, and
`MeshData.init` answers `error.MeshTriangleDegenerate` — a typed refusal ACCUSING valid caller
data. Worse than the NaN it replaced, because it is silent and shaped like a diagnostic. The class
is not `f32`-only: at `f64` the same happens for `2^920` against `2^-919`, and both are pinned.

Fixed as directed, PER EDGE: one power of two per pair, from the largest of that pair's six
components, applied before the subtraction so the difference-overflow protection survives. On the
repro the edges become `(0.5877, 0, 0)` and `(0, 0.5, 0)` and their cross `(0, 0, 0.2939)`. A
convention note on the finding's own figures: `frexp` normalises into `[0.5, 1)`, so the second
edge is `0.5` and the cross `0.2939` rather than the `1` and `0.588` the finding writes — a factor
of two in the illustration, with no bearing on the substance. Both properties the single factor had
bought survive intact and both are asserted: exact collinearity is preserved bit for bit, since
both factors are powers of two, so the true-zero guard keeps its verdict; and the two factors
COMPOSE into one common factor on the cross, which `normalizeScaled` cancels, so the unit normal is
bit-identical whatever the two exponents are. The `−37..+37` sweep is extended to the two exponents
INDEPENDENTLY, 49 combinations rather than 7.

**The completeness argument I was asked to write is FALSE, and this is the finding I owe back.**
Two parts of it fail. First, the stated bound — each reduced edge having a component in `[0.5, 1)`,
so the cross being bounded below by their product times the sine of the angle — does not hold: a
near-coincident pair reduces to two nearly equal vertices whose difference is a single ulp, in no
such interval. Second, and decisively, a false degenerate is NOT impossible by construction.
Measured with a big-integer EXACT oracle over triangles drawn across the whole exponent range,
restricted to the cases whose exact answer is REPRESENTABLE in `Real` — the only ones any
implementation working in `Real` can be held to — with one exponent per vertex:

| form | `f32` false degenerates | `f64` |
|---|---|---|
| single factor over three vertices | 1524 / 6447 = 23.6 % | 2086 / 6315 = 33.0 % |
| **per edge (this fix)** | **1103 / 6447 = 17.1 %** | **1391 / 6315 = 22.0 %** |
| scale only when a step leaves the range | 959 / 6447 = 14.9 % | 1319 / 6315 = 20.9 % |

So the class is not closed by ANY arrangement of power-of-two factors. The reason is structural and
it is the same one the finding gives for refusing the widened accumulator: the intermediate needs an
exponent range the format does not hold. Where a reduction is required to avoid overflow it must
scale DOWN, and scaling down is exactly what loses a component expressible only at the original
magnitude. I have not written the argument as given, because writing an argument the measurement
disclaims is the defect class this milestone has already paid for once.

**What IS guaranteed, and is asserted rather than hoped.** Two things, both measured over every
seed at both precisions. There is NO FALSE ACCEPT: an exactly-degenerate triangle is always
reported degenerate, which is the safety-critical direction since accepting one would put a
zero-area triangle in the store where `faceNormal`'s `orelse unreachable` fires. And the per-edge
form STRICTLY dominates the single factor on every seed — `f32` 47→38, 44→34, 40→32 and `f64`
60→47, 50→37, 56→37 false degenerates on 200 non-collinear draws each — which is why the per-seed
assertion is strict `<` and not `<=`: the weaker form would have let a regression through on two
seeds out of three.

**The test closes the class, not the repro.** A randomized property on three FIXED seeds compares
the verdict against an exact oracle in big-integer arithmetic — every component written as an
integer times one common `2^emin`, so the differences and the cross are exact integer operations
and the zero test carries no tolerance. `f128` is deliberately not used at either precision, being
software-emulated on most targets and unnecessary once the arithmetic is integral. Two families:
one exponent per VERTEX (the class the fix targets) and exactly-collinear constructions where
`v₂ − v₀` is an exact small-integer multiple of `v₁ − v₀`, so the degenerate side is populated and
non-vacuous — a first attempt built the collinear family by floating-point interpolation, which
ROUNDS, and produced zero truly degenerate cases out of 4000. The normal's length is asserted tight
on every non-degenerate at 16 ULP, the repo's existing `unit_k` budget; an exact 1 is reachable only
for an axis-aligned cross, which the named pins assert and an arbitrary triangle cannot — measured
at `0.99999994`, one ULP below. Counter-factual: reverting `math.triangleCross` to the single factor
fails BOTH F6 tests at BOTH precisions and no other test, 417/419.

**A third design measured better on both axes, and it is reported rather than substituted.**
Scaling only when a step actually leaves the range — the edge unreduced unless its subtraction
overflows, the cross unreduced unless it overflows, and a retry that scales UP, which cannot lose a
component, when the cross comes out zero — measured 14.9 % / 20.9 % against per-edge's 17.1 % /
22.0 %, and it pays nothing on the common path since it scales nothing there. It was found while
measuring, not before, and the finding names the per-edge factor as the correctif, so substituting
it silently is the deviation the milestone forbids. The figures are here for a one-line ruling.

**Cost, measured fairly.** Best of three runs in one session, mesh bench raycast column:
`822 / 1724 / 3166 ns` at 1 000 / 4 000 / 16 000 triangles against the pre-F5 baseline's
`740 / 1651 / 2989.5` — a consistent `+11 % / +4.4 % / +5.9 %`, the second `frexp` and the two extra
`scalePow2` calls per triangle. Unlike the F5 reordering, whose movement showed both signs and sat
under the noise floor, this one is small but consistently signed, and it is reported as such.

## Blockers encountered

**Two counts in the brief's framing corrected, both in the safe direction.** The brief
names SIX exhaustive `switch` sites on `Core`; there are ELEVEN, and every one of them
broke on the new variant exactly as advertised — `support()` and `supportingFace()` in
`support.zig`, `coreExtent` in `gjk.zig`, the canonical order and `coreKind` in
`manifold.zig`, `fastSeed`'s outer switch plus its `.point` and `.box` inner switches in
`fast_paths.zig`, `raySupportsShape`-adjacent `rayShape` and `containsPoint` in
`raycast.zig`, and three more in the acceptance suites (`epa_robustness_test.zig`,
`fast_paths_test.zig`, `gjk_test.zig`). `fastSeed`'s `.segment` inner switch is the ONE
that carries an `else` and did not break; its outer switch caught the case anyway. The
net is therefore wider than claimed, not narrower. Likewise the brief expects six
`@panic` bodies to survive gate C: there are EIGHT — five query adapters for gate D and
three `collidePairOrdered` arms for gate E — the difference being that the contact
dispatch is 3×3 and owes three separate decisions rather than one.

**Spec item owed at closure, Guy's charge, recorded here so it is not lost.** §1.11.17
states the three active-edge rules in the order OPPOSITE / concave / convex; the
implementation tests the ORIENTATION first and reaches the opposite-normals case only
through its parallel branch. The near-antiparallel residual is the consequence and is
named at `edgeIsActive`. The reference settles it differently, with a second named
constant at `cos(179°)`. Guy reconciles the spec onto the implementation at closure; the
code is not to change.

## Closing notes

**The RD-4 obligation is discharged in the code, not only in this brief.**
`api/types.zig`'s `OverlapQuery.back_face_mode` now carries the measured inertia on the field
itself — that the two modes agree at the entry grain except inside GJK's contact margin, why the
field exists anyway (this entry returns BODIES by a Weld choice, and the day it returns a normal
the field becomes load-bearing while being impossible to add after the M1.1.15 freeze), and the
contrast with `ShapeCastQuery`, where the mode is fully observable. A caller setting it expecting
a different set of bodies reads that where the field is, not in a brief.

**The `worldAabb` measurement promised at gate A, and the decision it forced.** Measured in
ReleaseFast on `bench/forge_3d_mesh.zig`, per call, on one static mesh body:

| triangles | `worldAabb` (O(V) pass) | value read back | `overlapAabb` before | `overlapAabb` after |
|---|---|---|---|---|
| 1 000 | 9.0 µs | 34.5 ns | 8.1 µs | 49.5 ns |
| 4 000 | 20.4 µs | 12.5 ns | 21.2 µs | 24.5 ns |
| 16 000 | 72.8 µs | 11.5 ns | 85.8 µs | 18.5 ns |

It bit, and by three orders of magnitude: `overlapAabb`, the cheapest entry of the family, was
its most expensive, dominated by that single pass. So the cache landed IN THIS GATE, as agreed
— a `Body.world_aabb` column filled at `addBody` for a `.triangle_soup` shape and NaN on every
other body, so a wrong read is loud rather than plausible. It carries no invalidation LOGIC
because a mesh forces a static body whose pose no solver pass writes; what it does instead is
POISON, `setPosition` / `setRotation` resetting it to NaN for any non-dynamic body, after which
the mesh arm falls back to the pass — correct, merely slower. That is what keeps the cache's
correctness from resting on a promise about M1.1.15. The poisoning branch is gated on the body
type so the solver's hot path, which writes dynamic poses every tick, pays nothing.

**The three inherited forge benches re-measured against `main`, and the answer is bit
identity.** The brief's reason for asking was that the fourth `Core` variant touches the hot
`switch` in `support()`. Every bench was run in ReleaseFast on this branch and on `main` at
`03157b7` through a worktree, at BOTH precisions — twelve runs, each `exit=0`. The
load-bearing result is the anti-DCE checksum, which aggregates the answers rather than the
timings, and it is **identical to the last digit in all six pairs**:

| bench | precision | checksum on `main` | checksum on branch |
|---|---|---|---|
| narrowphase | `f32` | `1356124.4937987747` | `1356124.4937987747` |
| narrowphase | `f64` | `1287587.393326787` | `1287587.393326787` |
| raycast | `f32` | `24596700.872` | `24596700.872` |
| raycast | `f64` | `24887120.879` | `24887120.879` |
| shapecast | `f32` | `19343205.471` | `19343205.471` |
| shapecast | `f64` | `19428596.316` | `19428596.316` |

So the new `Core` variant, the promotion of `contactMargin` / `coordScale` / `coreExtent` out
of `gjk`'s locals, and the two `gjkPair` asserts changed NO answer on any pre-existing path,
at either precision. Timings, ns per pair / ray / query, best rep, `f32` then `f64`:

| bench | mode | `main` f32 | branch f32 | `main` f64 | branch f64 |
|---|---|---|---|---|---|
| narrowphase | sphere/sphere disp. | 40.79 | 40.26 | 37.40 | 38.82 |
| narrowphase | sphere/box disp. | 57.62 | 56.09 | 42.69 | 41.34 |
| narrowphase | box/box disp. | 200.69 | 194.33 | 213.87 | 211.30 |
| narrowphase | capsule/capsule disp. | 14.96 | 15.19 | 30.05 | 31.17 |
| narrowphase | box/box generic | 955.56 | 925.13 | 936.48 | 955.42 |
| raycast | closest | 815.2 | 822.5 | 1002.9 | 983.6 |
| raycast | any | 607.5 | 593.5 | 731.4 | 724.3 |
| raycast | all (buffer 32) | 1561.4 | 1603.8 | 1860.4 | 1961.7 |
| raycast | closest (5 m bound) | 403.9 | 406.3 | 493.9 | 486.6 |
| raycast | closest (swept order) | 191.7 | 196.5 | 264.0 | 265.5 |
| raycast | closest (swept, permuted) | 254.4 | 262.9 | 332.7 | 339.5 |
| shapecast | sphere cast | 1249.0 | 1186.4 | 1404.8 | 1389.3 |
| shapecast | box cast | 1309.8 | 1223.7 | 1388.7 | 1417.6 |
| shapecast | capsule cast | 1227.0 | 1140.6 | 1306.5 | 1311.8 |
| shapecast | shape overlap (buffer 32) | 226.4 | 234.7 | 265.1 | 249.0 |
| shapecast | point cast (radius 0) | 1293.3 | 1231.9 | 1415.1 | 1421.8 |
| shapecast | raycast (same rays) | 783.5 | 810.9 | 986.3 | 976.6 |

**Every bench shows BOTH signs at BOTH precisions**, and the extremes are the branch being
FASTER, not slower — capsule cast `−7.0 %` and box cast `−6.6 %` at `f32`, against the largest
slowdown of `+5.4 %` on `raycast all` at `f64`. A single added `switch` prong cannot make three
cast modes 7 % faster, so what these columns measure is this harness's run-to-run spread and
code-layout luck, not a cost. No envelope is registered for a quantity whose sign is not
stable — the rule already applied at M1.1.11 for the half-space, for the same reason.

**One inherited discrepancy found while doing this, and it is not this milestone's.**
`bench/results/forge_narrowphase.md` is the M1.1.4 record and states checksum
`1356124.4934110916`; `main` at `03157b7` produces `1356124.4937987747` today, so the
committed record has not reproduced since some commit BEFORE this branch's base. It was
ATTRIBUTED rather than guessed, by running that bench at each of the four commits that have
touched `pipeline/narrowphase/` since M1.1.4: `6e9ad44` (M1.1.5, the last commit before the
suspect) still gives `1356124.4934110916`, the committed value to the digit, and `7e63912`,
the M1.1.3-HF EPA frame-dependence hotfix, gives `1356124.4937987747`, today's value — so the
drift is that hotfix's, which is coherent, the bench's generic column being GJK-then-EPA and
the hotfix having rewritten the expansion. `dd7fa1f` and `712e4b5` reproduce the post-hotfix
value unchanged. The two
files M1.1.11 last wrote, by contrast, reproduce exactly on `main` — `24596700.872` and
`19343205.471`, digit for digit — so the drift is confined to that one file. The three tracked
result files were restored after these runs rather than refreshed: each is the record of the
milestone that authored it, and rewriting the M1.1.4 numbers inside this milestone would
attribute an inherited movement to the mesh. The mesh's own bench file,
`bench/results/forge_3d_mesh.md`, is this milestone's and is committed.

## Execution log

| Gate | Delivered | Tests |
|---|---|---|
| A | Descriptor, domain, owned storage. `ShapeDescriptor.triangle_mesh` payload (`api/types.zig`, pre-freeze extension, frozen-signature pin extended field by field). New `forge_3d/mesh.zig`: `MeshData` with owned vertices + indices at solver precision, the tight local bound, `triangleCount` / `triangle` / `faceNormal`, and the five typed domain refusals — all validated BEFORE the first allocation, so a refusal allocates nothing. `ShapeClass` gains `.triangle_soup` and its docstring is corrected to state what the code does. `ShapeStore` becomes an OWNER: `createShape` transactional (build-then-reserve-then-commit, `errdefer` on the reservation half), `destroyShape(gpa, id)` releases, `deinit` walks the LIVE slots (not the columns — a dead slot still holds the bytes of a released mesh). **The four non-exhaustive class sites converted to dispatches**: `addBody`'s static-only refusal, `bodyAabb`, `closestPointBody` (the dangerous one — a third-category shape fell THROUGH into the convex path), `worldAabb`; plus the two class asserts of `body.zig` (`computeSleepRadius`, `computeMotion`). A mesh takes the bounded arm everywhere it has an answer: valid `local_aabb`, defined sleep radius, a world box TIGHT over its transported vertices, off-centre local origin included, an ordinary BVH leaf in the harness. `unit_inertia` is NaN — no volume, no tensor. `error.ShapeMustBeStatic` reused, not re-minted. `probeAdmissible` answers `.triangle_soup => false` (final answer, and the guard that keeps a mesh probe off the not-yet-wired arms). The six query adapters and the 3×3 contact arms carry named `@panic` bodies until gates C–E: a mesh creates and destroys, no query answers yet. | 13 new (`tests/mesh_test.zig`); 5 inherited `destroyShape` call sites given the allocator; the `ShapeClass` variant-count pin EXTENDED 2 → 3 and the three variants named. **369/369 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (356 → 369). Full `zig build test` 264/264 steps, 1536/1553 (17 skipped), exit 0. `zig fmt --check` and `zig build lint` clean. Leak check proven in BOTH directions: with the mesh release deliberately removed, `test-forge-3d` reported `24 leaks` and a crash on the forced-`safety` allocator; restored, zero. |
| A (fix) | **RD-1 of gate A WITHDRAWN on Guy's finding.** `worldAabb`'s mesh arm now returns the TIGHT box over the transported vertices, not the enclosure of the transported local box. The cost argument was void: a mesh forces a static body, so its pose never changes and its broadphase proxy is never updated — there is no per-tick path through that arm, and the only per-candidate caller is `aabbOverlapsBody`, which is a `@panic` until gate D, i.e. code whose cost I had estimated without it existing. A one-line note in the arm records that the pass is O(V) and why that is free on a static body; the fallback, if gate D ever measures it hot, is a per-body box cached at `addBody` with no invalidation logic — decided on figures, not guessed. | The off-centre test EXTENDED with an eighth turn about `+Y` on the flat triangle (tight `30 − √2/2` against the enclosure's `30 − √2` on the Z minimum) and a second body carrying the corner triangle `(1,0,0)`,`(0,1,0)`,`(0,0,1)` at an eighth turn about `+Z` (tight `√2/2` against the enclosure's `√2` on the Y maximum), both in closed form. Counter-factual run: reinstating the enclosure takes the test down. Both rotations are set at SOLVER precision — the descriptor's is `f32` by design (§1.11.8), and an eighth turn built there lands ~1e-8 off, which at `f64` swamps any ULP-scale tolerance and would have made the test measure the descriptor widening. |
| B (fix) | `ShapeDescriptor.triangle_mesh.active_edge_cos_threshold: f32` (pre-freeze extension, last window), `createShape` passes it, `default_active_edge_cos_threshold` becomes `f32` and `MeshData.init` widens once. See RD-1 amended and RD-2. | 2 new: the descriptor default pinned equal to the mesh constant (the only place that can see both), the constant's TYPE and exact `f32` bit pattern pinned with a `Real`→`f32` round-trip proving the widening exact; and a caller-supplied threshold moving the verdict in BOTH directions on BOTH folds (4° against `cos 5°` then `cos 3°`, 6° against `cos 5°` then `cos 8°`) — a field read but ignored, or read in one branch only, fails half of them. 380/380 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe. |
| F | Internal edges — the flags baked at gate B are CONSUMED. `internalEdgeNormal` snaps an edge-derived contact normal to the FACE normal when every edge the contact could have come from is inactive. Which edges those are is read off the contact's own surface point ON THE MESH, which §3 makes exactly recoverable (`position ± ½·penetration·n`), by distance to each of the three edge SEGMENTS with the candidates inside a float-noise band of the minimum — using the segment rather than the line is what makes a VERTEX contact fall out for free, both adjacent edges entering the set and both having to be inactive, which errs toward KEEPING a catch at a sharp corner. A contact whose normal already IS the face normal is left alone. The penetration is deliberately not recomputed (§1.11.17 prescribes the normal, and only that); the over-statement is bounded by the cosine of the angle and is documented. Applied AFTER the back-face cull so the two cannot fight. | 5 new: the SLIDER and its counter-factual asserted in ONE test on identical geometry — vertex for vertex, only the index topology differing — 5.000001 m/s retained with the seams paired against 4.647478 unpaired, with the second FAILING the first's bound; the COMPLEMENT, without which a blanket smoother would pass everything — a 30° fold still catches (0.769745 retained) — plus the descriptor threshold moving one 2° fold's behaviour in BOTH directions (4.969233 inactive against 4.833944 active); the mechanism at the MANIFOLD grain in closed form, a sphere 5 cm into a two-quad seam whose trailing triangle yields `(0.40614, 0.91382, 0)` unpaired and exactly `(0, 1, 0)` paired, with the face contact `+Y` in both so the correction is shown targeted and not blanket; a CONCAVE seam inactive against a threshold tight enough to activate a convex fold of the same 10°, so the angle cannot be the explanation; and bit-identity over two runs in both the smooth and the perturbed regime. **411/411 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (406 → 411). Full `zig build test` 264/264, 1578/1595 (17 skipped), exit 0. |
| E | Contacts. `collidePairOrdered` becomes `collidePairEachOrdered`, a 3×3 dispatch over nine arms, and the three `@panic` bodies are GONE — zero remain. Mesh × convex yields SEVERAL manifolds, one per contacting triangle, through a collector; a pair with no sub-shapes yields at most one, so `collidePair` is now that same entry with a single-slot collector — ONE implementation of the dispatch, not two — with the precondition "neither body carries sub-shapes" asserted at its head. `NegatingManifoldCollector` carries the `a > b` canonicalisation. The back-face mode is `ignore`, FIXED and internal: the cull compares the manifold normal ORIENTED FROM THE MESH TOWARD THE CONVEX against the triangle's outward normal, strict `>`, because that orientation is the one the resolution acts along. `ContactConstraint` gains `subshape_id` and `prepare` takes it; `emitPair` appends one constraint per manifold through `ConstraintCollector`, and the wake now FOLLOWS `prepare` — provably equivalent, the two field sets being disjoint (`prepare` reads motion/pose/velocity/material, `wakeBody` writes the sleep columns), and forced by the collector, which would otherwise hold a mutable `BodyManager` while `collidePairEach` walks it through a `*const`. The warm-start cache key's second term is FILLED at last, at both its sites. Mesh × mesh and mesh × half-space are asserted preconditions with the adapter's own motive written and the `BodyType`-determines-layer argument explicitly refused. Active edges are NOT consumed here. | 7 new: one manifold per contacting triangle with every `subshape_id` distinct and in range and every normal `+Y` in closed form, against a convex pair still yielding exactly one; the constraint build emitting one constraint per manifold, all sharing the pair key and differing in the sub-shape; warm-start per triangle proved on the DECISIVE property — every stored cache key pairwise distinct AND at least one `feature_id` recurring under two different sub-shapes, so the collision the term prevents is live and not hypothetical — then a reversed re-traversal reading each contact's own value back by key with `hits > 0` and `misses == 0`; the two forbidden pairs with both static-only refusals exercised and both ADMITTED pairs answered, plus the back-face cull observed at the contact grain (a sphere at the centre of the closed cube produces ZERO manifolds, all its contacts being from behind); a box settling on the mesh floor, sleeping, and its body AABB and pose bit-FROZEN over 120 further ticks; two runs bit-identical over 300 steps; and creation-order permutation on a MEASURED physical bound. **406/406 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (399 → 406). Full `zig build test` 264/264, 1573/1590 (17 skipped), exit 0. |
| D | The five remaining entries, and the back-face mode on the two probe entries. `shapeCast` traverses the SWEPT walk in the BODY's frame while the kernel stays in the probe's — three frames, named — with `supportShapeAabb` as the core-grained sibling of `worldAabb` (deliberately NOT merged with it: the capsule arms differ in operation order and M1.1.9/10 pin exact world boxes). `overlapShape` is GJK on the cores per triangle with the back-face predicate ahead of it. `overlapAabb` answers as a bounded convex does — the body's TIGHT world box, NO descent. `pointQuery` returns FALSE for a mesh, categorically, including inside the volume a closed mesh encloses. `closestPoint` traverses bounded by distance and reuses `closestPointOnCore`, factored out of the convex arm so the two grains cannot drift. `subshape_id` threaded through the cast and closest-point paths as well — `BodyCastHit` and `BodyClosestPoint` gained it, and it was NEVER filled before this gate on any family. `ShapeCastQuery` / `OverlapQuery` gain `back_face_mode`, and the carried item from gate C is done: `fast_paths.zig`'s `.segment` inner `else` is now three explicit cases. See RD-3 and RD-4. | 10 new: closed-form time of impact for sphere, box and capsule probes — the expected value computed from the DESCRIPTOR's `f32` extent, since at f64 the decimal `8.7` fails by `1.2e-8`, which is `f32(0.3)`'s quantisation and not the kernel's error; a receding direction, a zero direction at TRUE ZERO, and a grazing PAIR either side of tangency (centre `1.55` clears, `1.45` strikes, 0.1 apart); a cast and an overlap from behind with both verdicts on one geometry and the flip asserted as the negation of the triangle's own normal; the straddling case at the predicate grain, which decides the sign of the radius term, plus the no-radius counter-factual; `pointQuery` empty at the centre, on a face, on an edge, on a vertex and just inside, with a convex body beside it answering so the empty result is a decision; `closestPoint` outside (3), inside (0.5), at the centre (1) and ON the surface (0) — the inside case being what separates a surface from a solid; `overlapAabb` returning the body for a box strictly inside the HOLLOW cube where `overlapShape` returns nothing, pinning the documented granularity; the cache answering as the pass does and a teleport poisoning it while the answer stays correct, with a dynamic body proving the poisoning branch leaves the hot path alone; exact brute-force agreement on all five entries over 4 seeds × 40 queries with a non-vacuity guard per family; mask, exclusions, a sleeping body answering and staying asleep with a mesh in the scene, creation-order invariance keyed on the ENTITY, and two bit-identical runs. **399/399 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (389 → 399). Full `zig build test` 264/264, 1566/1583 (17 skipped), exit 0. |
| C | The triangle as a FOURTH `Core`, and the ray family through a mesh. `SupportShape.Core.triangle` (three vertices inline) with its `support()` arm — the max of three dot products, vertex 0 winning an exact tie — and its `supportingFace()` arm returning the WHOLE triangle at `face_id = 8`, the choice the box arm already makes. `triangle.zig` new: Möller–Trumbore in its signed-determinant form, where the DETERMINANT IS THE FACING (`det = −d · n`) and there is ONE division, at the end, against `\|det\|` — not a reciprocal, because for a subnormal determinant `1/det` overflows and `0 · inf` is a NaN that passes both barycentric bounds and would let a NaN distance out. `isBackFace` written once and shared; `localHit` is where the normal FLIP lives, since `normal · direction <= 0` is an invariant and not a preference. `raySupportsShape` becomes an exhaustive switch refusing a rounded TRIANGLE exactly as it refuses a rounded box; `containsPoint`'s triangle arm is FALSE everywhere, the categorical surface rule at the grain of one triangle, which is also what keeps `rayShape`'s membership short-circuit from ever firing for a triangle. `LocalHit` gains `subshape_id`, `raycastBody` gains the mode and the mesh arm, `evaluate` fills the field, `RaycastHit` returns it — the first shape ever to write it. ONE HIT PER BODY is decided inside `raycastBody`, so the three collectors are untouched; the tie-break at equal distance is the SMALLER triangle index, never the traversal order. `BackFaceMode` + `RaycastQuery.back_face_mode` land on the frozen surface (pre-freeze, last window); `ShapeCastQuery` and `OverlapQuery` gain theirs with the entries that read them, so no declared field is unwired. | 9 new (2 inline in `triangle.zig` + 7 in the suite): the determinant sign against the shared predicate with both facings and the flip in both modes, boundary-inclusive on a vertex and a hypotenuse, all three edges refused outside; nearest triangle AND its index against closed forms at three different indices — 0 from the front, 2 from behind, 1 from between, so a `subshape_id` left at its default fails; `raycastAll` ONE entry for a ray crossing three triangles of one body and TWO for two bodies; back faces ignored by default and hit under `.collide` on all three entries, with a front hit answering in both modes so the assertions cannot pass under a blanket refusal; the flipped normal plus `normal · direction <= 0` and a TIGHT unit norm asserted over 200 oblique rays × 2 modes; the grazing ray at TRUE ZERO, with one ULP off parallel SERVED at an ordinary distance and the finding that a triangle's barycentric bound rejects the far-crossing case where §1.11.15's half-space needs `max_distance`; exact brute-force agreement over 5 seeds × 60 aimed rays × 2 modes on triangle identity AND bit-exact distance, across all three entries; the far field with the norm tight everywhere and the ORIENTATION bounded by `floatEps(f32) · L / edge` in the SHAPE'S LOCAL frame, with a near-origin control two orders inside it. **389/389 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (380 → 389). Full `zig build test` 264/264, 1556/1573 (17 skipped), exit 0. |
| B | `MeshData` gains the acceleration structure and the adjacency, both inside the SAME transaction as the storage. Static binned-SAH builder into a FLAT node array — no insertion, no removal, no fat margin, no rebalancing; three axes × 12 bins, cost `A_L·n_L + A_R·n_R`, fixed tie-break (axes then split positions ascending, strictly-better only), in-place two-pointer partition, MEDIAN fallback when every centroid coincides so the split always makes progress. One precise reservation of `2T − 1` nodes so no growth can fail mid-build, shrunk on the way out. `Aabb.rayInterval` and `Aabb.inflate` REUSED verbatim; `traverseRay` is `traverseCast` at a zero extent, one traversal and not two. Four traversals — ray, swept, AABB, bounded distance — each an EXPLICIT fixed-depth stack, never recursion, with `max_tree_depth = 64` a build-time invariant the builder holds by forcing a leaf there and asserts, and every push asserted against the stack. Adjacency by SORTING `(lo, hi, triangle, edge)` keys and pairing adjacent runs — no hashed container; a run of one (open boundary) or of three-or-more (non-manifold) is ACTIVE. Three flags per triangle in the reference's layout. `edgeIsActive` derives convexity as `(n₁ × n₂) · edge_direction > 0` — sign derived, not guessed — with the parallel branch at TRUE ZERO and the near-antiparallel residual named rather than papered over. The threshold is RD-1. | 6 new: tree partition (every triangle in exactly one leaf, child boxes contained in parents, `2L − 1` node count, root box equal to the tight vertex bound), brute-force agreement over 7 seeds × 40 aimed queries × 4 traversals against TWO exact contracts (per-triangle completeness and per-leaf equality) with a vacuity guard on each family, the depth invariant on a self-similar adversarial staircase (achieved depth strictly greater than the balanced `⌈log₂ 16⌉ = 4`), branch-and-bound pruning with a tightening collector, exact pairing on a closed flat square (`0b011` / `0b110` — an asymmetry only pairing produces), boundary and non-manifold runs, the threshold at ±1 ULP in closed form plus 4°/6° end to end plus a 45° concave, and order-independence under a triangle permutation. **378/378 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (369 → 378). Full `zig build test` 264/264 steps, 1544/1561 (17 skipped), exit 0. Counter-factual measured and recorded in the test: near-first visits 345/369/398/390/347 nodes against 450/442/487/453/422 unbounded, and inverting the descent to far-first takes the pruning assertion down at the first seed. |
| Closing review | The four findings Guy relayed, all fixed in-milestone with no deferral. F1: both mesh candidate filters inflated by the NORMATIVE GJK margin, `contact_margin_conv_k` / `contactMargin` / `coordScale` promoted out of `gjk`'s locals so the filter evaluates the classification's own expression, with `MeshData.maxVertexMagnitude` supplying the O(1) mesh side of the scale before the traversal can choose. F2: shared `Vec.normalizeScaled` in `foundation`, reduce-by-largest-component before squaring, three consumers (`unitDirection` delegating bit-identically, `MeshData.faceNormal`, the ray↔triangle kernel), and the false comment above `faceNormal` rewritten on the real failure domain — OVERFLOW, not NaN. F3: `gjkPair`'s inherited precondition asserted AT ITS SITE, and the `ShapeClass` docstring now enumerates FIVE closed holes so the advertised net stays checkable. F4: TOTAL sort keys at both sites, `(pair_key, subshape_id)` and `(rank, pair_key, subshape_id)`, both comparators exposed for the test. | 4 new. Counter-factuals RUN for F1 (removing the inflation takes the test down), F2 and F4 (the partial key takes both assertions down); F3's assert cannot be caught in-process, so its test asserts the predicate the assert rests on. **415/415 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (411 → 415), each of the four combinations with `exit=0` captured BEFORE any filtering. Full `zig build test` 264/264 steps, 1582/1599 (17 skipped), exit 0. `zig fmt --check` and `zig build lint` clean. |
| G | Closure. The three inherited forge benches re-measured against `main` at `03157b7` through a worktree, ReleaseFast, both precisions — twelve runs, every one `exit=0`. Language audit §3.6.1 re-run in Python over the whole branch diff (a byte-wise `grep` class silently matches nothing here): 142 non-ASCII letter hits, every one either `×` or the `ö` of Möller–Trumbore, and a lexical sweep of the added lines returns only the English word "plus" — zero French. The narrowphase drift found while measuring was ATTRIBUTED to `7e63912` rather than guessed, and the three tracked result files restored rather than refreshed. Brief status closed. | No new tests. **The six anti-DCE checksums are identical to the last digit between `main` and the branch**, at both precisions — the fourth `Core` variant changed no answer on any pre-existing path. Timings move in BOTH directions on every bench at both precisions, extremes `−7.0 %` (branch faster) against `+5.4 %`, so no envelope is registered. See § Closing notes for the two full tables. |
| Closing review (2) | F5 and the second finding it uncovered. The cross product and the EDGE behind it overflow before `normalizeScaled` is reached — F2 had closed only the square of the norm — so a legal descriptor produced a NaN normal at `f32` and, in the spanning case, at `f64` too. Fixed on the VERTICES: one common power-of-two reduction over all nine components, then subtract, then cross, shared by all three sites through `math.triangleCross`; `Vec.scalePow2` applies the factor in two halves because the exponent can be unrepresentable while the result is not. Second finding, measured as required: Möller–Trumbore's own chain returned `t = NaN` on the same descriptors, closed with the same two exact scales — the input's own attempted FIRST, the reduced one as a retry on an exactly zero determinant or an intermediate outside the range, `Attempt` reporting `.degenerate` and `.unrepresentable` apart from `.miss` because conflating either with a miss is what let the NaN out. The ORDER was measured, and my first implementation had it backwards. | 2 new (417 total). Counter-factuals RUN: reverting the shared cross fails exactly the two F5 tests (415/417), removing the reduced retry fails exactly the F5 ray test (416/417), neither touches a pre-existing test; and inside the test, the non-power-of-two divisor is SEARCHED rather than fixed, a hard-coded 7 being vacuous at one of the two precisions. **417/417 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (415 → 417), each corner a LITERAL command with its own exit code. Full `zig build test` 264/264 steps, 1584/1601 (17 skipped), exit 0. `zig fmt --check` and `zig build lint` clean. Bench re-measured fairly, three runs per form in one session, best of three: 740→729, 1651→1698.5, 2989.5→2964 ns, both signs and under ±3 %, against the +38.7/+22.8/+37.8 % the reduce-first ordering cost. |
| Closing review (3) | F6 — my F5 fix regressed a class of its own: one factor over the three vertices cannot serve a triangle whose components span more than the format's exponent range, and the symptom was a typed refusal ACCUSING valid caller data, silent and shaped like a diagnostic. Fixed PER EDGE as directed, one power of two per pair from the largest of its six components, applied before the subtraction. Both properties the single factor bought survive and are asserted — exact collinearity bit for bit, and a normal bit-identical under the two exponents, now swept INDEPENDENTLY over 49 combinations. **The completeness argument I was asked to write is refuted by measurement and I have not written it**: over triangles drawn across the whole exponent range and restricted to representable answers, false degenerates run 23.6 %/33.0 % for the single factor, 17.1 %/22.0 % per edge, 14.9 %/20.9 % for a scale-only-on-overflow variant — so no arrangement of power-of-two factors closes the class, for the same reason the finding gives for refusing a widened accumulator. What IS guaranteed and asserted: zero false accepts, and strict per-seed dominance over the single factor. | 2 new (419 total). A big-integer EXACT oracle — every component an integer times one common `2^emin`, so the cross is exact integer arithmetic and no `f128` is needed at either precision — drives a randomized property on three FIXED seeds over two families, one exponent per vertex and exactly-collinear constructions (a first attempt built those by float interpolation, which ROUNDS, and yielded zero truly degenerate cases in 4000). Per-seed dominance asserted STRICT on the measured figures — f32 47→38, 44→34, 40→32; f64 60→47, 50→37, 56→37 — since `<=` would have passed a regression on two seeds. Normal length tight at 16 ULP, `0.99999994` measured. Counter-factual: the single factor fails BOTH F6 tests at BOTH precisions, 417/419, and no other test. **419/419 green at f32 AND `-Dphysics_f64=true`, Debug AND ReleaseSafe** (417 → 419). Full `zig build test` 264/264, 1586/1603 (17 skipped), exit 0. Bench cost consistent and reported: +11 %/+4.4 %/+5.9 % on the raycast column. |
