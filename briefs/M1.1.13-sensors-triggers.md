# M1.1.13 — Forge 3D: sensors and triggers

> **Status:** CLOSED
> **Phase:** 1
> **Branch:** `phase-1/forge/sensors-triggers`
> **Planned tag:** `v0.11.13-sensors-triggers`
> **Base:** `main` at `cbb02b3` (`v0.11.12-character-controller`)
> **Dependencies:** M1.1.0 (shape store, `BodyManager`, `PackedId`, `BodyFlags`), M1.1.1 (broadphase BVH, the broad-layer matrix, moved-driven pair generation), M1.1.2 (GJK and its three regimes), M1.1.3 (EPA, manifold), M1.1.8 (islands, sleep, the eleven-step cycle), M1.1.9–M1.1.10 (the eight query entries, object-layer masking), M1.1.11 (half-space, unbounded lists), M1.1.11.1 (mesh, `collidePairEach`), M1.1.12 (the character controller and its three collection paths)
> **Opened:** 2026-08-10
> **Closed:** 2026-08-11

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

A **sensor** is a body that detects without responding. The corpus has declared `TriggerEnter` and `TriggerExit` since its first physics draft and specifies `CollisionShape.is_trigger` as the authoring surface, yet nothing in the interface can express the role and nothing in the engine reads it: `BroadphaseLayer.trigger` exists with no producer and no consumer, exactly the situation `BodyType.kinematic` was in before M1.1.12. This milestone closes that chain.

It delivers the **lower half** of the capability, and the boundary is deliberate. The engine produces an **overlap state** and two **deltas**; the translation into typed events on the Tier 0 bus belongs to M1.1.15 with the rest of the orchestration, because `forge_3d` depends only on `foundation/math` and the public component types of `src/modules/forge/api/` — a C1.1 exit metric — and the bus lives on `World`, which the solver cannot reach. The state, not the event stream, is the source of truth: the Tier 0 bus drops its oldest entry on saturation, so an ownership set reconstructed from the flow would be wrong on the first saturation.

Two fields land on `BodyDescriptor` here. This is the **second-to-last window**: after M1.1.15 freezes `PhysicsModule`, neither could land at all, and without the first, no caller can declare a trigger.

## Scope

- **`BodyDescriptor.is_trigger` and `BodyDescriptor.trigger_layer_mask`**, transcribed from `engine-tier-interfaces.md` §1 at version **0.9** field for field, name for name, default for default. No interface function is added: the §12 count stays at 27, and a reviewer expecting a new entry should read `engine-physics-solver.md` §1.13.12 for why there is none.
- **The role is a property of the INSTANCE, never of the geometry.** The shape store is shared and a `ShapeId` is referenceable by several bodies, so the field cannot sit on `ShapeDescriptor` without forcing two bodies that share a sphere to share their nature (§1.13.1). A mixed body does not exist: an entity that must both collide and detect carries **two bodies**.
- **`CollisionShape` gains `trigger_layer_mask`** as the ECS authoring source of the descriptor field, and its pinned layout moves from 48 to **52 bytes**. Both the comptime assert and the test that doubles it are updated together; a pin changed in one place only is the defect this bullet exists to prevent.
- **`BodyFlags` carries `is_trigger`** on one of its five reserved bits — no size change. The mask is a **separate column** of the body store, not a flag.
- **Broad-class assignment, and it is this milestone's by prior decision.** M1.1.1's scope boundary assigned the trigger/debris layer-assignment sources here by name. Fixed priority: `is_trigger` first, then body type — `static` to `static`, **everything else to `dynamic`**. A kinematic body therefore lands in `dynamic`, deliberately: the class names what *moves*, not what is *simulated*.
- **The `trigger` row and column of `default_layer_pairs` go to `false` in full.** This **reverses a cell asserted at M1.1.1** — `dynamic × trigger` was `true`, and `broadphase_test.zig` asserts that pair positively today. The reversal is a revision of an M1.1.1 decision, recorded as such and not slipped in: killing the pair at the source is less work than filtering it downstream every tick, and it is a stronger guarantee, since a trigger that never reaches step 4 cannot be forgotten by a downstream filter.
- **Detection and physical response are two axes with no shared mechanism** (§1.13.2). `is_trigger` removes the response; the mask governs what is seen. A trigger can detect a `static`, `dynamic` or `debris` body and another trigger, and none of that produces a contact. Making the broad class carry a gameplay policy is the error this bullet forbids.
- **A dedicated sensor traversal, and it is structurally required rather than merely preferable.** `computePairs` is **moved-driven** — it enumerates proxies that left their fat AABB since the last call — so it returns a delta and never a snapshot: two motionless bodies overlapping for a hundred ticks do not appear in it. The pass enumerates the **trigger proxies** and descends the trees and the unbounded lists for each one's candidates. That direction and not the reverse: triggers are rare against bodies, and the set is indexed by them.
- **The mask is unilateral.** It belongs to the trigger and describes what *it* sees. Two overlapping triggers are two relations evaluated separately, and A may see B without B seeing A. Same mechanism and same semantics as the object-layer mask of the eight query entries (§1.11.5), which is the only object-layer filtering the engine owns — there is **no object-layer matrix**, and none is built here.
- **Detection is exact, never AABB.** A candidate retained by the mask is confirmed by the narrowphase under the conditions of §1.11.12: accepted as soon as the GJK regime is not `separated`. The tree descent bounds the candidates; the AABB never decides membership.
- **No threshold of its own, and no hysteresis.** The boundary is the GJK contact margin (§3), not a second margin invented for sensors. A body resting exactly on a trigger's surface, inside that few-ULP band, may flip between ticks. That is accepted and stated in §1.13.6: a hysteresis would need a length-dimensioned, therefore tuned, therefore scale-dependent threshold, which §1.11.2's discipline forbids on this path.
- **Three exclusions from the physical response** (§1.13.7). No constraint, no impulse, no island membership — no pair reaches step 4. No wake cause: a detection is not a solicitation under §1.8.4 and is not a fifth cause beside W1–W4. And the **character controller ignores trigger bodies by construction, in all three of its collection paths** — sweeps, depenetration, ground probe.
- **The controller exclusion is by construction and not by mask, and the distinction is load-bearing.** All three of `character.zig`'s collectors read `collisionLayer` and test it against a mask; none consults the broad class. Left alone, a trigger would stop blocking rigid bodies while still blocking the player — an invisible wall for the player alone. The controller's mask is over **object** layers and a trigger can live on any of them, so requiring the game to reserve one for triggers would rebuild in mirror the confusion of §1.13.2. « No physical response » is the definition of the role; a controller sweep *is* a physical response.
- **The eight query entries, by contrast, keep seeing triggers.** They traverse every broad class (§1.11.1 point 3), editor selection and debug overlays depend on it, and a caller that wants none has its own mask. The symmetry with the controller would be an error here, and it is tested in **both** directions so that nobody restores it by reflex.
- **The observable state is a set of ORIENTED ENTITY PAIRS**, never body handles. Four normative rules: several body overlaps between the same two entities aggregate to **one** pair, whose exit fires only when the **last** of them disappears; a pair whose two members are the same entity is dropped; the pair is oriented from the trigger, so two mutually detecting triggers produce **two** pairs; and the current set is **rebuilt in full every tick**.
- **Full rebuild is what makes handle recycling harmless**, and this is a property of the reconstruction rather than of the type. No body handle appears in the state and no membership is carried forward, so a reissued `BodyId` inherits nothing. State held by increments would lose that immunity — say so where the state is declared, or a later optimisation will silently break it.
- **Two deltas: `entered` and `exited`**, the two set differences. There is no third list — §1.13.12 states why `TriggerStay` does not exist in either the 3D or the 2D corpus, where both do carry a `CollisionStay`.
- **The pass is filtered by no sleep state whatsoever**, on either side, and this is what forbids the phantom exit **structurally** rather than by a special case. Membership derived from step 4 — which skips any pair with both ends non-awake — would make a body that **falls asleep inside a trigger** leave the current set without having moved by one ULP. The pass therefore runs at **step 10 bis**: after the proxy update, so poses are final and every proxy is valid including sleepers', and **before** step 11, so a body's membership is established for the tick in which it falls asleep.
- **The price is explicit: sleep does not economise this pass.** A fully resting scene pays it every tick. Nothing forbids an implementation from skipping the work when neither the trigger nor any of its candidates moved — the set is then unchanged by the same argument that lets step 10 skip sleepers — but the observable invariant is the one that is tested: **falling asleep never produces an exit**.
- **Disappearance produces an exit** whatever its cause — separation, removal of the candidate body, removal of the trigger body, shape change, or loss of the sensor role. None of these needs a dedicated path: the full rebuild covers them all, a removed body having no proxy left to visit.
- **Determinism, and M1.1.14 is next.** State and both deltas are deduplicated and sorted by the lexicographic key `(trigger_entity, other_entity)` compared on the **complete entity value** — index **and** generation, never the index alone, two successive occupants of an index having to be distinct and ordered. No hashed container anywhere on this path. The output order is a pure function of the set, independent of tree visit order, worker count and body insertion order, so M1.1.14 **verifies** it instead of having to establish it.

## Out of scope

- **Any emission.** No `TriggerEnter`, no `TriggerExit`, no `EventBus` registration, no `emit`. `forge_3d` does not import `weld_core` and must not start here — C1.1 makes its dependency set an exit metric. The translation is M1.1.15's.
- **The bus-to-Etch bridge.** `World.event_bus` is comptime-typed and the interpreter keeps its own dynamic per-tick `EventStore` because the tree-walker cannot drive the former; no bridge exists between them, so no Etch rule can observe an event produced by Zig today. This is a Tier 0 / Etch problem, it is **not** a physics problem, and M1.1.15 now owns it by name in `engine-phase-1-plan.md`.
- **Any treatment of the `debris` class.** It has no consumer before destruction, and deriving a broad class from an object layer touches no signature, so no freeze imposes a date on it. **Add no `is_debris` field**: debris is a Forge optimisation class, where sensor is a semantic every backend shares.
- **An object-layer matrix.** The engine's object-layer filtering is a mask, here as in the queries. A configurable pair matrix from the project settings stays a separate piece of work with no deadline here.
- **`step()` / `PhysicsWorld` / `PhysicsModule` instantiation / production `BodyType → BroadphaseLayer` wiring / ECS `Transform` sync** — all M1.1.15. Nothing in production inserts a body into the broadphase today; this milestone proves its behaviour through the test harness and must **say so** rather than imply a wiring that does not exist. **The character's presence is inserted by the harness**, exactly as the query suites insert their own proxies.
- **Renaming the `dynamic` broad class.** The name lags its meaning — it is what moves, not what is simulated — and correcting it touches the broadphase and its suites. Noted, not done.
- **Any hysteresis, debounce, dwell time or minimum overlap duration.** §1.13.6 refuses them with an argument; do not reintroduce one under another name because a test flickers. A flickering test at the ULP boundary is measuring the boundary.
- **`TriggerStay`**, in any form, including an internal « still overlapping » list offered as a convenience.
- **`forge_2d`.** `PhysicsModule2D` carries no sensor entry either and `Collider2D.is_sensor` names the same role with a different word; the symmetry is recorded for M1.8.11. Touch nothing under `forge_2d/`.
- Cross-platform determinism (M1.1.14), joints (M1.1.16–18), advanced shapes (M1.1.19–20), vehicle (M1.1.21), the deformable branch (M1.1.22–23).
- `src/c-api/`, editor overlays, the Tier 1 service, any Etch surface.

## Specs to read first

1. `engine-physics-solver.md` — **§1.13 first, in full, all twelve subsections.** Then §1.7's eleven-step cycle, because step 10 bis is defined relative to it. Then **§1.8.4, §1.8.5 (W1–W4) and §1.8.6** — the table of what sleep economises is the one this milestone deliberately does not extend.
2. `engine-tier-interfaces.md` — §1 at **version 0.9**, in full: the two new `BodyDescriptor` fields with their doc comments, and §12's counting convention (27, unchanged).
3. `engine-physics-queries.md` — §1.11.1 (a query is not a simulation step; the mask/pair distinction; traversal of every broad tree), §1.11.5 (object-layer filtering and the `[0, 32)` bound), §1.11.12 (the three overlaps and their exact kernels), §1.11.2 (the epsilon discipline and what escapes it), then **§1.12** for the controller's three collection paths.
4. `engine-physics-forge.md` — §2 (`CollisionShape` and the authoring surface), §3 (collision layers and the configurable matrix that does not exist yet), §5 (the declared events and their payload), §1.2 (the four broad classes).
5. `engine-physics-shapes.md` — §1.11.15, for the unbounded lists a traversal must visit alongside the trees.
6. `engine-phase-1-plan.md` — the M1.1.13 row and its criterion, then M1.1.14 and M1.1.15.
7. `engine-phase-1-criteria.md` — C1.1, in particular the dependency metric that forbids `forge_3d` from importing `weld_core`.
8. `engine-zig-conventions.md` — naming, unmanaged-first allocation, the `root.zig` convention, the lazy-analysis guard, the file-length guideline.

## Specs read

*Checked by Claude Code at Étape 2, with a real `YYYY-MM-DD HH:MM` timestamp each.*

- [x] `engine-physics-solver.md` — 2026-08-10 20:05 (§1.13 in full, all twelve subsections; §1.7 and its eleven-step cycle; §1.8 in full, including §1.8.4, §1.8.5 W1–W4 and §1.8.6)
- [x] `engine-tier-interfaces.md` — 2026-08-10 20:11 (version 0.9; §0; §1 in full — the two new `BodyDescriptor` fields with their doc comments; §12 and its counting convention, 27 unchanged)
- [x] `engine-physics-queries.md` — 2026-08-10 20:15 (in full: §1.11.1, §1.11.2, §1.11.5, §1.11.6, §1.11.7, §1.11.12, §1.11.14, §1.11.16 and §1.12 with its three collection paths)
- [x] `engine-physics-forge.md` — 2026-08-10 20:19 (§1.1–§1.6, §1.10 with the declared `TriggerEnter` / `TriggerExit` events, §2 and the `CollisionShape` authoring surface, §3 collision layers and the configurable matrix, §13 the calling surface)
- [x] `engine-physics-shapes.md` — 2026-08-10 19:58 (§1.11.15 unbounded lists and their per-layer traversal; §1.11.17)
- [x] `engine-phase-1-plan.md` — 2026-08-10 20:22 (the M1.1.13 row and its criterion, M1.1.14, M1.1.15, and the M1.1 context block)
- [x] `engine-phase-1-criteria.md` — 2026-08-10 19:56 (C1.1 in full, including the dependency metric that forbids `forge_3d` from importing `weld_core`)
- [x] `engine-zig-conventions.md` — 2026-08-10 20:27 (§1 naming, §3 unmanaged-first, §2 the `root.zig` convention, §7, §8, §13 the lazy-analysis guard, §15, §16, §19 the file-length guideline)

## Files to create or modify

- `src/modules/forge/api/types.zig` — modify — the two `BodyDescriptor` fields and their frozen-surface pins.
- `src/modules/forge/api/components.zig` — modify — `CollisionShape.trigger_layer_mask`; the comptime layout assert **and** the test that doubles it, 48 → 52.
- `src/modules/forge/forge_3d/body.zig` — modify — `BodyFlags.is_trigger` on a reserved bit.
- `src/modules/forge/forge_3d/body_manager.zig` — modify — the mask column, its accessor, the trigger predicate, and broad-class derivation at creation.
- `src/modules/forge/forge_3d/pipeline/broadphase.zig` — modify — the `trigger` row and column of `default_layer_pairs`.
- `src/modules/forge/forge_3d/pipeline/sensor.zig` — create — the traversal, the state, the two deltas. Sibling of `sleep.zig`, which is the other per-body sweep at pipeline level. If the import graph forces a different placement or a split, that is a **Recorded deviation**, not a silent move.
- `src/modules/forge/forge_3d/character.zig` — modify — the three collectors exclude trigger bodies.
- `src/modules/forge/forge_3d/root.zig` — modify — re-exports and the comptime pin for the new suite.
- `src/modules/forge/forge_3d/tests/sensor_test.zig` — create — the sensor acceptance suite.
- `src/modules/forge/forge_3d/tests/broadphase_test.zig` — modify — the reversed matrix cells, in both directions.
- `src/modules/forge/forge_3d/tests/character_test.zig` — modify — non-obstruction by a trigger, on all three paths.
- `src/modules/forge/forge_3d/tests/overlap_test.zig`, `.../raycast_test.zig` — modify — a trigger stays visible to the query entries.
- `build.zig` — modify only if a new test file needs registering in `test-forge-3d`.
- `CLAUDE.md` — modify — the Tags table, the `Current state` block (**stale on `main`: it still reads M1.1.12 code-complete, PR open, and names `v0.11.11-mesh-shape` as the last tag**), the open decisions — **the `engine-physics-forge.md` decomposition entry is DONE and is removed**, and a new entry opens for the bus-to-Etch bridge owner — and the footer date. Patch content supplied by Claude.ai, committed **inside the PR**.

## Acceptance criteria

### Tests

RED-first on every pinnable behaviour. Every expectation is a **closed form computed in the comment above it**, never a value read back from the implementation. Every suite runs at `f32` and `f64` × Debug and ReleaseSafe. Entry state: **495 tests** in `test-forge-3d`, green at `cbb02b3`.

**Produce the wiring table at the first gate that writes a property.** For every quantity the suite measures: its name, its nature — **delivered guarantee** or **historical metric** — and its treatment. A guarantee is an assertion **per case**; a counter belongs only to a shape allowed to fail. Nine of M1.1.11.1's last twelve findings were in the measuring apparatus rather than the code.

**An absence witness is worth a presence witness.** Half of this milestone's claims are negative — no constraint, no impulse, no wake, no obstruction, no exit. A negative assertion over a configuration that could never have produced the positive proves nothing. Every negative case ships with the **positive control** that proves the apparatus can observe the thing being denied.

**`tests/sensor_test.zig`** (create):

- One trigger, one body entering then leaving: exactly one `enter`, no delta across the maintained ticks, exactly one `exit`. The maintained ticks are asserted as **empty deltas**, not skipped.
- Aggregation: one entity carrying two bodies overlapping the same trigger produces **one** `enter`; removing one of the two produces **no** `exit`; removing the second produces one.
- Reflexive suppression: an entity carrying a trigger body and a solid body that overlap produces nothing at all.
- Orientation: two mutually detecting triggers produce **two** pairs, one per direction. With asymmetric masks, A sees B and B does not see A — asserted in both directions.
- Mask: a candidate on a layer outside the trigger's mask is never detected; the same candidate moved onto a layer inside it is. Same body, same pose, mask alone changed.
- Exactness: a candidate whose AABB overlaps the trigger's while the exact shapes do not is **not** detected. Without this case the suite cannot tell an exact traversal from an AABB one.
- Detection across classes: `static`, `dynamic`, `debris` and another trigger are each detected when the mask allows, and **none** of the four produces a constraint, an impulse or an island. Positive control: the same scene with `is_trigger` cleared does produce them.
- Sleep: a body that falls asleep inside a trigger produces **no** `exit`, and none on any subsequent tick while it stays asleep. Positive control: the same body, woken and moved out, produces one.
- The trigger itself asleep, or static, or never awake: detection is unchanged. A sleeping trigger detects an arriving body.
- Disappearance, four causes, one `exit` each: candidate removed, trigger removed, shape changed out of overlap, `is_trigger` cleared.
- No wake: a body detected by a trigger, and a trigger moved by pose write over a sleeping island, leave the sleepers asleep. Positive control: a real W4 teleport on the same scene wakes them.
- Order: a scene producing several deltas in one tick yields them sorted by complete entity value. Include two entities sharing an index across a generation wrap, deliberately, and assert they are distinct and ordered.
- Rebuild immunity: destroy a detected body, create another that reuses its `BodyId` slot, and assert the new one is not reported as still inside.
- Frozen surface pins: both `BodyDescriptor` fields by name and default; `CollisionShape` at 52 bytes.

**`tests/character_test.zig`** (modify) — non-obstruction, one case per collection path: a trigger straddling the walk path does not stop a sweep, does not appear in depenetration, and is never selected as ground. Each with its positive control — the same volume with `is_trigger` cleared **does** block, **does** depenetrate, **is** selected as ground. Without the controls these three cases pass on a controller that sees nothing at all.

**`tests/broadphase_test.zig`** (modify) — the reversed cells asserted negatively in both directions (`dynamic × trigger` and `trigger × dynamic`), joined to the three that were already `false`. The M1.1.1 assertion this replaces is **deleted**, not commented out.

**`tests/overlap_test.zig` / `tests/raycast_test.zig`** (modify) — a trigger body answers the query entries like any other, and a caller masking its object layer out does not see it. Both directions.

### Benchmarks

Reported, not gated. Cost of the pass on a resting scene — the case sleep does not economise — and on a scene with many triggers and many candidates. Interleave runs rather than best-of-three; best-of-three does not decide a gap under 5 %.

## Gate sequence

Six gates, push-early, STOP/GO on real pushed diffs. One fenced block per verdict.

- **A — the frozen surface.** The two `BodyDescriptor` fields, `CollisionShape` and its two pins, `BodyFlags`, the mask column and its accessor. No algorithm, nothing observable. This is the irreversible part and it is reviewed alone, before a line of algorithm exists.
- **B — broad class and the matrix.** Assignment priority at creation; the `trigger` row and column to `false`; the M1.1.1 assertion reversed and its replacement asserted in both directions. **The reversal is recorded in the log with the M1.1.1 decision it revises**, not merely committed.
- **C — the controller's three exclusions.** The three collectors, each with its positive control. Testable with no sensor pass in existence: this gate is about what the controller does **not** see.
- **D — the traversal.** Trigger-proxy enumeration, trees and unbounded lists, unilateral mask, exact narrowphase confirmation. Produces a set, no deltas yet. **The wiring table is due at this gate.**
- **E — state and deltas.** Entity aggregation, reflexive suppression, orientation, full rebuild, the two differences, the total order. Sleep and disappearance are proved here.
- **F — closure.** Bench reported, the query-visibility cases, `CLAUDE.md` patch **inside the PR**, PR opened, then every check verified one by one **at the end** — the total is only legible then.

## Conventions

- **Branch:** `phase-1/forge/sensors-triggers`
- **Final tag:** `v0.11.13-sensors-triggers` — patch 13 for sub-milestone 13, the normal bijection. `vMAJOR.MINOR.PATCH-slug` per `engine-development-workflow.md` §2.3.
- **PR title:** `Phase 1 / Forge: sensors and triggers`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`). Squash trailer: `Closes M1.1.13.` then `Brief: briefs/M1.1.13-sensors-triggers.md`. No `Co-Authored-By` exists anywhere in this history and none is added.
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

**The phantom exit is the way this milestone ships a silent bug, and it has exactly one cause.** Any path that derives membership from something the sleep system filters — step 4's pair walk, the retained candidate set, the moved logs — produces an exit for a body that did not move. It will pass every test written on moving bodies. The pass is independent of the solver for this reason and for no other, and the sleeping-body case is not an edge case in this suite: it is the suite's centre.

**`computePairs` returns a delta, and reading it as a snapshot is the second way.** Two motionless overlapping bodies do not appear in it. A traversal that reuses it will look correct in every test where something moves and be empty in the frame where nothing does. A second call after step 10 does not repair this: proxy updates may repopulate the moved logs, so the result is not reliably empty either — it is simply not the current set. Neither state is a snapshot.

**Never derive engine behaviour from a validation criterion.** C1.1 lists « sensors » in the exit gate; it does not specify the mechanism. The mechanism is `engine-physics-solver.md` §1.13 and nothing else.

**Positive controls, not because tests should be thorough but because half the claims are negative.** « No exit was produced » is satisfied by a pass that produces nothing at all. « A trigger does not block the controller » is satisfied by a controller that collides with nothing. Each negative case is paired with the positive that proves the apparatus can see what is being denied. This is the M1.1.11.1 apparatus-defect class, and it is the dominant one in this milestone by construction.

**The matrix reversal touches a decision, not a value.** M1.1.1 chose `dynamic × trigger = true` and wrote a test asserting it. That choice was reasonable then — nothing consumed the trigger class — and is wrong now. Record it as a revision with its reason; a matrix cell flipped without a trace is indistinguishable from a typo six months later.

**Two entries added to `BodyDescriptor` and zero to `PhysicsModule`.** If the implementation finds itself wanting a function entry — `setBodyTrigger`, `getOverlaps`, anything — that is a STOP and a Claude.ai round-trip, not a decision to take in session. One window remains after this one and it is not this milestone's to spend.

**The `dynamic` broad class holds kinematic bodies, and that will look wrong.** It is what moves, not what is simulated. A reviewer reading `dynamic` as « simulated by the solver » will call the character presence's placement a bug. It is not; the name is.

**Tooling facts established at M1.1.11, M1.1.11.1 and M1.1.12, not to be rediagnosed.** The leak checker's `safety` flag is false by default in ReleaseFast. The pre-commit hook lints only staged files. The build runner's `failed command:` line appears on tests that pass while writing to stderr, and is not a failure. A pipeline's status is its **last** command's: capture `$?` before any filtering and keep the **complete** log on failure. `zsh` does not word-split an unquoted flags variable. Best-of-three does not decide a gap under 5 %: interleave the runs. On a `pull_request` event, `paths-ignore` filters evaluate over **every** file in the PR. `bench.yml` carries `timeout-minutes: 10`, marginal on the Windows runner: a cancellation with no output there is a budget overrun rather than a hang — rerun before suspecting the code. `git checkout <path>` restores from the index and destroys unstaged edits with no prompt — copy to the scratchpad before injecting a counter-factual break, or commit first and revert against the commit.

**The `f64` leg still runs on one machine only.** `-Dphysics_f64` appears nowhere in `.github/`. Every claim this suite makes at `f64` rests on a single target and a single compiler. That is not this milestone's to fix — M1.1.14 owns CI — but a per-precision pin written here carries that standing, and should be worded as a measurement rather than as a property.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone. The log is not a marketing report: it serves review and post-mortem debugging.*

## Execution log

**2026-08-10 — Étape 0 to 2.** The eight specs were attached with the opening message, so the
Étape 0 request was moot and not issued. Branch cut from `main` at `cbb02b3`, brief copied
verbatim (`diff` empty), specs read in the brief's order, status `ACTIVE`.

**2026-08-10 — Gate A, the frozen surface.** Four production files, one test file, no algorithm.

- `api/types.zig` — `BodyDescriptor.is_trigger: bool = false` and
  `trigger_layer_mask: u32 = 0xFFFFFFFF`, transcribed from `engine-tier-interfaces.md` §1 at
  version 0.9 name for name and default for default. Pinned in the existing
  `BodyDescriptor defaults` test by name, default and type, plus a **field-count assert at 16**
  (the by-name references catch a rename or a removal at compile time; only the count catches an
  addition, which after the M1.1.15 freeze is an ABI break — `engine-c-api.md` carries no
  `struct_size` and no minor version). §1.13.1 is pinned as an ABSENCE: no `ShapeDescriptor`
  payload carries either field, walked over the union rather than asserted on one variant.
- `api/components.zig` — `CollisionShape.trigger_layer_mask`. **48 → 52**, `alignOf` unchanged at
  4, and the three offsets that decide it are pinned rather than described: `collision_layer` at
  44, `is_trigger` at 45, the `u32` at 48 — so bytes 46 and 47 are padding in the 48-byte layout
  and stay padding in this one, the struct gaining four full bytes and reusing nothing. The
  comptime assert and the test that doubles it were changed in the same edit, offsets included. A
  second test pins the authoring pair (`CollisionShape.is_trigger`/`trigger_layer_mask` against
  the descriptor's) with a field count of 7.
- `forge_3d/body.zig` — `BodyFlags.is_trigger` takes one of the five `_reserved` bits
  (`_reserved: u5` → `u4`), byte unchanged and asserted so. The disjointness of the new bit is
  asserted against `sleeping` with a discrimination guard (the two byte patterns differ), without
  which the mask comparison would hold between one state and itself. `Body` gains a
  `trigger_layer_mask: u32` **column** beside `collision_layer` — 32 bits do not fit in the flag
  byte, and the two object-layer quantities (what the body IS, what it SEES) belong together.
- `forge_3d/body_manager.zig` — `addBody` stores both; the mask is stored VERBATIM on a
  non-trigger body rather than zeroed, so the column never becomes a second representation of the
  role. New stale-safe getters `isTrigger` and `triggerLayerMask`.
- `tests/body_manager_test.zig` — one wiring case. It carries its positive control (a body that
  declares the role reads back as one, so `isTrigger` cannot pass by returning a constant), a
  mask value that is neither the default nor zero (so a dropped assignment and a zeroed column
  are distinguishable), and the instance-not-geometry proof: the three bodies share ONE `ShapeId`
  and disagree on the role.

Green at `f32` and `f64` × Debug and ReleaseSafe: `test-forge-3d` 495 → **497/497** in all four
corners. `zig build test` 1669/1686 (17 skipped, pre-existing), of which the `weld_forge` api
target 14 → 15. `zig fmt --check` and `zig build lint` clean on the tree.

**Gate A, STOP 1 — the layout comment stated two false facts.** The first version explained the
48 → 52 growth by "the three trailing padding bytes are consumed", and both halves were wrong:
the three padding bytes are INTERNAL (after `shape_type`, bytes 1..4), the 48-byte layout's
trailing padding was TWO bytes (46, 47), and those two are not consumed at all — the `u32` needs
4-alignment, so it starts at 48 and they stay padding. MEASURED with a temporary `@compileLog`
over all seven offsets rather than re-reasoned: `0, 4, 16, 28, 44, 45, 48`, size `52`. The three
offsets that decide the size are now pinned in the comptime block AND in the test that doubles
it, so a different arrangement landing on 52 fails; the comment was rewritten to say no more than
those pins establish. This is the "justification the code does not carry" class — a layout
comment nothing asserts is a source that gets cited six months later.

**Gate A, STOP 2 — three assertions whose REACH was never measured.** Same class as STOP 1, one
level up: the gate had fixed the comment, not the oracles.

- **The offset counter-factual was of the wrong kind.** Moving the expected value from 44 to 43
  changes the CONSTANT, so it establishes only that the line executes; no layout ever changed, and
  the oracle was never confronted with the object it claims to judge. Redone by PERMUTATION:
  `collision_layer` and `is_trigger` swapped in the declaration — both one byte, both adjacent, so
  size 52 and alignment 4 are preserved and only the two offsets exchange. Measured in two runs:
  with the three offset asserts REMOVED the whole suite still passes **497/497**, so the size and
  align pins genuinely accept the permutation; with them present the build fails at
  `components.zig:108`. That is what proves "size alone would not catch it", which the comment
  already claimed. The rule generalises and is the M1.1.12 closing method one level over: an
  oracle that judges a LAYOUT must be tested by a change of layout, never by a change of its own
  constant — as an oracle judging a ROUNDING must use an arithmetic that rounding cannot reach.
- **Both field counts measured**, and each with a probe chosen so that the count line is the only
  one that moves. `CollisionShape`: a `u8` inserted between `is_trigger` and `trigger_layer_mask`
  lands in padding byte 46, so size, alignment and the three pinned offsets are all unchanged and
  the run reports `expected 7, found 8` at `components.zig:187` with every prior assert green.
  `BodyDescriptor`: one appended defaulted field gives `expected 16, found 17` at `types.zig:889`.
  The "counter-factual measured" mention was written only after the measurement, never before.
- **The absence loop now counts its visits** and asserts 5 of 12 variants. Measured against a real
  union change rather than against the constant: giving `cylinder` a payload reports
  `expected 5, found 6` while the variant total of 12 still passes.

**Gate A, STOP 3 — the visit counter was justified by a case where it adds nothing.** The comment
said it existed because "nothing else would notice a struct variant being REMOVED", and that is
false: a removal drops the total to 11 AND `visited` to 4, so both lines fire and the total alone
would suffice. What the counter alone carries is a change of DISTRIBUTION at a CONSTANT total — a
`void` variant gaining a payload, or a `struct` variant losing one — which is exactly what the
measurement already demonstrated. The measurement was right and the sentence above it described
something else. Rewritten on redistribution, in both senses, with the division of labour stated:
the total sees variants appear or disappear, the counter sees the loop's reach move under a
constant total.

**And the second sense is deliberately NOT measured**, recorded here so a later reader does not
read the omission as a gap. `expectEqual` is a single code path and an equality falls on both
sides by construction; once the line is established as reached and sensitive to `visited`, both
senses are covered. The two-directions rule bites on a THRESHOLD, where each side is a distinct
path — not on an equality. Applying it here would be zeal, and zeal costs rounds exactly as an
omission does.

**Tooling fact, self-reported, in its CONCRETE form** — the generic version is already in the
frozen Notes ("a pipeline's status is its last command's"), and the generic version is not what
bites. Piping `git commit` masks the `commit-msg` rejection, so `$?` is the filter's and reads 0;
the commit does not land and HEAD is unchanged; the `git push` launched behind it therefore
answers **`Everything up-to-date`**, which reads as success. Net effect: a gate reported as pushed
that was never pushed — the exact failure the push-before-signal rule exists to prevent. The only
signal left in the piped output is lefthook's GLYPH, `✔️` when the hook passed and `🥊` when it did
not, which is what caught it here. Run `git commit` unpiped, confirm with `git log --oneline -1`,
and after any push compare `git ls-remote origin <branch>` against `git rev-parse HEAD`. The title
that triggered it was 74 characters against the 72 the hook counts over the FULL first line.

**Second tooling fact, and it belongs BESIDE the first because they are one schema.** A RELATIVE
redirection from the repo's CWD — `git push > $PWD/../push.log` — writes into the repo's PARENT, so
the file lands outside the working tree and `git status`, the tool queried next to confirm the tree
is clean, can never see it. It reported clean, correctly and uselessly. Together with the piped
`git commit` above: in both cases the effect leaves the region the verifying tool inspects, so the
check is not weak but VACUOUS — it would report the same thing whether or not the fault occurred.
That is the same class as the engine-side rule this milestone has been applying all along, that a
counter-factual must change the OBJECT and not the expected constant: a probe that cannot reach the
fault proves nothing about it. Every write path is ABSOLUTE and points into the session scratchpad,
without exception.

**Closed without action, same review:** `trigger_layer_mask` carries no domain guard where
`collision_layer` is rejected outside `[0, 32)` by a typed error, and that asymmetry is correct.
A layer is a bounded INDEX; a mask is a SET whose every 32-bit pattern is legal, `0` included —
a trigger that detects nothing is a configuration the game may want. There is nothing to reject.

**2026-08-10 — Gate B, broad class and the matrix.**

**THE MATRIX REVERSAL IS A REVISION OF AN M1.1.1 DECISION, not a corrected value**, and it is
recorded as such because a matrix cell flipped without a trace is indistinguishable from a typo
six months later. M1.1.1 set `dynamic × trigger = true` and wrote a test asserting that pair
positively. The choice was reasonable then — nothing consumed the `trigger` class, so the
permissive cell cost nothing — and it is wrong now that the class carries a meaning. The whole
`trigger` row and column go to `false`: a trigger detects without responding, so every pair that
cell produced would have to be discarded downstream, on a hot path, every tick. Killing them at
the source is less work AND a stronger guarantee — a trigger never reaches step 4 of the cycle, so
no downstream filter can forget it (`engine-physics-solver.md` §1.13.3). The consequence is
written where the constant is declared: **the sensor pass cannot consult this matrix**, it reads
`false` everywhere, and detection is filtered by the trigger's own unilateral mask instead.

The M1.1.1 assertion was **deleted**, not commented out. Its replacement asserts the absence of the
same pair on the same scene, and a second test reads the constant directly in BOTH index orders
across the whole row and column, plus full symmetry of the matrix — which no single-cell assertion
states. Both carry positive controls: the emission test keeps the four allowed combinations, on a
scene of eight boxes at one point, so an implementation emitting nothing satisfies every negative
and fails those; the matrix test asserts three cells outside the trigger row still `true`,
including `dynamic × dynamic` on the diagonal, without which an all-false matrix would pass.

**Broad-class derivation** lands as `BodyManager.broadLayerFor(is_trigger, body_type)` — a pure
function of the two, so it is available before a body exists, which is what a caller inserting a
proxy needs — plus the stale-safe per-body `broadLayer`. **Derived and never stored**: the class is
a function of two facts already in the store, and a fourth column would be a second representation
that can drift. Priority fixed, role first: a trigger goes to `trigger` whatever its body type,
then `static` to `static` and **everything else to `dynamic`**. A kinematic body therefore lands in
`dynamic`, deliberately — the class names what MOVES, not what is SIMULATED, and a moving platform
must pair against statics exactly like a simulated body. `debris` is produced by no input and that
absence is pinned; there is deliberately no `is_debris` field.

**Three counter-factuals measured, each by changing the OBJECT and never an expected constant.**
The count first read "four", and the discrepancy was settled by going back to the run logs rather
than by lowering the word to fit the table: three probes ran, and the first of them made TWO tests
fall — the emission test and the matrix test — which is what got miscounted. Failures are not
probes, and a table of probes is not a tally of failures.

| Change | Result |
|---|---|
| `dynamic × trigger` restored to `true` (the M1.1.1 value) | `rc=1`, both the emission test and the matrix test fall |
| the role-priority line disabled, body type deciding alone | `rc=1`, both broad-class tests fall |
| `kinematic` mapped to `static` instead of `dynamic` | `rc=1`, both broad-class tests fall |

Every probe restored by copy from the scratchpad, `git diff --stat` checked after each.

**Instruction received in session for GATE D, recorded now so it is not lost by the time that gate
opens.** The sensor harness must DERIVE its insertion layer by calling `broadLayerFor`, never pass
it as a literal. The 32 insertions in the existing suites all pass it hard-coded — 16 `.dynamic`,
5 `.static`, 5 `.debris`, 6 `.trigger` — and following that reflex would put the trigger in
`.trigger` by COINCIDENCE rather than by the rule: the rule would be tested beside the pass, and
the path producing the observable result would never exercise it. With the derivation, the gap
between this harness and what `PhysicsWorld` will do narrows to one question — does M1.1.15 call
the same function — instead of an open assumption about tree topology. Not closable here, but
reducible to a line.

**2026-08-10 — Gate C, the controller's three exclusions.**

**The three collectors shared one filter shape and now share one FUNCTION.** `GroundCollector`,
`SweepCollector` and `WorstOverlap` each carried the same two lines — the stale-safe layer read and
the mask test — and the trigger refusal is exactly the kind of rule that gets posted in two places
out of three. `admitsCandidate(bm, body, layer_mask)` is written once and called by all three, and
the refusal ORDER is normative rather than incidental: stale, then TRIGGER, then mask. The role
comes before the mask because it is absolute and belongs to the engine, where the mask is the
caller's choice (§1.13.7) — a controller sweep IS a physical response, so the exclusion is the
contract and not an option. The asymmetry with the query family is written at the same place so
nobody restores symmetry by reflex.

**A fourth body-visiting path was examined and deliberately left alone**, named here rather than
discovered later: `CandidateWaker` (`character.zig`) wakes every broadphase candidate of a moving
presence. It is NOT one of the three collection paths — it consults no layer and no role, being a
deliberate SUPERSET, since W4 exists so a sleeper is never missed and waking one that turns out not
to be in contact costs a tick. Waking a trigger is not a §1.13.7 violation either: the cause is the
character's pose write, not the trigger's detection, and a trigger carries no constraint for the
wake to serve. Left untouched, with the reason, rather than extended on reflex.

**The harness derivation lands HERE and not at gate D**, which is a deliberate early application of
the instruction recorded above, and the reason is that gate C is the first gate that puts a trigger
body in a world at all. `solver_test.zig` carried its own `broadphaseLayer(bt)` —
`if (bt == .static) .static else .dynamic` — correct for the two classes it knew and with no
opinion about the role. Building gate C's scenes on it would have put the trigger in `.dynamic` by
omission, so the tests would have been written on the reflex the instruction forbids. It is deleted
and the one call site now calls `BodyManager.broadLayerFor(desc.is_trigger, desc.body_type)`.
`solver_test.zig` is outside the brief's file list; this is the written justification.

**Four cases, one per observable, each paired with its positive control on geometry identical
to the byte** — same shape, same pose, same displacement, `is_trigger` the only difference. The
three closed forms are NOT re-derived: each control reproduces a value an existing test in this file
already establishes (the wall at `1.7 − padding·3/√10`, the 30° depenetration at
`(0.02, 0.0315470)`, the resting plane `.grounded` with normal `+Y`), because re-deriving them
would be a second source for one geometric fact — the defect class §1.12.6 names. The sweep case
asserts the role reached the store and the body really is in the `trigger` class, so a descriptor
field that failed to arrive cannot make the test pass for the wrong reason. The ground case asserts
all five absence quantities including both HANDLES: an `.in_air` verdict still naming a support
would be reporting two contradictory things.

**Two counter-factuals measured, in OPPOSITE directions**, which is what the paired structure is
for:

| Change | Result |
|---|---|
| the trigger refusal disabled inside `admitsCandidate` | `rc=1`, all gate-C tests fall — so each negative branch is carried by the refusal and not by an accident of geometry |
| `admitsCandidate` forced to refuse EVERYTHING | `rc=1`, the same tests fall again (through their positive controls) along with 34 others — so the controls are not vacuous |

**Gate C, STOP — a fourth case, and it is about a VERDICT rather than a position.** `WorstOverlap`
serves TWO call sites, not one: the depenetration (`character.zig:1278`) and the target-volume
occupancy test of `resizeCharacter` (`:1811`). The shared filter covers both by construction — that
is what sharing it is for — but the OBSERVABLE differs, and none of the three cases above exercised
it: they read a position back, and `resizeCharacter` answers a `bool`. What would have been
retested is not the collector, it is the verdict.

The consumer is named and was verified at source: `engine-movement.md:895` gates the stand-up on
that return (`if physics_resize_character(entity, vc.radius, config.stand_height)`). A trigger
ceiling answering `false` is a crouched character inside a detection volume who can never stand up
again — silent in unit tests, visible only in play. The same document (`:863-865`) records that the
occupancy test was moved INSIDE the operation precisely so it could not disagree with it; a trigger
counting as an occupant brings that disagreement back in another form.

Added: a TRIGGER ceiling on geometry identical to the existing
`growing under a low ceiling returns false and changes NOTHING` (`character_test.zig:2292`), which
is reused AS IT STANDS as the positive control — same plane, same slab whose underside is at
y = 1.2, same 1.0 m character, same target height, `is_trigger` the only difference. The success is
asserted on the three quantities a resize actually moves (height, the presence's centre, the new
capsule's half-height) plus the preserved `BodyId` and the live-shape count, because `true` alone is
satisfied by an entry that returns it and does nothing. The base is asserted UNCHANGED: the resize
is anchored at the feet, so the volume grows upward and the base never moves — the reviewer's "the
position changes" reads on the volume, not on the anchor, and the assertion says which. Same
counter-factual as the other three: with the refusal disabled it falls.

**2026-08-11 — Gate D, the traversal.** Unblocked by the §1.13.6 revision (see Recorded deviations).

`pipeline/sensor.zig` created — sibling of `sleep.zig`, a pipeline-level sweep with no `rigid/`
type in it. `collectOverlaps` enumerates the TRIGGER proxies and descends per trigger; the output
is body-level `BodyOverlap{trigger, other}`, the entity mapping and the deltas being gate E.

Three additions outside the brief's file list, each required by the traversal and justified here:
`Bvh.forEachLeaf`, `Broadphase.forEachInLayer` and `Broadphase.queryHalfSpaceTrees`. There was no
way to enumerate one layer's proxies — `leafCount` gives a count and `isLiveLeaf` a test, neither an
iteration — and §1.13.5 requires that direction by name. `forEachLeaf` walks the node POOL and
`isLiveLeaf` makes it exact in both directions: `freeNode` sets a retired node's height to `-1` and
an internal node's is `>= 1`, so `height == 0` admits live leaves and nothing else.
`forEachInLayer` covers the tree AND the layer's unbounded slots, without which a half-space trigger
would be silently skipped. `queryHalfSpaceTrees` is named for what it visits because what it OMITS
is the point — it does not walk the unbounded lists, the only pair that could yield being
half-space against half-space, which the domain bound excludes.

**The domain bound was DECORATIVE when first written, and that was caught by asking what its
counter-factual would look like rather than by a failing test.** With the bound removed, the
`orelse return` in the flipped branch returned the same answer for the same pair, so the bound
changed no behaviour and could have been deleted without a single test noticing — the exact shape
of "a guarantee that is stated and not carried" this milestone has been chasing since gate A. The
flipped branch now UNWRAPS the candidate probe: the trigger being non-convex there, the bound has
already guaranteed the candidate is convex, and `classOf` succeeded one branch above, so null is
provably unreachable. Removing the bound now fires in Debug and ReleaseSafe.

**Three counter-factuals measured, each by changing the OBJECT:**

| Change | Result |
|---|---|
| the domain bound removed | `rc=1` — the out-of-domain test aborts on the unwrap, so the bound is load-bearing |
| the probe forced to the CANDIDATE always | `rc=1` — configuration (a), convex trigger × mesh, aborts |
| no flip rule at all (the naive trigger-probe-only form) | `rc=1` — configuration (b), half-space trigger × convex, fails |

A fourth was attempted and **discarded as not modelling what it claimed**: forcing
`probeOf(trigger)` into the `if` merely reproduced the rule, because a half-space has no support
shape, so the optional was null and control fell to the same flipped branch — `rc=0`. It is
recorded rather than dropped, because a counter-factual that passes is either a proof the rule is
structural or a badly built probe, and here it was the second.

**Gate D, STOP — the pool walk had no test, and my own framing of the gap was too wide.** I had
reported `forEachLeaf` as established by reading rather than by measurement and deferred it to gate
E. Both halves were wrong, and both were corrected by verifying at source rather than by argument:

- **The gap is NARROWER than I described.** `forEachLeaf` writes no predicate of its own — it
  delegates to `isLiveLeaf`, which pre-dates it and which PRODUCTION already consumes
  (`broadphase.zig:1246`, the stale-id skip in `computePairs`). So the predicate is exercised. What
  was not exercised is the POOL ITERATION: the existing suites do hole the node pool — `remove` at
  `broadphase_test.zig:121, 142, 183, 191, 202` — but none of them walks it, and `forEachInLayer`'s
  only consumers are `sensor.zig` and the new suite.
- **And it could NOT wait for gate E, because the deferral rested on a false equivalence.** Gate E
  recycles a `BodyId`, which lives in `slot_alloc`'s pool. The BVH node pool is a DIFFERENT pool
  with its own `free_list` (`broadphase.zig:145`). Holing one does not hole the other, so gate E's
  case would never have covered this one.

The weight is what makes it urgent rather than tidy: `forEachInLayer` enumerates THE TRIGGERS
THEMSELVES, so it is the entry point of the whole pass, and a missed leaf is a trigger that detects
nothing in silence — with everything gate E measures resting on top of it.

One case added, in this gate: three proxies inserted, one retired — the walk reports the two
survivors and not the retired one, so the non-vacuity lives inside the same case — then reinserted
so the slot is RECYCLED, and the leaf must reappear under the NEW `user_data` and never the old.
That is the configuration closest to the real danger, a slot coming back to life under another
identity. Two more assertions close the shape: the three survivors answer `isLiveLeaf`, so the walk
is not agreeing with a tree that lost its contents, and the reported count equals `leafCount`, so
internal nodes are not being reported as leaves.

| Change | Result |
|---|---|
| the liveness test dropped from the walk | `rc=1` — the new case aborts, and two sensor tests with it |
| the pool bound replaced by `leaf_count` (the stale-length form) | `rc=1` — the new case fails |

### Wiring table — every quantity the sensor suite measures (due at this gate)

| Quantity | Nature | Treatment |
|---|---|---|
| Membership of a named `(trigger, other)` pair | delivered guarantee | assertion per case (`hasOverlap`) |
| NON-membership of a named pair | delivered guarantee | assertion per case, each with a positive control |
| Cardinality of the rebuilt set | delivered guarantee | assertion per case (`out.items.len`) |
| Orientation — the pair appears from the trigger and not from the other side | delivered guarantee | assertion per case |
| Asymmetry under asymmetric masks (A sees B, B does not see A) | delivered guarantee | assertion per case, both directions |
| Broad class of each body in the scene | delivered guarantee | assertion per case (scene precondition, so a scene that drifted cannot pass for a rule that held) |
| The sensor role reached the store | delivered guarantee | assertion per case |

**There is no counter and no historical metric in this suite, and that is the whole table.** Every
quantity above is a delivered guarantee asserted per case; nothing here is a shape allowed to fail,
so nothing here is counted. If a later gate introduces a rate or a yield, it belongs to a form that
may fail and must be listed as such rather than folded in beside these.

NOT measured at this gate, and named so the table is not read as complete coverage of the
milestone: the two deltas, sleep invariance, entity aggregation, reflexive suppression and the
total order (gate E), and the bench (gate F).

**2026-08-11 — Gate E, first commit: the unbounded half of `forEachInLayer`, named.** Carried over
from gate D's closing reserve as a dedicated case rather than as a hope that a gate-E case would
happen to cover it — which would have been the false equivalence the previous STOP corrected. The
distinction that kept it out of a STOP is real and worth recording: that half IS covered and active
today, two gate-D cases building a half-space trigger and failing immediately without it, whereas
the pool iteration was covered by nothing at all. The risk was a future silent regression, not a
present hole.

One case, both halves asserted TOGETHER in one layer so each is the other's control — a walk that
lost either fails, and an empty walk fails twice — plus a proxy in another layer so "reports the
layer" is a statement about the layer and not about the whole broadphase.

| Change | Result |
|---|---|
| the unbounded half dropped | `rc=1` — the new case fails, with two sensor tests |
| the tree half dropped | `rc=1` — the new case fails, with two other sensor tests |

**Both runs were re-done.** The first attempt used a local named `ground`, which shadows a
file-scope declaration, so both probes failed to COMPILE and their `rc=1` measured nothing. The
runner now greps for `compilation errors` before reading any failure list, because a compile error
and an assertion failure are the same exit code and only one of them is a measurement.

**2026-08-11 — Gate E, the state and the two deltas.** `SensorState` lands in `sensor.zig`: the
current set, the previous copy kept for the comparison and for nothing else, and `entered` /
`exited` as the two set differences. **No hashed container anywhere on the path** — sorted flat
array, adjacent dedup, two linear merges — so the output order is a pure function of the SET,
independent of tree visit order, worker count and body insertion order (§1.13.11).

**Aggregation IS the dedup**, and that is why it needs no special case: several body overlaps
between the same two entities collapse to one pair, so the exit fires only when the last of them
disappears, which falls out of the set difference. The ordering comparator is **written out** —
index then generation — rather than bitcast to the packed `u64`: a bitcast would order by whichever
field the layout places high, which is a property of the layout and not a decision, and a later
field reorder would silently reorder the state.

**A reservation defect, found and fixed before it could be tested around.** `update` reserved
capacity on `current`, then swapped — which makes the reserved buffer the new `previous` and leaves
the rebuilt one short. The first version papered over that with a post-swap
`ensureTotalCapacityPrecise(...) catch unreachable` on a path that really can fail. The reservation
is now on `previous`, the buffer that BECOMES current, so every fallible step precedes the first
observable mutation and an allocation failure leaves the whole state exactly as it was — the
reserve-then-mutate invariant the ECS slot allocator holds. No `catch unreachable` remains in the
file.

**Two mechanisms added outside the brief's file list, each named here.** `BodyManager.setTrigger`,
without which §1.13.10's fourth disappearance cause has no mechanism at all — it carries the
statement that **the caller owes the proxy move**, the broad class being derived from the role, so
flipping the role without re-placing the proxy leaves a body the pass still enumerates as a trigger
or one no pass enumerates. And the harness `World` gains **step 10 bis**, off by default so every
inherited test keeps its exact cycle: after the proxy update, so poses are final and every proxy is
valid including sleepers', and before step 11, which is what makes a body's membership established
for the tick in which it falls asleep.

Ten cases, each with its positive control or its non-vacuity assertion inside the same case: one
enter / **empty deltas over three maintained ticks, asserted and not skipped** / one exit, and the
exit produced once rather than every tick; aggregation over two bodies of one entity, with the
two-overlap count asserted first so the collapse is not measured against a scene that only ever had
one; reflexive suppression, with the BODY-level overlap asserted present so it is visibly the
entity mapping that drops it; orientation, two pairs for two mutually detecting triggers; sleep,
where the body is asserted to have ACTUALLY fallen asleep before the invariant is read; a sleeping
DYNAMIC trigger still detecting an arriving body — dynamic, because a static body never carries the
sleeping flag and asserting on it would prove nothing; the four disappearance causes, each starting
from a detected state asserted first; the total order with two entities sharing an index across a
generation wrap, the order re-stated independently rather than by reusing the production
comparator; and the recycled slot, with same-index-different-generation asserted so it cannot pass
on an allocator that never recycles.

**Four counter-factuals measured, each firing on exactly the case written for it:**

| Change | Result |
|---|---|
| a SLEEP FILTER added to the traversal | `rc=1` — the sleep case fails: this is the phantom exit, the milestone's central defect, and it is caught |
| reflexive suppression removed | `rc=1` — the reflexive case fails |
| the adjacent dedup removed | `rc=1` — the aggregation case fails |
| the sort removed | `rc=1` — the order case fails |

**2026-08-11 — Two corrections carried into gate F.**

**The step-10-bis comment claimed two things at one rank and carried only one.** Marked in place,
as at gate A: sleep invariance is MEASURED (the sleep-filter counter-factual makes the case fail),
pose finality is a DESIGN REASONING NOT MEASURED. It is deliberately not measured — a scene
calibrated so the NGS correction crosses a trigger boundary within the tick costs more than it
would establish, and the placement after step 10 is correct by the DEFINITION of the poses the tick
publishes rather than by that probe. The argument stays in the spec, which is its place; the comment
now records that it is an argument.

**Third tooling fact, and it is RETROACTIVE.** A compile error and an assertion failure share the
exit code, so a counter-factual can have "failed" while measuring nothing. Replayed the
`compilation errors` grep over the retained logs of EVERY counter-factual of the milestone, gates A
to E — 27 logs, all timestamped:

| Verdict | Count | Which |
|---|---|---|
| assertion failure — a real measurement | 21 | every gate-B to gate-E probe bar the four below |
| GREEN, expected | 3 | the pins-REMOVED half of the gate-A permutation; the `test-forge-3d` run of the `CollisionShape` field-count probe, whose target does not carry the api tests and which was re-run in full; and the fourth gate-D probe already reported as passing and therefore badly built |
| compile error that IS the measurement | 2 | `cf.log`, `perm_with_pins.log` — `reached unreachable code` with a `called at comptime here` note at `components.zig:108`, i.e. the comptime `@offsetOf` assert firing |
| compile error that is NOT a measurement | 2 | `cf_unbounded.log`, `cf_tree.log` — a local shadowing a file-scope name; already found, already redone as `cf_unbounded2` / `cf_tree2` |

**No unknown probe was invalidated**, so no guarantee of this milestone rests on a run that
measured nothing. But the sweep refined the rule rather than merely applying it: **for a COMPTIME
assert the compile error IS the result**, so grepping `compilation errors` is a filter and not a
verdict — `reached unreachable code` plus `called at comptime here` is the assert firing, anything
else is a broken probe. Recorded that way in the tooling note.

## Recorded deviations

**2026-08-11 — `engine-physics-solver.md` §1.13.6 REVISED, lifting the gate-D blocker.** The
milestone's first design blocker (see Blockers encountered) was resolved by a Claude.ai round-trip
that revised §1.13.6 rather than by any choice taken in session. The revision lands two rules, and
the fifth option it takes was not among the four I journaled:

1. **The probe is fixed by a RULE, never by availability.** The trigger when the trigger is convex,
   the candidate otherwise, and **always the trigger** when both are. My own framing — "I can call
   it in either direction" — was true of the ANSWER and insufficient for the spec: the overlap
   boolean is symmetric in exact arithmetic, but nothing guarantees bit equality of the two orders
   in floating point, and M1.1.14 must be able to VERIFY that order rather than establish it.
2. **A pair whose two sides both carry a static-only shape is OUT OF THE DOMAIN of detection.** No
   membership, as a written and motivated bound, tested as such — which is the exact difference
   between my option 1 (skip silently) and what was asked, and it is my own objection to silence
   that produces it.

> **SUPERSEDED 2026-08-11 by the second closing review — rule 2 above no longer holds and the
> paragraph that followed it was doubly wrong.** It read: "the uncovered cell is ALWAYS static ×
> static, `addBody` forcing it, so its overlap cannot vary under simulation — one entry on the first
> tick and never a transition again." Both halves fell. A static body IS movable by pose write, so
> the overlap can vary; and the partition itself was wrong, grouping half-space and mesh by BODY
> TYPE where the question is whether the shape has an INTERIOR. §1.13.6 now REMOVES the bound
> entirely: the sensor role is refused on a mesh by typed error, the half-space keeps it, and the
> two missing kernels are written. Kept in place rather than rewritten, because what this entry
> recorded is what was decided then — and the milestone's own rule is that a correction upstream
> must not leave its earlier version standing downstream unmarked.

Why the other three were refused, recorded so they are not re-proposed: option 4 (refuse the role
to non-convex shapes) would amputate a case that works — a half-space trigger against a convex is
the kill plane under the level — to eliminate one that carries nothing, and the role stays a
property of the INSTANCE (§1.13.1) while it is the PAIR that is bounded; option 3 (write the
kernels) would narrow the hole rather than close it, mesh × mesh remaining, so the bound would be
needed anyway and the two cheap kernels would avoid no decision; option 2 (assert) is excluded by
the warning carried by the very file I cited in raising the blocker.

**The gate-C half-space trigger is unaffected and stays as it stands.** No FROZEN SECTION text was
modified.

## Blockers encountered

### 2026-08-11 — Gate D. Which exact kernel confirms a NON-CONVEX trigger against a NON-CONVEX candidate

**Status: LIFTED 2026-08-11** by a Claude.ai round-trip revising `engine-physics-solver.md`
§1.13.6 — see Recorded deviations. The entry below is kept as it was written, because what it
established about the uncovered cell is what the fifth option was built on.

**Blocked before any line of `sensor.zig` was written.** Nothing was half-built.

**What the recon established, verified at source and not inferred.**

`BodyManager.overlapShapeBody(store, body, probe, pose, back_face_mode) ?bool` is the exact
predicate §1.13.6 asks for — "GJK on the cores, accepted as soon as the regime is not `separated`"
— and it dispatches on all three classes of the BODY side (`body_manager.zig:1044`): convex via
`gjk`, half-space via `plane.separation(...) <= 0`, mesh via a per-triangle traversal. Its PROBE
side, by its type, is a bounded convex `SupportShape`.

So for a pair (trigger T, candidate C) the predicate is reachable in either direction:

| T | C | Covered by |
|---|---|---|
| convex | convex, half-space, mesh | probe = T, body = C |
| half-space, mesh | convex | probe = C, body = T (the predicate is symmetric) |
| half-space, mesh | half-space, mesh | **nothing** |

**The uncovered cell is reachable, and that is the whole problem.** `addBody` refuses a half-space
or a mesh on a non-static body (`error.ShapeMustBeStatic`), but a STATIC one carrying either is
legal — this milestone's own gate-C suite already creates a half-space trigger — so a kill-plane
trigger and a static level mesh in one scene land exactly there.

**The existing `unreachable` cannot be borrowed.** `collidePairEachOrdered` has the same hole and
declares it an asserted precondition (`body_manager.zig:1556` and `:1571`), but its stated motive is
that "it presumes its pair came from `computePairs` under a correct layer assignment, and a
static↔static pair is a programming error at the call site". **The sensor pass satisfies neither
half of that**: it does not come from `computePairs` (§1.13.5 forbids it — that output is a delta,
not a snapshot) and it cannot consult `default_layer_pairs` (§1.13.3 — it reads `false` everywhere
after gate B). Those very comments warn against justifying an assertion by a property the code does
not carry; reusing it here would be doing exactly that.

**Four answers, none of them free, and none of them mine to take.**

1. **Skip the pair silently.** A false negative on a legal scene — the class this corpus refuses by
   name (§1.11.3, "fail loudly rather than miss silently").
2. **Assert / `unreachable`.** Crashes a scene the engine allowed the caller to build, and only in
   Debug and ReleaseSafe — undefined behaviour where the game runs.
3. **Write the missing kernels.** Half-space × half-space, half-space × mesh, mesh × mesh overlap
   predicates. The first two are cheap; the third is a real piece of work (triangle-vs-triangle over
   two static trees) that nothing in the corpus has scoped, for a pair whose answer is CONSTANT —
   both bodies are static and neither can ever move.
4. **Refuse the role at creation for a non-convex shape**, by typed error, so the trigger side is
   always a bounded convex and the table above collapses to its first row. Cheapest and total — but
   it changes `addBody`, i.e. the surface gate A froze and two reviews accepted, and it forbids a
   half-space trigger, which is a plausible object (a kill plane under the level) and which the
   gate-C suite already builds.

**Why I am not choosing.** Option 4 rewrites a frozen-surface behaviour; options 1 and 2 each
instantiate a defect class this milestone has spent three gates refusing; option 3 is unscoped work
whose payoff is a constant answer. The brief's scope says detection is confirmed "under the
conditions of §1.11.12" and does not name the case where neither side can be the probe, and §1.13.6
does not either.

**What I need back:** which of the four, or a fifth. If it is 4, the gate-C half-space trigger has
to move to a box and that is a one-line change I will make in the same pass.

**2026-08-11 — Closing review, NO-GO, two corrections.** Both findings verified at source before
acting, and both hold.

**P1 — `setTrigger` becomes ONE-WAY, `clearTrigger`.** The mechanism is confirmed and is worse than
a precondition: `World.active` is not merely persistent, it is NEVER pruned — its own comment makes
that a CORRECTNESS condition of sleep (§1.8.7) — and `contact_constraint.zig` contains ZERO
occurrences of `isTrigger`, so `build` revalidates nothing. A body flipped `false` → `true` would
therefore keep producing constraints and impulses for its already-accumulated pairs, indefinitely,
while the sensor pass reported it as a trigger: a body that both detects and responds, which
§1.13.1 states does not exist.

The fix is not to document the precondition. The asymmetry neither the finding nor my doc-comment
had drawn: **the `true` → `false` direction is SAFE**, because a body in the `trigger` class has NO
retained pairs at all — the row and column have been `false` since gate B — so it has nothing to
purge, only a proxy to re-place. And it is the only direction with a consumer here, the fourth
disappearance cause being the LOSS of the role. So the dangerous direction ceases to be offered
instead of being documented: `clearTrigger(id)`, no value parameter, its doc-comment carrying the
exact mechanism and what M1.1.15 must orchestrate atomically the day it opens the other direction —
purge the retained pairs, re-place the proxy, wake the bodies that lose their support. Pinned as an
ABSENCE (`!@hasDecl(BodyManager, "setTrigger")` plus the parameter count), since that is the form
the regression would take.

**P2 — the bound stands, its justification was FALSE, and the error was mine to transcribe.**
> **SUPERSEDED by the second closing review, below: the bound does not stand either.** What this
> entry corrected was the JUSTIFICATION; the next review found the PARTITION underneath it wrong
> too, and removed the bound outright. Kept in place and marked, rather than rewritten — leaving a
> corrected statement standing downstream of its correction is the class this milestone met three
> times.

`engine-physics-solver.md` §1.13.6 is revised again: I had written that two static bodies' overlap
cannot vary, and that premise is false — `setBodyTransform` allows a static body to be moved, and
this module treats the case EXPLICITLY, the world-AABB cache invalidation being conditioned on body
type precisely because a non-dynamic body reaching it signals an external teleport. Streaming,
removal and shape change produce transitions the same way. A half-space × mesh pair can therefore
enter and leave without ever appearing in the state.

The bound is maintained on its real motive — mesh × mesh has no kernel anywhere in the engine and
is a piece of work in its own right, and writing the two cheap cells would reduce the bound to one
without removing it, for configurations with no identified use. Three consequences here: the
comment in `sensor.zig` carries the revised motive and no longer claims the pair cannot change
overlap; the test's comment now pins a DELIBERATE LIMITATION rather than an impossibility, its
title being already correct; and the limitation enters `CLAUDE.md`'s open decisions with its owner
— the milestone that introduces a mesh × mesh kernel. An attributed debt, not a property.

No test was added for P2: the case exists, its STATUS changed.

**2026-08-11 — Second closing review, NO-GO, three corrections.** All three findings hold, and the
second invalidates a decision taken at the gate-D unblock rather than anything in the
implementation.

**P2 — the domain bound is REMOVED, and the partition under it was the error.** §1.13.6 revised a
second time. The bound grouped half-space and mesh as "static-only shapes" — true of the BODY TYPE
and irrelevant to the question, which is whether the shape has an INTERIOR. `mesh.zig` states it
categorically: a mesh is a SURFACE and not a solid, membership is false everywhere on it. A sensor
answers "who is inside"; a surface has no inside. Three consequences, all delivered:

1. **`addBody` refuses `is_trigger` on a `.triangle_soup` shape**, by typed
   `error.TriggerShapeMustBeSolid`, at the same rank as `ShapeMustBeStatic` and named on the
   INVARIANT so `HeightField` reuses it. The HALF-SPACE KEEPS THE ROLE — it is a volume with a
   well-defined interior, the kill plane under the level.
2. **The two owed kernels are written**, both analytic and neither iterating: two half-spaces meet
   unless their normals are exactly opposite with disjoint boundaries (antiparallel test at TRUE
   ZERO — two normals merely close to opposite still meet, in a wedge, so an epsilon there would
   deny a real overlap); and a surface meets a half-space iff one of its VERTICES does, which is
   exact by convexity, a triangle being the convex hull of its three so `n·x − d` attains its
   minimum at a vertex. The one that would have been a piece of work — mesh × mesh — is now
   unreachable, having required a mesh trigger.
3. **The bound, its comment and its test are gone.** The out-of-domain test is REPLACED by the
   typed refusal (with two positive controls: the same mesh without the role is accepted, and a
   half-space with the role is accepted) plus one case per new kernel.

**And removing the bound removed the motive for a NAME.** `queryHalfSpaceTrees` existed under that
name because the bound excluded the unbounded lists; with the bound gone the omission had no
justification left, so it is `queryHalfSpace` and visits trees AND unbounded lists, symmetric with
`queryAabb`. It is the omission that went, not merely the name.

**A duplicate predicate, caught by its own counter-factual.** Forcing the half-space × half-space
KERNEL to answer always-true changed no test — `rc=0` — because the traversal carried a COPY of the
same closed form and was deciding instead of it. The copy is removed: the broadphase BOUNDS
candidates and the exact kernel DECIDES, and for two unbounded shapes there is nothing to bound. Re-
run after removal, the kernel's counter-factual fires. A probe that passes is either a proof the
rule is structural or a badly built probe; here it was neither — it was a second implementation.

**P1 — the owed wake is NAMED and pinned with REAL sleep.** The non-activating contract is
unchanged and is §1.8.4's: the store holds neither broadphase nor islands and cannot wake. What was
missing is WHICH bodies are owed, and it is not obvious — clearing the role turns a volume that
opposed nothing into a SOLID, so the bodies owed are those already OVERLAPPING it, discoverable only
by a query. Pinned with `World.init` and not `initNoSleep`: a sleeping body inside the newly solid
volume stays asleep and interpenetrated for ten further ticks, because a sleeper emits nothing in
broadphase and the retained set never held a pair for a body in the `trigger` class. The positive
control performs the composition the doc-comment names — overlap query, then wake — and asserts the
query really found the sleeper.

**P3 — the class was swept, repo and brief.** Every formulation of the retracted premise was
grepped for and each hit classified rather than blanket-edited: most `static-only` occurrences are
the M1.1.11 usage and remain TRUE (those shapes DO force a static body — what was false is the
consequence drawn from it), and the `out of domain` hits in `character.zig` are unrelated domains.
Four sites carried the premise live and were taken: the suite header, the first closing-review entry
(marked SUPERSEDED in place rather than rewritten), the `CLAUDE.md` tag row, and the `CLAUDE.md`
open decision, **deleted** — there is no attributed false negative any more because there is no
false negative.

**2026-08-11 — Third closing review, NO-GO, two corrections. Both were reached by verifying the
claim at source first, and both are the SAME lesson met in three places.**

**P1a — the predicate walked the wrong collection.** `halfSpaceMeetsMesh` took the STORED vertex
array. `MeshData` validates `MeshIndexOutOfRange` — every index inside the array — and never the
converse, so an UNREFERENCED vertex is legal and an imported mesh may carry one. The convexity
argument in the comment is CORRECT and it is about the vertices of a TRIANGLE; the stored array is
not that set. A surface lying wholly outside the solid that merely stores an orphan inside it
answered overlap. It now walks the vertices referenced by `indices`, and the announced exactness is
true again.

And the phrase "conservative in the direction that cannot produce a false negative" is DELETED. It
is what let the defect through: in a sensor a false positive is a phantom `TriggerEnter` — not a
conservative answer, a wrong one, and more visible in play than a miss. Orphans are NOT forbidden at
creation: an imported mesh may legitimately carry them, and rejecting them would break valid assets
for a defect that lives in the predicate. Regression added — triangle entirely outside, orphan
inside, no overlap — with the traversal's REACH asserted in the same case (the body's world box
spans the orphan, so it really is offered and really is refused by the kernel) and the positive
control of the same mesh with a triangle vertex inside. Counter-factual: walking the stored array
again makes it fail.

**P1b — the recipe I documented was the reviewer's and it does not work.** The `clearTrigger`
comment said the caller finds the owed bodies "with an overlap query". `overlapShape` takes a
BOUNDED CONVEX probe and answers `error.UnsupportedShape` otherwise — and a half-space is exactly
the non-convex shape the mesh refusal left legal as a trigger. The contract therefore demanded an
impossible composition on a configuration the previous review deliberately preserved.

No query entry was added: the family freezes at M1.1.15 and the need already has a path. The
comment DISPATCHES instead — convex body: `query.overlapShape`; half-space body:
`Broadphase.queryHalfSpace` for the candidates, then `overlapShapeBody` per convex candidate and
`halfSpaceOverlapsBody` per non-convex one, which is exactly what the sensor pass does for a
half-space trigger. Both shapes are now pinned: the box case already written, plus a half-space
whose role is cleared above a sleeper — and that case ASSERTS the failing recipe
(`expectError(error.UnsupportedShape, …)`) before performing the working one, so the dispatch reads
as a necessity rather than a preference.

**And the same lesson bit a third time inside the fix.** The test helper's PROXY MOVE called
`bodyAabb` unconditionally, which asserts on a half-space: it worked for a box trigger and crashed
on the other shape the role admits. One recipe does not serve both shapes — met in the wake, in the
proxy move, and in the predicate's collection, all in one review.

**2026-08-11 — Fourth closing review, NO-GO, one correction and it is the same lesson a FOURTH
time.** Verified at source before acting, and the mechanism is exact rather than plausible:
`sleep.isAwake` returns `isMoving` for a NON-DYNAMIC body (`sleep.zig:192-197`), so a resting static
is NOT awake; `build` skips and DEFERS any pair with neither endpoint awake (`bothAsleep`,
`contact_constraint.zig:390`). A sleeping DYNAMIC trigger — a configuration this suite already
builds — overlapping a STATIC body therefore has both ends non-awake once the role is cleared: the
pair never produces a manifold, nothing wakes the trigger, and the newly solid body stays
interpenetrated indefinitely. Waking the static changes nothing, since `isAwake` still answers
`isMoving`.

**The composition must also wake THE BODY WHOSE ROLE CHANGED**, when it is dynamic. The `continue`
that skips `id == trigger` inside the enumeration stays correct — the trigger finds itself in its
own query — but an explicit wake of the modified body brackets the loop, and the `build` fixpoint
propagates from there. Regression added: dynamic sleeping trigger over a static, role cleared; the
NEGATIVE half asserts that waking the overlapped body alone — the recipe that sufficed in the two
earlier cases — leaves it asleep with zero constraints for ten ticks, which is exactly what
distinguishes this case from those.

**And the doc-comment stops being a recipe.** Three branches for one role change, composed by hand,
is the signal that this belongs to the ORCHESTRATOR. It is now written as the CONTENT of the atomic
operation M1.1.15 owes, at the same rank as the purge and the proxy replacement the entry already
names for the direction it refuses to offer.

**Fourth instance of one lesson in two reviews**: one recipe does not serve every configuration —
met in the predicate's collection, in the proxy move, in the wake's probe, and now in which body the
wake is owed for.

## Closing notes

- **What worked — the counter-factual discipline, applied to the ORACLES and not only to the code.**
  Every probe of this milestone changes the OBJECT and never the expected constant: an oracle that
  judges a LAYOUT is tested by permuting the layout, one that judges a SET by changing the set, one
  that judges a UNION's reach by giving a variant a payload. That rule is what turned up the two
  defects nothing else would have: the `CollisionShape` layout comment stating two false facts about
  padding, and the domain bound in `sensor.zig` being DECORATIVE — an `orelse return` below it
  returned the same answer for the same pair, so it could have been deleted without a single test
  noticing. Both were found by asking what the counter-factual would look like, before running it.
  The paired negative/positive structure did the rest: half this milestone's claims are negative,
  and each one ships with the control that proves the apparatus can observe what is being denied.

- **What deviated from the original spec.** One design blocker and TWO revisions of the same
  section, `engine-physics-solver.md` §1.13.6, both by Claude.ai round-trip. The first lifted the
  blocker with a probe RULE plus a domain bound; the second REMOVED that bound, its partition
  having grouped half-space and mesh by BODY TYPE where the question is whether the shape has an
  INTERIOR. What ships is: the probe fixed by a rule, the sensor role REFUSED on a mesh by typed
  error — a surface has no inside for a sensor to ask about — the half-space keeping the role since
  it is a volume, and the two remaining kernels written. **No false negative remains.** Full record
  in Recorded deviations. Beyond that: the FROZEN SECTION was never modified, no interface function
  was added, and the §12 count stays at 27.

- **What to flag explicitly in review.**
  1. **The `CLAUDE.md` patch is MY DRAFT.** The brief says its content is supplied by Claude.ai; it
     was not supplied by the time gate F opened, so it is written from verified facts — the
     current-state block, one Tags row, the decomposition entry removed as DONE, a new open-decision
     entry for the bus-to-Etch bridge owner, the footer date — and is offered for wholesale
     replacement rather than merge.
  2. **Three additions outside the brief's file list**, each justified in the Execution log:
     `Bvh.forEachLeaf` / `Broadphase.forEachInLayer` / `Broadphase.queryHalfSpaceTrees` (there was no
     way to enumerate one layer's proxies, which §1.13.5 requires by name), `BodyManager.setTrigger`
     (§1.13.10's fourth disappearance cause had no mechanism at all), and step 10 bis in the test
     harness plus its layer derivation.
  3. **The M1.1.1 matrix reversal** is a revision of a decision, not a corrected value, and the
     assertion it replaces is deleted rather than commented.

- **Final measurements.** `test-forge-3d` 495 → **526/526**, green at `f32` AND `-Dphysics_f64=true`
  × Debug AND ReleaseSafe. `zig build test` 1699/1716 (17 skipped, pre-existing). `zig build`,
  `zig fmt --check`, `zig build lint` all `rc=0`. Bench REPORTED, not gated, ReleaseFast over 1000
  static bodies: floor 5.0 ns with nothing to enumerate, **1005.0 ns for one trigger holding 8
  pairs**, 122745.0 ns for 64 triggers holding 1002. The resting row is the number §1.13.9 owes: at
  1005 ns it is 0.006 % of a 16.67 ms frame, which is what "sleep does not economise this pass"
  costs in practice. The 64-trigger row is 122× the one-trigger row for 125× the pairs, so the cost
  follows the TRIGGER count and its pair yield rather than the body count — which is what the
  triggers-outward direction of §1.13.5 intends, measured rather than assumed.

- **Residual risks / tech debt left intentionally.**
  - **The step-10-bis placement is half measured.** Sleep invariance is carried by the sleep-filter
    counter-factual; pose finality is a design reasoning, deliberately not measured, and the comment
    now says which is which.
  - **`back_face_mode` is `.ignore` on the sensor confirmation**, the family default. §1.11.17
    records that the field is nearly inert for an overlap; no case here distinguishes the two modes.
  - **The `f64` leg still runs on one machine only** — `-Dphysics_f64` appears nowhere in
    `.github/`, so every `f64` figure above rests on a single target. Not this milestone's to fix
    (M1.1.14 owns CI), restated because the pins written here carry that standing.
  - **Nothing in production inserts a body into the broadphase**, so the whole pass is proven
    through the test harness. What narrows the gap to one question is that the harness now DERIVES
    its insertion layer from `broadLayerFor`: whether M1.1.15 calls the same function.
