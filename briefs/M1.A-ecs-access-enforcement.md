# M1.A — ECS access enforcement (Tier 0)

> **Status:** CLOSED
> **Phase:** 1
> **Branch:** `phase-1/core/ecs-access-enforcement`
> **Planned tag:** `v0.11.19-ecs-access-enforcement`
> **Dependencies:** M1.B (hybrid storage — the comptime view is born over both storage modes), M1.E (the four comment rules are blocking on `src`, `tools`, `bench` and `build.zig`)
> **Opened:** 2026-09-12
> **Closed:** 2026-09-12

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

`ARCH-030` requires that a system reach only the components and resources it declared, and that the restriction be carried by the TYPE of the view it receives. Today `SystemDescriptor` carries an `accesses` field and a body that receives an unrestricted `*World`: two neighbouring fields that nothing relates. A system may declare `Reads(T)` and write `T`; the scheduler, reading only the declaration, posts no edge, places both systems on the same topological level and dispatches them to two workers. No diagnostic fires, at build time or at run time.

This milestone closes that class in Zig and brings the Tier 3 C surface to the form its owning document specifies. It delivers no gameplay capability: it is a safety barrier, and its cost is linear in the number of registered systems, hence growing with every Tier 1 module delivered.

## Scope

- A `comptime` view type parameterised by a declared access set. `get` and `getMut` carry a `comptime` membership test; an undeclared component, or a mutable access to one declared read-only, is a `@compileError`.
- A monomorphic trampoline generated per system. `AccessDescriptor` is a runtime, type-erased value and `SystemFn` a single function pointer, so a context parameterised by the declared set cannot be stored by the scheduler directly. The trampoline builds the view from the world and hands the scheduler an erased pointer. The view and the DAG edges derive from ONE declaration; no second list exists.
- Removal of the empty default on `SystemDescriptor.accesses`. Registering a system without a declared set is a registration error, never an implicit empty set.
- Removal of the `world` field from `CommandBuffer`. Its only use is `ensureComponentRegistered` at record time; resolution moves to `flush`, carried per command on the `AccessDescriptor` pattern. `flush` takes the world as a parameter. Without this, the restricted view is bypassable through the neighbouring field, and `flush` / `applyOne` make the effect immediate.
- Structural mutation from a system goes through the command buffer and nowhere else. No third access category: `Structural(T)` would be exploitable only by serialising every structural system against every system touching the archetypes involved, which annuls the parallelism `ARCH-010` requires.
- Migration of the one registered production system and of its call chain (`forge/sync.zig`, `sync_in.zig`, `services/physics.zig`), with `*World` removed from the system entry point.
- Migration of the 51 registration sites in `tests/` and `bench/`.
- Tier 3 alignment on `engine-c-api.md` §5.5, whole chain: `query_create` declares reads and writes separately, `WeldQueryChunk` receives its two index spaces and its `struct_size`, and a witness C plugin fails to compile the moment a read column becomes assignable.
- Counter-proofs distributed from the gate that introduces each guarantee, each paired with a POSITIVE witness in the same harness.

## Out of scope

- Deriving a rule's access set from its body (the Etch rules → scheduler bridge). Inter-rule parallelism is not implemented in Phase 1; rules run sequentially in the interpreter.
- Making `doc_comments` blocking on nested declarations, and writing the 512 nested doc comments (`M1.D.28`). Measured: 43 of them fall inside this milestone's eleven files, and writing that share does not make the rule blocking.
- The conservation pass on `tests/` (`M1.D.25`) and the re-derivation of the AST partition (`M1.D.26`). Neither overlaps this milestone's object; touching 51 registration sites in `tests/` is not a comment pass.
- Any execution-level conformance claim about the Tier 3 surface. Its ECS entries are inert stubs; the guarantee this milestone delivers there is a compilation witness and nothing more.
- Implementing those stubs.

## Exit rule on debt

**This milestone leaves no debt of its own.** A scope decision that opens a hole closes in the session that opens it, not in a plan row: at close, ZERO new `M1.D` entry whose cause is M1.A. A defect this milestone FINDS in pre-existing code and does not fix is reported to Claude.ai at the gate that finds it, with its measurement and the cost of treating it here — never recorded and stepped over.

## Specs to read first

1. `engine-invariants.md` — `ARCH-030` in full (decision, five conformance tests, consequences), then `ARCH-010`, `ARCH-013`, `ARCH-018`
2. `engine-ecs-internals.md` — § *La déclaration d'accès est vérifiée, jamais assertée*, including the two points on structural mutation and on the command buffer carrying no world reference
3. `engine-c-api.md` — §5.4 and §5.5 (`query_create`, `WeldQueryChunk`, the callback), §1.1 bis (`struct_size`, minor version)
4. `engine-phase-1-plan.md` — § M1.A, normative on the four sub-milestones
5. `engine-zig-conventions.md` — §12 (comment rules, all four blocking on this perimeter) and its `doc_comments` paragraph

## Files to create or modify

- `src/core/ecs/view.zig` — create — the `comptime` view type and its membership tests
- `src/core/ecs/scheduler.zig` — modify — `SystemDescriptor`, `SystemContext`, `SystemFn`, the monomorphic trampoline, `registerSystem`
- `src/core/ecs/command_buffer.zig` — modify — `world` field removed, deferred resolution, `flush(world)`
- `src/core/ecs/root.zig` — modify — re-exports
- `src/core/plugin_loader/api.zig` — modify — `WeldQueryChunk` two index spaces and `struct_size`, `query_create` signature
- `src/modules/forge/sync.zig` — modify — the registered system and its chain
- `src/modules/forge/sync_in.zig` — modify — three entries taking `*World`
- `src/modules/forge/services/physics.zig` — modify — one entry taking `*World`
- `tests/core/ecs/access_view_test.zig` — create — membership, read/write distinction, positive witnesses
- `tests/core/ecs/access_counterproof/` — create — the non-compilation cases and their harness
- `tests/c_api/read_column_constness/` — create — witness C plugin, compiled as an acceptance step
- 8 files under `tests/` and 2 under `bench/` — modify — 51 registration sites

## Acceptance criteria

### Tests

- `tests/core/ecs/access_view_test.zig` — `test "a declared read is reachable and a declared write is mutable"` — the POSITIVE witness without which every counter-proof below is vacuous
- `tests/core/ecs/access_view_test.zig` — `test "the view compiles to the same pointer arithmetic as a direct access"` — disassembly of a witness system, compared instruction for instruction
- `tests/core/ecs/access_counterproof/` — three cases — undeclared access, mutable access on a read-declared component, registration without a declared set. The harness distinguishes an EXPECTED compilation failure from a failure for any other cause: a counter-proof log shares its exit code with an ordinary compile error, so the expected diagnostic text is matched, and a control case that must COMPILE runs in the same execution.
- `tests/core/ecs/command_buffer_test.zig` — `test "a system reaches no world through its command buffer"` — the type carries no field of that shape, asserted structurally and not by name
- `tests/core/ecs/command_buffer_test.zig` — `test "a deferred add resolves its component id at flush and not at record"` — with the counter-factual that resolving at record time is unreachable
- `tests/physics/*` — the existing suites, migrated, green unchanged
- `tests/c_api/read_column_constness/` — the witness plugin compiles as published, and FAILS to compile with a one-line patch assigning through a read column; both directions asserted in the same step

### Benchmarks

- `bench/ecs_benchmark.zig` — iteration throughput on the migrated systems — target: no regression beyond 5 % against `251db05` on C0.1, measured interleaved and not best-of-three

### Observable behavior

- `zig build test` on the four corners; the test floor is re-derived FROM THE SUITE at every gate, never from the closure's own arithmetic — entry floor 2252 (`ubuntu-24.04`, `ubuntu-24.04-arm`, macOS) and 2250 (`windows-2025`)
- `zig build forge-determinism` replayed on the rebuilt DAG, eight witnesses byte-identical
- The disassembly of the witness system, captured in the execution log

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (Debug + ReleaseSafe, f32 + f64)
- `zig fmt --check` green
- `zig build lint` green — the four comment rules are blocking on `src`, `tools`, `bench` and `build.zig`; every edit of this milestone is subject to them
- `commit-msg` hook green on every commit of the branch
- `build-and-test (windows-2025, ReleaseSafe)`: this milestone edits the tree's most-depended-upon Tier 0 headers, so a cold-cache run on that cell costs a measured factor of 11.9 on `zig build test` (`M1.D.27`). Push Tier 0-wide edits onto a WARM cache. A re-run at the SAME SHA is the only admissible clearance, for this class as for the hang class, and never a successor commit.

## Conventions

- **Branch:** `phase-1/core/ecs-access-enforcement`
- **Final tag:** `v0.11.19-ecs-access-enforcement`
- **PR title:** `Phase 1 / Core / ECS access enforcement`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

**The gate order, and why it is this one.**

- **G1** — the view type and the monomorphic trampoline, old path kept alive. Its three counter-proofs ship WITH it, not at the end: a guarantee no test exercises is a declaration.
- **G2** — the empty default removed, registration without a declared set made an error.
- **G3** — `world` removed from `CommandBuffer`, resolution deferred to `flush`. Placed before any migration because it decides what a migrated system can still reach.
- **G4** — the production system and its chain. The body does not consume `ctx.world` directly, it PASSES it to `resolve`, `syncIn` and `syncOut`; ten functions of `sync.zig`, three of `sync_in.zig` and one of `services/physics.zig` take `*World`. The view propagates through the chain.
- **G5** — the 51 sites in `tests/` and `bench/`. `stepAndPublish` is realigned on the shape the registered system drives: its doc comment claims the tests exercise that shape, and it diverges from it on the direct-versus-deferred path, which is the point at issue here.
- **G6** — the Tier 3 chain. Aligning the chunk alone would produce two index spaces that no declaration populates.
- **G7** — zero-cost disassembly, determinism replay, C0.1.

**Two measurements that shape the work, taken at `251db05`.**

One registered system exists in production across the whole tree, `forge/sync.zig:747`. The core registers none: the two calls in `scheduler.zig` are inline tests. The remaining 51 sites are tests and benches. The early placement of this milestone remains justified by the growth curve; the word "mechanical" does not survive the measurement.

`syncOut` removes and adds `Sleeping` — an archetype migration — and `step_accesses` expresses it as `Writes(Sleeping)`, the comment at the site saying it is the closest the access model can express. The same body performs `ecs.get(Sleeping, entity)`, a per-entity random access. The view therefore covers random access and not only chunk iteration.

**Traps recorded.**

Zig has no private field. An interface wrapping the command buffer leaves `cmd.inner.world` reachable; encapsulation here is achieved by REMOVING the datum, not by enveloping it.

A counter-proof suite is green when nothing compiles at all. Every non-compilation case is paired, in the same execution, with a case that must compile and whose success is asserted.

`is_singleton` is NOT filtered by the initial scan of `World.queryFiltered` (`M1.D.23`). This milestone builds the view over that path. If the view is constructed such that the gap is promoted into the new type rather than left where it is, stop and report — it is a Claude.ai round-trip, not a judgement call at the gate.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone. The log is not a marketing report: it serves review and post-mortem debugging.*

## Specs read

- [x] `engine-invariants.md` (`ARCH-030`, `ARCH-010`, `ARCH-013`, `ARCH-018`) — read 2026-09-12 09:05
- [x] `engine-ecs-internals.md` (§ *La déclaration d'accès est vérifiée, jamais assertée*) — read 2026-09-12 09:09
- [x] `engine-c-api.md` (§1.1 bis, §5.4, §5.5) — read 2026-09-12 09:13
- [x] `engine-phase-1-plan.md` (§ M1.A) — read 2026-09-12 09:17
- [x] `engine-zig-conventions.md` (§12) — read 2026-09-12 09:21

## Execution log

- 2026-09-12 09:25 — Recon before G1, eight read-only passes over the perimeter. What it establishes, and what it refutes, below. Nothing written yet.
- 2026-09-12 09:25 — **The migration surface is 55 call sites across 11 files, not 51 across 10.** Counted with `grep -rn "\.registerSystem(" tests/ bench/ src/ --include="*.zig"`: 41 under `tests/` in 8 files, 11 under `bench/` in ONE file, 3 under `src/` (2 inside `scheduler.zig`'s own inline test, 1 in `forge/sync.zig`). The naive grep returns 62 lines under `tests/` — the 21-line excess is 14 calls to the plural forge helper `sync.registerSystems(` plus 7 prose mentions. See Recorded deviations.
- 2026-09-12 09:25 — **The enforcement has teeth on production code, and the proof is a real undeclared access.** `authorityOf` reads `RigidBody` (`sync.zig:340`), is reached from inside the registered system at `sync.zig:392` and from `sync_in.zig:318`, and `RigidBody` appears nowhere in `step_accesses` (`sync.zig:672-679`). The declared set is already incomplete; the comptime view refuses it on its first compile.
- 2026-09-12 09:25 — **`bench/ecs_benchmark.zig:428` is the second real instance.** `bench_integrate` declares no accesses and its worker-dispatched body mutates two columns — `velocities[i].linear[1] -= 9.81 * dt` and `transforms[i].pos[...]` (`:101-112`). 14 of the 52 sites declare nothing at all; this is the only one whose body writes.
- 2026-09-12 09:25 — **The declared set carries ZERO reads and the whole chain is read-first.** `step_accesses` is four writes; `syncOut` reads before it writes at every step (`sync.zig:247` then `:252`). `get` is therefore gated on reads ∪ writes and `getMut` on writes alone — the only reading under which the one production system compiles, and the one `ARCH-030` states: the error it names is a mutable access on a read-declared component, never a read on a write-declared one.
- 2026-09-12 09:25 — **The registered system iterates no chunk and builds no query.** Every ECS access in the three chain files is per-entity `get`/`getMut` by `EntityId`; the outer loops walk the solver's SoA (`for (pw.bodies.items, 0..)`). Measured across all 52 test and bench sites: no system body constructs a query either — each pulls a pre-built one from `ctx.frame.user`. The view therefore ships the accessors that have a caller and no others.
- 2026-09-12 09:25 — **The change-detection and resource paths bypass typed access today.** `changedAt`/`changedSince` go `componentId(@typeName(T))` then `changedTickOf(entity, cid)` (`sync_in.zig:276-278`), and `resolve` reads the published world through `ecs.resources.getResource(id)` (`sync.zig:605`). Both are already `comptime T`-shaped, so both migrate to gated view entries rather than remaining holes.
- 2026-09-12 09:25 — **Half of `ARCH-030`'s first conformance test already holds and must not be rebuilt.** `foundation/job_bound.zig` refuses a marked type anywhere in a dispatched body's argument tuple, `*World` reaches `CommandBuffer`'s marker transitively, and `tests/ecs/hybrid_query_test.zig:610-611` asserts both flips. That covers the DISPATCH side; the registration side is this milestone's.
- 2026-09-12 09:25 — **No harness in the tree asserts a compilation failure.** `tests/lint/bad/` fixtures are read as text by the linter, never compiled, and the runner reads an exit code only — which `build.zig:2373-2379` states cannot say which rule fired. The counter-proof harness is built here, and it matches a unique marker in the compiler's stderr rather than an exit code, because a build that dies earlier fails identically.
- 2026-09-12 10:40 — **G1 delivered: `src/core/ecs/view.zig`, the trampoline, and the removal of both defaults.** `View(spec)` gates `get` on reads ∪ writes and `getMut` on writes alone; `SystemDescriptor.of(phase, name, spec, body)` derives BOTH the view's type and the DAG's descriptors from one `spec`, so no second list exists. `SystemContext.world: *World` becomes `world_erased: *anyopaque` and `SystemDescriptor.accesses` loses its default.
- 2026-09-12 10:40 — **The view withholds the world's TYPE because Zig cannot withhold a field.** Every accessor needs a `*World` and no field is private, so the pointer is stored erased: recovering a world costs an explicit `@ptrCast(@alignCast(…))` at the call site instead of a field access. The escape is made loud, not closed — and the erasure opens a hole the marker closes, so `View` declares `weld_no_job_body` and `foundation.job_bound` refuses it in a dispatched body's arguments.
- 2026-09-12 10:40 — **`WELD_ECS_PROTOCOL_VERSION` 1 → 2, on the rule `root.zig` itself states.** Its doc comment names "Query/CommandBuffer/SystemScheduler signatures" as part of the versioned surface and prescribes a bump on any breaking change — "a tracked migration, not a freeze failure". Three of those shapes changed and all three break a Tier 1 caller. The four ADDITIONS beside them (`view`, `View`, `Access`, `SystemContextOf`) would not on their own have moved the number, and the surface pin in `hybrid_query_test.zig` now says so at the site.
- 2026-09-12 10:40 — **Three additive `World` accessors, outside the brief's file list, with their justification.** `changedTick(T, e)`, `resourceBytes(R)` and `currentTick()`. The forge chain has two real regimes — a view inside the dispatch, a `*World` everywhere else — and both must answer to ONE spelling, or every function in the chain carries a comptime two-branch dispatch: two implementations of one question, which is the defect class the corpus names repeatedly. Each is a typed wrapper over an existing entry, and `changedTick` is the typed twin `changedTickOf` lacked.
- 2026-09-12 10:40 — **The enforcement's first catch is in production code.** `step_accesses` gained `Access.reads(RigidBody)`: `authorityOf` reads it from inside the registered system and the declared set named it nowhere. Second catch, in the bench: `bench_integrate` declared nothing while its worker body mutates the velocity and transform columns; it now declares both writes.
- 2026-09-12 10:40 — **The counter-proof corpus is the repository's first harness asserting a compilation failure.** `tests/core/ecs/access_counterproof/` is a sub-project on the `synth_100` idiom — nested `zig build` rather than objects in the parent graph, because a failing compile step fails the parent build and a subprocess re-entering the parent's own `build.zig` would contend for its cache. Four fixtures: three that must not compile, one that must, in the same execution.
- 2026-09-12 10:40 — **The harness matches the diagnostic TEXT, never the exit code**, and the first version of it was WRONG in a way only a counter-factual found: with the guard deliberately disabled it reported GREEN. A `Run` step with no file argument is cached on its argv alone and `setCwd` does not make the directory an input, so it replayed a cached success. `has_side_effects = true` fixes it, and both halves are now proven to redden — exit-code half on a disabled predicate (`2/5 steps succeeded`), stderr half on a renamed marker.
- 2026-09-12 10:40 — **Zero cost is established more strongly than the brief asked, and the oracle had to learn to say so.** `zig build ecs-access-zero-cost` emits the assembly of a witness pair and compares it. At `-OReleaseSafe` the backend finds the two bodies byte-identical and emits ONE, aliasing both exported names to it — identity, not equality. The scanner's first version read that as a missing symbol and failed; it now resolves alias chains and reports folding, with a vacuity guard refusing a resolved body of zero instructions. Counter-factual: a runtime check added inside `getMut` takes it to `23 instruction(s)` against `8`.
- 2026-09-12 10:40 — Floor re-derived FROM THE SUITE at each step, never from the closure: 2252 → 2263 → **2271** collected on macOS, with the closure agreeing independently at each value. Four corners green — Debug and ReleaseSafe, f32 and f64 — at `2252/2271`.
- 2026-09-12 12:10 — **G3: the command buffer holds no world.** `init(gpa)`, `flush(world)`, `applyOne(world, cmd)`, `flushWithObservers(cmd, world, registry)`. A command carries the type it needs as a RESOLVER and the world arrives at the flush. Two recorders, both real and both expressible: one holds a type and no world (the typed `addComponent(T)`), one already holds the id (the Etch interpreter, the observer flush, every direct literal) — `resolve` is optional and `component_id` has NO default, so a literal that says neither does not compile.
- 2026-09-12 12:10 — **A ricochet outside the brief's file list, with its reason.** `ensureDeferred` no longer needs a world, so `world` became unused on the whole `registerOn*` family — an unused parameter is a defect, so it goes, at 42 call sites across 13 files. `observers.zig` was already unavoidable (`flushWithObservers` read `cmd.world`). `dispatchOnSpawned`, `spawnWithObservers` and `fireList` KEEP theirs: they use it.
- 2026-09-12 12:10 — **The mechanical pass over those 42 sites was wrong twice, and both were caught by reading the diff rather than by the compiler.** A regex removing the second argument also ate `gpa: std.mem.Allocator,` from every MULTI-LINE signature (`[^,()]+` matches newlines), and removed a real argument at every call whose receiver is a `World` rather than a registry — `world.registerOnAdd(gpa, Tag, null, cb)` lost `Tag`. Ten such sites restored by re-reading the diff pair by pair. *A blanket rewrite over a family whose members differ in arity is not mechanical.*
- 2026-09-12 12:10 — **G6: the Tier 3 chain, entire.** `query_create` declares reads and writes separately; `WeldQueryChunk` gains `struct_size` and two index spaces — reads behind `const void* const*`, writes behind `void* const*`. Measured before the change: the struct had NO `struct_size` anywhere in the file, one index space of non-const pointers, and `query_create` took only `include`/`exclude`. Aligning the chunk alone would have produced two spaces nothing fills.
- 2026-09-12 12:10 — **The C witness is the first C the repository compiles, and it is driven through `zig cc` rather than a build-graph step**: a compilation expected to FAIL cannot be a step of the graph it would fail. It builds as published and must not build with one line taking a mutable pointer to a read column — `initializing 'WeldVec3 *' with an expression of type 'const void *const' discards qualifiers`. Counter-factual: dropping the inner `const` in the header makes the counter-proof compile and the step reddens.
- 2026-09-12 12:10 — **The hand-written C mirror is pinned on BOTH sides.** No binding generator exists in this tree, so `weld_query_chunk.h` is hand-written and would drift. `_Static_assert`s in the header and `tests/c_api/chunk_layout_test.zig` pin the same layout in the two languages, neither derived from the other; the Zig side also asserts the QUALIFIERS, which a layout pin alone would not distinguish.
- 2026-09-12 12:10 — **G7, determinism.** `zig build forge-determinism` replayed on the rebuilt DAG — the forge system's declared set gained `Reads(RigidBody)`, so its edges changed — on all four corners: eight committed witnesses byte-identical, the four discrete invariants OK, `divergence frame: none within K=60`.
- 2026-09-12 12:40 — **`ARCH-030`'s first conformance test read MECHANICALLY, not by inspection.** `tests/core/ecs/command_buffer_test.zig` walks the type graph of `SystemContext` and of `SystemContextOf(spec)` and asserts neither reaches a `*World` at any depth — the invariant's own first clause, verified on the type rather than by a reader checking fields one at a time. Non-vacuity is asserted on the SUBJECT and not on a toy: the same walk still finds the command buffer, so it really traversed a live graph and came back empty.
- 2026-09-12 12:10 — **G7, C0.1 and S1, interleaved and not best-of-three.** C0.1 ReleaseFast against `251db05` through a worktree, three interleaved rounds: base medians 3.66 / 3.68 / 3.65 ms against head 3.65 / 3.67 / 3.66 — **delta 0.0 %**, both GO against the 16.6 ms gate. p99 moves in BOTH directions, so no envelope is registered for it. S1 at `--workers=4`, EIGHT interleaved rounds: base median 52 646 ns, head 52 666 ns, **delta +0.0 %**. A first three-round sample had shown base 78.1 / 66.5 / 53.0 µs and crossed the 62 µs gate on the BASE — the machine's tail, not this branch, and the reason the sample was widened rather than reported.
- 2026-09-12 13:55 — **An adversarial review pass over my own diff, before the PR was read. Fifteen findings raised, five survived verification, four of them mine.**
- 2026-09-12 13:55 — **P1, and it is the exact class this milestone's design was supposed to exclude.** Resolution was written as a pre-pass over a BUFFER — `resolveComponentIds`, called from `flush` and from `flushWithObservers`. That covers the buffers a caller remembered to hand it. `Interpreter.flushStructural` (`interp.zig:5508`) is a THIRD drain: it reads `observer_registry.deferred`, takes the commands BY VALUE and passes them to `applyWithObservers`, through neither entry. So a native observer recording through the typed `spawn`/`addComponent` API into that buffer produced a command whose id was never resolved, and `spawnDynamicWithValues` received a slice of `undefined`. Before this milestone the path was correct — record-time resolution made it so — **so the branch introduced it.** My enumeration of "every path that applies commands" at design time was wrong by one, and the file's own comment asserted the enumeration was complete.
- 2026-09-12 13:55 — **Closed at the CLASS and not the instance.** `resolveComponentIds` is DELETED; `resolveInPlace(cmd, world, gpa)` runs at the head of the three functions that apply a command — `CommandBuffer.applyOne`, `observers.applyWithObservers`, `observers.applyRawCommand` — which is the derivation *every function that consumes a `Command` and mutates the world*. The batch pre-pass also carried an ordering claim I had overstated: `ensureComponentRegistered` is idempotent and monotone, so per-command resolution gives the same ids, and the comment justifying the pre-pass went with it. Counter-factual, with the resolution removed from `applyWithObservers`: `panic: index out of bounds: index 2863311530, len 0` — `0xAAAAAAAA`, the `undefined` fill.
- 2026-09-12 13:55 — **Three comment blocks were FALSE about the code beside them, and two of them were false because this branch falsified them.** (a) `registerSystems`'s doc enumerated the declared set as four entries and called the enforcement one that "will check" — the branch made it five and made the enforcement live. (b) `WeldQueryChunk`'s doc claimed the plugin author's compiler "refuses" a mutable pointer to a read column and that "nothing relies on the author's discipline": C11 6.5.16.1 requires a DIAGNOSTIC, not a refusal, and the verifier reproduced it — `clang -std=c11 -Wall -pedantic` on the repository's own witness warns and exits 0. The refusal comes from `-Werror`, which the spec mandates for published examples and which the build step applies. The block now says that, at the strength C actually gives. (c) `computeLevels` asserted its cycle branch unreachable "since the conflict detection at registerSystem rejects the only construction path that creates one" — see below.
- 2026-09-12 13:55 — **A PRE-EXISTING defect found and reported rather than stepped over** (the brief's exit rule). `registerSystem`'s pass 1 refuses two writers of the same id and nothing more, so two systems that cross — one `Reads(T), Writes(U)`, the other `Writes(T), Reads(U)` — both register successfully and close a two-node DAG cycle; `computeLevels` then returns `error.WriteWriteConflict` at the FIRST dispatch, permanently for that phase, under a name `RegistrationError` documents as something else. Byte-identical at the merge base, so it is not a regression — but **this branch makes it newly reachable for Forge**, whose `step_spec` now declares a read where it declared none. Reported at the gate that found it, and **FIXED at review (see 2026-09-12 16:40)**: leaving it would ship a path this milestone opened toward an error naming something other than its cause.
- 2026-09-12 13:55 — **P3, mine: a zero-byte `witness.o` was committed** by an earlier form of the C harness that wrote beside its source. Removed, and `*.o` added to `.gitignore` so a hand-run of the witness cannot re-add it.
- 2026-09-12 15:05 — **`M1.D.19`'s eleventh occurrence, measured rather than assumed.** `build-and-test (windows-2025, ReleaseSafe, f32)` at `3f22cf6`. Discriminants, in the order that debt's own note prescribes: signature `test runner failed to respond` present ONCE and the sibling class `failed without output` ZERO times — the never-co-present property holds; NO `error: '…' failed:` line anywhere, so no assertion fired; `2245/2277 tests passed (32 skipped)` against a declared windows floor of 2279, hence **2 tests lost**, joining the recorded series 1, 5, 6, 8, 14, 54; and 52.7 minutes against a 55-minute budget, which rules out `M1.D.27` — that class is `cancelled` with every step green, not `fail`. Cleared by a re-run at the SAME SHA, which is the only admissible clearance for this class.

- 2026-09-12 16:40 — **The DAG cycle is refused at registration, under its own name.** Guy's review made the call: the defect is pre-existing and byte-identical to the base, but this branch is what makes it reachable for Forge, and a path a milestone opens is that milestone's to close. `RegistrationError` gains `DependencyCycle`; `registerSystem` gains a third pass — a reachability walk from the new system's successors over the existing edges, asking whether any of them reaches one of its predecessors — placed ahead of the commit, so the refusal touches nothing OF THE SCHEDULER — exactly as `WriteWriteConflict` does, and with the same bound: resolving the accesses has already registered the named types in the world's registry, idempotently, and that survives a refusal. The doc at the site says so rather than claiming a clean slate it does not give; the first form of that comment read « before the first mutation », which a reader would take literally and be wrong about the registry. The two codes are deliberately distinct: a write-write conflict is a property of ONE component, a cycle is a property of a SET and of no single member, and a caller told `WriteWriteConflict` for a cycle would hunt a duplicated write that does not exist.

  The walk is two-colour where `registry.zig`'s `@requires` closure needs three, and the asymmetry is written at the site: the question here is REACHABILITY in a graph already known acyclic, not cycle-finding, so a diamond is a node reached twice and revisiting it cannot change the answer. The acyclicity it rests on is maintained by this very check from an empty graph onwards — stated as an induction, and bounded honestly: it does NOT cover a scheduler left half-mutated by a registration that failed on `OutOfMemory`, which `forge/sync.zig`'s preflight already documents as UNUSABLE and `engine-ecs-internals.md` carries as a Tier 0 debt. That is why `computeLevels`' branch is KEPT rather than made `unreachable` — an `unreachable` there is undefined behaviour in ReleaseFast on a scheduler nothing promises is intact — and its comment now says what holds the invariant and what does not.

  **Three tests, and the counter-factual discriminates all three.** With the third pass removed, the full suite reports `310/312 steps succeeded; 2263/2284 tests passed (19 skipped, 2 failed)`, both failures reading `expected error.DependencyCycle, found void` — the registration really did succeed. The two-node case pins the refusal and asserts the other direction too, that a genuine duplicated write still reports `WriteWriteConflict`, so the two codes cannot silently collapse into one. The three-node case is the discriminating oracle for the WALK: a check comparing predecessors against successors DIRECTLY passes the two-node test and lets this one through. And the third test stayed GREEN under the counter-factual, which is what it is for — a crossing declaration that closes no cycle must still register, and it exercises the walk with both sets non-empty so a check refusing on that condition alone would fail it.

  **A fourth breaking shape, riding the bump rather than asking for a second.** `RegistrationError` is part of the surface `WELD_ECS_PROTOCOL_VERSION` covers, and a new variant breaks an exhaustive `switch` even though `registerSystem`'s inferred `!void` reads unchanged. `root.zig`'s doc enumerated THREE changed shapes and now enumerates four — *a list too short closes by quantifying*, and an enumeration left at three while the set moved is the stale-contract motif this repository sweeps. One version covers one milestone's breaking set; a number incremented per change would stop meaning « the surface a caller compiled against ».

  Outside the brief's file list, with its reason: the tests landed in `tests/ecs/scheduler_dag.zig` rather than beside this milestone's own files, because the refusal they pin exists to be TOLD APART from the write-write refusal, whose test lives there. Co-location is what keeps the pair from drifting; the file's header, which enumerated exactly three acceptance tests, is extended to say why there are now six.

  **Two ricochets, both of the same class — an enumeration that stayed at its old length while the set moved.** `src/core/ecs/root.zig`'s protocol doc listed three changed shapes (now four) and `src/core/ecs/README.md` listed one registration refusal (now two). Neither statement was false; both were short, which is how a reader concludes the list is the whole of it. Swept with the change that made them short rather than left for whoever notices.

- 2026-09-12 17:30 — **An adversarial pass over the cycle fix, five independent lenses: 27 findings, 20 surviving contradictory verification, and they deduplicate to SEVEN classes — every one mine.** The lenses were the walk's correctness, the acyclicity induction, the state contract, comment truthfulness and test power; each finding was then handed to a verifier told to REFUSE it unless the source confirmed it, which killed seven.

  **The sharpest is the class this repository names its costliest, and I wrote it in the commit whose object was to stop it.** `computeLevels`' branch carried TWO reasons for staying an error rather than an `unreachable`, and the second was FALSE: it said the branch covers a scheduler left half-mutated by a failed registration. It does not. Such a scheduler carries an edge naming an index the rollback popped, and the in-degree count TWENTY LINES ABOVE faults on it — `in_degree[target] += 1` with `target == n` — before a single level is built. The scenario cannot reach the line it was cited to justify. *A justification the code cannot honour survives review by resembling an argument*, and the branch's first reason — the invariant is held by a sibling function and a later milestone can move it — was always sufficient on its own. Deleted, with what is true written in its place.

  **The test class is the milestone's own doctrine turned back on it.** The walk exists to compute a TRANSITIVE closure, and nothing pinned it: in both the two-node and the three-node case the predecessor sits ONE hop from a seed, so an implementation that checks its seeds, expands a single level and stops passes them both. Worse, this brief had called the three-node case « the discriminating oracle for the WALK ». **Measured, not argued**: with the walk replaced by that bounded form the suite reports `310/312 steps succeeded; 2266/2286 tests passed (1 failed)` — exactly ONE red, the new four-node ring, every other test green. So the ring is the discriminant and the chain never was; the overclaiming comment says so at the site now. A diamond case joins it, where the walk reaches one node by two paths — the only shape in the file that takes `visited`'s second guard — and it is written for what it establishes, termination and the answer, NOT for necessity: in a graph registration keeps acyclic, deleting the guard changes no verdict, which is what the source already said.

  **`RegistrationError` is not the error set `registerSystem` returns.** `AccessDescriptor.resolve` is `anyerror!ComponentId`, and `registerSystem` `try`s it, so the inferred set collapses to `anyerror` — a caller annotating `RegistrationError!void` does not compile. PRE-EXISTING, and my own edit re-asserted it in both the declaration and `root.zig`'s re-export. Both now say what the alias IS good for — naming the refusals in a `switch` or an `expectError`, which is every use in the tree — and name the resolver's `anyerror` as the gap, not narrowed here because it is a public function-pointer type and therefore frozen surface.

  **`forge/sync.zig`'s preflight enumerated two deterministic failures and there are now three, and the third is REACHABLE** — which is Guy's reserve made concrete rather than restated. `wouldConflict` compares write against write on `type_name` and nothing else, so a cycle is invisible to it; and `step_spec` writes `Transform`, `Velocity` and `Sleeping` while reading `RigidBody`, so any later system in `fixed_update` reading one of those three and writing `RigidBody` closes a two-node cycle. The enumeration is corrected and the omission ARGUED rather than papered: predicting a cycle means rebuilding the scheduler's edge construction inside a Tier 1 module, and a second implementation of that is how the two come to disagree — so it surfaces from `registerSystem`, which refuses it ahead of its commit.

  **Two contracts were wider than what holds.** « A scheduler that has seen an allocation failure here is unusable » reasoned about the COMMIT and was scoped to the whole function — false for every pre-commit allocation, including the two this change adds. And « NOTHING WAS COMMITTED » promised four things — no descriptor, no edge, no tracker entry, no command buffer — while the test asserted a count of descriptors; an implementation that moved the tracker loop ahead of the walk would leave `writers[TagA]` naming a system that does not exist and every count in sight would still read 1. The assertion now walks all four. The `topologicalLevels` check beside it was DELETED rather than strengthened: with one system and no edges its value is fixed, and the assertion above it aborts first in the only world where it could differ — *an assertion dominated by its neighbour detects nothing, and its comment named a failure it could not see.*

  **And the linter caught me.** The first form of the Forge correction carried a milestone identifier in a comment, which the comment doctrine forbids outright; `zig build lint` refused the tree. Reported because the guard firing on its author is the only evidence that it guards anything.

- 2026-09-12 19:10 — **A Codex review refused the closure on three findings, all confirmed against source before any of them was acted on, and all one class: a guarantee affirmed in a comment that the code does not give.** This milestone closed that class twice on itself during its own adversarial pass; it comes back a third time on the central surface. The cause is named rather than absorbed: *the counter-proofs were written from the intention rather than against the attack*, and one of them — `case_missing_accesses.zig`, which OMITS the field — measures that Zig requires a field without a default, which Zig did already. **A counter-proof that tests what the compiler does anyway is green for a reason unrelated to the invariant.**

- 2026-09-12 19:10 — **P2, and it is deeper than the increment.** `WELD_API_VERSION_MAJOR` sat at 0 under a doc comment prescribing the increment on every binary break, while `query_create` went from five parameters to seven and `WeldQueryChunk` changed layout. Same C0.5 rule that took the ECS surface to 2, applied on one axis and missed on the axis the loader ENFORCES. Bumped to 1, minor reset to 0 — and the bump is what made the missing half of the check live: the loader refused only `api_version_min > host`, so a plugin built against major 0 would have LOADED on a major-1 runtime and called entries whose signatures had moved. A major is a break, so the admissible band is one value; `error.ApiVersionTooOld` is its lower bound, with a `weld_stub_plugin_legacy` fixture that could not have existed before (nothing is older than the first major). Counter-factual, the bound removed: the legacy stub reaches `state = .loaded`, `2268/2288 (1 failed)`, exactly one red.

- 2026-09-12 19:10 — **P1-2, and the free bypass was not the one the file header described.** `world_erased` was `*anyopaque` — the same type for every declared set — so `View(&write_spec).fromErased(ctx.view.world_erased)` promoted a read declaration to a write one with no cast, no builtin and no diagnostic. The header claimed the escape cost "an explicit `@ptrCast(@alignCast)` at the call site — a deliberate and greppable act", which was true of recovering a `*World` and false of the promotion, which is the bypass that is actually useful. Closed at the TYPE: `ErasedFor(spec)` is an opaque generated per declared set, the field and `fromErased`'s parameter are spec-typed, and the one narrowing left is generated inside the trampoline where the spec is known.

  **The design rests on comptime memoisation, which was MEASURED before the design was written and is now pinned in the suite.** Zig keys type memoisation on the slice's IDENTITY, not its value: one `const spec` names one type wherever it appears — which is what lets a trampoline and the body it calls agree — while two separately declared identical sets name two, which refuses a promotion the language would otherwise wave through. The second direction is stricter than necessary and costs nothing, no legitimate path promoting a view to a set it was not built from. It was first measured in a throwaway program, *which is exactly where a load-bearing fact must not live*, and the suite now asserts all three cases plus the declaration the opaque carries.

  Counter-proof in BOTH directions as required: `case_view_promotion.zig` must NOT compile and fires on the compiler's own type error rather than the view's marker — which is the stronger outcome, a marker meaning the second view had been built and was objecting afterwards — and a positive test builds a view the way the trampoline does and reaches what it declared, because a `fromErased` accepting nothing at all would satisfy the refusal perfectly.

- 2026-09-12 21:40 — **P1-1, and the general form is worse than the witness the review names.** Refusing the empty set would have closed one value; what was open is that `registerSystem` took a `SystemDescriptor` — a struct with public fields — so nothing tied `accesses` to `run`. Take the descriptor `of(&.{Writes(A)})` produces, assign `.accesses = &.{Reads(Z)}`, and the body writes `A` while the DAG is told it reads `Z`. That is `ARCH-030`'s original defect rebuilt by field assignment, and no check inside `registerSystem` could see it: both halves are individually well-formed.

  **Zig gives no way to seal a pair, and three forms were considered and refused before the fourth.** A non-`pub` witness field does not help, because a caller can copy an `of`-produced descriptor and overwrite the field beside it. A `*const Bound` indirection does not help either: the pointer is const, but `Bound` is reachable through `@typeInfo(@TypeOf(d.bound)).pointer.child` and a literal can then be written. Field access through a non-`pub` TYPE is legal in Zig — measured, the new `expectNothingCommitted` helper reads `phases[i].tracker.readers` from a test file exactly that way. So the closure is not to hide the pair but to **refuse to accept one**: `registerSystem` takes the phase, the name, the declared set and the body, derives the descriptors AND the body's context type from that one set, and `SystemDescriptor.of` is private. The type stays public because the scheduler stores it and `systemsInPhase` returns it; nothing anywhere consumes one from outside, measured.

  **80 call sites across 11 files**, and the pass was scripted with a dry run first — *which is what caught a defect in the script rather than in the tree*. The first version keyed each generated declaration on the BODY's name, and `nopSystem` serves twenty-five registrations declaring twenty-five different sets: every one after the first would have silently inherited the wrong declaration. The dry run reported 4 specs for 31 sites and that number is what gave it away. Keyed on the SYSTEM name it parses clean, and a body used at more than one site becomes generic over the spec — which is not a workaround but the consequence: a body's parameter type IS its declaration now, so one body cannot serve two.

  **The identity rule earned its keep immediately.** Two tests registered "the same system in another phase" through two separately declared but identical sets, and the compiler refused the second — correctly, since two declarations are two claims. They now share one declaration, which is what the tests were saying all along.

  `descriptorsOf` became public so a preflight can derive the ACCESSES alone — safe where `of` is not, there being no `run` beside them to disagree with — and the frozen-surface guard in `hybrid_query_test.zig` fired on it, refusing the tree until the addition was NAMED. Reported because a guard firing on its author is the only evidence it guards anything; this milestone has now been refused by its own linter twice.

  **`case_missing_accesses.zig` is DELETED, not repaired.** It omitted the field and matched `missing struct field: accesses` — which measures that Zig refuses a struct literal missing a field without a default, a fact that owed nothing to this milestone and would have held before any of it. *A counter-proof that tests what the compiler does anyway is green for a reason unrelated to the invariant*, and it is how the general form stood unclosed with a fixture standing guard over it. `case_mismatched_pair.zig` replaces it: a body typed against `writes(Transform)` registered with a declaration of `reads(Velocity)`, neither half wrong alone. The four surviving cases moved off `SystemDescriptor.of` onto a wiring function that is never called, `of` being private — the reference is what forces analysis, without which the fixture compiles by not looking.

- 2026-09-12 22:15 — **The full matrix the review required after correction, plus the two instruments it did not replay.** Four corners `314/314 steps succeeded; 2271/2290 tests passed (19 skipped)`, exit 0 on each, ZERO assertion failures across all four. `zig build forge-determinism` on all four: eight witnesses self-reproducible, chain verdict OK over 1000 frames, all four discrete invariants OK — island partition, sleep state, per-pair manifold cardinality, retained pair set — `divergence frame: none within K=60`, on a DAG whose registration path was rebuilt entirely. Benches ReleaseFast: C0.1 median **3.36 ms**, p99 3.95, imbalance 0.28 % — GO against 16.6 ms; S1 **52 750 ns**, imbalance 0.99 % — GO. *Both deltas against M1.A's recorded 3.66 ms and 52 666 ns are INTER-SESSION and are therefore not a comparison*, which this repository has already been wrong about once.

  **The instrument that decides here is the zero-cost oracle, not the timings**, the bench's own bodies having changed parameter type: `zig build ecs-access-zero-cost` still reports `weld_zero_cost_via_view` and `weld_zero_cost_via_world` as **ONE body of 8 instructions**, the backend folding them byte-identically. The view side gained a narrowing `@ptrCast` when the erased world stopped being `*anyopaque`; that it costs nothing is now measured rather than asserted. The C witness under `-Werror` and the counter-proof corpus (6/6, control plus four refusals) are green.

- 2026-09-12 23:05 — **`M1.D.19`'s TWELFTH occurrence, measured before it was named.** `build-and-test (windows-2025, ReleaseSafe, f32)` at `e054b26`. Discriminants in the order that debt prescribes: signature `test runner failed to respond for 1m5.819ms` present ONCE and the sibling class `failed without output` ZERO times — the never-co-present property holds for a twelfth time; NO `error: '…' failed:` anywhere, so no assertion fired; `2255/2287 tests passed (32 skipped)` against a declared windows floor of 2288 gives **ONE test lost**, joining the series 1, 5, 6, 8, 14, 54, 2; and **53.0 minutes against a 55-minute budget with conclusion `failure` and not `cancelled`**, which excludes `M1.D.27` on both its discriminants at once. Its f64 twin was cancelled behind it and `ci-gate` failed on the aggregate: 15 of 20 checks pass. Cleared by a re-run at the SAME SHA, the only admissible clearance for this class.

  **THE TWELFTH FAILED ITS FIRST SAME-SHA RE-RUN, and that is what produced a THIRD discriminant the debt records as impossible.** Attempt 2 on the same cell: signature present once (`failed to respond for 1m8.135ms`), sibling absent, zero assertions, `2256/2287 tests passed (31 skipped)` against 2288 — **one test lost again**, where every previous pair of failures at one SHA had lost DIFFERENT counts. Two identical losses is the shape a specific implicated test would produce, so the reading was tested rather than repeated.

  It is refuted, and by a measurement the debt's own note says cannot be taken. That note reads: *"Naming the step from the log does not work: a hung step emits no output at all, so the per-step summary that would name it is exactly what is missing."* True of the step's SOURCE name; false of its IDENTITY. **The `failed command:` line that immediately follows the `failed to respond` message carries the hung step's build-cache hash**, and the two attempts differ — `01a580472aec240c02328baaccfbc5f8` against `c810df3f34968acc6a2110b9fd5002da`. Two DIFFERENT executables hung at one SHA. The equal loss count then explains itself without a culprit: both hung steps sit in the contiguous family whose neighbours all report `0 pass, 1 skip (1 total)`, so a hang there costs exactly one test whichever member it lands on. *The count was never the discriminant; the identity is.*

  A second re-run is running. At `0664f28` this class also needed two.

  **A collateral worth naming, because two debts met on one job.** The run carries the annotation `Zig cache is 11222302720 bytes, over the 10737418240 cap — SKIPPING the final save`, which is `M1.D.10`'s mechanism; a skipped final save means the next run on that cell starts COLDER, which is `M1.D.27`'s trigger. Neither is new, their interaction is not recorded anywhere, and this job is where it is visible.

  **And a measurement error of my own, reported because it is the family this reprise keeps finding.** The first read of the failing job said every step was green, which would have made the failure inexplicable. It came from `gh run view --json jobs --jq 'select(.name|contains("ReleaseSafe, false"))'` — a filter that matches **three** jobs, `ubuntu-24.04`, `ubuntu-24.04-arm` and `windows-2025`, so jq emitted three step lists concatenated and the first one, a green ubuntu job, was read as the windows one. Querying the job by its numeric id gave the truth immediately: step 9, `zig build test`, `failure`, exit 1. *An instrument whose selector is looser than its subject reports someone else's result with total confidence.*

- 2026-09-13 — **A third review pass, and its P1 is the one this milestone was most likely to produce: a remedy that opened the next breach.** `weld_no_job_body` sat on `View` and not on `ErasedFor`, so `foundation.job_bound` refused a view in a dispatched body's arguments and admitted `ctx.view.world_erased` — whose type is `*ErasedFor(spec)`, and from which a body rebuilds `View(spec).fromErased(p)` with NO cast, `fromErased` taking precisely that type. Word for word the reach the marker's own text declares impossible.

  **Confirmed by reading, then MEASURED in both forms before anything was written**: `declaresMarker(ErasedFor)` false, `carriesMarked(*ErasedFor)` false BARE and false WRAPPED in a struct, against `carriesMarked(View)` true and `carriesMarked(SystemContextOf)` true. The guard had nothing to find.

  **What matters more than the fix is that P1-2 created the hole.** `ErasedFor` did not exist before `9223a8d`; it was minted to close the promotion between views, and it was born without the guarantee its twin carried — because that guarantee is implemented in ANOTHER FILE, `foundation/job_bound.zig`, and nothing at the point of creation recalled that a new carrier of a world owes it. **Second time in this milestone a remedy opened the next breach**, the first being the id-resolution fix that covered two drain sites and missed the third. So the remedy could not be « add the constant »: the site now carries the RULE it must satisfy — *any type through which a `*World` can be recovered must declare this marker, whatever else it is for* — written where the next such type will be written, since the file that enforces it is not the file that will forget it.

  **Census measured BEFORE, as required, and unchanged after**: `6 arg-passing entries derived, 4 dispatch across workers, 4 of those GUARDED, 5 guarded in total`, identical on both sides. It could not have moved, and that was measured rather than assumed: nothing in the tree transports an `ErasedFor` into a job — the only uses are the declaration, the narrowing INSIDE the trampoline, and tests.

  **Counter-proofs in both forms, with their diagnostics measured and not predicted.** Bare, the refusal carries the marker's own text in full, which is what says the REASON travelled and not merely that something was refused. Wrapped, it reads `refuses that: no reason declared` — `reasonOf` walking only pointers and optionals where `carriesMarkedIn` enters everything, which is `M1.D.24`, named by this milestone and left to the bound's owner. **The wrapped fixture's mark deliberately avoids that tail** and anchors on the type name instead: a fixture anchored on a defect's symptom goes red the day the defect is repaired, which is how a repair gets read as a regression.

  And the property is pinned in the SUITE as well as in the harness — with a non-vacuity control, since a predicate answering true for everything would satisfy all four assertions — because the harness runs on one CI cell and the suite runs on thirteen. Counter-factual, the marker removed: exactly ONE test reddens.

- 2026-09-13 — **A finding raised and deliberately NOT closed: the plugin entry is called before the version check.** `loader.zig` calls `entry_fn(@ptrCast(&api_mod.stub_api))` and only then compares `api_version_min` against the runtime major in both directions — so a plugin built against major 0 receives the major-1 table and can call through it before returning the descriptor that would have got it refused.

  **Bounded by measurement rather than by argument.** `stub_api.ecs` is `stub_ecs_api`, which is `WeldEcsAPI{}` — every entry its default stub — and `stub_query_create` carries the NEW seven-parameter signature while discarding all of them; the tree has exactly ONE site that hands a plugin a table, and it hands that one. So the ordering is a defect whose consequence is latent while the table is inert, and which becomes live the day a real table is handed over, which is what delivering the plugin system means.

  **Not fixed here, and the reason is this milestone's own lesson.** The ordering predates M1.A, and the remedy is a version negotiation that does not hand over an operational table — a change to the loading protocol, which is PluginLoader design and not this milestone's object. Closing it hastily at the end of a milestone is exactly what produced a naked `ErasedFor`. Raised to Claude.ai with its measurement, for arbitration.

- 2026-09-13 — **`M1.D.19`'s THIRTEENTH occurrence, and the discriminant found at the twelfth paid immediately.** `build-and-test (windows-2025, ReleaseSafe, f64)` at `565012c`, its f32 twin cancelled behind it. Signature present TWICE — two hangs in one run, the shape the tenth occurrence had — sibling absent, zero `error: '…' failed:`, `2253/2285 tests passed (32 skipped)` against a declared floor of 2288 giving **THREE tests lost across two hung steps**, and 52.6 minutes against a 55-minute budget with conclusion `failure`, which excludes `M1.D.27` on both its discriminants.

  **The two `failed command:` lines name two different executables in ONE build** — `fecde19354ea9ccbd9ecb5d20cdb00ac` and `2fe9c3b586059afa4881744abc5715ef`. That is the within-RUN twin of the within-SHA comparison the twelfth produced, and it settles the same question from the other side: four distinct identities across three attempts, no two agreeing. Where the tenth occurrence could only say *"at least one of the two cost no test at all"* and left the asymmetry unexplained, the identities now say plainly that two unrelated steps hung, so three tests across two steps needs no theory of a culprit.

  Re-run at the same SHA, which this class has needed twice before.

- 2026-09-13 — **A SECOND class of CI red, distinct from the hang, and its discriminant is the strongest this session found.** `build-and-test (windows-2025, Debug, f32)` at `0aff532`: hang signature ABSENT, ZERO tests lost (2288 collected, exactly the floor), and ONE assertion genuinely fired — `error.Win32ThreadSafetyTimeout`, a 30-second wall-clock bound on three threads doing 100 `createWindow`/`destroyWindow` pairs each. The job ran 9.65 minutes against the ~50 a hang costs.

  **The discriminant that settles it is general to every flake class and costs one command: A DIFF WITH NO ZIG IN IT.** That commit's diff is two Markdown files and seven lines, sitting on top of a commit where this very cell had passed — so no code changed between the green and the red. The sibling `Debug / f64` cell passing at the same SHA corroborates and is weaker, Win32 windowing having nothing to do with the physics scalar. Cleared by a re-run at the same SHA.

  **And it must not be confused with the class `M1.1.9` retired from `crash_recovery.zig`.** Those assertions ran AFTER the blocking receive had returned and so guarded nothing — a duration asserted post hoc. This loop BOUNDS a wait and abandons it, which is a correctly shaped hang guard; what fires is its budget on a runner the suite itself saturates. Removing it removes a real guard and raising it weakens the bound it exists to give, which is why it is recorded rather than touched.

  *A collateral cost, stated because it is not free:* four of this session's CI runs were spent on documentation-only commits recording flakes, and two of those runs themselves flaked. Recording a measured class is worth a run; a reader deciding how much of that to keep should know the price was paid four times.

## Blockers encountered

- **The brief's empty-set rule — RESOLVED by Claude.ai at the final review, and the resolution is that the rule is already satisfied.** It was raised here with its measurement: § *Scope* requires that « registering a system without a declared set is a registration error », the refusal was written as a `@compileError`, compiled, and it fired on twelve registrations that are all honest — five exercising the PHASE pipeline and touching no component, the rest mutating through `ctx.cmd`, whose effects are deferred to a flush the DAG deliberately does not order.

  **The ruling does not amend the rule; it reads it.** `ARCH-030`'s object is the IMPLICIT empty set — « L'OMETTRE est une erreur d'enregistrement, pas un ensemble vide IMPLICITE » — and since the generic entry made `spec` a mandatory comptime parameter, omitting it is a COMPILE error, which is stronger than the registration error the invariant asks for. A hand-written `&.{}` is a declaration its author made, not an omission that happened to them.

  **And the second reason is the decisive one, because it removes the question rather than arbitrating it**: the P1-1 correction made an empty declaration SELF-VERIFYING. The body receives `SystemContextOf(&.{})`, hence `View(&.{})`, whose `get` and `getMut` refuse at comptime for every `T` — already pinned by `view.zig`'s « an empty declaration grants nothing ». A system that declares nothing cannot reach a column, so it has no edge to place. Before the pairing was closed an empty set could lie; after it, it cannot. The refusal would therefore forbid a set of systems the type system now guarantees honest.

  *The measurement was right and the reason offered with it was weak.* « Twelve sites protest » is an argument from the tree; « the type guarantees it » is an argument from the design, and only the second survives someone deleting the twelve. The comment at the site now carries the second.
## Recorded deviations

- **The FROZEN SECTION's count of registration sites is refuted by measurement, and the frozen text is left untouched.** § *Scope* reads « Migration of the 51 registration sites in `tests/` and `bench/` » and § *Files to create or modify* reads « 8 files under `tests/` and 2 under `bench/` ». Measured at the branch point over the SAME perimeter the frozen text names: **52 call sites across 9 files — 41 under `tests/` in 8 files, 11 under `bench/` in ONE**. So the site count is short by one and the bench file count is long by one. The likely arithmetic behind 51 is 41 test sites plus 10 of the 11 bench registrations, which would omit `bench/ecs_benchmark.zig:428` — the single site in the repository that declares nothing while its body writes two component columns. The milestone is executed on the measured figure; the frozen text is a Claude.ai round-trip, not an edit made here.

  **An earlier form of this entry recorded 55 across 11, and that figure was wrong in a way worth keeping visible.** It reached 55 by adding the 3 `src/` sites the file list does not name, counted at the branch HEAD — where 3 of the 5 in `src/core/ecs/scheduler.zig` are inline tests THIS BRANCH added. *A migration count that counts what the migration created measures the wrong thing*, and mixing a second perimeter into the refutation blunts it: the frozen text speaks of `tests/` and `bench/`, so the refutation must speak of `tests/` and `bench/` and nothing else. The `src/` sites are real and unnamed by the file list — `src/core/ecs/scheduler.zig`'s inline tests and `src/modules/forge/sync.zig`'s one production registration — and they belong to the Execution log, which records them, not to a count of what was migrated.

## Closing notes

- **What worked:**
  - **Deriving the view and the DAG from ONE `spec` removed the failure the milestone exists to close, rather than detecting it.** `SystemDescriptor.of(phase, name, spec, body)` produces the descriptors AND the type of the context; there is no second list, so nothing can disagree. It also retired a hazard nobody asked it to: `registerSystem` stores the caller's `accesses` slice without duplicating it, and `forge/sync.zig` documents a real crash from an inline `&.{ … }` temporary dangling — a derived set is a comptime constant and cannot.
  - **Erasing the world's TYPE instead of trying to hide it.** Zig has no private field and the brief says so; what the view does is store the pointer as `*anyopaque`, so the bypass costs an explicit cast at the call site instead of a field access. Pairing that with the job-bound marker was necessary rather than decorative: the erasure makes `carriesMarked` blind, and a view in a worker's arguments is exactly the hazard the marker exists for.
  - **The counter-proofs shipping WITH the guarantee, not after it.** Both halves of the harness were shown to redden before either was believed, and one of those counter-factuals found a defect in the harness itself.
  - **Reading the diff after a mechanical pass.** Two separate regressions from one regex — an eaten allocator parameter in every multi-line signature, and a real argument removed at every call whose receiver was a `World` — were caught by reading the diff pair by pair, not by the compiler, which was happy with several of them.
- **What deviated from the original spec:**
  - The FROZEN SECTION's registration count is refuted by measurement: **52 sites across 9 files**, not 51 across 10. Recorded above; the frozen text is a Claude.ai round-trip and was not edited here.
  - `WELD_ECS_PROTOCOL_VERSION` 1 → 2. Not named by the brief; prescribed by `root.zig`'s own doc comment for exactly this case.
  - Three additive `World` accessors (`changedTick`, `resourceBytes`, `currentTick`) outside the brief's file list, and `observers.zig` likewise — both justified in the Execution log.
  - The instruction-for-instruction comparison is `zig build ecs-access-zero-cost`, not a `test` block: a test cannot disassemble the binary it lives in, and the repository's only precedent for reading a claim in emitted code is a build step feeding a Zig scanner (`forge-asm-inventory`). `tests/core/ecs/access_view_test.zig` pins the structural precondition and names the step as the oracle.
- **What to flag explicitly in review:**
  - **A P1 of my own making was found by a review pass and fixed here**, and the way it was missed is worth more than the fix: resolution was a pre-pass over a BUFFER, so it covered the buffers someone remembered to pass it, and a third drain site read the same commands through neither entry. The comment at the site asserted the enumeration was complete. The repair moved the resolution to the apply boundary, which is a derivation rather than a list.
  - **A pre-existing DAG defect was reported, and FIXED at review** — crossed read/write declarations built a cycle `registerSystem` accepted and the first dispatch reported as `WriteWriteConflict`. It is byte-identical at the base and this branch is what made it reachable for Forge, which is the reason it did not stay reported: `registerSystem` now refuses it as `error.DependencyCycle` before committing anything. The scope really is bounded — one error variant, one reachability walk, no change to what the DAG MEANS — and `computeLevels` keeps its branch rather than gaining an `unreachable`, because the invariant is held by a sibling function and does not survive a failed registration.
  - **The Tier 3 guarantee is weaker than its first comment claimed**, and the comment now says so: C requires a diagnostic, not a refusal; the refusal is `-Werror`.
  - **The protocol bump.** Three shapes `root.zig` names as versioned changed, all breaking for a Tier 1 caller. If the intent was to keep the surface at 1, that is a decision to take before merge, not after.
  - **`FrameContext.user` is the residual the view cannot close.** It is byte-identical to the base and it carries an `?*anyopaque` the view does not restrict, so a body can reach anything through it. What it is NOT is a hole in the production path: measured, `frame.user` has **zero readers under `src/`**, and the eight files that read it are all under `tests/` and `bench/`. The one registered system in the tree does not use it. An earlier form of this line called it "the path every system in the tree actually uses to reach its query" — true of the test corpus, false of production, and the difference is the whole weight of the residual.
  - **The Tier 3 C mirror is hand-written** because no binding generator exists in this tree. It is pinned on both sides, but the pin is a layout pin: the day `weld_api.h` is generated, this header should be deleted rather than reconciled.
  - **The Tier 1 seam takes `anytype`, so the restriction is not checked AT that boundary — it is carried into it.** Eight functions were widened to accept either a view or a `*World` under one spelling: `sync.zig`'s `mirrorSolverState`, `authorityOf`, `syncOut` and `resolve`, the unnamed publisher at `sync.zig:377`, and `sync_in.zig`'s `changedAt`, `changedSince` and `syncIn`. That is what lets one body serve the dispatched regime and the direct one without a second copy, and it is also the price: `syncOut(gpa, pw, ecs, cmd)` type-checks against an unrestricted world exactly as well as against a view, and nothing at the call site says which arrived. The enforcement holds because the ONE registered caller passes `ctx.view`, which is typed by the declared set — upstream of the seam, not at it. Zig offers no way to say « any view, no world » without naming a trait the language does not have; what would close it is a comptime predicate on the parameter, which is a design decision and not a fix. **The migration of the production path is therefore less firm than the view's own surface**, and that should be read before the next module copies the shape.
  - The `registerOn*` family lost its `world` parameter at 42 call sites. Mechanical, but wide.
- **Final measurements:**
  - Test floor **2252 → 2286** (macOS, measured: `2267/2286 tests passed, 19 skipped`) and 2284 on `windows-2025`. The windows figure is DERIVED from the two `only_on = .windows` entries of the guard's own table, and the derivation was MEASURED mid-milestone: the CI cell reported `2245/2278 tests passed (32 skipped, 1 failed)` at a block where the derivation said 2278, matching exactly. Re-derived from the suite eight times as blocks landed — 2263, 2271, 2275, 2279, 2280, 2281, 2284, 2286 — the closure agreeing independently at each, the last two steps being the three cycle-refusal tests added at review and the two the adversarial pass then showed were missing.
  - Four corners green: Debug and ReleaseSafe, f32 and f64, `312/312 steps`.
  - `zig build forge-determinism` on all four corners: eight committed witnesses byte-identical, island partition / sleep state / manifold cardinality / retained pair set all OK, `divergence frame: none within K=60` — on a DAG whose edges changed, the forge system having gained `Reads(RigidBody)`.
  - C0.1 ReleaseFast, interleaved against `251db05` through a worktree, three rounds: **3.66 ms against 3.66 ms, delta 0.0 %**, both GO against the 16.6 ms gate. p99 moves in both directions; no envelope registered.
  - S1 at `--workers=4`, eight interleaved rounds: **52 666 ns against 52 646 ns, +0.0 %**. The base crossed the stale 62 µs gate once in eight; a first three-round sample had it at 78.1 / 66.5 / 53.0 µs, which is why the sample was widened instead of reported.
  - Zero cost: `weld_zero_cost_via_view` and `weld_zero_cost_via_world` are ONE body of **8 instructions** at ReleaseSafe — the backend found them byte-identical and emitted a single function. Counter-factual: a runtime check inside `getMut` takes it to 23 against 8.
  - The three refusals, verbatim: `weld-access-refused: this system attempts a read of component 'ecs.components.Velocity', which its declared access set does not grant. Declared: { writes(ecs.components.Transform) }`; the same for a write on a read declaration; and `missing struct field: accesses` for the registration with no declared set. The C witness: `initializing 'WeldVec3 *' with an expression of type 'const void *const' discards qualifiers`.
- **Residual risks / tech debt left intentionally:**
  - **No new `M1.D` entry has M1.A as its cause**, which is the milestone's own exit rule. The residuals are named in `CLAUDE.md` under *M1.A residuals* and none is deferred work: `FrameContext.user`, the absent `view.query` (no consumer), `services/physics.zig`'s `*World` on an Etch-callable path (not a system entry point, so outside the invariant's object), `M1.D.23` neither fixed nor promoted, and the Tier 3 guarantee bounded to a compilation witness.
  - Pre-existing and untouched, reported rather than stepped over: `M1.D.23` (the singleton gap on `queryFiltered`'s initial scan) and `M1.D.24` (`job_bound.reasonOf` narrower than its own walk, which this milestone's `SystemContext` change keeps in view).
