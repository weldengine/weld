# M0.0 — Custom linter and spec housekeeping

> **Status :** PLANNED
> **Phase :** 0.0
> **Branche :** `phase-0/tools/lint-custom`
> **Tag prévu :** `v0.0.8-M0.0-lint-custom`
> **Dépendances :** Phase −1 close (tag `v0.0.7-S6-ipc-round-trip`, 2026-05-18)
> **Date d'ouverture :** 2026-05-20
> **Date de fermeture :** —

---

# SECTION FIGÉE

*Produite par Claude.ai. Non modifiable par Claude Code hors aller-retour Claude.ai (cf. § Déviations actées).*

## Contexte

First milestone of Phase 0. M0.0 absorbs two debts inherited from Phase −1: the custom Zig linter deferred from S0 (`zig build lint` + `zig build lint-commit`) and the dead-reference cleanup in the spec (3 KB files pointing to `engine-spec.md §22.3 / §22.4`, neither of which exists at those numbers anymore — the roadmap has shifted to §25.3, §22.4 no longer exists). Deliberately small and low-risk — also serves to dry-run the milestone workflow on a simple case before M0.1 touches the ECS core.

## Scope

- **`tools/weld_lint/`** — in-tree Zig linter, zero external dependencies, two entry points:
  - `zig build lint` enforces **four patterns**: `@cImport` prohibited, `usingnamespace` prohibited, doc comments `///` mandatory on every `pub` declaration outside `test` blocks, `*_c` module imports allowed only from files carrying the `// AUTO-GENERATED — DO NOT EDIT` header on the first line.
  - `zig build lint-commit -- <file>` validates Conventional Commits (cf. `engine-development-workflow.md §4.3`).
- **`lefthook.yml`** updated: `pre-commit` invokes `zig fmt --check` and `zig build lint` in parallel; `commit-msg` invokes `zig build lint-commit -- {1}`, replacing the shell-script invocation in the default path. `pre-push` unchanged at M0.0.
- **`scripts/check-commit-msg.sh`** retained in the repo with a clarified header marking it as fallback only (no longer invoked by lefthook by default; remains as executable regex documentation and emergency fallback when the Zig toolchain is unavailable).
- **Test corpus under `tests/lint/`** : 3 invalid fixtures per pattern (`bad/cimport/`, `bad/usingnamespace/`, `bad/missing_doc_comment/`, `bad/c_module_isolation/`), 5–8 valid fixtures under `good/`, ≥5 valid + ≥5 invalid commit messages under `commit/{good,bad}/`, single driver `tests/lint/runner_test.zig`.
- **Spec dead-reference patch** in three KB files: `engine-phase-0-criteria.md`, `engine-phase-1-criteria.md`, `engine-tier-interfaces.md`. Mechanical replacement `§22.3 (Roadmap)` → `§25.3 (Roadmap)`. For `§22.4 (Carte par module)`: line-by-line audit during implementation, either repoint to the relevant current section in `engine-spec.md` identified at reading time, or remove the reference if purely decorative — Cas 2 blocker if ambiguous.
- **`README.md` patch** : remove the "S6 closed (tag pending merge)" block (now stale — tag `v0.0.7-S6-ipc-round-trip` is on `main` since 2026-05-18), update current-state section to "Phase 0 — M0.0 in progress", focused audit of build/lefthook instructions to ensure consistency with the new `zig build lint-commit` invocation. No broader rewrite.

## Out-of-scope

- **Lazy analysis guard enforcement** (cf. `engine-zig-conventions.md §13` rule "module rooting obligatoire"). Requires import-graph reachability analysis from each test target root — overshoots M0.0 calibration (≥ 2 h beyond budget). Deferred to M0.0.1 or Phase 0.1. The manual `zig build test 2>&1 | tail -5` count-check remains the interim review-side mitigation.
- **Opt-out annotation `// weld-lint:allow-no-doc`** for trivial re-exports. Added only if real friction emerges — purely additive, zero refactoring cost in deferring (no call sites affected).
- **Advanced lint rules** (ECS `extern struct` POD comptime check, strict doc-comment format, type-only imports, ABI stability check on `weld_c_api.api.zig`). Phase 0.1+.
- **`bindgen-lint` rules** from `engine-c-bindings.md §9.2` beyond the four covered above (e.g. `AUTO-GENERATED` header check on `*_binding.zig` files themselves, all keepers have a `.api.zig`, no manual `.d.etch`). These require the bindgen pipeline itself, not present at M0.0.
- **Any spec patch outside the three KB files explicitly listed.** Phase −1 retrospective already covered `engine-spec.md §25.3` and `engine-development-workflow.md`.
- **Any modification of Tier 0 code** (ECS, jobs, IPC, platform).
- **Any extension of the `c-api/` or `bindgen/` modules.**
- **`spec/` directory addition to the repo** (cf. `engine-development-workflow.md §3.5` — to be re-evaluated separately if friction emerges).

## Documents de spec à lire en premier

1. `engine-phase-0-plan.md` — section "M0.0 — Linter custom + housekeeping spec" (canonical scope source).
2. `engine-zig-conventions.md` — §13 (tests, lazy analysis guard for context), §14 (interop C, isolation rule), §17 (Zig version policy), §18 (migration), §19 (rules summary table).
3. `engine-c-bindings.md` — §9.1 (CI jobs), §9.2 (bindgen-lint rules — canonical formulation of the `*_c` isolation rule via `AUTO-GENERATED` header).
4. `engine-development-workflow.md` — §3.6 (cross-doc audit protocol), §4.3 (Conventional Commits format), §4.5 (lefthook hooks current and target state).
5. `engine-phase-0-criteria.md` — criterion C0.9 (Build/CI/conventions, partially advanced by M0.0).

## Fichiers à créer ou modifier

**Linter sources** :
- `tools/weld_lint/main.zig` — création — CLI dispatcher (`lint` vs `commit-msg` subcommand) and main entry point.
- `tools/weld_lint/scan.zig` — création — file walker, ignore list (build cache, vendored dirs).
- `tools/weld_lint/diagnostic.zig` — création — diagnostic struct, `file:line:col:rule:message` formatting.
- `tools/weld_lint/rules/no_cimport.zig` — création — tokenizer-based detection.
- `tools/weld_lint/rules/no_usingnamespace.zig` — création — tokenizer-based detection.
- `tools/weld_lint/rules/doc_comments.zig` — création — `std.zig.Ast`-based check on `pub` declarations.
- `tools/weld_lint/rules/c_module_isolation.zig` — création — tokenizer check for `@import("..._c")` combined with first-line `AUTO-GENERATED` header check on the importing file.
- `tools/weld_lint/rules/conventional_commit.zig` — création — message format validation per `engine-development-workflow.md §4.3`.

**Build integration** :
- `build.zig` — édition — wire `weld_lint` as build steps `lint` and `lint-commit`.
- `lefthook.yml` — édition — replace `scripts/check-commit-msg.sh` invocation by `zig build lint-commit -- {1}`, add `zig build lint` (parallel) to `pre-commit`.
- `scripts/check-commit-msg.sh` — édition — header comment marking the script as fallback only.

**Test corpus** :
- `tests/lint/runner_test.zig` — création — drives the linter on each fixture, asserts exit code and matched diagnostics.
- `tests/lint/bad/cimport/*.zig` — création — 3 fixtures with `@cImport` usage in varying contexts.
- `tests/lint/bad/usingnamespace/*.zig` — création — 3 fixtures.
- `tests/lint/bad/missing_doc_comment/*.zig` — création — 3 fixtures (`pub fn`, `pub const`, `pub var` without `///`).
- `tests/lint/bad/c_module_isolation/*.zig` — création — 3 fixtures importing a `*_c` module without `AUTO-GENERATED` header on the first line.
- `tests/lint/good/*.zig` — création — 5 to 8 fixtures covering legal usages of each pattern (well-documented `pub` decls, generated `*_c` consumer with `AUTO-GENERATED` header, etc.).
- `tests/lint/commit/good/*.txt` — création — ≥5 valid Conventional Commits messages.
- `tests/lint/commit/bad/*.txt` — création — ≥5 invalid messages (wrong type, trailing dot, uppercase first letter, exceeding 72 chars, missing scope where required, missing colon-space).

**Repo doc patch** :
- `README.md` — édition — remove "S6 closed (tag pending merge)" block, update current-state line, focused audit of build/lefthook instructions.

**KB spec patches (not committed to the branch — produced as chat output for Guy to upload)** :
- `engine-phase-0-criteria.md` — édition — `§22.3` → `§25.3`, `§22.4` resolution case-by-case.
- `engine-phase-1-criteria.md` — édition — same as above.
- `engine-tier-interfaces.md` — édition — same as above.

> **Note** : `engine-*.md` files live in the KB only at this stage (cf. `engine-development-workflow.md §3.5`). Claude Code produces patched content as chat output ; Guy uploads to the KB. They are NOT committed to the branch. The `README.md` patch IS committed.

## Critères d'acceptation

### Tests

- `tests/lint/runner_test.zig` — `test "rule no_cimport flags bad fixtures"` — runner invokes linter on each file under `tests/lint/bad/cimport/`, asserts non-zero exit code and at least one diagnostic mentioning `@cImport`.
- `tests/lint/runner_test.zig` — `test "rule no_usingnamespace flags bad fixtures"` — same pattern for `bad/usingnamespace/`.
- `tests/lint/runner_test.zig` — `test "rule doc_comments flags bad fixtures"` — same pattern for `bad/missing_doc_comment/`.
- `tests/lint/runner_test.zig` — `test "rule c_module_isolation flags bad fixtures"` — same pattern for `bad/c_module_isolation/`.
- `tests/lint/runner_test.zig` — `test "good fixtures pass clean"` — runner invokes linter on each file under `tests/lint/good/`, asserts zero exit code and empty diagnostic output.
- `tests/lint/runner_test.zig` — `test "production tree passes clean"` — runner invokes linter on the production tree (`src/`, `bench/`, `tests/` excluding `tests/lint/bad/`), asserts zero exit code.
- `tests/lint/runner_test.zig` — `test "commit messages valid set accepted"` — runner invokes `lint-commit` on each file under `tests/lint/commit/good/`, asserts zero exit code.
- `tests/lint/runner_test.zig` — `test "commit messages invalid set rejected"` — runner invokes `lint-commit` on each file under `tests/lint/commit/bad/`, asserts non-zero exit code with a diagnostic.

### Benchmarks

Not applicable — M0.0 is tooling, no performance target.

### Comportement observable

- `zig build lint` on the current production tree exits 0 with no output.
- Introducing `@cImport(@cInclude("stdio.h"))` in any production `.zig` file outside an `AUTO-GENERATED` module makes `zig build lint` fail with a `file:line:col:rule:message` diagnostic.
- Same outcome for `usingnamespace`, missing `///` on a new `pub fn`, and `@import("foo_c")` from a hand-edited file (no `AUTO-GENERATED` header on line 1).
- `zig build lint-commit -- <file with "feat(ecs): add change detection">` exits 0.
- `zig build lint-commit -- <file with "Add change detection.">` exits non-zero with a diagnostic naming the violated rule(s).
- `git commit -m "feat(ecs): add change detection"` succeeds via the migrated `commit-msg` hook ; `git commit -m "Add change detection."` is rejected with a clear message.

### CI

- `zig build` clean, zero warnings, on Linux + Windows matrix.
- `zig build test` green (debug + ReleaseSafe), on Linux + Windows matrix.
- `zig fmt --check` green.
- `zig build lint` green on the entire repo.
- `commit-msg` hook green on every commit of the branch.
- Pre-PR cross-doc audit per `engine-development-workflow.md §3.6.1` documented in the PR description (zero drift / N drifts patched / N drifts deferred to global audit).

## Conventions

- **Branche** : `phase-0/tools/lint-custom`
- **Tag final** : `v0.0.8-M0.0-lint-custom`
- **Titre de PR** : `Phase 0 / Tools / Linter custom and housekeeping`
- **Convention de commits** : Conventional Commits (cf. `engine-development-workflow.md §4.3`). Typical scopes for this milestone: `tools`, `lint`, `build`, `docs`, `ci`.
- **Stratégie de merge** : squash-and-merge (cf. `engine-development-workflow.md §4.6`).

## Notes

- **Tokenizer vs AST split.** Rules 1–3 (`@cImport`, `usingnamespace`, `*_c` isolation) operate at the token level — they match specific identifiers/builtins/keywords without needing semantic context. `std.zig.Tokenizer` is the right tool: avoids false positives from naive string matches inside comments, string literals, or doc comments. Rule 4 (doc comments on `pub`) requires attached-trivia → declaration mapping, which is what `std.zig.Ast` provides natively. Reimplementing trivia attachment is wasted work.
- **Canonical `*_c` isolation rule.** `engine-c-bindings.md §9.2` defines the rule as *"any `@import("<lib>_c")` outside a generated file fails the build"*, where "generated file" = carries `// AUTO-GENERATED — DO NOT EDIT` header. The brief encodes this header-based formulation. A directory-adjacency formulation would be brittle and inconsistent with the canonical rule.
- **Doc comment edge cases.** `std.zig.Ast`-based detection may produce false positives on advanced patterns (generic functions returning types, type aliases, anonymous struct fields, comptime decls exported via `pub const X = struct { ... }`). Strategy: start strict in M0.0. If real false positives surface during implementation on actual production code, iterate via *Déviations actées* with a Claude.ai roundtrip — do not invent opt-out annotations on the fly.
- **`§22.4` resolution.** Section §22.4 *"Carte par module"* does not exist in the current `engine-spec.md`. The content it referred to (likely a per-module map) has been refactored into `§1.6` (Tier 0/1 catalog) or dispersed across §1.x and §25.3. The brief instructs Claude Code to perform a line-by-line audit during implementation and either (a) repoint each occurrence to the relevant current section identified at reading time, or (b) remove the reference if it is purely decorative in the document header. If the choice is ambiguous on a given occurrence, this is a Cas 2 blocker — return to Claude.ai.
- **Pre-PR cross-doc audit.** Before closing the milestone, Claude Code performs the local cross-doc audit per `engine-development-workflow.md §3.6.1`: `grep -rn` on `/mnt/project/` (NOT `project_knowledge_search` — semantic indexing is structurally insufficient for literal occurrence audit) for the following terms introduced or modified by M0.0:
  - `tools/weld_lint/`
  - `zig build lint`
  - `zig build lint-commit`
  - `scripts/check-commit-msg.sh`
  - `§22.3` and `§22.4` (must have disappeared from the three patched files)
  - `tag pending merge` (must have disappeared from `README.md`)
  - `lazy analysis guard` (should appear in *Notes de fin* as "deferred to M0.0.1 or Phase 0.1")
  Result documented in the PR description.
- **Lefthook installation.** Lefthook is not in the repo (installed via system package manager). The migration here only changes the `lefthook.yml` configuration. No additional install step required for existing developers — running `zig build hooks` again refreshes the hook bindings (cf. `engine-development-workflow.md §4.5`).
- **Shell script fallback rationale.** `scripts/check-commit-msg.sh` is retained because (a) it serves as executable documentation of the Conventional Commits regex, (b) it is a natural fallback if the Zig toolchain is unavailable on a contributor machine. The header comment is updated to make the fallback status explicit.

---

# SECTION VIVANTE

*Tenue par Claude Code pendant le milestone. Le journal n'est pas un compte-rendu marketing : il sert à la review et au debug post-mortem.*

## Specs lues

*À cocher avant toute écriture de code de production.*

- [ ] `engine-phase-0-plan.md` (§ M0.0) — lu YYYY-MM-DD HH:MM
- [ ] `engine-zig-conventions.md` (§13, §14, §17, §18, §19) — lu YYYY-MM-DD HH:MM
- [ ] `engine-c-bindings.md` (§9.1, §9.2) — lu YYYY-MM-DD HH:MM
- [ ] `engine-development-workflow.md` (§3.6, §4.3, §4.5) — lu YYYY-MM-DD HH:MM
- [ ] `engine-phase-0-criteria.md` (C0.9) — lu YYYY-MM-DD HH:MM

## Journal d'exécution

## Déviations actées

## Blocages rencontrés

## Notes de fin

*À remplir au passage Status → CLOSED, juste avant ouverture de la PR.*

- **Ce qui a marché** :
- **Ce qui a dévié de la spec d'origine** :
- **Ce qui est à signaler explicitement en review** :
- **Mesures finales** (taille `tools/weld_lint/` en LOC, temps de `zig build lint` sur le repo, faux positifs résiduels) :
- **Risques résiduels / dette technique laissée volontairement** : lazy analysis guard reporté à M0.0.1 ou Phase 0.1 ; opt-out annotation reporté jusqu'à friction réelle ; règles bindgen-lint avancées hors scope.
