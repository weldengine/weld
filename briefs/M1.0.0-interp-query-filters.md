# M1.0.0 — Interpreter ↔ filtered ECS queries (dynamic query path)

> **Status:** CLOSED
> **Phase:** 1
> **Branch:** `phase-1/etch/interp-query-filters` (existing — extends PR #25)
> **Planned tag:** `v0.10.0-interp-query-filters`
> **Dependencies:** Phase 0 closed (`v0.9.0-phase-0-complete`); M0.1 (Tier-0 comptime query engine: `Query(Components, filters)`, `With`/`Without`/`Predicate`/`Changed`, `archetypeMatches`, lazy re-scan option β); M0.8 (full-grammar tree-walking interpreter + dynamic ECS bridge)
> **Opened:** 2026-06-17
> **Closed:** 2026-06-17

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Acted deviations).*

## Context

This brief **supersedes** the first M1.0.0 brief. A repo audit established the real state of the code, which differs from the prior brief and from the specs as they stood:

- The resolver does **not** produce an `archetype_set` (an archetype is a runtime notion). It produces a `WhenComponentSet` (`components_in_when` / `resources_in_when` in `src/etch/types.zig`) used only to gate `entity.get(T)`. The specs were corrected accordingly (`etch-resolver-types.md §12.2`, `etch-ast-ir.md §4`, `etch-validation-ecs.md §7.2`, `engine-ecs-internals.md §4`).
- The Tier-0 comptime query engine (`src/core/ecs/query.zig`) is correct and complete for native Zig systems: `Query(Components, filters)` with `With`/`Without`/`Predicate`/`Changed`, archetype matching via `archetypeMatches(arch, required_ids, with_ids, without_ids)` (already runtime — it takes resolved `ComponentId`s), and lazy re-scan (option β) via `rescanIfStale` + `matching_archetypes` bitset.
- **But the interpreter cannot use it.** `world.query` / `world.queryFiltered` are `comptime`-typed (they take `comptime Components: []const type`). Etch components are dynamic (runtime registry), so the interpreter has `ComponentId`s, not Zig types. There is **no dynamic query entry point** on `World`.
- Consequently the interpreter (M0.8 + the first M1.0.0 pass on PR #25) **reimplements** archetype selection by hand: a per-rule `matching_archetypes` bitset + `last_seen_archetype_count` + tail re-scan in `src/etch/interp.zig` — a near-literal copy of `query.zig`'s option-β machinery.

This sub-milestone removes that duplication at the root (decision B): expose a **dynamic query entry point** on `World` (matching by `ComponentId`, reusing `archetypeMatches` + the shared lazy re-scan), route the interpreter's rule entity selection through it, and delete the interpreter's hand-rolled matching cache. Filtering semantics (presence, exclusion, field-filter, composition) are wired onto this single path. It is the first Phase 1 prerequisite for piloting any module from Etch, and it pays down the M0.8/S4 "interp reimplements the query engine" debt instead of extending it.

## Scope

- **Livrable 0 — audit (no gate, chained into implementation).** In the execution journal, record: the exact shape of the interpreter's current hand-rolled selection (`matching_archetypes`, `last_seen_archetype_count`, `or`-bitset threshold path, `has_with_filter`); the exact signature of `archetypeMatches` and whether it already accepts slices or fixed arrays; how `not entity has T` and field-filters are currently represented in the resolved AST (dedicated node vs `not(has)` expression pattern). Identify every file touched.
- **Dynamic query entry point on `World`** (Tier-0): a selection primitive matching archetypes by runtime `ComponentId` (a `with`/`without` id set), reusing `archetypeMatches` and the same lazy re-scan as the comptime `Query` (option β). `archetypeMatches` is generalized to slices (`[]const ComponentId`) if it is not already, so the comptime and dynamic paths share one matcher (comptime call sites coerce `*const [N]ComponentId` → slice).
- **Interpreter rerouting**: rule entity selection goes through the dynamic query primitive. The hand-rolled `matching_archetypes` / `last_seen_archetype_count` / `or`-threshold cache in `interp.zig` is **removed**.
- **Presence** (`has T`) → `with`; **exclusion** (`not entity has T`, recognized as the `not(has)` pattern) → `without`.
- **Field-filter** (`has T { field op value }`) → `T` in `with`; the `{ … }` predicate is evaluated **per-entity** at iteration (like `Predicate`), over the entities of the selected archetypes.
- **Composition** `and`/`or`/`not`: `and` of structural conditions → single `with`/`without` set; `or` → union of dynamic-query results over each conjunctive term (the `when` grammar is a flat `and`/`or` sequence, no parentheses — DNF is trivial); non-structural `ecs_condition = expression` terms (e.g. `entity.health > 0`) stay per-entity predicates, never reduced to archetype filters.
- Etch test cases under `tests/etch/v1/` + Tier-0 tests for the dynamic query primitive.

## Out-of-scope

- `Changed<T>` / `entity has T changed` (the `Changed` filter exists in `query.zig` but is **not** exposed to the interpreter here) → **M1.0.1**.
- Events, `emit`, observers → **M1.0.2**.
- Resources without receiver (`get(T)` / `resource T` in `when`) → **M1.0.3**.
- Tag conditions (`tag_op`) — no new tag work beyond keeping M0.8 behaviour green.
- `W1210 RuleNeverMatches` emission — the static criterion is now well-defined (`components_in_when ∩ excludes ≠ ∅`) but its implementation is **tracked as Phase 1 debt**, not done here.
- Inter-rule parallelism / `ECSAccessMap` / rules-as-systems scheduling (future, cf. `etch-resolver-types.md §12.3`).
- Zig codegen of filtered queries (Phase 2) — the comptime path stays as-is.
- Any grammar / lexer / parser / resolver change. `WhenComponentSet` is consumed as-is.

## Spec documents to read first

1. `etch-grammar.md` — §6 (`when_clause`, `ecs_condition`), §7 (`rule_decl`).
2. `etch-resolver-types.md` — §1 (outputs: `WhenComponentSet`), §12.2 (when → component sets, runtime matching), §5.3.
3. `engine-ecs-internals.md` — §4 (Query Compilation: two paths, `archetypeMatches`, option-β lazy re-scan).
4. `etch-validation-ecs.md` — §7 (`when` validation recap, W1210).
5. `etch-ast-ir.md` — §4 (resolver outputs), §5.2 (ECS opcodes).

## Files to create or modify

- `src/core/ecs/query.zig` — edit — generalize `archetypeMatches` to slices if needed; expose the dynamic matching helper + shared lazy re-scan so both paths reuse it.
- `src/core/ecs/world.zig` — edit — add the dynamic query entry point (by `ComponentId`). Additive to the World API (does not break the C0.5 freeze, which covers Tier-0↔Tier-1 module interfaces, not internal World methods).
- `src/etch/interp.zig` — edit — route rule selection through the dynamic query; **remove** the hand-rolled `matching_archetypes` / `last_seen_archetype_count` / `or`-threshold cache; apply field-filter + non-structural predicates per-entity post-selection; compose `or` by union.
- `src/etch/ecs_bridge.zig` — edit if the selection plumbing lives there (confirm at Livrable 0).
- `tests/etch/v1/` — create — filter cases (see Acceptance).
- `src/core/ecs/` tests — create/extend — dynamic query primitive parity with the comptime `Query`.

## Acceptance criteria

### Tests

- Tier-0 — `test "dynamic query matches comptime query"` — for a seeded world, the dynamic query (by `ComponentId`) returns the **same** archetype set as the equivalent comptime `Query(Components, filters)`.
- Tier-0 — `test "dynamic query lazy rescan"` — a dynamic query picks up an archetype created after its first iteration (option-β parity).
- `tests/etch/v1/` — `test "filter has only"` — `when entity has T` visits exactly the entities whose archetype contains `T`.
- `tests/etch/v1/` — `test "filter not has"` — `when entity has A and not entity has B` excludes entities carrying `B`.
- `tests/etch/v1/` — `test "field filter eq / ordered"` — `{ field == value }` and a `>`/`<` case visit only matching entities (per-entity predicate over selected archetypes).
- `tests/etch/v1/` — `test "compose or union"` — `when entity has A or entity has B` matches the union; cross-checked against a hand-computed entity list.
- `tests/etch/v1/` — `test "non-structural predicate"` — `when entity.<expr>` (no `has`) is evaluated per-entity, not turned into an archetype filter.
- `tests/etch/v1/` — `test "dynamic archetype appears"` — a rule with no matching archetype at first tick activates once the matching archetype is created at runtime.
- Differential interp↔codegen corpus stays green (parity preserved).
- Zero leak under `std.testing.allocator`.

### Benchmarks

- Non-regression of the S4 interpreter bench (`run-demo-etch-interp` / the entity×rule sweep) within +5 % on the reference hardware (ReleaseSafe, cold-isolated protocol). Removing the hand-rolled cache must not regress steady-state; the dynamic primitive's `rescanIfStale` no-op is a single compare-and-branch (cf. `engine-ecs-internals.md §4`).

### Observable behaviour

- An `.etch` file with mixed-filter rules (has / not-has / field-filter / `or` / non-structural predicate), run through the interpreter, emitting a per-rule matched-entity count to the log; counts match a hand-computed expectation for the seeded world.

### CI

- `zig build` clean, zero warning; `zig build test` green (debug + ReleaseSafe); `zig fmt --check` green; `zig build lint` green; `commit-msg` hook green on every commit.

## Conventions

- **Branch:** `phase-1/etch/interp-query-filters` (existing; extend PR #25 rather than opening a new one).
- **Final tag:** `v0.10.0-interp-query-filters`
- **PR title:** `Phase 1 / Etch / Interpreter ↔ filtered ECS queries`
- **Commit convention:** Conventional Commits (scope `ecs` for the Tier-0 primitive, `etch` for the interpreter rerouting); cf. `engine-development-workflow.md §4.3`.
- **Merge strategy:** squash-and-merge (`engine-development-workflow.md §4.6`).

## Notes

- The plan title uses Tier-0 nomenclature `With<T>`/`Without<T>`. There is no such Etch surface — filtering is the `when` clause. Do not add a turbofish filter syntax.
- One matcher, two callers: do **not** leave two copies of the archetype-matching / lazy-re-scan logic. The whole point of this sub-milestone is that `interp.zig` stops duplicating `query.zig`.
- `not entity has T` is grammatically the `expression` alternative of `ecs_condition` (a boolean `not entity.has(T)`), not a dedicated exclusion operator. Recognize the `not(has)` pattern to derive `without`; everything not matching a structural pattern stays a per-entity predicate.
- This brief touches Tier-0 (`world.zig`, `query.zig`). Keep changes additive and minimal; the comptime path must remain byte-behaviour-identical.
- PR #25 already implemented filtering on the hand-rolled cache. That work is not wasted: the filter semantics and tests carry over; only the selection engine underneath is replaced.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone.*

## Specs read

- [x] `etch-grammar.md` (§6, §7) — read 2026-06-17 20:50 (lu intégralement, 2196 lignes)
- [x] `etch-resolver-types.md` (§1, §12.2, §5.3) — read 2026-06-17 20:50 (lu intégralement, 1497 lignes)
- [x] `engine-ecs-internals.md` (§4) — read 2026-06-17 20:50 (lu intégralement, 1198 lignes)
- [x] `etch-validation-ecs.md` (§7) — read 2026-06-17 20:50 (lu intégralement, 1353 lignes)
- [x] `etch-ast-ir.md` (§4, §5.2) — read 2026-06-17 20:50 (lu intégralement, 1727 lignes)

## Execution journal

### 2026-06-17 — Livrable 0 (audit, no gate)

Audit chaîné directement sur l'implémentation (lecture du code, pas du
souvenir). État réel de la branche après la 1ʳᵉ passe PR #25.

**Cache de sélection maison de l'interp (`src/etch/interp.zig`).** La sélection
vit dans `interp.zig`, pas dans `ecs_bridge.zig`. Forme exacte :
- `RuleDesc.matching_archetypes: std.DynamicBitSetUnmanaged` (interp.zig:218) +
  `RuleDesc.last_seen_archetype_count: usize` (interp.zig:221).
- `rescanMatching` (interp.zig:1037) : rescan lazy option-β sur la *queue* de
  `world.archetypes` — `resize` du bitset, puis pour chaque nouvel archétype
  `evalPredicate(rd.predicate_pool, root, arch)` (interp.zig:2731) et `set` du
  bit. O(0) en steady-state.
- `runRule` (interp.zig:947→985-990) : appelle `rescanMatching`, itère le
  bitset `matching_archetypes`, dispatche `iterateArchetype` par archétype.
- `evalPredicate` : tree-walk récursif sur un `PredicateNode` pool
  (`and_`/`or_`/`not_`/`has`) contre `arch.hasComponent(cid)`. **C'est le
  matcher dupliqué** — `query.zig` matche via `archetypeMatches(required, with,
  without)` (flat), l'interp via un arbre booléen `has`-feuilles.
- **`or`-bitset threshold (`uses_or` / `or_bitset_threshold`) : déjà supprimé**
  par la 1ʳᵉ passe PR #25 (commit b8ccfd6 — le chemin caché est universel).
  `grep` ne trouve plus aucune occurrence. Le mandat Livrable 0 le citait
  d'après l'état pré-PR#25 ; consigné comme déjà absent.
- `predicate_archetype_evals` (RuntimeReport, interp.zig:68) : compteur
  observable prouvant le rescan tail-only (1 éval/archétype à l'apparition).
- Field-filters : `field_filters: []FieldFilter` (`==` seul via `Value.eql`,
  interp.zig:2751), `expr_filters` (`has T { expr }` général), `changed_filters`,
  `tag_predicates`, `expr_conds` — tous per-entity, **flat-AND**, évalués dans
  `iterateArchetype` (interp.zig:1002-1019). Modèle plat conservé (imprécision
  S4 documentée pour un filtre sous `or`/`not`).

**Signature de `archetypeMatches` (`src/core/ecs/query.zig:494`).** **Prend déjà
des slices** `(arch, required_ids: []const ComponentId, with_ids: []const
ComponentId, without_ids: []const ComponentId)`. Le chemin comptime coerce
`*const [N]ComponentId → slice` (query.zig:304). **Aucune généralisation
nécessaire** — le matcher est déjà partageable tel quel. Le rescan lazy vit dans
le `Query.maybeRescan` générique (query.zig:292-327) via `ArchetypeView` +
`archetypeMatches`. `world.zig:935 worldArchetypesSlice` est l'accesseur
`ArchetypeView` réutilisable ; `world.zig:876 queryFiltered` est la construction
comptime. `world.archetypes` est **append-only** (seul `append` + `errdefer
pop`), donc un verdict caché ne devient jamais obsolète (invariant de soundness
option-β).

**Représentation AST de `not entity has T` et des field-filters.** `not entity
has T` = nœud `when` `.logical_not` enveloppant `.has` (parser.zig:4643
`parseWhenNot`), abaissé en `PredicateNode{.not_, lhs=has}` — **pattern
`not(has)`, pas d'opérateur d'exclusion dédié** (conforme brief Note). Field-
filters : `.has_with_filter` (`==`) et `.has_expr_filter` (`{ expr }` général)
émettent un nœud `.has` (contrainte structurelle) + un filtre per-entity
(interp.zig:3278-3338). Précédence `when` : `parseWhenOr → parseWhenAnd →
parseWhenNot → parseWhenPrimary` ⇒ `and` lie plus fort que `or`, l'arbre est un
*or*-spine de chaînes *and* (DNF triviale, conforme brief). Le parser accepte
des parenthèses (parser.zig:4669) que l'EBNF §6 interdit — non bloquant : la
conversion DNF ci-dessous est générale (De Morgan) et reste correcte pour les
arbres parenthésés.

**Sortie resolver (`src/etch/types.zig:3326`).** `WhenComponentSet` =
`components_in_when` / `resources_in_when` (sets de noms), pour le *gating* de
`entity.get(T)`. Pas de `excludes` matérialisé (la spec corrigée le mentionne
comme rôle ; le code ne trace que les deux sets positifs — commentaire
types.zig:3331 « no archetype query is derived »). **Non consommé par la
sélection** : l'interp construit sa sélection depuis l'AST `when_root`, pas
depuis le resolver. Hors-scope (pas de modif resolver).

**Décision de conception (tranchée sur la base du brief — § Acted deviations
AD-2).** L'union `archetypeMatches(with, without)` sur les termes d'une DNF est
**logiquement équivalente** à l'arbre booléen `evalPredicate` (DNF ≡ formule
d'origine). Donc : on **conserve** la construction du pool `PredicateNode` par
`lowerWhen` (logique de feuilles éprouvée — has/not-has/positive-tag→`has
TagSet`/absorption `no_child`), puis on **convertit ce pool en DNF** au compile.
La sélection d'archétypes runtime est alors **prouvablement identique** à
l'ancienne → parité différentielle préservée par construction. `evalPredicate`
disparaît du runtime (remplacé par `archetypeMatches`, le matcher partagé).

**Fichiers touchés (pinned).**
- `src/core/ecs/query.zig` — ajout `DynamicQuery` (single-term, by `ComponentId`)
  + helper de rescan partagé `rescanNewArchetypes` ; refactor `Query.maybeRescan`
  pour le réutiliser (comportement byte-identique). `archetypeMatches` inchangé.
- `src/core/ecs/world.zig` — ajout `queryDynamic(gpa, with_ids, without_ids)`
  (réutilise `worldArchetypesSlice`). Additif.
- `src/etch/interp.zig` — `RuleDesc.selection: []DynamicQuery` (un par terme DNF)
  remplace `matching_archetypes`/`last_seen_archetype_count`/`predicate_pool`/
  `predicate_root` ; `runRule` itère l'union (terme unique = direct ; multi-terme
  `or` = k-way merge ascendant dédupliqué) ; `rescanMatching`/`evalPredicate`
  supprimés. `dnfFromPool` (compile) convertit le pool en termes. Per-entity
  filters inchangés. 2 tests inline (interp.zig:5367/5415) adaptés au nouveau
  compteur.
- `tests/etch/v1/` — suite filtres (carry-over PR #25, sémantique préservée) +
  cas `or union` / `non-structural predicate` / `dynamic archetype appears`.
- `src/core/ecs/` tests — parité `DynamicQuery` ↔ comptime `Query` + rescan lazy.
- `ecs_bridge.zig` — **aucune modif** (ne porte aucune sélection ; confirmé).

**Conclusion (no gate).** Enchaînement direct sur l'implémentation : (1) Tier-0
`DynamicQuery` + helper de rescan partagé ; (2) `queryDynamic` sur World ;
(3) rerouting interp via DNF→queries dynamiques, suppression du cache maison ;
(4) tests Tier-0 + v1 + adaptation inline ; (5) observable + bench.

### 2026-06-17 — Implémentation

- **Tier-0 (`query.zig`/`world.zig`).** Helper de rescan partagé
  `rescanNewArchetypes` (corps unique : skip singleton + `archetypeMatches` +
  `onMatch` comptime) ; `Query.maybeRescan` (comptime) refactoré pour l'appeler
  (byte-identique — `tests/ecs/queries.zig` reste vert). Nouveau `DynamicQuery`
  single-term, et `World.queryDynamic` additif (`last_seen=0` → 1ᵉʳ `maybeRescan`
  = scan initial via le même chemin). `archetypeMatches` **inchangé** (déjà
  slices). Aucun symbole comptime retiré.
- **Interp (`interp.zig`).** `RuleDesc.selection: []DynamicQuery` (un par terme
  DNF) remplace `predicate_pool`/`predicate_root`/`matching_archetypes`/
  `last_seen_archetype_count`. `dnfFromPool` convertit le pool `PredicateNode`
  (toujours bâti par `lowerWhen`, inchangé) en DNF (De Morgan ; `and`
  cross-product, `or` concat) ; termes insatisfiables droppés. `runRule` →
  `iterateSelection` : terme unique = itération directe ; multi-terme `or` =
  `iterateUnion` (k-way merge ascendant par `archetype_id`, dédup, buffer
  `merge_cursors` réutilisé). `rescanMatching` + `evalPredicate` supprimés.
  `predicate_root == no_child` normalisé en `null` (match-all per-entity) —
  corrige au passage un footgun latent (l'ancien `rescanMatching` aurait indexé
  `pool[maxInt]` sur une règle entity-bound à `when` purement non-structurel).
  Per-entity filters inchangés. `ecs_bridge.zig` **non modifié**.
- **Parité par construction.** DNF ≡ formule `evalPredicate` ⇒ sélection
  d'archétypes prouvablement identique. Corpus différentiel interp↔codegen vert.

### 2026-06-17 — Validation

- `zig build`, `zig build test` (Debug + ReleaseSafe), `zig fmt --check`,
  `zig build lint` verts (exit 0). Le corpus différentiel interp↔codegen et la
  suite `tests/ecs/queries.zig` (parité comptime↔dynamique + rescan lazy)
  inclus.
- **Finding — UB latent du harness v1 (PR #25), exposé par le reroute.**
  `query_filters_test.zig` `load()` passait `&pr.ast` à `compile` (qui stocke
  `interp.ast`) puis **déplaçait `pr` par valeur** dans le `Loaded` retourné →
  `interp.ast` pendait sur la frame morte. Fonctionnait sur `main` par chance ;
  le changement de layout l'a exposé (`exprs.len` lu à 0 → index OOB). Corrigé :
  `ParseResult` heap-alloué (adresse stable). Contrat moteur inchangé (les tests
  inline interp gardent `pr` en scope). `tests/etch/v1/` est dans le scope.
- 2 tests inline « cache » (interp.zig) adaptés au comptage par-terme : le
  fixture `Counter and (A or B)` → 2 termes DNF, donc N × 2 évals/rescan
  (8→16, 3→6, tail 1→2) ; dédup union vérifiée (entité `{Counter,A,B}` dispatché
  une fois). Bench S4 : voir Closing notes › Measures.

## Blockers encountered

Aucun.

## Acted deviations

### AD-2 — DNF dérivée du pool `PredicateNode` (réalisation, pas déviation)

Le brief impose : Tier-0 = primitive single-term `(with, without)` réutilisant
`archetypeMatches` + le rescan lazy ; interp = union des résultats sur les termes
conjonctifs de la DNF. Réalisation choisie, tranchée sur la base du brief :

1. **Conversion via le pool existant.** Plutôt que de réécrire l'extraction des
   feuilles structurelles depuis l'AST, on conserve la construction du pool
   `PredicateNode` par `lowerWhen` (inchangée — feuilles éprouvées : has/not-has,
   positive-tag → `has TagSet`, absorption `no_child` des feuilles non
   structurelles) et on convertit ce pool en DNF (`dnfFromPool`, De Morgan).
   **Justification :** l'union `archetypeMatches(with, without)` sur les termes
   DNF est logiquement équivalente à l'arbre booléen `evalPredicate` (DNF ≡
   formule). La sélection d'archétypes est donc **prouvablement identique** à
   l'ancienne → parité différentielle préservée par construction, risque minimal.
   `evalPredicate` quitte le runtime (remplacé par `archetypeMatches` partagé) ;
   le pool reste un IR transitoire de compile, libéré après conversion.
2. **Primitive Tier-0 single-term.** `DynamicQuery` matche un seul `(with,
   without)` (fidèle au brief). L'interp tient `selection: []DynamicQuery` (un par
   terme) et compose l'`or` par union dédupliquée (terme unique = itération
   directe, cas courant ; multi-terme = k-way merge ascendant par `archetype_id`,
   sans allocation par tick via un buffer de curseurs réutilisé).
3. **Un seul matcher + un seul rescan.** `archetypeMatches` (déjà slices) est le
   matcher unique ; `rescanNewArchetypes` (query.zig) est le corps de rescan
   unique, appelé par `Query.maybeRescan` (comptime, byte-identique) ET
   `DynamicQuery.maybeRescan`. `interp.zig` ne contient plus aucun rescan ni
   matcher — l'objet même du milestone.
4. **`predicate_archetype_evals` conservé** (observable du tail-only rescan),
   sémantique ajustée : nb d'évaluations `archetypeMatches` pendant les rescans
   des queries dynamiques de la règle (somme sur les termes). Les 2 tests inline
   sont adaptés à ce comptage.

Aucune modif grammaire / lexer / parser / resolver. Chemin comptime
`query.zig`/`world.zig` byte-identique (additif).

## Closing notes

### What was delivered

Le moteur de matching d'archétypes est désormais **unique et partagé** entre le
chemin comptime (`query.Query`) et le chemin dynamique de l'interpréteur. La
duplication ciblée par le milestone — l'interp recopiant l'option-β de
`query.zig` (`rescanMatching` + `matching_archetypes` + `evalPredicate`) — est
supprimée à la racine.

- **Tier-0** : `query.DynamicQuery` (single-term, by `ComponentId`) +
  `World.queryDynamic`, réutilisant `archetypeMatches` (inchangé, déjà slices) et
  un **corps de rescan unique** `query.rescanNewArchetypes` que le
  `Query.maybeRescan` comptime appelle aussi (refactor byte-identique).
- **Interpréteur** : la sélection d'entités de chaque règle entity-bound passe
  par une **DNF de queries dynamiques** (`RuleDesc.selection`, un `DynamicQuery`
  par terme conjonctif). `has T` → `with`, `not entity has T` (pattern
  `not(has)`) → `without`, field-filter → `with` + prédicat per-entity, `or` →
  **union** dédupliquée des termes (k-way merge). Le cache maison
  (`matching_archetypes`/`last_seen_archetype_count`) et `evalPredicate` sont
  retirés ; `interp.zig` ne contient plus aucun matcher ni rescan.
- **Sémantique de filtrage** : présence, exclusion, field-filter (`==` via
  `field_filters` ; `>`/`<`/général via `expr_filters`/`expr_conds`),
  composition `and`/`or`/`not` — préservées (carry-over PR #25), portées sur le
  chemin unifié. La DNF étant logiquement équivalente à l'ancien arbre booléen,
  la sélection d'archétypes est **identique par construction**.
- **Tests** : Tier-0 `tests/ecs/queries.zig` (`dynamic query matches comptime
  query`, `dynamic query lazy rescan`) ; `tests/etch/v1/` (has / not-has /
  field eq / field ordered / compose and-or-not / **compose or union** /
  **non-structural predicate** / **dynamic archetype appears** / observable).
  Observable étendu aux 5 formes de filtre avec log per-rule. Zéro fuite
  (`std.testing.allocator`).

### Notable items for review

- **AD-2** : réalisation single-term Tier-0 + union interp + conversion via le
  pool `PredicateNode` existant (DNF ≡ formule → parité par construction). Pas
  une déviation du brief.
- **UB harness v1 corrigée** : `load()` déplaçait `pr` après que `compile` ait
  capturé `&pr.ast` → `interp.ast` pendant. Latent sur PR #25 (marchait par
  chance), exposé par le changement de layout. Fix : `ParseResult` heap-alloué.
  Le contrat moteur (« le caller garde l'AST vivant à une adresse stable ») est
  inchangé et honoré par tous les autres call sites.
- **`W1210 RuleNeverMatches`** : non émis (hors-scope, dette Phase 1). Une règle
  dont **tous** les termes DNF sont insatisfiables (`has T` ∧ `not has T`) a une
  `selection` vide → ne matche rien (comportement runtime correct, sans warning
  resolve-time).
- **Parenthèses dans `when`** : l'EBNF §6 les interdit mais le parser les
  accepte ; `dnfFromPool` est général (De Morgan) et reste correct pour les
  arbres parenthésés (le fixture `compose` / `or_union` en exerce).
- `ecs_bridge.zig` **non modifié** (surface publique du bridge inchangée).

### Measures

Bench S4 interpréteur, ReleaseSafe, apple_m4, `caffeinate -i` (thermal-aware),
best-of-2 (médiane la plus froide = opposable), baseline = `main` 25a5cf3 même
machine/session :

| Sweep | Baseline (main) | M1.0.0 (B2) | Δ médiane | Gate |
|---|---|---|---|---|
| 1 000 ent × 5 règles × 1 000 ticks | 0.672 ms | 0.678 ms | **+0.9 %** | < 10 ms → **GO** (14.7×) |
| 10 000 ent × 5 règles × 100 ticks | 6.995 ms | 6.958 ms | **−0.5 %** | < 100 ms → **GO** (14.4×) |

Le sweep 1 000 (1 000 ticks, statistiquement stable) : **+0.9 %, dans le gate
+5 %**, cohérent avec le +1.2 % historique PR #25. Le sweep 10 000 (100 ticks)
est dominé par le bruit thermique (p99/max oscillent 8→18 ms) — le delta médian
−0.5 % est dans le bruit. Base algorithmique : les 5 règles du demo sont
`and`-only ⇒ 1 terme DNF ⇒ chemin d'itération directe, flot de contrôle
identique au cache maison (un compare-and-branch `maybeRescan` + walk). Aucune
régression. Rapport : `bench/results/s4-etch-interp-20260617-1942.md`.

### Residual debt

- **`W1210 RuleNeverMatches`** : critère statique bien défini
  (`components_in_when ∩ excludes ≠ ∅`) mais non implémenté — dette Phase 1
  (brief Out-of-scope).
- **Imprécision S4 des filtres per-entity** (inchangée, hors-scope) : un
  field/tag/expr-filter sous `or`/`not` est ANDé à plat (les prédicats per-entité
  ne participent pas à l'arbre booléen ; seule la sélection **structurelle**
  d'archétypes est en DNF). Le corpus différentiel n'exerce que AND. À traiter si
  un field-filter sous `or` devient nécessaire.
- **`merge_cursors`** (buffer k-way merge) : alloué au plus une fois, réutilisé.
  Trivial mais nouveau champ d'état sur l'interpréteur, propre au chemin `or`.
