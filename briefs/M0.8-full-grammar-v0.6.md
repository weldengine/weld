# M0.8 — Etch full-grammar: parser + interpreter + codegen

> **Status:** ACTIVE
> **Phase:** 0.2
> **Branch:** `phase-0/etch/full-grammar-v0.6`
> **Planned tag:** `v0.8.0-M0.8-etch`
> **Dependencies:** M0.1 (ECS Tier 0). The S3/S4/S5 Phase −1 spikes are the baseline to extend (parser `v0.0.4`, tree-walking interpreter `v0.0.5`, direct AST→Zig codegen `v0.0.6`).
> **Open date:** 2026-06-06
> **Close date:** —

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Accepted deviations).*

## Context

This milestone extends the Etch S3/S4/S5 subset to the **full EBNF v0.6 grammar** and advances criterion **C0.2** (Etch usable end-to-end). The parser stays **recursive-descent + Pratt**; the hybrid LR(1) incremental rewrite is Phase 1 (C1.11, `etch-parser.md §23`) and is out of scope. This is the largest Phase 0 milestone — it is delivered in **seven reviewed stages (E1–E7)**, each landing a testable vertical slice, on the M0.5/M0.6/M0.7 model.

**What S3/S4/S5 actually delivered (verify in code before scoping each stage).** The S3 subset is narrow: `component`, `resource`, `rule`, the `when` clause, and basic arithmetic expressions only. The infrastructure delivered is reusable as-is and is what this milestone *extends*: a UTF-8 lexer, a tabular SoA AST (`MultiArrayList(Item|Stmt|Expr|TypeNode)`), a two-pass type-checker, typed diagnostics (`src/etch/`, ~2050 lines, public API in `src/etch/root.zig`), a tree-walking interpreter with a dynamic ECS bridge (runtime component registry, dynamic SoA archetype, resource store), and a **direct AST→Zig codegen with no HIR** (`comptime_query.query(world, .{T...})`, one non-generic function per rule, `Registry.registerAlias`). **`fn`, `struct`, `enum`, `trait`, `impl`, `event`, `tags` are NOT in the delivered subset — they are net-new construct support in this milestone, not completion of existing code.** The "extension" is of the *infrastructure*; most constructs themselves are new. Claude Code confirms the delivered surface (read `src/etch/root.zig` + the S3/S4/S5 corpus) before scoping each stage; it does not re-spec from this brief what already exists.

**Three construct support levels (governing distinction — do not collapse it).** The plan says "interpreter executes all constructs", which is unrealistic as stated: real Tier 1 modules (Cortex, UI/Islandz, Kinesis, Pulse, Ember, Sequencer, Render) are out of Phase 0 scope beyond their interface stub. One cannot *execute* an `audio_graph` or an `anim_graph` without an audio/animation engine behind it. The dependency of Level B constructs on Tier 1 modules is a deliberate architectural property, not a gap. Hence three levels:

- **Level A — real behavioural execution.** `fn`, `component`, `resource`, `event`, `rule`, `tags`, control flow, expressions. The interpreter genuinely executes against the mini-ECS; the codegen emits functional Zig; **the byte-exact differential is the world state after N ticks.**
- **Level B — declaration + internal IR/descriptor, no module wiring.** `behavior`, `routine`, `quest`, `dialogue`, `ability`, `data`, `widget`, `theme`, `motion`, `locale`, `input_mapping`, `shader`, `effect`, `anim_graph`, `audio_graph`, `audio_score`, `sequence`. Full sub-grammar parse + semantic validation + structure construction. The differential is on the **serialized internal IR/descriptor**, not on absent module behaviour.
- **Level C — serialization.** `scene`, `prefab`. Parse + typed extensions (`.scene.etch` / `.prefab.etch`, `E0858`) + validation. Runtime instantiation is M0.9 (`engine-scene-serialization.md`), not this milestone.

Consequence for the 500-line reference differential: the file is built so the diffed observable behaviour comes from **Level A** (rules/events/ECS state). Level B/C constructs appear in it to validate parse + codegen-compiles, not for a behavioural diff.

## Scope

The work is delivered as **seven vertical stages** (parser→interpreter→codegen per construct group), in dependency order. Each stage lands a testable slice and ends with a review gate (`Review Ei`): Claude Code completes the stage, pushes, and waits for a Claude.ai review relayed by Guy before starting the next stage. The stage decomposition is the work breakdown for Step 3 of the execution protocol; it is not a re-scope mechanism and Claude Code never re-cuts it mid-milestone.

**Execution grain inside a stage (fixed — do not ask how to sequence it).** Claude Code implements the entire `Ei` block in a single pass, then stops at the `Review Ei` gate and presents the stage diff. There is no per-construct sub-decomposition and no intra-stage checkpoint: a stage is reviewed once, at its end, on its differential — not feature by feature. Claude Code does not ask Guy how to run a stage; the grain is settled here.

**E1 — Language foundations (non-ECS) + 5 S3 debts + breaking interface + EBNF harness mechanism**
- v0.6 expression/control-flow foundations: exhaustive tiered `match` (Maranget algorithm), closures `|a| e`, `loop` / labeled loops / `break label value`, ranges `..=`, collections `T[N]` / `T[]` / `Map` / `Set` / `Iterator`, casts `as`, error handling `throws` / `throw` / `try` / `catch` / `assert`, `type` aliases. Exercised inside `rule` bodies (delivered at S3). Full stack: parse + interpret + codegen.
- The 5 S3 debts (all lexer/parser/resolver core): **D-S3-trivia** (trivia/doc comments attached to nodes via `TriviaMap`); **D-S3-doccomment** (`///` lexed as doc comment, not line comment); **D-S3-annot-applicability** (annotation applicability validated, cf. `etch-resolver-types.md §13`); **D-S3-annot-field-access** (`@requires(self.health)` parses, fix `continuePostfixAndBinary` → `parsePostfix`); **D-S3-resource-receiver** (`get(T)` / `get_mut(T)` resources without receiver supported in rule body).
- Breaking interface change: `ParseResult.diagnostic: ?Diagnostic` → `diagnostics: []Diagnostic`, plus the top-level recovery sync-point (after a diagnostic, advance to the next top-level keyword or EOF, then resume `parseTopLevel`). Landed here because every later stage consumes this interface.
- EBNF harness mechanism: the `@embedFile` + feed-to-parser machinery for `ebnf_examples_test.zig`, then fed continuously as constructs land.
- **Review E1:** foundations differential (expressions, `match`, closures, error handling) + recovery sync-point test + the 5 S3 debts each with a test.

**E2 — Type-declaration layer (Level A)**
- `fn` (top-level + associated), `struct`, `enum`, `trait`, `impl`. Net-new (the S3 subset lacks them — verify, do not re-spec the existing). Full stack: parse + interpret + codegen.
- Generic syntax `<T>` is **parsed** and **interpreted** (the tree-walker monomorphises at runtime trivially). **Generic codegen monomorphisation is out of scope** (HIR-dependent, Phase 2 — see Out-of-scope). `async fn` syntax is **parsed** here; its interpretation and codegen boundary are set in E3.
- **Review E2:** differential on `fn` / `struct` / `enum` / `trait` / `impl` behaviour; generic instances validated through the interpreter only.

**E3 — Executable ECS layer (Level A complete) + 5 S4 debts + 2 S5 build debts**
- `event` declarations + observers + ECS event bridge.
- `tags` hierarchical declaration: compact bitfield encoding (`etch-validation-ecs.md §5`), operators `has_tag` / `has_no_tag` / `has_any_tag` / `has_all_tags` / `has_no_tags`, `add_tag()` / `remove_tag()` deferred structural mutations at the tick boundary.
- `rule` extended to complex queries with `With<T>` / `Without<T>` / `Changed<T>` filters (`etch-validation-ecs.md §12`).
- Async **interpretation** (the tree-walker is its own runtime: suspend on `await`, resume on wake). Async **codegen is out of scope** (see Out-of-scope).
- Absorb the 5 S4 debts: **D-S4-runtime-query** (`RuntimeQuery` / `world.query_dynamic` consumed on the interpreter hot path, or the abstraction dropped); **D-S4-runtime-report** (`RuntimeReport.last_error` assigned by `execBody`, typed `RuntimeError`/`SourceSpan` propagated to the caller); **D-S4-ecs-bridge-panic** (`ecs_bridge.writeValueAsBytes` returns a `TypeMismatch` variant instead of `@panic`); **D-S4-or-archetype** (`or` predicate gets a bitset short-cut above an archetype-count heuristic); **D-S4-multifilter** (per-rule field filter supports several `has_with_filter` — array, not single).
- Absorb the 2 S5 build debts: **D-S5-etchcook-inproc** (`etch_cook` consumed as an in-process build helper, not a subprocess); **D-S5-synth100-proper** (`synth_100/build.zig` is a proper sub-project depending on `weld_core`, not a placeholder).
- **Review E3:** byte-exact ECS differential (events, observers, filtered queries, tags) + the 7 debts each with a test. **End of E3 = Level A complete in both interpreter and codegen** (async excluded from codegen).

**E4 — Level B gameplay** (6 constructs)
- `behavior`, `routine`, `quest`, `dialogue`, `ability`, `data`: exhaustive sub-grammar parse + semantic validation (`etch-validation-ecs.md`, relevant gameplay sections) + internal IR/descriptor + interpreter builds the structure + codegen emits the Zig structure.
- **Review E4:** internal-IR differential.

**E5 — Level B presentation** (5 constructs)
- `widget`, `theme`, `motion`, `locale`, `input_mapping`: exhaustive parse + validation (`etch-validation-ecs.md`, UI + input sections) + internal IR + interpreter/codegen structure. `input_mapping` is Level B strict: parse + validation + IR, **no input execution**.
- **Review E5:** internal-IR differential.

**E6 — Level B render / animation / audio / cinematic** (6 constructs)
- `shader`, `effect`, `anim_graph`, `audio_graph`, `audio_score`, `sequence`: exhaustive parse + validation (`etch-validation-ecs.md`, render/anim/audio sections) + internal IR + interpreter/codegen structure. `shader` includes the resolver's shader mode (rejects heap/string/Entity/closures/async). SPIR-V/MSL/DXIL/WGSL emission is out of scope (Phase 2+).
- **Review E6:** internal-IR differential.

**E7 — Level C + closure**
- `scene`, `prefab` (Level C): parse + typed-extension enforcement (`.scene.etch` / `.prefab.etch` via `E0858`) + validation.
- **AST stable interface freeze:** audit `src/etch/ast.zig`, document the Level-1 stable surface in `etch-parser.md §10.3` ("Interface contract stable cross-phase"), test via `tests/etch/ast_stable_interface.zig` exercising ≥20 distinct public entry points (NodeKind/ExprKind/StmtKind discriminants, span helpers, intern tables, public iterators). Placed here because all `NodeKind`/`ExprKind` variants are then settled.
- `tests/etch/reference_500_lines.etch` (500+ lines, mix of all constructs), parsed < 50 ms, with a global differential: Level-A behaviour byte-exact interpreter↔codegen; Level-B/C constructs present to validate parse + codegen-compiles, not for a behavioural diff; generic/async fragments present for parse+interpret validation only (no codegen diff).
- Interpreter hot-reload (edit → AST swap) < 500 ms.
- EBNF harness validated: ≥30 Etch blocks extracted from `etch-grammar.md` (and `etch-reference-part*.md` blocks tagged `<!-- ebnf-example -->`) parse without error; a doc example without parser support fails CI.
- Re-bench reference machines (Win11 + Fedora 44) for S3/S4/S5, plus incremental-compile non-regression.
- **Review E7:** all C0.2 acceptance criteria.

## Out-of-scope

- **Hybrid LR(1) + Pratt parser** — Phase 1 / C1.11 (`etch-parser.md §23`). The parser stays recursive-descent + Pratt.
- **Async codegen to Zig** (state-machine / CPS lowering of `async` / `await` / `race` / `sync` / `branch` / `spawn`). Zig 0.16 has no language-level `async`, so this needs a hand-emitted state machine + task runtime; the spec wants that desugaring factored through the HIR (`etch-ast-ir.md §5.3, §9.4, §11`), and the M0.8 codegen is the direct AST→Zig path with no HIR. Phase 2 per `etch-ast-ir.md §11`. Async is parsed (E2/E3) and interpreted (E3) only.
- **Generic monomorphisation codegen** — same root cause as async (HIR-dependent lowering not present in the direct AST→Zig path). Phase 2 per `etch-ast-ir.md §11`. Generics are parsed and interpreted (E2) only.
- **VM bytecode** (`.etchc`) — Phase 2 (`etch-bytecode.md`).
- **Shader backend codegen** (SPIR-V / MSL / DXIL / WGSL via AIR) — Phase 2+. E6 delivers shader parse + resolver shader-mode validation only.
- **Highlighting / folding / structural-selection visitors** — Phase 2 (`etch-parser.md §14`).
- **Full recovery cascade** (virtual tokens, panic-mode, fine sync points) — Phase 1 / S2+. E1 delivers top-level resync only.
- **Separate CST** — Phase 1 / S0 (`etch-parser.md §10.1`).
- **Runtime execution / wiring of any Level B construct against a real Tier 1 module** (Cortex, UI/Islandz, Kinesis, Pulse, Ember, Sequencer, Render). Level B is declaration + IR only in Phase 0.

## Documents to read first

1. `engine-phase-0-plan.md` — § M0.8 (scope source), Phase −1 results S3/S4/S5.
2. `etch-grammar.md` — full EBNF v0.6 (source of truth, IMPLEMENTED not modified by this milestone).
3. `etch-parser.md` — parser architecture; §10.1 (CST out of scope), §10.3 (AST stable interface), §14 (visitors out of scope), §23 (LR Phase 1 out of scope).
4. `etch-ast-ir.md` — §3 AST, §3.5 domain sub-ASTs, §5 lowering, §9.4 async, §11 phasing.
5. `etch-resolver-types.md` — §11 const contexts, §13 annotation applicability.
6. `etch-validation-ecs.md` — §5 tags bitfield, §12 rules/when, and the per-construct validation sections (gameplay, UI, render/anim/audio, scene/prefab).
7. `etch-diagnostics.md` — diagnostic codes (incl. `E0858` typed extensions).
8. `etch-reference-part1.md` — language foundations.
9. `etch-reference-part2.md` — the 29 constructs.
10. `etch-reference-part3.md` — annotations catalogue, patterns, cross-refs.
11. `etch-stdlib.md` — stdlib resolution.
12. `etch-abi-zig.md` — Zig codegen / ABI boundary.
13. `etch-bytecode.md` — read only to bound what is Phase 2 (VM, async state machines).
14. `etch-style-guide.md` — Etch style for the reference file.
15. `engine-scene-serialization.md` — `scene`/`prefab` serialization, typed extensions.
16. `engine-ecs-internals.md` — archetype matching, deferred commands, event bus.
17. `engine-spec.md` — S3/S4/S5 results, §25 Etch pipeline.
18. `engine-phase-0-criteria.md` — C0.2.
19. `engine-development-workflow.md` — brief/commit format, language criterion.
20. `engine-zig-conventions.md` — §13 module rooting (lazy analysis guard), naming, `extern struct` `u8`-not-`bool`.

## Files to create or modify

*Paths below are anchored on the S3/S4/S5 delivered layout. Claude Code confirms the actual file names from `src/etch/root.zig` before placing new code, and journals any deviation. The directory boundaries are firm; exact `.zig` splits inside `src/etch/` may follow the existing module organisation.*

- `src/etch/` — edit — extend lexer, parser, AST, resolver/type-checker, diagnostics, interpreter, codegen, ECS bridge to full v0.6. New domain sub-ASTs per `etch-ast-ir.md §3.5`.
- `src/etch/root.zig` — edit — public API surface extended; the AST stable interface (E7) is documented against this surface.
- Tier 0 ECS (e.g. `comptime_query.zig`, `registry.zig`, event bus) — edit — only as required by event/observer/filtered-query support; no surface widening beyond what the bridge needs.
- `tests/etch/v0.2/*.etch` — create — corpus covering every v0.6 construct.
- `tests/etch/reference_500_lines.etch` — create — 500+ line mixed-construct reference for the global differential.
- `tests/etch/ast_stable_interface.zig` — create — exercises ≥20 distinct public AST entry points (compilation is the invariant).
- `tests/etch/recovery_toplevel_test.zig` — create — 3 constructs, 1 broken → ≥1 diagnostic per broken construct, sane constructs present in the returned AST.
- `tests/etch/ebnf_examples_test.zig` — create — extracts and feeds ≥30 Etch example blocks from `etch-grammar.md` (and tagged `etch-reference-part*.md` blocks) to the parser.
- Per-stage differential tests `tests/etch/...` — create — one byte-exact (Level A) or internal-IR (Level B/C) differential test per stage.
- `synth_100/build.zig` — edit — proper sub-project depending on `weld_core` (D-S5-synth100-proper).
- `briefs/M0.8-full-grammar-v0.6.md` — create — this brief, committed verbatim as the first commit.

## Acceptance criteria

### Tests

- `tests/etch/v0.2/*.etch` — every v0.6 construct parses without error.
- `tests/etch/recovery_toplevel_test.zig` — `test "top-level resync surfaces one diagnostic per broken construct and keeps sane constructs"` — 3 constructs / 1 broken → ≥1 diagnostic per broken construct, the 2 sane constructs appear in the returned AST.
- `tests/etch/ast_stable_interface.zig` — compiles and exercises ≥20 distinct public AST entry points.
- `tests/etch/ebnf_examples_test.zig` — parses ≥30 example blocks from `etch-grammar.md` without error; a doc example with no parser support fails CI.
- One dedicated test per debt (12 total): D-S3-trivia, D-S3-doccomment, D-S3-annot-applicability, D-S3-annot-field-access, D-S3-resource-receiver, D-S4-runtime-query, D-S4-runtime-report, D-S4-ecs-bridge-panic, D-S4-or-archetype, D-S4-multifilter, D-S5-etchcook-inproc, D-S5-synth100-proper.
- Per-stage differential test (E1–E7): Level-A stages byte-exact interpreter↔codegen on world state after N ticks; Level-B/C stages on serialized internal IR/descriptor.
- `tests/etch/reference_500_lines.etch` differential — byte-exact on Level-A behaviour; Level-B/C and generic/async fragments validate parse + codegen-compiles only.

### Benchmarks

- Incremental compile (1 line / 1 file change) — **< 2 s** on the reference machines (S5 non-regression; **sensitive zone** — S5 margin was ~1.9×, extended codegen erodes it; monitor in CI).
- Cold compile (100 `.etch` files) — **< 30 s** (S5 non-regression).
- `reference_500_lines.etch` parse — **< 50 ms**.
- Interpreter hot-reload (edit → AST swap) — **< 500 ms**.
- Re-bench S3/S4/S5 metrics on Win11 + Fedora 44 (reference machines), archived.

### Observable behavior

- A command that parses, interprets, and codegen-compiles `reference_500_lines.etch`, runs both backends N ticks, and reports the byte-exact diff result (Level A) plus the codegen-compiles-clean result (Level B/C).
- A hot-reload demo: edit a rule body in a running interpreter session, observe the AST swap and behaviour change under 500 ms.
- CI log showing `ebnf_examples_test.zig` count of parsed blocks (≥30).

### CI

- `zig build` clean, zero warning, on the configured matrix (`{ubuntu-24.04, windows-2025} × {Debug, ReleaseSafe}`).
- `zig build test` green (Debug + ReleaseSafe). All inline tests reachable from the test root (`engine-zig-conventions.md §13` module rooting — no silently-skipped tests).
- `zig fmt --check` green (generated Zig under `.weld/generated/` excluded per existing config).
- `zig build lint` green.
- `commit-msg` hook green on every commit of the branch.
- Language compliance: `grep` on the diff before PR confirms 100% English in all repo artifacts (code, comments, identifiers, commit messages, branch, brief).

## Conventions

- **Branch:** `phase-0/etch/full-grammar-v0.6`
- **Final tag:** `v0.8.0-M0.8-etch`
- **PR title:** `Phase 0 / Etch / Etch full-grammar: parser + interpreter + codegen`
- **Commit convention:** Conventional Commits (`engine-development-workflow.md §4.3`). Scope coherent with the touched module (`etch`).
- **Merge strategy:** squash-and-merge (`engine-development-workflow.md §4.6`). Guy performs the merge and the tag, never Claude Code.

## Notes

- **A/B/C levels rationale.** Level B constructs depend on real Tier 1 modules (Cortex, UI/Islandz, Kinesis, Pulse, Ember, Sequencer, Render), which are out of Phase 0 scope beyond their interface stub. They are therefore declaration + internal IR/descriptor only in this milestone; their runtime wiring is post-Phase-0. This is deliberate, not a gap.
- **Async + generics codegen deferral.** Both are HIR-dependent lowerings (async = state machine / CPS; generics = monomorphisation) that the spec factors through the HIR (`etch-ast-ir.md §5.3, §11`). The M0.8 codegen is the direct AST→Zig path delivered at S5, with no HIR. Both are parsed and interpreted in M0.8; their Zig codegen is Phase 2 per `etch-ast-ir.md §11`. This is the boundary of the plan's "implementable shipping-side" codegen clause.
- **Sensitive criterion.** Incremental compile < 2 s had ~1.9× margin at S5 (the only tight Phase −1 gate). Extended codegen risks eroding it; treat it as a monitored gate, not a safe non-regression.
- **Scope clarification (Claude.ai decision relayed by Guy, 2026-06-06).** for-in + collection indexing/slicing confirmed in E1 as the entailed consumers of the scoped ranges/collections; not a scope expansion. Rationale: a collection without indexing/slicing is unusable, an `Iterator` without for-in is unusable, a range without for-in is inert — so `for ... in`, `arr[i]`, and `arr[0..3]` are not new scope but the consumers the scoped `ranges`/`collections` lines already entail. They are implemented together so each feature has a real differential (`for i in 0..10 { sum += i }`, `arr[i]`, `arr[0..3]`). Anti-hallucination: these forms are implemented strictly from `etch-grammar.md` (`for_stmt` §line 621, `range_expr` §410-411, `postfix_op "[" expression "]"` §425) + `etch-reference-part1.md` (§6.5 ranges, §7.3 for-in, §11 collections) — verified present before writing.
- **Two documentation drifts (out of scope here; patch at M0.8 close, not mid-milestone).** (1) `engine-phase-0-plan.md §M0.8` says "11 debts" but lists 12 (5×D-S3 + 5×D-S4 + 2×D-S5) — it is **12**; the "11" is a typo. (2) `etch-ast-ir.md §11` uses the pre-renumbering Phase 0.5/1/2 labels and an HIR-centric model out of sync with the direct AST→Zig codegen delivered at S5; the relative ordering (async/generics codegen after the basics) is correct, only the phase labels are stale. Both are KB realignments for the plan-close patch (delete-and-reupload), neither touches M0.8 code.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone.*

## Specs read

Read in the brief's "Documents to read first" order. Two docs were read to the scope the brief itself annotates (noted below) rather than cover-to-cover; the scoping is recorded in the Execution journal for review.

- [x] `engine-phase-0-plan.md` (§ M0.8, S3/S4/S5 results) — read 2026-06-06 14:15
- [x] `etch-grammar.md` — read 2026-06-06 14:15
- [x] `etch-parser.md` (§10.1, §10.3, §14, §23) — read 2026-06-06 14:15
- [x] `etch-ast-ir.md` (§3, §3.5, §5, §9.4, §11) — read 2026-06-06 14:15
- [x] `etch-resolver-types.md` (§11, §13) — read 2026-06-06 14:15
- [x] `etch-validation-ecs.md` (§5, §12, per-construct sections) — read 2026-06-06 14:15
- [x] `etch-diagnostics.md` — read 2026-06-06 14:15
- [x] `etch-reference-part1.md` — read 2026-06-06 14:15
- [x] `etch-reference-part2.md` — read 2026-06-06 14:15
- [x] `etch-reference-part3.md` — read 2026-06-06 14:15
- [x] `etch-stdlib.md` — read 2026-06-06 14:15
- [x] `etch-abi-zig.md` — read 2026-06-06 14:15
- [x] `etch-bytecode.md` — read 2026-06-06 14:15 (scoped per the doc-list annotation "read only to bound what is Phase 2": §1 overview + §17 phasing + §18 — the entire bytecode VM, opcodes, and async state machines are confirmed Phase 2, out of M0.8 scope)
- [x] `etch-style-guide.md` — read 2026-06-06 14:15
- [x] `engine-scene-serialization.md` — read 2026-06-06 14:15
- [x] `engine-ecs-internals.md` — read 2026-06-06 14:15
- [x] `engine-spec.md` (S3/S4/S5, §25) — read 2026-06-06 14:15 (scoped per the doc-list annotation "(S3/S4/S5, §25)": §1–4 architecture + §24 Langage Etch + §25 roadmap/Phase −1 spike results in full, §26–27; §5–§23 are Tier 1 module designs out of M0.8 scope — each Level-B construct's module surface is covered by `etch-reference-part2.md` + `etch-validation-ecs.md`)
- [x] `engine-phase-0-criteria.md` (C0.2) — read 2026-06-06 14:15
- [x] `engine-development-workflow.md` — read 2026-06-06 14:15
- [x] `engine-zig-conventions.md` (§13) — read 2026-06-06 14:15

## Execution journal

- 2026-06-06 — Step 1 (branch + brief). Branch `phase-0/etch/full-grammar-v0.6` created from up-to-date `main` (in sync with `origin/main`). Brief committed as the first commit, translated to English (see Acted deviations for Guy's authorization). Confirmed the delivered S3/S4/S5 layout against the real tree: `src/etch/` = lexer.zig, token.zig, parser.zig, ast.zig, types.zig, value.zig, interp.zig, ecs_bridge.zig, diagnostics.zig, root.zig + zig_codegen/ — matches the brief's "Files to create or modify" anchoring.
- 2026-06-06 14:15 — Step 2 (spec ingestion). All 20 spec docs read in the brief's order. Two read to the scope the brief itself annotates: (a) `engine-spec.md` "(S3/S4/S5, §25)" → read §1–4 architecture + §24 Etch + §25 full roadmap (all S0–S6 spike results, Phase −1 closure, Phases 0–4) + §26–27 in full; §5–§23 (Tier 1 module designs: render/physics/audio/networking/AI/animation/UI/asset/sequencer/etc.) left unread as out-of-M0.8-scope — Level-B construct surfaces are covered by reference-part2 + validation-ecs. (b) `etch-bytecode.md` "read only to bound what is Phase 2" → read §1 + §17 + §18, confirming the whole bytecode VM / opcodes / async state machines are Phase 2 (engine), out of M0.8 scope. Both scopings follow the FROZEN doc-list annotations, not silent skimming; flagged here for review in case Claude.ai wants full cover-to-cover reads of either.
- 2026-06-06 14:15 — Key facts confirmed for scoping the stages: M0.8 backend = tree-walking interpreter (extends S4, `src/etch/interp.zig`) + direct AST→Zig codegen (extends S5, `src/etch/zig_codegen/`), NO HIR, NO VM. Async/generics are parse+interpret only (codegen Phase 2). The 12 debts (5×D-S3, 5×D-S4, 2×D-S5) match the S3/S4/S5 "Dette Phase 0.2" lists in `engine-spec.md §25` and `engine-phase-0-plan.md §M0.8`. The hard CI gate is `engine-zig-conventions.md §13` module rooting (no silently-skipped inline tests) + extern-struct-POD for component codegen. Incremental-compile < 2 s is the one tight gate (~1.9× S5 margin).
- 2026-06-06 — E1 reconnaissance (parser front). Read the delivered `src/etch/` surface per the brief's "verify in code before scoping each stage": `root.zig` (108), `ast.zig` (791), `parser.zig` (1083). Findings: (1) AST already declares every v0.6 reserved kind (ItemKind/StmtKind/ExprKind/TypeNodeKind) — E1 *produces* the reserved variants + their side slabs/add-helpers, no enum redefinition. (2) Breaking-change target = `parser.ParseResult.diagnostic: ?Diagnostic` (consumed by `root.zig` `parseSource` + 2 tests). Recovery sync-point requires restructuring `Parser.parseFile` (l.194) to catch `ParseError` per top-level item, record the diagnostic, advance to next top-level keyword/EOF, resume. (3) D-S3-annot-field-access located: `parseAnnotationArg` (l.273) routes the ident arg through `continuePostfixAndBinary` (l.708, binary-only) instead of `parsePostfix` (l.751, the `.field`/`.get` chain) — matches the brief's exact fix description. (4) S3 corpus = ~40 `.etch` files under `tests/etch/corpus/{valid,invalid}/`. Still to read before implementing each layer: `lexer.zig`+`token.zig` (D-S3-doccomment, new operator tokens), `types.zig` (D-S3-annot-applicability + D-S3-resource-receiver + type-checker extension), `interp.zig`+`value.zig`+`ecs_bridge.zig` (interpret layer), `zig_codegen/*` (codegen layer).
- 2026-06-06 — Reading-scope ruling confirmed by Guy (live decision): the two doc-list-annotated reads are correct as-is, no cover-to-cover required. (1) `engine-spec.md` §5–§23 = Tier 1 module runtime wiring, out of M0.8 scope; Level-B construct surface (parse + validation + IR) is covered by `etch-ast-ir.md §3.5` + `etch-reference-part2` + `etch-validation-ecs`; §1-4/§24/§25/§26-27 reads sufficient. (2) `etch-bytecode.md` §1/§17/§18 = exactly "bound what is Phase 2"; VM/opcodes/async state machines out of M0.8. E1 plan validated unchanged; GO to implement E1 as a single block (breaking interface → expr/control-flow foundations → 5 S3 debts → EBNF harness + tests), then STOP at the Review E1 gate. No sub-decomposition, no E2 start.

- 2026-06-06 — E1 deliverable 1/4 landed: **breaking interface + top-level recovery sync-point** (commit `7bc84dd`). `ParseResult.diagnostic: ?Diagnostic` → `diagnostics: []Diagnostic` + `ParseResult.deinit`. `Parser` accumulates diagnostics; `parseFile` catches `ParseError` per top-level item, records it, and resyncs to the next top-level keyword / EOF (`recoverToTopLevel`) — broken constructs no longer abort the whole file. Migrated all 11 consumers (root, types `CheckOutcome`, interp ×2, demo, zig_codegen root + lower_test, corpus_test, keyword_ident, runner_interp, etch_cook, bench ×2). New `tests/etch/recovery_toplevel_test.zig` (3 tests: single broken middle construct keeps the 2 sane ones; multiple broken constructs → one diagnostic each; clean-source regression) wired into `build.zig` (`.etch=true`). `zig build test` green (exit 0 — the 2 `failed command` lines are the documented plugin_loader/events spurious noise; `macos-full-suite-flakiness` memory). `zig fmt --check` clean. Remaining E1: 5 S3 debts (trivia/doccomment/annot-applicability/annot-field-access/resource-receiver), expr/control-flow foundations (match Maranget / closures / loop+labeled+break-value / ranges / collections / cast / error-handling / type-alias × parse+interpret+codegen), EBNF harness, Review E1 tests.

- 2026-06-06 — E1 debt **D-S3-annot-field-access** landed (commit `d4a9895`). The positional-ident path of `parseAnnotationArg` routed the argument through `continuePostfixAndBinary` (binary operators only), so an argument like `@requires(self.health)` lost its `.field` / `.get` postfix chain. Extracted `continuePostfix` from `parsePostfix` and ran it before `continuePostfixAndBinary` on that path, so an annotation argument now accepts the full postfix chain (field access, method get) ahead of any binary continuation. Inline parser test added.

- 2026-06-06 — E1 debts **D-S3-doccomment** + **D-S3-trivia** landed (commit `700ec91`). Lexer: `skipLineComment` distinguishes `///` doc comments (exactly three slashes, not four) from ordinary `//` line comments, bucketing spans into `doc_comment_spans` vs `comment_spans`. AST: `AstArena` gained a `doc_comment_spans` pool plus `leading_comments` / `doc_comments` maps (`AutoHashMapUnmanaged(NodeId, SpanRange)`) with `docCommentsOf` / `leadingCommentsOf` accessors. A free `attachTrivia(arena, gpa)` pass runs at the end of `parse` — two-cursor byte-range bucketing of comment + doc spans onto the top-level item that follows them; intra-item and trailing trivia is left unattached (finer node-level attachment is a Phase 2 concern, per brief scope). Inline lexer + parser tests added.

- 2026-06-06 — E1 debt **D-S3-annot-applicability** landed (commit `2ccbdc3`). New `AnnotTarget` enum + `annotationAppliesTo(kind, target)` predicate + `TypeChecker.validateAnnotations(start, len, target)`, invoked in pass 1 per item (component/resource/rule) and per field (`validateFieldsInDecl`). A builtin annotation on the wrong target now emits **E0502 AnnotationMisapplied**; `.custom` (plugin) annotations are accepted on any target (their schema validation is Phase 3, not this debt). Argument-schema checks (E0503/E0504) are a separate §13 concern, out of scope. Applicability table implemented (**flagged for the Review E1 gate audit** against `etch-resolver-types.md §13.2` + `etch-reference-part3.md` Part III):
  - `@custom` → any target
  - `@save` → component, resource
  - `@requires`, `@storage` → component
  - `@config`, `@state` → resource (confirmed by RTTI `Lifecycle`, `src/core/rtti/type_info.zig`)
  - `@transient` → resource, field
  - `@phase`, `@priority`, `@run_on`, `@pause_group`, `@id` → rule
  - `@unit`, `@range`, `@hidden`, `@readonly`, `@replicated` → field
  - `@networked`, `@loc` → no valid E1 target (their constructs — event, expression — arrive in later stages)

  Boundary calls least certain against the spec prose, to verify at the gate before sign-off: `@id` target (rule vs component), `@networked` (no-target vs component/field), `@save` (component+resource — does it also cover fields?). Fixtures: `components/annotated.etch` corrected (`@replicated` moved onto a field; `@networked` component dropped — no valid E1 target); new invalid fixture `tests/etch/corpus/invalid/E0502_annotation_misapplied.etch` (`@config` on a component) registered in the corpus facade. Inline `types.zig` test asserts misapplied → E0502 plus a fully-valid control asserting no E0502. E0301/E0302 (resource/component-given) are deliberately deferred to the D-S3-resource-receiver commit where they are first used. `zig build test` green (exit 0), `zig fmt --check` clean. Remaining E1: D-S3-resource-receiver (5th debt), expr/control-flow foundations, EBNF harness, Review E1 tests.

- 2026-06-06 — E1 debt **D-S3-resource-receiver** landed (commit `98631c9`) — the 5th and last S3 debt. Receiver-less `get(R)` / `get_mut(R)` is now the resource counterpart of `entity.get(T)`. **Parse:** `parsePrimary` accepts the `get` / `get_mut` keywords at the head of a primary (`parseResourceGetCall`), stored as a `method_get` / `method_get_mut` expr with `receiver == NodeId.none` — no new ExprKind, the absent receiver is the discriminator everywhere downstream. **Resolve:** the bare form resolves `T` as a resource (E0301 ResourceExpectedComponentGiven if `T` is a component) and requires it in the when clause (reusing E1213); the receiver form now rejects a resource `T` with E0302 ComponentExpectedResourceGiven. E0301/E0302 added to the `DiagnosticCode` catalogue (the additive reservation deferred from the annot-applicability commit). `isAssignTargetReachable` needed no change — it already dispatches on `.method_get_mut` vs `.method_get`, identical for both forms, so `get_mut(R).field = x` is allowed and `get(R).field = x` rejected. **Interpret:** new `Value.resource_ref { resource_id, mutable }`; `field_access` read and `execAssign` write dispatch on it through the bridge `readResourceField` / `writeResourceField` (already present, previously unused). **Codegen — deferred, flagged for the gate:** receiver-less get emission is rejected with `UnsupportedConstruct` rather than emitted. Reason: the resource store backs each resource with a byte-aligned `gpa.dupe(u8, …)` buffer, so the natural `@as(*R, @ptrCast(@alignCast(…)))` lowering would trip the `@alignCast` safety check in ReleaseSafe (the CI build mode). Sound codegen needs either aligned resource storage (a core-ECS change, outside this frontend "lexer/parser/resolver core" debt) or byte-level field emission; it folds into the E3 "Level A complete in codegen" gate (brief §Scope E3). `walkExprForComponents` was still fixed to exclude resources from the query tuple. The interpreter is the reference execution and fully exercises the feature. **Tests:** `types.zig` (E0301 / E0302 / E1213 + a valid `get_mut` control), `interp.zig` (a rule reads `get(Score).base` and writes `get_mut(Score).points += …`, asserted end-to-end over 3 ticks), and 3 corpus fixtures (`valid/rules/resource_access.etch`, `invalid/E0301_*`, `invalid/E0302_*`). `zig build test` green (exit 0), `zig fmt --check` clean. **All 5 S3 debts now done.** Remaining E1: expr/control-flow foundations (match/closures/loops/ranges/collections/casts/error-handling/type-alias × parse+interpret+codegen — with `kw_type` added to the `recoverToTopLevel` stop-set IN LOCKSTEP + a recovery test when `type` aliases land), EBNF harness, Review E1 tests.

- 2026-06-06 — E1 foundations started. **Cast (`as`)** landed (commit `a566cc1`) — the first v0.6 expression foundation, full vertical (lexer→token→ast slab→parser→types→interp→codegen→differential). `operand as Type` parses as a tight postfix; `as` graduates from plain identifier to `kw_as`. Type-check: numeric-scalar → numeric-scalar only (E0200). Interpret: int↔float domain flip. Codegen: `@as(T, @floatFromInt/@intFromFloat/@floatCast/@intCast(...))`. Differential program `21_cast_int_to_float` proves interpreter↔cooked-Zig parity (the established per-construct validation pattern: inline types + interp tests, plus a parity program when codegen is in scope). Remaining foundations: ranges, type aliases (with the `recoverToTopLevel` lockstep), loop/labeled/break-value, closures, match (Maranget), collections, error handling. Then EBNF harness + Review E1 tests.

- 2026-06-06 — **Type aliases** landed (commit `9671ab5`) — second foundation, full vertical. `type Name = Type` top-level, resolved through a shared `AstArena.resolveTypeAliasName` chain (cycle-bounded). **Lockstep constraint honored:** `kw_type` joined `parseTopLevel` AND `recoverToTopLevel`'s stop-set together; a dedicated recovery test proves a valid `type` after a broken construct survives in the AST (Guy's E1 forward requirement). Resolve: alias registers as a symbol (E0101 collision), target validated (E0102), substituted at field types / cast targets / `namedTypeToResolved`. Interp: field-kind resolution follows the chain. Codegen: all six field/param/cast/inference sites resolve aliases (the differential program `22_type_alias_field` caught two I'd missed — `emitRegisterCall` + an inference path — proving the value of the interp↔codegen parity check). Remaining foundations: ranges, loop/labeled/break-value, closures, match (Maranget), collections, error handling. Then EBNF harness + Review E1 tests.

## Acted deviations

- (this commit, `docs(brief): add M0.8 milestone brief`) — The brief was delivered by Claude.ai with French section headers in both the FROZEN and LIVING sections (e.g. `Documents de spec à lire en premier`, `Fichiers à créer ou modifier`, `Critères d'acceptation`, `Comportement observable`, `Specs lues`, `Journal d'exécution`, `Déviations actées`, `Blocages rencontrés`, `Notes de fin`) plus French `édition` / `création` / `lu` labels. This conflicts with `engine-development-workflow.md §3.2` (briefs are produced 100% English; safety net forbids committing French and forbids silent translation), with this brief's own CI criterion (`Language compliance ... 100% English ... brief`), and with the committed M0.7 brief (100% English LIVING SECTION). Per Guy's explicit live authorization (2026-06-06, AskUserQuestion answer "M'autoriser à traduire en EN"), the brief was committed already translated to English, aligned on the M0.7 template. Only header/label language was normalized; FROZEN substantive content (Context, Scope E1–E7, Out-of-scope, Documents list, Files list, Acceptance-criteria text, Conventions, Notes) is semantically unchanged. The KB-side brief regeneration (so the Claude.ai source matches) is an out-of-repo follow-up for the milestone close.

- 2026-06-06 — Specs re-attached (`etch-grammar.md`, `etch-reference-part1/2/3`, `etch-resolver-types.md`); foundations resumed from the checkpoint. **Scope clarified by the spec:** the E1 foundations are exactly cast / type-alias / `assert` / `match` / closures / `loop`+labels+break-value / ranges / collections — NOT `if`/`for`/`while` (those stay rejected keywords past E1). `match` Phase-1 patterns are the flat fragment (`_` / literal / binding); enum-variant / optional / tuple / struct-destructure patterns need their types (E2+).
- 2026-06-06 — **assert** landed (commit `e2b8da3`) — the `assert` slice of error handling (`etch-reference-part1.md` §10.3). `kw_assert` + `assert_stmts` slab; type-check requires a bool cond (E0200); interpret counts a false cond as a RuntimeReport error stopping the body; codegen emits `if (!(cond)) unreachable;` (self-contained, no `std` — etch_cook strips imports). Differential `23_assert_guard`. `throws`/`throw`/`try`/`catch` (recoverable errors, need the Error struct + error-unions) remain.
- 2026-06-06 — **match** landed (commit `57a2619`) — the headline foundation, full vertical. `match scrut { pat => expr, ... }`; `kw_match` + `fat_arrow` token + lexer `=>`. Patterns: `_` / literal / `IDENT` binding. Type-check (`etch-resolver-types.md` §12.4): scrutinee gates literal-pattern fit, arm bodies unify (E0200), exhaustiveness via Maranget-lite — catch-all, or a bool match covering true+false; int/float/string need the catch-all (`E1230 NonExhaustiveMatch`, added to the catalogue). Interpret: first-match-wins, binding binds scrutinee. Codegen: a labeled block (`blk<n>:`) binds the scrutinee once + if-else chain; binding arms `{ const <name> = __m<n>; break … }`; catch-all-free matches get a trailing `unreachable`. Differential `24_match_dispatch`. **Foundations done so far: cast, type-alias, assert, match (4/8).** Remaining: closures `|a| e`, `loop`/labeled/`break label value`, ranges `..`/`..=`, collections `T[N]`/`T[]`/`Map`/`Set`/`Iterator`, error-handling `throws`/`throw`/`try`/`catch`. Then EBNF harness + Review E1 tests.

- 2026-06-06 — **ranges + for-in** landed (commit `b16d434`) — 5th foundation, delivered with its entailed consumer per the relayed scope decision. `..`/`..=` → `dotdot`/`dotdot_eq` (lexNumber already declines a non-digit `.`, so `0..10` splits); `for`/`in` → keywords. Range (`etch-grammar.md` §410-411): non-associative, lbp 6 (below additive); type-check requires same-integer bounds → new `ResolvedType.range` carrying the element type. for-in (§621): `for IDENT [,IDENT] in iterable block`; E1 iterates ranges, loop var binds the element type. **New `parseStmtRun` (temp-list + bulk-append) replaces the rule body's direct `extra`-append** so a `for` body nested in a rule body doesn't interleave its statement-ids in `extra` (the first nested-block construct; rule-body parsing refactored onto it). Interpret: `RangeVal`, loop rebinds var each iteration over [start,end)/[start,end]. Codegen: ranges have no standalone Zig value (emitExpr rejects); for-in → `{ var i: i64 = start; while (i </(=) end) : (i += 1) { body } }`. Differential `25_for_range_sum`. **Foundations: 5/8 done** (cast, type-alias, assert, match, ranges+for-in). Remaining: closures `|a| e`, `loop`/labeled/`break label value`, collections `T[N]`/`T[]`/`Map`/`Set`/`Iterator` (+ indexing/slicing — the entailed consumers), error-handling `throw`/`try`/`catch`. Then EBNF harness + Review E1 tests.

- 2026-06-06 — **SESSION 2 — collections** landed (6th E1 foundation, first of the fixed resume order), in three full-vertical slices:
  1. **fixed arrays + indexing + slicing** (commit `6f611fe`, diffs 26/27). `[` `]` `;` tokens; the reserved `array_lit`/`map_lit`/`index` expr kinds and `array`/`slice`/`map_type`/`set_type` type kinds are now *produced* (+ side slabs + add-helpers). Parser: `T[N]`/`T[]`/`[K:V]`/`Set<T>`/`Map<K,V>` types (`etch-grammar.md` §264/§278/§270), `[a,b,c]`/`[v;n]`/`[:]`/`[k:v]` literals (§493-498), postfix `recv[index]` (§425, range index → slice). Type-check: `ResolvedType` array_fixed/array_dyn/map_t/set_t (builtin elements in E1); element unification, index→elem, slice→array_dyn, integer-index + non-collection diagnostics. Interp: `CollectionStore` (heap arrays via `Value.array_ref`, reset at body boundary). Codegen: fixed arrays → `[_]T{...}`, `arr[@intCast(i)]`, slice.
  2. **for-in over array + dynamic arrays** (commit `9057144`, diff 28). for-in binds the element type; interp iterates `array_ref`; codegen `for (arr) |x|`. Dynamic `T[]` is interp-only.
  3. **map literals + map for-in** (commit `71f8822`). `Value.map_ref` + maps slab; `[k:v]` + `for k,v in m` (two bindings); interp reference; codegen deferred.

  **Scope boundaries flagged for the Review E1 gate** (precedent: D-S3-resource-receiver codegen deferral — interp stays the reference execution, codegen defers what needs out-of-path infrastructure):
  - (a) **Heap-collection codegen deferred** — dynamic `T[]`, map, set codegen needs an allocator in the `rule_x(world)` signature + the Etch arena model, outside the direct AST→Zig path. The byte-exact interp↔codegen differential covers **fixed arrays** — exactly the `arr[i]` / `arr[0..3]` / `for-in` consumers the §Notes scope clarification requires.
  - (b) **Collection methods + constructors deferred** — the stdlib API (`.push`/`.len`/`.contains`/…) and `Set.from`/`Set.new` need the **call mechanism** (postfix `.method(args)` / `(args)`), which is E2-adjacent (shared with `fn`/`impl`). Iterator lazy methods (`.map`/`.filter`) additionally need closures (next tranche).
  - (c) **Optional-returning accessors deferred** — `arr.pop()→T?`, `m[k]→V?`, `.get→V?` need `Optional T?`, which is E2. Map access in E1 is via for-in only.
  - (d) **Set runtime deferred** — Set has no literal; its constructors are calls (see b), so E1 ships only the `Set<T>` type (parse + resolve).
  - (e) **Collection field types rejected** — component / resource fields stay scalar POD in E1 (collections are locals).

  `zig build test` + `zig build lint` + `zig fmt --check` green. Remaining E1 (fixed order): closures → loop/labeled/break-value → error-handling → EBNF harness → Review E1 tests.

- 2026-06-06 — **SESSION 2 — closures** landed (7th E1 foundation, commit `3d4e0d0`, diff 29). `|` token; the reserved `closure`/`fn_call` expr kinds are now *produced* (+ closure_exprs/closure_params/call_exprs slabs + helpers). Parser: `closure_expr` `|a,b| e` / `|| e` (§524), postfix call `callee(args)` (§424, positional args). Type-check: `ResolvedType.closure` (the closure node); a call checks arity then lazily types the body for the return with params bound (annotation, else arg type) in the caller's scope; non-closure call → E0200. Interp: closure value (by-value capture snapshot of the definition-site locals, §5.6) + call-frame invocation; `ClosureStore` reset at body boundary; **interp is the reference and handles capture**. Codegen: capture-free closures → `struct { fn call(params) ret {...} }`, call → `callee.call(args)`.
  **Scope boundaries flagged for the Review E1 gate** (interp = reference, codegen defers heavy infra):
  - **Capturing-closure codegen deferred** — needs capture analysis + a struct-with-fields lowering (closer to the full closure model); the interp captures by value and is the reference. The byte-exact differential covers capture-free closures (diff 29); an inline interp test covers capture (`scale(5)` capturing `factor` → 15).
  - **Closure body = expression in E1** — a `{ block }` body needs block expressions, which arrive with the loop/break tranche.
  - **Param-type inference from context deferred** — closure params should be annotated in E1 (`|x: int|`); contextual inference (`entities.filter(|e| …)`) needs the call-site context and the Iterator methods (post-closures).
  `zig build test` + `zig build lint` + `zig fmt --check` green. Remaining E1: loop/labeled/break-value → error-handling → EBNF harness → Review E1 tests.

- 2026-06-06 — **SESSION 2 — loop / labeled / break-value** landed (8th E1 foundation, commit `6d1bfb2`, diffs 30/31). `loop`/`break`/`continue` keywords (out of `non_s3_keywords`); `loop_expr` added to `ExprKind` (loop is an expression); `break_stmt`/`continue_stmt` produced. Parser: `loop { body }` (§522) + labeled `outer: loop { ... }` (§624, statement position, wrapped as an expr-stmt); `break [label] [value]` (§632), `continue [label]` (§633). The `break [IDENT] [expression]` ambiguity (no statement separator) is resolved by a parser **label stack** — a leading IDENT is a label only when it names an enclosing loop, else it starts the value. Type-check: a loop's value = the type of a top-level break value (permissive). Interp: control-flow via a `self.control` signal (break/continue cross expression↔statement boundaries) + `handleLoopControl`; for-in honours break/continue. Codegen: `loop` → `[label:] while (true) { ... }`, break/continue → Zig (labeled). Diffs 30 (`loop { break 42 }` → 42) + 31 (labeled nested `break outer` → 7).
  **Scope boundary flagged for the Review E1 gate:**
  - **Block expressions + match-statement deferred.** A `{ block }` value-expression and `match` arms whose body is a block enable (a) conditional loop exit (`loop { ...; match c { _ => break } }`) and (b) closure block bodies (`|x| { ... }`). The journal anticipated this ("may entail extending match arms to blocks"). E1 ships the full loop/break/continue mechanism with **break-value + labeled-break** differentials (unconditional breaks); the conditional-exit path (block_expr parse/interp + match-statement if-else codegen) is a coherent refinement deferred to keep the error-handling foundation on schedule. `continue` is parsed + interpreted + lowered, but its differential needs the conditional-exit path (so it is parse-tested only in E1).
  `zig build test` + `zig build lint` + `zig fmt --check` green. Remaining E1: error-handling → EBNF harness → Review E1 tests.

- 2026-06-06 — **SESSION 2 — error-handling** landed (9th E1 foundation, commit `257ac53`). `throw`/`try`/`catch` keywords (`throws` stays reserved — a fn marker, E2). `throw_stmt`/`try_catch_stmt` produced. Parser: `throw expression` (§641), `try { ... } catch IDENT { ... }` (§640) — both bodies are statement runs, so no block-expression support is needed. Type-check: the thrown value is typed; the catch binding is `unknown`. Interp: a `self.thrown` signal unwinds (like break/continue) through statement runs / loops / for-in until a `try` clears it and binds the catch var; an uncaught throw at the rule top level is a runtime error.
  **Scope boundaries flagged for the gate:** error-handling codegen deferred (a manual error-union / propagation flag is out-of-path infra, interp is the reference); E1 throws an arbitrary value (the builtin `Error` struct + `ErrorCode` enum arrive with struct/enum in E2); `throws` (fn marker) waits on fn (E2). Interp tests: caught throw (→ 99), uncaught throw (runtime error).
- 2026-06-06 — **SESSION 2 — EBNF harness** landed (commit `18aee12`). `tests/etch/ebnf_examples_test.zig` embeds an in-repo example corpus `tests/etch/ebnf_examples.md` (the spec docs are not in the repo), extracts every ```` ```etch ```` fence via a markdown iterator, feeds each to the parser, and asserts all parse clean with **count ≥ 30** (31 E1 blocks: every landed construct). The fence-extraction machinery is the reusable part — it can target `etch-grammar.md` directly if the grammar enters the repo (Phase 0 re-evaluation). Wired into `build.zig` (`etch`).
- 2026-06-06 — **SESSION 2 — E1 COMPLETE — Review E1 gate reached.** All eight v0.6 expression/control-flow foundations (cast, type-alias, assert, match, ranges+for-in, collections, closures, loop/break, error-handling) + the 5 S3 debts + the breaking interface + the EBNF harness are delivered. **Recovery:** no session-2 tranche added a top-level keyword (collections / closures / loop / error are body/expression constructs), so `recoverToTopLevel`'s stop-set is unchanged; added a survival test (a broken construct followed by a rule exercising every E1 body construct → the rule survives in the AST). **Differentials 21–31** give per-construct interp↔codegen byte-exact parity on the sound-codegen path; the deferred-codegen paths (heap collections T[]/map/set, capturing closures, error-handling, block-expr/match-statement) are interp-reference with the gate flags recorded above. `zig build` + `zig build test` (Debug **and** ReleaseSafe) + `zig build lint` + `zig fmt --check` all green. **STOP at the Review E1 gate** — E1 diff presented for the Claude.ai review (relayed by Guy). E2 not started.

- 2026-06-07 — **Review E1 rulings (Claude.ai decision, relayed by Guy — gate PASSED).** Targeted audit OK: every deferred-codegen path returns `CodegenError.UnsupportedConstruct` (no silently-wrong Zig), differentials 21–31 cover the sound path only, interp is the reference on the deferred paths; the 8 scope flags are sound and consistent with the brief's async/generics doctrine; recovery stop-set unchanged + survival test, generics rejected beyond Set/Map, collection fields POD-only. Three rulings recorded to carry into E2/E3 planning:
  1. **Deferred E1 codegen = Level-A codegen completion blocked on E2 primitives, NOT E2 work.** The deferred-codegen paths (heap collections `T[]`/`Map`/`Set`, capturing closures, error-handling, conditional-loop-exit / block-expr / match-statement) are Level-A codegen completion gated on E2 primitives (call mechanism + `Optional` + `Error`/`throws`), not new E2 constructs. Home = the **E3 gate "Level A complete in codegen"**, to be closed once E2 ships those primitives — same fold as the D-S3-resource-receiver codegen deferral (already homed at E3, brief §Scope E3).
  2. **`if` / `while` / block-expressions / match-statement have no scoped home yet.** Omitted from the E1 foundations list and currently rejected. Recommendation: **E2 (expression/statement completions)**. To be confirmed at E2 planning — these keywords must not be left in permanent rejection without a decision.
  3. **Heads-up E3 — Tier 0 ECS may need aligned resource storage.** resource-receiver codegen (and heap-collection codegen) may require aligned resource storage on the Tier 0 ECS side (the `@alignCast` ReleaseSafe constraint) — potentially touches the core, not just the Etch frontend.

- 2026-06-07 — **E2 OPENED — reconnaissance + internal order.** E1 archived; E2 resumes on the same branch (no new branch/brief, Status already ACTIVE). Re-read the full delivered E1 surface before scoping (per the brief's "verify in code before scoping each stage"): `root.zig`, `token.zig`, `value.zig`, `ast.zig` (1234), `parser.zig` (1957), `lexer.zig`, `diagnostics.zig`, `types.zig` (1879), `interp.zig` (1938), `zig_codegen/{root,errors,type_map,lower,emit}`. Confirmed the per-construct vertical pattern (lexer → token → AST slab + add-helper → parser → types → interp → codegen → inline tests + a numbered differential under `tests/etch_interp/programs/`) and that the AST already declares every reserved E2 kind (`if_expr`/`if_stmt`/`while_stmt`/`match_stmt`/`block_expr` + `fn_decl`/`struct_decl`/`enum_decl`/`trait_decl`/`impl_decl`) — E2 *produces* them, no enum redefinition.
  **E2 internal order (fixed, Guy-relayed) — one block, a single Review E2 gate at the end, no intra-stage checkpoint:** (1) control-flow completion (`if`/`while`/block-expr/`match`-statement) → (2) call mechanism (postfix `.method(args)` / `(args)`, 4-kind dispatch per `etch-resolver-types.md §5`) → (3) declaration layer (`fn`/`struct`/`enum`/`trait`/`impl` incl. `impl Trait for T when ...` + coherence `§7`) → (4) generics (parse + resolve + interp; **codegen monomorphisation deferred Phase 2 → `CodegenError.UnsupportedConstruct`**, no codegen differential) → (5) `Optional T?` + `Error`/`ErrorCode` builtins. `async fn` parsed in E2, interpreted E3, codegen Phase 2. The E1-deferred codegen paths (heap collections / capturing closures / error-handling) stay homed at the E3 gate (Review E1 ruling 1) — E2 does not reopen them.
  **recoverToTopLevel stop-set:** unchanged by control-flow (`if`/`while`/block-expr/`match`-statement are body/expression, not top-level — Review E1 ruling 2 + launch). `kw_fn`/`kw_struct`/`kw_enum`/`kw_trait`/`kw_impl` join `parseTopLevel` AND the stop-set IN LOCKSTEP as the declaration layer lands, each with a survival test.

- 2026-06-07 — **E2 block 1 plan — control-flow completion (EBNF-confirmed before writing, anti-hallucination).** Forms verified in `etch-grammar.md`: `block = "{" {statement} [expression] "}"` (§4.1 l.645 — the trailing expression is the block value); `if_expr`/`if_stmt` = `"if" expression block {"else" "if" expression block} ["else" block]` (§3.2 l.500 / §4.1 l.618); `while_stmt` = `[loop_label] "while" expression block` (§4.1 l.622); `match_arm` = `pattern "=>" (expression | block)` (§3.2 l.504); `block_expr` = `block` as a primary expression (§3.2 l.520). Design:
  - **Trailing-value block:** extend `BlockExpr` with `value: NodeId` (`NodeId.none` = value-less). A new `parseBlock` collects statements; an expression-led item that is not an assignment and is immediately followed by `}` becomes the block value (Etch has no `;`, so "last bare expression before `}`" is the value — reference §6.11/§6.12). Rule/`for`/`while`/`loop`/`try` bodies keep `parseStmtRun` (statement-only, no value extraction) — no regression.
  - **`if`** parsed as an if-expr in `parsePrimary` (mirrors `loop`); in statement position it wraps as an expr-stmt. `while` is statement-only (no while-expr in the EBNF). `match` arms gain block bodies (the deferred match-statement). The `control`/`thrown` signal + `execStmtRun` infra from E1 loop/break already crosses expr↔stmt boundaries, so conditional loop exit (`loop { if c { break } }`) and closure block bodies (`|x| { ... }`) fall out once block-expr + if land — closing the two E1 loop/break deferrals.
  - **`if let` / `while let` deferred to block 5** (Optional dependency: they unwrap `T?`, reference §7.2/§10.1) — a planned within-E2 coupling (deliver with the consumer, as E1 did for ranges + for-in), not a re-scope. Block 1 ships plain `if cond` / `while cond`.
  - Full vertical incl. codegen (Zig has native `if`/`while`/labeled value-blocks) + a differential per construct (Review E1 ruling 2 homes these at E2; launch block 1 = parse + resolve + interp + codegen + differential).

- 2026-06-07 — **E2 block 1 — control-flow completion COMPLETE** (4 tranches, full vertical each, all green Debug + ReleaseSafe + lint + fmt). The control-flow constructs are now Level-A complete in interpreter **and** codegen.
  1. **block expressions** (`4021dbe`, diff 32) — `BlockExpr` gains `value: NodeId`; `parseBlockBody` extracts the trailing value (last bare expression before `}`, no `;` in Etch); codegen `__bex<n>: { ...; break :__bex<n> v; }`. Rule/loop/for/try bodies keep `parseStmtRun` (statement-only). **This single tranche unblocked match-arm block bodies + closure block bodies on the parse + interp side for free** (both route through `parseExpr`).
  2. **if / else** (`32fcfc8`, diff 33) — `IfExpr` slab (recursive `else_branch`: none | `block_expr` | nested `if_expr`); parsed as if-expr in `parsePrimary` (wrapped as expr-stmt in statement position); `synthIf` (bool cond + branch unification); codegen value-position `if (c) <v> else <v>` (concrete `let` annotation so comptime-literal branches coerce under a runtime condition — a Zig requirement) and statement-position `if (c) { ... } else { ... }`.
  3. **while** (`3cb9af3`, diff 34) — `WhileStmt` slab; `parseWhileStmt`; `handleLoopControl(0)` reused from E1 loop/break; codegen `while (<cond>) { <body> }`. Unlabeled (`while let` → Optional tranche).
  4. **match statements + deferral closure** (`7548a85`, diffs 35/36/37) — match-arm block bodies (diff 35, value-block path) and conditional loop exit (diff 36, `if`-guarded break in a loop) needed **no new code** (block-aware `parseExpr`/`synthExprE`/`evalExpr`); the only new codegen is `emitMatchAsStmt` (statement-position match with control-flow / value-less arms → if-else chain of statement blocks, diff 37). Closure block body (`|x| { ... }`) lands parse + interp (interpreter test) — codegen deferred to the E3 gate, as for capturing closures.
  **Scope flags for the Review E2 gate:**
  - **`if let` / `while let` deferred to block 5** (Optional dependency, reference §7.2/§10.1) — a planned within-E2 coupling, ships with the Optional primitive.
  - **Closure-block-body codegen deferred to the E3 "Level A complete in codegen" gate** (`closureHasCaptures` returns `UnsupportedConstruct` for any non-trivial body, including blocks — fail loud, interp is the reference). Same fold as capturing-closure codegen (Review E1 ruling 1).
  - **block / if value-less in value position** → `UnsupportedConstruct` (the type-checker treats value-less control flow as unit, so a sound program never lands one in a value slot; fail loud guards the rest).
  **recoverToTopLevel stop-set unchanged** (control-flow constructs are body/expression, not top-level — per ruling 2). EBNF-confirmed before writing: every form verified in `etch-grammar.md §3.2`/`§4.1` (commas required between match arms — caught a missing-comma in a draft differential, fixed per EBNF, not by loosening the parser).
  **Next: E2 block 2 — call mechanism** (postfix `.method(args)` / `(args)`, 4-kind dispatch per `etch-resolver-types.md §5`). Read `etch-resolver-types.md §5` before writing.

- 2026-06-07 — **E2 block 2 plan — call mechanism (EBNF + dispatch confirmed before writing, anti-hallucination).** Re-read `etch-resolver-types.md §5` first (the brief's explicit prerequisite): the 4-kind dispatch is the strict order inherent (`impl Type`) → trait (`impl Trait for Type`) → builtin (`entity.get`/`get`/…) → service (Tier 1) (§5.5). Kinds 1–2 need `impl` (block 3); a free call `f(args)` is the §4.2 rule (synth callee → `fn(A,B)->R`, check args, result R). Forms verified in `etch-grammar.md`: `function_decl = [async] fn IDENT [generic_params] "(" [param_list] ")" [throws] ["->" type] [where_clause] block` (§5.3 l.776); `regular_param = IDENT ":" type ["=" expression]` (§5.3 l.788); `return_stmt = "return" [expression]` (§4.1 l.630); postfix method call `"." IDENT "(" [arg_list] ")"` and function call `"(" [arg_list] ")"` (§3.1 l.421/424); `arg = expression | IDENT ":" expression` (§3.3 l.557). Layout: `etch-abi-zig.md §3.1` — POD layout identical Etch↔Zig, so a free `fn` lowers to a plain Zig `fn` (the trampoline/marshalling machinery of §4–§5 is the VM/service path, Phase 2, out of M0.8). Code surface verified (per "verify in code before scoping"): `fn_decl`/`method_call` are reserved-but-unproduced (`ast.zig` ItemKind l.127 / ExprKind l.223); `fn_call` is produced (`addCall`, the E1 closure-call path in `continuePostfix` l.1374 + `synthCall` l.1029 + interp l.864 + codegen l.1225); `parseTopLevel` l.346 + `recoverToTopLevel` stop-set l.331 are the lockstep sites; `non_s3_keywords` (`token.zig` l.193) still gates `fn`/`return`/`throws`/`async`.

  **Two tranches, each a full vertical (lexer→token→ast→parser→types→interp→codegen + a differential + inline tests), committed + journaled separately:**
  - **A — `fn` top-level + free-function calls + `return` + LOCKSTEP.** Graduate `fn`/`return`/`throws`/`async` from `non_s3_keywords` to real tokens. `parseFnDecl` parses `[async] fn IDENT "(" params ")" [throws] ["->" type] block` (body via `parseBlockBody` → trailing expression = implicit return). `synthCall`/interp/codegen gain the free-`fn` path (callee `.ident` naming a top-level `fn` symbol) alongside the existing closure path; codegen disambiguates by `LocalCtx.lookup` (a closure is a recorded local; a free fn is not → direct `name(args)` vs `callee.call(args)`). `return [expr]` graduates: interp unwinds via a `returning` flag mirroring `thrown`; codegen `return <expr>;`. **LOCKSTEP — same commit:** `kw_fn` (+`kw_async`) join `parseTopLevel` AND `recoverToTopLevel`'s stop-set, with a survival test (broken construct + valid top-level `fn` → the `fn` survives in the AST). Differential `38_fn_free_call` (`fn double(x: int) -> int { x * 2 }` + `double(21)` = 42).
  - **B — method-call parsing.** `recv.method(args)` produces the reserved `method_call` ExprKind (lookahead in `continuePostfix`: `.` `IDENT` `(` → method call, else field access). Parser-testable only; the 4-kind method **dispatch** (resolve/interp/codegen) is block 3 once `impl` provides methods → `method_call` returns `CodegenError.UnsupportedConstruct` / a clear interp deferral until then (fail loud, no silently-wrong Zig).

  **Scope boundaries for the Review E2 gate (block 2):**
  - **Params `IDENT: type` only, positional calls only.** Default-valued params + named args (`f(x: 5)`) are an E2 refinement not in the block-2 vertical (`etch-grammar.md` allows them; the existing `continuePostfix` comment anticipates them) — deferred, flagged, not re-scoped.
  - **Generics + `where` deferred to block 4.** The `[generic_params]`/`[where_clause]` slots are parsed in block 4; `fn foo<T>` errors cleanly at `<` in block 2.
  - **`async fn` parse-only** (interp E3, codegen Phase 2): calling an async fn in the interpreter is a clean deferral; codegen of an async fn decl → `UnsupportedConstruct`. **`throws` marker parsed**; a `throws` fn's codegen is deferred to the E3 error-handling-codegen gate (`UnsupportedConstruct`), its interp works (E1 throw/try/catch).
  - **`.d.etch` declaration_only out of scope** — a body is required; a bodyless `fn` in a `.etch` file is a parse error.
  - **Call diagnostics reuse `type_mismatch` (E0200)** for arity / arg-type / return-type mismatches, consistent with the sibling closure-call path; the spec's dedicated E0203/E0204 are an additive follow-up, flagged for the gate.

- 2026-06-07 — **E2 block 2 tranche A — `fn` + free calls + `return` + LOCKSTEP COMPLETE** (commit `6e30e55`, all green Debug + ReleaseSafe + lint + fmt). Full vertical:
  - **Lexer/token:** `fn`/`return`/`throws`/`async` graduated from `non_s3_keywords` to `s3_keywords` (real tokens); new `arrow` token (`->`) lexed by the `-` branch (`-`/`-=`/`->`). Lexer test updated (`fn` → `kw_fn`; `enum`/`behavior` still `error_unknown_keyword`) + a new token test for the E2 keywords + `->`.
  - **AST:** `FnDecl` (params range into a new `fn_params` slab + return_type + is_async/throws + body run + trailing `value` + annotations), `MethodCall` slab (reserved, produced in tranche B), `addFnDecl`/`addMethodCall`/`addReturnStmt` helpers. `return_stmt` data = value `NodeId.raw()` (no slab, like `expr_stmt`).
  - **Parser:** `parseFnDecl`/`parseFnDeclFrom` (`[async] fn IDENT (params) [throws] [-> type] block`, body via `parseBlockBody` for the implicit-return trailing value); `parseReturnStmt` (bare before `}`/EOF, else an expr); `kw_return` joined `parseStmt` + `startsKeywordStmt`. Free-call parsing was already E1 (`addCall`), so block 2 only extends resolve/interp/codegen.
  - **Resolve:** `SymbolKind.fn_` + `AnnotTarget.function`; pass-1 registers fn symbols, pass-2 `checkFn` (params as locals, `current_fn_return` for return checks, trailing-value check); `synthCall` gains the free-fn branch (callee ident → `fn_` symbol, not shadowed by a local) → `synthFreeFnCall` (arity + arg + return typing, E0200); `checkStmt` `.return_stmt` checks the value against `current_fn_return`.
  - **Interp:** `fns` name→FnDecl map (compile pass C); `returning`/`return_value` signals mirroring `thrown` at every unwind site (execStmtRun, the 4 loop sites, loop_expr/block_expr eval, execBody reset + top-level consume); `.fn_call` free path → `callFn` (args in caller scope, fresh frame, `return` consumed at the boundary, else trailing value); `.return_stmt` raises `returning`.
  - **Codegen:** pass C emits one Zig `fn` per Etch fn (`fnTypeZig` maps params/return); `.fn_call` disambiguates free-fn (`name(args)`) vs closure (`callee.call(args)`) by `LocalCtx.lookup`; `.return_stmt` → `return [<expr>];`. async fn / throws fn decls → `UnsupportedConstruct` (fail loud).
  - **LOCKSTEP honoured:** `kw_fn` + `kw_async` added to `parseTopLevel` AND `recoverToTopLevel`'s stop-set in the same commit; two survival tests (broken construct + valid `fn` / `async fn` → the fn survives).
  - **Tests:** parser (4 fn decls incl. async, return + free call counts), types (clean / arity / arg-type / return-type-mismatch), interp (free call → 42; early `return` unwinds past a later `return`), differential `38_fn_free_call` byte-exact. **Stale S3 fixtures repointed:** `fn` no longer emits E0001, so `E0001_unsupported_fn.etch` → `E0001_unsupported_behavior.etch` and two `fn`-as-unsupported tests (lexer/parser) repointed to `behavior` (E4). Manual cook confirmed `let r = double(21)` → `const r = double(21);` ast-checks clean (Zig-inferred local type).
  - **Next: tranche B — method-call parsing** (`recv.method(args)` → reserved `method_call`; dispatch is block 3).

- 2026-06-07 — **E2 block 2 tranche B — method-call parsing COMPLETE → BLOCK 2 DONE** (commit `e336878`, all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). `recv.method(args)` now produces the reserved `method_call` ExprKind: a one-token lookahead in `continuePostfix` (`.` `IDENT` `(` → `parseMethodCall`, else field access) stores receiver + method name + positional args (`MethodCall` slab + `addMethodCall`, added in tranche A). `recv.field` (no parens) stays a field access. The 4-kind dispatch (`etch-resolver-types.md §5`) is block 3 once `impl` provides methods — until then `method_call` **fails loud in both backends** (interp → `RuntimeFailure`, codegen → `UnsupportedConstruct`; explicit cases, not a catch-all), never a silently-wrong value. Resolver leaves it `unknown` (no spurious diagnostic; parser-only in block 2). Parser test: `normalize()` / `calculate(target, 5)` → `method_call` (calculate carries 2 args), `entity.length` → `field_access`. **Block 2 (call mechanism) is complete:** top-level `fn` + free-function calls (tranche A) + method-call parsing (tranche B). E2 remains 2 of 5 internal blocks done — not the Review E2 gate.

- 2026-06-07 — **E2 block 3 plan — declaration layer + dispatch (EBNF + dispatch confirmed before writing, anti-hallucination).** Re-read `etch-grammar.md §5.7–5.9` + `etch-resolver-types.md §5` (dispatch) + `§7` (traits/impls/coherence) and the full delivered surface (`ast.zig` 1349, `parser.zig` 2411, `types.zig` 2177, `interp.zig` 2315, `zig_codegen/lower.zig` 2074) before scoping. Forms verified in `etch-grammar.md`: `struct_decl = "struct" TYPE_IDENT [generic_params] "{" {annotated_field} "}"` (§5.7 l.859); `enum_decl = "enum" TYPE_IDENT [generic_params] "{" enum_variant {"," enum_variant} ["," ] "}"`, `enum_variant = IDENT | IDENT "{" fields "}" | IDENT "(" types ")"` (§5.8 l.869/873); `trait_decl = "trait" TYPE_IDENT [generic_params] "{" {trait_member} "}"`, `trait_member = function_signature | function_decl` (§5.9 l.881-886); `impl_decl = "impl" [generic_params] (impl_trait_for_type | TYPE_IDENT) [when_clause] "{" {function_decl} "}"`, `impl_trait_for_type = TYPE_IDENT ["<" type_list ">"] "for" TYPE_IDENT` (§5.9 l.890-894); `self_param = [mut] self` (§5.3 l.787); `struct_literal = TYPE_IDENT "{" [field_init {"," field_init} [","]] "}" | "." "{" [...] "}"`, `field_init = IDENT ":" expression | ".." expression` (§3.2 l.486-491); enum-variant pattern `"." IDENT` / `TYPE_IDENT ["." IDENT]` (§3.2 l.510-511). Dispatch (`§5.5`) is the strict order inherent (`impl Type`, §5.1) → trait (`impl Trait for Type`, §5.2) → builtin (`entity.get`/…, §5.3) → service (Tier 1, §5.4, Level B / out of M0.8 core). Coherence: orphan rule §7.4 (Trait OR Type local to the impl's module), conditional-impl proof §7.3 (`when self has H` provable from the calling rule's `when`). Code surface verified: `struct_decl`/`enum_decl`/`trait_decl`/`impl_decl` ItemKinds + `struct_lit`/`self_expr`/`path` ExprKinds are reserved-but-unproduced (no side slabs / add-helpers yet — block 3 *produces* them); `method_call` is produced + fail-loud in both backends (block 2 tranche B); `self` is a plain `ident` (token.zig note l.228), `mut` is `kw_mut`; the differential harness registers each program in BOTH `tests/etch_interp/corpus_facade.zig` (interp diff) AND `tests/etch_interp/codegen_corpus_build.zig` (codegen cook) — both run the world-state diff, so a differential program must be sound-codegen on both backends; next differential number is **39**.

  **Three tranches, each a full vertical (lexer→token→ast slab+helper→parser→types→interp→codegen + inline tests + a numbered differential), committed + journaled separately, in dependency order:**
  - **A — `struct` + struct literals + inherent `impl` + associated fns + inherent method dispatch (§5.1) + LOCKSTEP `kw_struct`/`kw_impl`.** `struct V {...}` → `extern struct` (builtin scalar fields in block 3, like component/resource; nested-struct / string fields deferred + flagged); explicit struct literal `V { f: v }` → struct value (interp) / `V{ .f = v }` (codegen); inherent `impl V { fn m(self|mut self,…) -> T {…} fn assoc(…) -> V {…} }` methods emitted as Zig `pub fn` inside the struct; `recv.method()` (instance, §5.1) and `V.assoc()` (associated, path receiver) dispatched in `synth`/interp/codegen (block-2's fail-loud `method_call` is wired here). `kw_struct`/`kw_impl` join `parseTopLevel` AND `recoverToTopLevel`'s stop-set in the same commit + survival tests. Differential `39`.
  - **B — `enum` + variants + `.variant` shorthand + enum `match` + LOCKSTEP `kw_enum`.** C-like enum → Zig `enum`; `Difficulty.easy` / `.easy` (type-driven shorthand); enum-variant `match` patterns (extends the E1 flat pattern set with enum variants). Struct-like / tuple-like variants (tagged union) parsed; their interp/codegen scoped to what the differential needs (data-carrying variants flagged if deferred). Differential.
  - **C — `trait` + `impl Trait for T [when …]` + trait method dispatch (§5.2) + orphan rule (§7.4) + conditional-impl proof (§7.3) + LOCKSTEP `kw_trait`.** Differential on a trait method called through a generic-free path.
  **Scope boundaries flagged up front for the Review E2 gate:** anonymous struct literal `.{ … }` (needs expected-type / bidirectional inference — the type-checker is synth-only) → deferred + flagged, an E2 refinement like block-2's named-args/defaults; struct `spread` `..base` (data-table feature, E4) → out of scope; generics on struct/enum/trait/impl → block 4 (parsed `<` rejected cleanly here). `return`/`throw`/`break` inside a method reuse the unified `returning`/`thrown` signal from block 2 — a `return` exits the method (its own call frame), not the caller (verified against `callFn`'s boundary-consume).

- 2026-06-07 — **E2 block 3 tranche A — struct + inherent impl + method dispatch COMPLETE** (commits `7912408` plan, `be207ce` feat; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). Full vertical:
  - **Lexer/token:** `struct` / `impl` graduated from `non_s3_keywords` to `s3_keywords` (real `kw_struct` / `kw_impl`).
  - **AST:** `SelfKind` (`none`/`by_value`/`by_mut`) + `FnDecl.self_kind`; `StructDecl` (reuses the `fields` slab), `ImplDecl` (`type_name` + `trait_name=0` inherent + `when_root` + a `(start,len)` run into a new `impl_methods` slab of `FnDecl`), `StructLitExpr` + `struct_lit_fields` slab; `addStructDecl`/`addImplDecl`/`addStructLit` helpers; `StringPool.find` (look up an interned id without inserting — for `*const`-only consumers resolving the `self` binding).
  - **Parser:** `parseStructDecl`, `parseImplDecl` (inherent only — `impl Trait for T` rejected with a tranche-C pointer; conditional `when` parsed); `parseFnLike(is_async, allow_self)` refactor extracted from the block-2 fn path, shared by top-level `fn` (`allow_self=false`) and impl methods (`allow_self=true`), `tryParseSelfParam` detects `self` / `mut self` by lexeme (`self` is a plain ident, interned there for downstream `find`); explicit struct literals `T { f: v }` in `parsePrimary` (the `{` lookahead after a `TYPE_IDENT`), with a **`no_struct_lit` context flag** suppressing them in `if`/`while`/`for`/`match` heads (the classic struct-literal-vs-block ambiguity, resolved as in Rust — re-enabled inside `( … )` / `[ … ]` / call+method args / struct-field values, and the body sees it restored to false by `parseExprNoStruct`'s defer). **LOCKSTEP:** `kw_struct` + `kw_impl` joined `parseTopLevel` AND `recoverToTopLevel`'s stop-set in the feat commit + a survival test (broken construct → valid struct + impl survive).
  - **Resolve:** `SymbolKind.struct_` + `ResolvedType.struct_t`; pass-1 registers structs + indexes inherent methods into a `methods` map keyed by `methodKey(type_name, method_name)`; `validateImpls` (post-pass-1, target must be a declared struct/component/resource); pass-2 `checkImpl`/`checkImplMethod` (binds `self` to the impl's target type for `self`/`mut self`, mirrors `checkFn`); `synthStructLit` (field-by-field type check, omitted fields default-filled), `synthMethodCall` (the block-2 fail-loud wired — associated dispatch on a `.path` receiver, instance dispatch on a struct-valued receiver, kind mismatch flagged), `checkMethodArgs`. Diagnostics reuse E0200 (the dedicated E0201 MethodNotFound is an additive follow-up, flagged).
  - **Interp:** `Value.struct_ref` + a per-rule-body `StructStore` (reset at the body boundary, like collections/closures); pass-D indexes `methods` + `struct_decls` by name; `struct_lit` materializes a value (declared fields filled, defaults for omitted); `method_call` dispatch → `callMethod` (binds `self`; a `mut self` method shares the struct handle so mutation propagates — reference semantics); field read + `execAssign` field write on `struct_ref`. **`return` inside a method unwinds to the method's own frame** (consumed in `callMethod`, mirroring `callFn`) — verified by an inline early-return-style test path.
  - **Codegen:** `emitStructDecl` (→ `extern struct` with fields + inherent methods as `pub fn` members); `emitMethod` (`self` → `self: T`, associated → plain fn); struct literal → `T{ .f = v }`; `method_call` → `recv.method(args)` / `Type.assoc(args)`; `inferExprZigType` + `receiverComponentName` + `fieldZigTypeOnComponent` extended for struct locals.
  - **Differential `39_struct_method`** (registered in both facades): `V2.make(3, 4)` (associated) + `v.sum()` (instance) → out = 7, byte-exact interp↔cooked-Zig. Inline tests: parser (struct/impl/struct-lit/method-call counts + self_kind + survival + no-struct-lit-in-head + trait-impl/generic rejection), types (dispatch + 6 error cases), interp (associated+instance dispatch; `mut self` mutation propagation). EBNF corpus +1 struct/impl block (harness 32 clean).
  **Scope flags for the Review E2 gate:**
  - **Anonymous struct literal `.{ … }` deferred** — needs expected-type / bidirectional inference (the type-checker is synth-only); not produced by the parser (a `.` `{` lands on the existing "expected identifier after '.'" path). An E2 refinement like block-2's named-args / default params.
  - **`mut self` method codegen deferred** (→ `UnsupportedConstruct`, fail loud) — needs a pointer receiver (`self: *T`) + an addressable call site. The interpreter is its reference (inline test covers it). Folds into the same family as the other E1/block-1 codegen deferrals (E3 gate).
  - **Component / resource instance-method dispatch deferred in interp** (`recv.method()` where `recv` is a component/resource ref) — the interpreter cannot recover the type name from a bare ref → fail loud. The resolver accepts it (general by-name dispatch); block-3 differentials use `struct` receivers. Associated-fn + struct-instance dispatch are complete in both backends.
  - **Struct fields = builtin scalars** (like component/resource) — nested-struct / string fields deferred. **Generics on struct/impl** → block 4 (`<` rejected). **`E0220 ImmutableReceiverForMutSelfMethod`** (§7.6) not enforced — a `mut self` call on an immutable receiver is not yet flagged (additive follow-up, flagged).
  **Next: tranche B — `enum` + variants + `.variant` shorthand + enum `match` + LOCKSTEP `kw_enum`.**

- 2026-06-08 — **E2 block 3 tranche B — enum + variants + `.variant` shorthand + enum match COMPLETE** (commit `d2b47e6` feat; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). Full vertical, C-like enums end-to-end:
  - **Lexer/token:** `enum` graduated from `non_s3_keywords` to a real `kw_enum`. Stale-fixture watch closed: the "unknown keyword" lexer test repointed `"fn enum behavior"` → `"fn trait behavior"` (`trait` stays out of scope until tranche C).
  - **AST:** `EnumDecl` + `EnumVariant` (`EnumVariantShape` = `c_like` / `struct_like` / `tuple_like`) in new `enum_decls` + `enum_variants` slabs; `PatternKind.enum_variant` + `EnumPatternPayload` (`enum_pattern_payloads` slab — `type_name=0` for the `.v` shorthand, the explicit enum for `Type.v`); `addEnumDecl` helper. Data-carrying variant data is recorded (struct-like → `arena.fields` run; tuple-like → `arena.extra` type-`NodeId` run) for a fail-loud downstream.
  - **value:** a C-like `enum_value` Value `(type_name, variant)`, compared by value in `eql`.
  - **Parser:** `parseEnumDecl` (`<` rejected → block 4; struct-like/tuple-like variant *shapes* parsed); `parsePattern` extended with the `.variant` shorthand (`.dot`) + qualified `Type.variant` (`.type_ident` `.dot` `IDENT`) — struct-destructure (`TYPE_IDENT {`) and bare-`TYPE_IDENT` patterns rejected with a pointer (post-Phase-1 advanced set). **LOCKSTEP:** `kw_enum` joined `parseTopLevel` AND `recoverToTopLevel`'s stop-set in the feat commit + an enum survival test.
  - **Resolve:** `ResolvedType.enum_t` + `SymbolKind.enum_`; pass-1 registers enums + `validateEnumVariants` (duplicate-variant E0101); enum-value detection in `synthExpr` field-access (`Difficulty.hard` → `.enum_t`, **E0105 EnumVariantNotFound** on an unknown variant); enum exhaustiveness in `synthMatch` (cover all variants ⇒ exhaustive, else E1230; qualified-pattern enum mismatch flagged). New diagnostic code `E0105`.
  - **Interp:** `enum_decls` indexed in pass D; enum value materialized in field-access (`enumVariantIndexOf`); `.enum_variant` match arm compares the scrutinee's `(enum, variant)` against the resolved arm variant.
  - **Codegen:** `emitEnumDecl` → Zig `enum(i32) { … }` (`etch-abi-zig.md §3.1`); enum value → `T.variant` in `emitExpr` + `inferExprZigType` (`enumValueName` helper); `.enum_variant` arms → if-else chain on `== T.variant` in both match emitters (`emitMatch` value form + `emitMatchAsStmt`), shorthand resolving its enum from the scrutinee (`enumPatternTypeName`, fail-loud if unresolvable). **Data-carrying variants + generic enums fail loud** (`UnsupportedConstruct`).
  - **Differential `40_enum_match`** (registered in both facades): `let d = Difficulty.hard; out = match d { Difficulty.easy => 1, .normal => 2, Difficulty.hard => 3 }` → out = 3, byte-exact interp↔cooked-Zig (cooked: `pub const Difficulty = enum(i32) { easy, normal, hard }`, `if (__m0 == Difficulty.easy) … unreachable`). Inline tests: parser (decl + qualified/shorthand patterns + survival + data-shape parse + generic reject), resolver (exhaustive / non-exhaustive E1230 / wildcard / E0105), interp (middle-arm selection). EBNF corpus +1 enum/match block (harness 33 clean).
  **Scope flags for the Review E2 gate:**
  - **Data-carrying enum variants** (`circle { … }` struct-like, `pair(int,int)` tuple-like) — the *shape* parses (grammar accepted), but **construction + destructuring patterns are deferred** (post-Phase-1 advanced pattern set, `etch-reference-part1.md §7.6`); fail-loud in codegen (`emitEnumDecl` → `UnsupportedConstruct`) and unreachable in interp. **Generics on enum** → block 4 (`<` rejected).
  - **EBNF-vs-reference variant case** — `etch-grammar.md §5.8` gives `enum_variant = IDENT` (snake_case) for all three shapes; `etch-reference-part2.md §6` *illustrates* data-carrying variants with PascalCase (`Physical`/`Magical`). Implemented per the EBNF (the brief's designated source of truth); flagged for review as a KB inconsistency to reconcile (likely an `etch-reference-part2.md` patch at M0.8 close).
  - **`.variant` shorthand in value position** (`let d: Difficulty = .hard`) — needs bidirectional check-mode (expected-type propagation); the synth-only checker resolves the **qualified** `Difficulty.hard` value form + the shorthand **in patterns** (scrutinee-typed). Shorthand-in-value folds into the same expected-type refinement as the anonymous `.{ … }` struct literal (tranche A flag).
  **Next: tranche C — `trait` + `impl Trait for T [when …]` + trait dispatch §5.5 + coherence/orphan §7.4 + conditional-impl proof §7.3 + E0220 + LOCKSTEP `kw_trait`.**

- 2026-06-08 — **E2 block 3 tranche C — trait + impl Trait for T + trait dispatch + E0220 COMPLETE → BLOCK 3 DONE** (commit `9c48bc2` feat; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). Heaviest tranche; the `etch-resolver-types.md §5.5/§7.1-7.6` surface was re-read in full before writing (anti-hallucination). Full vertical:
  - **Lexer/token:** `trait` graduated to `kw_trait`. Stale-fixture watch closed: the lexer "unknown keyword" test repointed `"fn trait behavior"` → `"fn behavior quest"` (both still out of scope); the `error_unknown_keyword` doc-comment examples refreshed (`fn`/`enum` no longer unknown).
  - **AST:** `TraitDecl` (`trait_decls` slab) — members reuse the `impl_methods` slab; `FnDecl.has_body` (default `true`) distinguishes an abstract `function_signature` from a bodied method; `addTraitDecl` helper.
  - **Parser:** `parseTraitDecl` (`trait_member = function_signature` no-body `| function_decl`); `parseFnLike` gained `allow_signature_only` (abstract members end without `{`, `has_body=false`); `parseImplDecl` flipped the tranche-A `for`-rejection to parse `impl Trait for Type` (first name = trait, post-`for` = target, `trait_name` stored; a `<` after the trait name → block 4). **LOCKSTEP:** `kw_trait` joined `parseTopLevel` AND `recoverToTopLevel` in the feat commit + a trait survival test.
  - **Resolve:** `SymbolKind.trait_`; pass-1 splits `.impl_decl` (inherent → `methods`, trait → a new `trait_impls` list) + registers `.trait_decl`; `validateTraitImpl` (trait declared; orphan rule §7.4 → E0217, structural single-module; **E0214** IncompleteTraitImpl — every abstract method provided or defaulted); `synthMethodCall` instance dispatch now follows the **§5.5 strict order** inherent (§5.1) → trait (§5.2), with an `Entity` receiver path for `impl Trait for Entity`; trait default-method resolution (`traitDefaultMethod`); **§7.3 conditional-impl proof** (`traitImplConditionsProven` walks the impl's `when` conjunction against the rule's `components_in_when` → **E0215** if unproven); **E0220** ImmutableReceiverForMutSelfMethod (`checkMutSelfReceiver` via `isAssignTargetReachable` — covers inherent `mut self` shipped tranche A *and* trait); **E0211** AmbiguousTraitMethod (cross-trait); `checkImpl` binds `self` to `Entity` for Entity trait impls and folds the impl's `when` into the method ctx so `self.get_mut(H)` is allowed in the body. New codes E0211/E0214/E0215/E0217/E0220.
  - **Interp:** `trait_methods` table built in pass D2 (impl-provided + un-overridden trait defaults), consulted **after** inherent `methods` (§5.5); instance dispatch on a struct receiver (inherent→trait) and on an `Entity` receiver (trait, keyed by interned `Entity`; the unified `returning`/`thrown`/`control` signal composes through `callMethod` unchanged — verified by the Entity test mutating via `get_mut`).
  - **Codegen:** trait methods emitted as `pub fn` members of a struct target — impl-provided ones via the existing `emitStructDecl` loop (`impl.type_name == decl.name`), plus the trait's **default** methods the impl does not override (`findTraitDecl`/`implHasMethod`). Entity trait-method codegen stays interp-reference (no struct member to attach to); `mut self` codegen still deferred (`UnsupportedConstruct`) as in tranche A.
  - **Differential `41_trait_method`** (both facades): a `trait Doubler { fn base(self)->int; fn doubled(self)->int { self.base()*2 } }` + `impl Doubler for N` (provides `base`, inherits the default `doubled`); `out = N{v:21}.doubled() = 42`, byte-exact interp↔cooked-Zig (cooked emits both `base` and the default `doubled` as `pub fn` members of `N`). Inline tests: parser (trait decl + impl-trait form + abstract/default + survival + generic reject), resolver (trait dispatch happy-path; E0220 immutable vs `let mut`; E0214; E0215 both directions), interp (Entity trait method, current 100−30=70). EBNF corpus +1 trait/impl block (harness 34 clean).
  **Scope flags for the Review E2 gate:**
  - **Trait default-method bodies are not abstractly type-checked** in the resolver (no `Self` type modelled); validated by emission (struct target → Zig compile) + execution (interp). An abstract-`Self` checker is a refinement.
  - **§7.3 same-trait conditional ambiguity** (multiple conditional impls of one trait → most-specific-wins / E0216 tie-break) simplified to **E0211**; the most-specific resolution is flagged for review.
  - **Trait impls on Entity / component / resource**: codegen of a *call* to such a method is interp-reference (no struct member); M0.8 differentials use struct targets. **`mut self` method codegen** still deferred (fail-loud), as in tranche A.
  - **Generic traits / generic trait impls** → block 4 (`<` rejected). **E0220 closed** (the launch's must-close validation): a `mut self` call on a `let` (immutable) receiver is now flagged at the resolver — the interpreter no longer accepts that invalid program.
  **Block 3 (declaration layer + 4-kind dispatch §5.1-5.2 + coherence) is COMPLETE** (tranches A struct/inherent, B enum, C trait). Differentials run 01–41. `recoverToTopLevel` stop-set now: S3 + `type` + `fn` + `async` + `struct` + `impl` + `enum` + `trait`. **Next: block 4 — generics (parse + resolve + interp; codegen `UnsupportedConstruct`).**

- 2026-06-08 — **E2 block 4 — generics (parse + resolve + type-erased interp) COMPLETE** (commit `5d66520` feat; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). Generics on `fn`/`struct`/`enum`/`trait`/`impl`, per `etch-grammar.md §2.4`+§270, `etch-resolver-types.md §6`, `etch-reference-part1.md §3.7`. **No codegen differential** — the brief's explicit exception: interp is the reference, codegen monomorphisation is Phase 2.
  - **AST:** `GenericParam` + `GenericBound` (`GenericBoundKind` trait_/component/resource/event) in new `generic_params`/`generic_bounds` slabs; `GenericTypeNode` (the reserved `generic` TypeNodeKind, args run into `arena.extra`) for `Foo<T>` in type position; `generics_start/len` on all 5 decl structs; `addGenericType` helper.
  - **Parser:** `parseGenericParamsBuffered` (buffered so a `where` clause can extend a param's bounds before commit) + `parseBoundsInto`/`parseOneBound` (`TYPE_IDENT` trait / `component` / `resource`; `event` needs the E3 keyword) + `parseWhereInto` + `commitGenerics` + `parseOptionalGenerics`; `parseFnLike` buffers params, parses `where` (detected by lexeme — `where` is a plain ident, not a token) after the return type, then commits; `parseGenericTypeApp` in `parseBaseType` (`Set`/`Map` keep dedicated nodes); `parseImplDecl` parses `impl<T> …` + erases the trait's `<type_list>` in the `for` form. The five block-2/3 `<`-rejections flipped to parse. **No top-level keyword added → `recoverToTopLevel` unchanged** (generics parametrise existing constructs).
  - **Resolve:** `ResolvedType.generic` (a type variable); `generic_scope` set while checking a generic `fn`/`impl`/`struct` body (param names → `.generic`, permissive like `unknown`); `namedTypeToResolved` resolves in-scope params + the `generic` type node (args erased → base type); generic struct fields typed by a param accepted. `synthGenericFnCall`: structural inference (`unifyGeneric` over bare `T` and element-of `T[]`/`T[N]`) → **E0603** GenericTypeAnnotationRequired (uninferable param) + **E0604** InconsistentGenericInference; `checkGenericBounds` (`component`/`resource` → RTTI category; trait → `typeImplementsTrait` scan of `trait_impls`) → **E0601** BoundNotSatisfied; `substituteGeneric` applies the inferred subst to the return type. New codes E0601/E0603/E0604.
  - **Interp:** the tree-walker monomorphises trivially — generic fns bind args by name, generic structs materialise like any struct (types erased). **No interp change beyond confirmation** (a test runs `id<T>(41)+1 + Box<T>{value:100}.value` = 142).
  - **Codegen:** `emitFnDecl`/`emitStructDecl`/`emitEnumDecl`/`emitMethod` fail loud on `generics_len > 0` (`UnsupportedConstruct`). A `generic` type node never reaches `fnTypeZig` in a cookable program (the generic decl fails the cook first).
  - **Tests + harness:** parser (generic params + bounds + where + generic type; inherent generic-target rejected EBNF-strict), resolver (inference happy-path; E0601/E0603/E0604; generic struct field accepted), interp (type-erased run = 142). EBNF harness +1 (**35** clean). Two stale block-B/C tests (generic struct/enum previously *rejected*) updated to expect a clean parse.
  **Scope flags for the Review E2 gate:**
  - **EBNF-strict, inference-only (session decision, Guy):** explicit fn-call type args (`f<int>(x)` / turbofish `f::<int>`) are NOT in the EBNF `postfix_op` (only `qualified_call` `<X as Trait>::m()` carries `::`, and that's trait disambiguation, out of generics scope). Type args are inferred (§6.4). The `nearest::<Player>` of §6.4 is doc shorthand absent from the EBNF — **flagged** for a KB reconciliation (prompt↔EBNF), like the tranche-B variant-case.
  - **`impl<T> Range<T>` inherent generic target (§3.7)** is not in the EBNF (`impl_decl` inherent branch = bare `TYPE_IDENT`) — followed the EBNF: the inherent target is bare (`impl<T> Range { … }`); the `Range<T>` form is rejected with a pointer. **Flagged** for KB reconciliation.
  - **Monomorphisation instance table (§6.2)** has no M0.8 consumer (codegen deferred) — the resolver computes the inferred substitution per call but does not persist a monomorphisation table. **Deep structural inference** is limited to bare `T` / `T[]`/`T[N]`; nested generic args (`Map<T,U>` params) leave the param unbound → E0603 (conservative). **E0602** (instance-depth) and **E0605** (conditional-impl bound proof) are Phase-2/refinement — a trait bound is satisfied if any (incl. conditional) impl exists. **Generic trait default-method bodies** are not abstractly checked (same as tranche C). **`T: event` bound** needs the `event` keyword (E3).
  **Next: block 5 — `Optional T?` + `Error`/`ErrorCode` + `if let`/`while let` + the deferred Optional-returning accessors → STOP at the Review E2 gate.**

- 2026-06-08 — **E2 — §891 grammar alignment (generic inherent impl target) COMPLETE** (commit `cbd1f04` fix; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). A bounded KB-reconciliation task before block 5: `etch-grammar.md §891` was patched while block 4 was in flight — the `impl_decl` target became `( impl_trait_for_type | generic_type | TYPE_IDENT )`, so `impl<T> Range<T> { … }` (generic inherent target, the canonical form §3.7 illustrated) is now grammatical. Block 4 had **rejected** it EBNF-strict (target = bare TYPE_IDENT) and flagged the inconsistency. Alignment: `parseImplDecl` now accepts a `<type_list>` after the inherent target (args erased — the impl-level `<T>` carries the params; `type_name` = base, e.g. `Range`). The trait-form target stays bare (`impl_trait_for_type` unchanged — `impl T for Bar<U>` still rejected). **Turbofish stays absent** (block 4 inference-only fn-call type args unchanged — the §6.4 patch removed the `nearest::<…>` shorthand, confirming inference-only). Updated the block-4 rejection test to assert a clean parse (+ a kept rejection for a generic trait-form target); added an interp-reference test (`impl<T> Range<T> { fn contains(self, v: T) -> bool }` → `Range{2,8}.contains(5)` = out 1; generic codegen stays `UnsupportedConstruct`, so interp-reference, not a codegen differential). EBNF harness +1 (**36** clean). Differentials still 01–41. **The block-4 §891 flag is now resolved** (the §3.7 inherent-generic-target form is supported); the turbofish flag is also resolved KB-side (§6.4 patched to inference-only). **Block 5 not started** — alignment was the gate before it.

- 2026-06-08 — **E2 block 5 — Optional (T?) + none/some + if let / while let COMPLETE → E2 COMPLETE, Review E2 gate reached** (commit `68543f9` feat; all green Debug + ReleaseSafe + test-codegen-diff + lint + fmt). The final E2 block, full vertical:
  - **Lexer/token:** a `question` token for the `T?` suffix (`?.` / `??` deferred — a bare `?` is always `question`).
  - **AST:** `addOptionalType` (`optional` type node, payload = node data); `let_binding: StringId` on `IfExpr` / `WhileStmt` (0 = plain). `none_lit` / `some_lit` (reserved) — `some_lit` data = inner expr.
  - **value:** `Value.optional` (handle into a per-rule-body `?Value` optional store; null = none).
  - **Parser:** `T?` suffix in `parseType`; `none` / `some(x)` by lexeme in `parsePrimary` (not keywords, per the token-table note); `if let x = e { } [else]` in `parseIf`; `while let x = e { }` in `parseWhileStmt`. **No top-level keyword → `recoverToTopLevel` unchanged.**
  - **Resolve:** `ResolvedType.optional(BuiltinType)` (scalar/Entity payload; `struct?`/`enum?` deferred → unknown); `T?`/`some`/`none` typed; `optionalPayload` unwraps for if-let/while-let (non-optional scrutinee → E0200); payload binds in the then-block / body scope.
  - **Interp:** `optionals` store (reset at the body boundary); none/some materialise an entry; if-let/while-let unwrap + bind + branch/loop.
  - **Codegen:** `T?` → Zig `?T` (scalar); `none` → `null`, `some(x)` → `@as(?T, x)`; if-let/while-let → `if (o) |x| …` / `while (o) |x| { … }`. Non-scalar optional payloads fail loud (`UnsupportedConstruct`).
  - **Differential `42_optional_if_let`** (both facades): byte-exact `out = if let x = some(41) { x + 1 } else { 0 }` = 42 (cooked: `const present: ?i64 = @as(?i64, 41); … if (present) |x| ((x + 1)) else (0)`). EBNF harness +1 (**37** clean). Tests: parser (T? + none/some + if-let + while-let), resolver (payload binding + non-optional E0200), interp (while-let loop, `some(3..1)` → 6).
  **Scope decisions flagged for the Review E2 gate:**
  - **`Error` struct / `ErrorCode` enum builtins DEFERRED to E3** (a scope-reduction vs the block-5 marker, flagged for ratification): they are the type layer of the error-handling vertical whose codegen is E3 (Review E1 ruling 1), and are inert in M0.8 without it; the canonical `Error { message: string, …, source: Error? }` needs `string` (E1-deferred) + a recursive optional. Shipping them resolve-only/string-less/throw-disconnected would be half-baked — better delivered whole with the E3 error-handling codegen.
  - **Deferred (interp-reference / fail-loud):** `?.` / `??` / `!` optional operators; `some(v)` / `none` **match patterns** (if-let is block 5's unwrap mechanism); non-scalar optional codegen; Optional-returning collection accessors (`pop()`/`last()` — heap, E1-deferred codegen). Bare `none` without an annotation/context is weakly typed (resolves to `unknown`).
  **E2 IS COMPLETE** (blocks 1 control-flow, 2 call-mechanism, 3 declarations struct/enum/trait/impl + dispatch, 4 generics, 5 Optional + if-let/while-let). **STOP at the Review E2 gate** — the full E2 diff (blocks 1–5) is presented for the Claude.ai review (relayed by Guy). E3 not started.

- 2026-06-08 — **REVIEW E2 GATE PASSED — E2 RATIFIED (Claude.ai verdict, relayed by Guy).** The full block-1–5 diff was audited on the four load-bearing zones, all sound: (a) the unified control-flow signal — `returning` consumed at the `callFn`/`callMethod` boundary, `thrown`/`control` propagated; all 6 loop sites break the native `while` on `returning`/`thrown` and consume `break`/`continue` via `handleLoopControl` (no leak, no infinite loop on an internal `return`); (b) `callFn`/`callMethod` clean consumption + `mut self` shares the struct handle (mutation propagates); (c) deferred codegen = exhaustive fail-loud (`generics`/`async`/`throws`/`mut-self`/data-variant/non-scalar-optional/`.{}`/enum-pattern → `UnsupportedConstruct`, no silently-wrong Zig); (d) §891 — generic inherent target `impl<T> Range<T>` accepted (args erased), `for`-form generic target rejected with a clear diagnostic, conforming to the patched grammar. **Ratified scope decisions** (now acted, not just flagged):
  1. **`Error` / `ErrorCode` builtins → E3** (reduction vs the block-5 marker, ACCEPTED as better than the plan): consolidates the whole error-handling vertical (type + codegen + `string`) at E3 instead of splitting it.
  2. **Optional operators / patterns** (`?.` / `??` / `!`, `some(v)` / `none` match patterns) + **Optional-returning accessors** → E3.
  3. **All E1/E2-deferred codegen** (heap collections, `string`, capturing / block-body closures, `mut self`, resource-receiver `get(R)`, `.{}` anonymous struct literal, `Error` + error-handling, Optional) closes at the **E3 "Level A complete in codegen"** gate.
  4. **No turbofish** (KB §6.4 patched), **enum variants snake_case** (KB §6 patched), **§891 aligned** — all acted.
  **Forward note for E3 (carry into the E3 RESUME MARKER):** when **block-body closures** land in E3, the closure call boundary must consume `returning` and decide that "a `return` inside a closure exits the closure, not the enclosing fn" (the unified-signal frontier, mirroring `callFn`/`callMethod`'s boundary-consume).
  **E2 is CLOSED.** E3 is NOT started here — its launch (with the wider scope: new constructs + Level-A codegen closure + the 7 debts) comes via a dedicated prompt relayed by Guy. Milestone merge/tag = Guy, at M0.8 close only. The complete E2 state is pushed to `origin` as the gate archive (not a merge).

- 2026-06-08 — **E3 OPENED — reprise + sub-slice A scoping + event *producer* landed (`67d0959`).** E2 ratified; E3 resumes on the same branch (no new branch/brief). Re-read the brief E3 scope (event/observers + `tags` + `With`/`Without`/`Changed` filters + 5 S4 + 2 S5 debts; gate = "Level A complete in interp AND codegen, async excluded from codegen"), the ratified E1/E2 codegen deferrals homed at this gate, and the E2 close. Re-read the 4 attached patched specs for sub-slice A — `etch-grammar.md` (§1.3 keywords, §3.2 `tag_expr`/§4.4 tag mutations, §5.10 `event_decl`, §5.11 `tags_decl`, §6 `when`/`ecs_condition`, §7 rules), `etch-validation-ecs.md` (§5 tag bitfield, §7 rule-when recap), `engine-ecs-internals.md` (event bus, command buffer, change detection, observers), `etch-abi-zig.md` (§3 POD/refs ABI) — and mapped the delivered rule/when/component vertical + the Tier 0 ECS surface (via an Explore pass, re-verified in code before each edit).

  **Architecture confirmed (verified in code, not from the map):** Tier 0 already ships `EventBus` (`src/core/events/bus.zig` — `register`/`emit`/`subscribe`/`poll`, all `comptime T: type`), `observers.zig`, `command_buffer.zig` (deferred spawn/despawn/add/removeComponent + flush), `change_detection.zig` (dirty bitset + ticks, NOT query-exposed). AST slots `event_decl`/`tags_decl` (ItemKind), `emit_stmt`/`tag_mutation_stmt` (StmtKind), `tag_path` (ExprKind) are reserved-unproduced — produce them, as E1/E2 did. **"Observers" in Etch = `@on_event(T)` rules** (no `observer` keyword; grammar §18.2 annotation + validation §7.1 event-based signature), NOT the Tier-0 structural observers. Because `EventBus` is comptime-typed, the dynamic tree-walker gets its OWN type-erased event store (parallel to its dynamic component store); the codegen drives the typed `world.event_bus`. `With` (`has`) / `Without` (`not has`) already exist in `parseWhenNot`/`parseWhenPrimary`; net-new filter work = tag-op when-conditions (§945, fully specified) + component `Changed<T>`. tags = the `TagSet [N]u64` bitfield component (validation §5), not zero-size components.

  **Sub-slice-C decisions NOT triggered by sub-slice A:** event/tags/tag-ops are 100% POD (ABI §3.1; `Tag`=u16, events/TagSet POD), so neither the allocator-threading nor the aligned-resource-storage decision fires here (both are sub-slice-C codegen-heap/resource concerns).

  **Event producer landed (`67d0959`):** `event` declaration + `emit` statement, full vertical. AST `EventDecl`/`EmitStmt`; `kw_event`/`kw_emit` graduated; `parseEventDecl` + LOCKSTEP `kw_event` (parseTopLevel + `recoverToTopLevel` stop-set + survival test) + `parseEmitStmt`; resolve `SymbolKind.event_` + `AnnotTarget.event` (`@networked` → event, `@config` on event → E0502) + emit field validation; interp dynamic per-tick `EventStore`; codegen event → `extern struct`, `world.event_bus.register(T, 256, .tick)`, `emit` → `world.event_bus.emit(T, T{...}) catch unreachable` (rule fns return void — rule error-propagation is sub-slice-C error-handling codegen, ABI §11). **Codegen Sema-verified** by a manual cook + `zig build-obj` with `weld_core` mapped (the cooked event program compiles; `ast-check` alone would miss the try-in-void class of error, so a full Sema build was used). Tests: parser ×2, resolver ×2, interp ×1, codegen ×1; EBNF harness +1 (**38** clean). Differentials still 01–42 (the event has no byte-exact differential yet — gated on the observer, see the §12 blocker). `recoverToTopLevel` stop-set now adds `kw_event`. **Next: tags vertical (fully specified), then the §12-gated observer + `Changed<T>` filter.**

- 2026-06-08 — **§12 spec-gap blocker RESOLVED at the design level (Guy's rulings, relayed; `etch-resolver-types.md §12` still re-attached next session for the surface details + E12xx codes).** Both flagged items are unblocked for implementation:
  1. **Component `Changed<T>` — mechanism confirmed.** `engine-ecs-internals.md §5`: `Health changed` lowers to `chunk.changed_ticks[Health][i] > system.last_run_tick`, with `get_mut` incrementing the component's `changed_tick`. The Etch *surface* + the E12xx diagnostic codes live in resolver-types §12 (re-attached next session). **Tier-0 work is in-scope (brief E3 authorises touching the ECS kernel for this):** `comptime_query` must expose change-detection (the chunk-level `changed_ticks` already exist, `src/core/ecs/change_detection.zig`) AND the dynamic interpreter must track a per-component `changed_tick`.
  2. **Observer `@on_event` binding — RESOLVED.** `event` stays a HARD keyword (`kw_event`, as graduated). The event entering an observer is an **implicit binding named `event`** in the observer body — exactly like `self` in a method. **Reuse the block-3 self-injection machinery (`callMethod`):** inject `event` of type `T` (from `@on_event(T)`) into the observer rule's frame; there is NO declared param named `event`. `E1203 OnEventTypeMismatch` is a *type-coherence* check (the `event` binding carries the annotation's type `T`), NOT a name mandate. This removes the `event`-keyword vs `event:`-param contradiction. The unified control-flow signal carries through `callMethod` unchanged (the forward note from the Review E2 entry).
  **Next-session order unchanged:** (1) `tags` (unblocked), (2) `@on_event` observer (implicit `event` binding, self-style) + `Changed<T>` (Tier-0 change-detection exposure). STOP at the Review E3 gate, not before.

- 2026-06-09 — **E3 sub-slice A — `tags` parse layer + global tag table landed (2 commits).** Re-attached patched specs verified (`Downloads/M0.8-full-grammar-v0.6/` is the authoritative PATCHED set: `etch-grammar.md` + `etch-resolver-types.md` patched 2026-06-08 — §891 + turbofish-removed §6.4; the in-repo knowledge-base copy is the stale 2026-05-19 one). Re-read before writing (anti-hallucination): `etch-grammar.md` tag productions §1.3 l.99 (`TAG_PATH`), §3.2 l.531-535 (`tag_expr`/`tag_op`/`tag_operand` — infix), §4.4 l.697-698 (`tag_mutation_stmt`), §5.11 l.914-918 (`tags_decl`/`tag_namespace`/`tag_body`/`tag_leaf`), §6 l.945 (`ecs_condition |= expression tag_op tag_operand`), keyword list l.135-137; `etch-validation-ecs.md` §5 (bitfield `TagSet`, global table, operators, E0830-E0833); `engine-ecs-internals.md` §5 (change detection l.527-600, command buffers l.644-680); the delivered event-producer vertical (`67d0959`) + the Tier-0 `command_buffer.zig` (`CommandKind {spawn,despawn,add_component,remove_component}` — no field/bit-mutation command yet) + `World` (`dynamicLocation`/`addComponentDynamic`/`removeComponentDynamic`/`dynamicArchetype`/`tickBoundary` l.915 all present). Every tag EBNF form matched the RESUME MARKER verbatim.
  1. **`tags` declaration parse** (`6343276`). `tags` → `kw_tags` (graduated from `non_s3_keywords`); `TagsDecl` + `TagNamespace` + `TagLeaf` AST slabs, **flat + parent-linked, appended in pre-order** (the parser descends top-down) = the depth-first declaration order §5.2 wants for `bit_index`. `parseTagsDecl` + recursive `parseTagNamespace`; `tag_body` homogeneity (leaves OR sub-namespaces) disambiguated by one-token lookahead (`IDENT "{"` → sub-namespace, else leaf). LOCKSTEP: `kw_tags` joined `parseTopLevel` AND `recoverToTopLevel`'s stop-set + a survival test. Resolve/interp/codegen skip `tags_decl` via their forward-compatible `else` arms (table + `TagSet` are the next commits). Parser tests: hierarchical decl (ns/leaf counts, parent links, pre-order leaf = bit_index order) + lockstep survival. Stale-fixture watch: no corpus/test collision for `tags`.
  2. **Global tag table** (`c3f9d97`). New shared module `src/etch/tags.zig` (`TagTable`) so resolver/interp/codegen build it identically (structural parity). `TagTable.build` merges every `tags {}` block → one hierarchy → DFS bit_index in declaration order (§5.2); **cross-block merge (§5.6) is why the merged tree must be walked, not the flat per-block leaf order** (they coincide only for a single block — proven by a cross-block test). API: `leafBit`/`lookup`/`collectUnder` (category mask for `has_any_tag(.ns)`)/`words` = `(leaf_count+63)/64` (§5.3). Diagnostics `E0831 TagPathConflict` (leaf-vs-namespace, or duplicate leaf) + `E0832 TagBitfieldOverflow` (bound is a `build` param, default 256, so testable without 257 tags). Wired into the resolver between pass 1 and pass 2 (`buildTags`), table stored on `TypeChecker` for pass-2 path resolution (next commit) + deinit'd with it. 4 unit tests. **`tags.zig` is reached by the real code path** (`types.zig` uses `TagTable.build`), so its inline tests are in the test graph (§13 module rooting satisfied). E0830 (use-site path-invalid) + E0833 (op on non-Entity) deferred to the query-operator commit (no use sites yet).
  All green: `zig build test` (exit 0; the 3 `failed command` lines are the documented events/ebnf/plugin_loader spurious noise — `macos-full-suite-flakiness`), `zig fmt --check`, `zig build lint`, all pre-commit + commit-msg hooks. Differentials still 01–42; EBNF harness 38 (no tags example block added yet — comes with the operators). `recoverToTopLevel` stop-set now: S3 + `type`/`fn`/`async`/`struct`/`impl`/`enum`/`trait`/`event`/**`tags`**.

- 2026-06-09 — **E3 sub-slice A — tag query operators in `when` clauses: parse + resolve (`658351e`).** Specs re-attached this session (the 5-doc sub-slice-A set, in context) — reconciled the surface details before writing: (a) the tag ops are **infix** (`tag_expr = expression tag_op tag_operand`, `etch-grammar.md` §3.2); the `.has_tag(.path)` of `etch-validation-ecs.md` §5.4 is signature documentation, the EBNF is the source of truth. (b) the when-clause path-invalid code is **E1212 UnknownTag** (`etch-validation-ecs.md` §7.2), distinct from the general-expression **E0830 TagPathInvalid** (§5.5) and from **E0833 TagInvalidOperation** (op on non-Entity) — both deferred with their contexts. (c) operator arity: `has_tag`/`has_no_tag` → a single leaf; `has_any_tag`/`has_all_tags`/`has_no_tags` → a leaf list or a category namespace (mask via `TagTable.collectUnder`).
  - **Lexer/token:** the 5 query keywords graduated to `kw_*` (`add_tag`/`remove_tag` stay reserved until the mutation commit). **AST:** `TagOp` enum; `tag_path` ExprKind produced (`addTagPath` + `tag_path_segs`); a `.tag_filter` `WhenNodeKind` + `TagFilter` side-slab (op + `(start,len)` operand run of `tag_path` ids in `tag_operands`) reached via `WhenNode.aux`. **Parser:** `parseWhenPrimary` dispatches the tag op before `has`; `parseTagPathExpr` (`.a.b.c`, parsed only in operand position → no `.variant` enum-shorthand clash) + `parseTagOperandRun` (single `.path` or `[.a, .b]`). **No top-level keyword → `recoverToTopLevel` unchanged.** **Resolve:** `collectWhen`'s `.tag_filter` arm validates each operand against the global `tag_table` (E1212; leaf required for `has_tag`/`has_no_tag`). **interp/codegen fail loud** on `.tag_filter` (`InvalidProgram` / `UnsupportedConstruct`) — the new arm is in every exhaustive `WhenNodeKind` switch (`lowerWhen`, `emitArchPredicate`, `walkWhen`, `collectComponents`) so the filter is never silently dropped; execution is the tag-execution commit.
  - **Tests:** parser (single + multi tag filters, operand path segments) + resolver (valid leaf / E1212 unknown / E1212 namespace-where-leaf-required / category accepted by `has_any_tag`). All green Debug + ReleaseSafe + lint + fmt + hooks. Differentials still 01–42 (tags do not execute yet); EBNF harness 38. **Scope deferred + flagged for Review E3:** the general-expression infix `tag_expr` (bare `TAG_PATH` primary — ambiguous with `.variant` outside the tag-op context) + the bare-`TYPE_IDENT` category operand form (a dotted path resolving to a namespace already expresses a category).

- 2026-06-09 — **E3 sub-slice A — tag-execution unit: parse+resolve + interp runtime + Tier-0 deferred command (3 commits). Codegen runtime + differential 43 remain.**
  1. **Mutations parse + resolve** (`244e283`). `add_tag`/`remove_tag` graduated to `kw_*` (out of `non_s3_keywords`); `tag_mutation_stmt = expression "." ("add_tag"|"remove_tag") "(" TAG_PATH ")"` (§4.4 l.697) detected after `.` in `continuePostfix` (keywords, not idents → not a `method_call`), producing the reserved `tag_mutation_stmt` StmtKind as a `.stmt`-category node that `parseStmt`/`parseBlockBody` route directly (no expr-stmt wrap). The bare-`TYPE_IDENT` category operand is deferred (a dotted namespace path already expresses a category), as on the query side. Resolve: receiver must be `Entity` (**E0833 TagInvalidOperation**, added) + operand a declared **leaf** (**E0830 TagPathInvalid**, added; a mutation sets one bit, so a namespace is rejected). Both backends still fail loud on `.tag_mutation_stmt` via their existing `else` arms. LOCKSTEP unchanged (postfix/expression keywords, not top-level — verified). Tests: parser (add/remove build `.stmt` nodes in the body) + resolver (Entity+leaf valid; unknown E0830; namespace E0830; non-Entity E0833).
  2. **Tag runtime in the interpreter** (`1b31b98`). `compile` builds the merged tag table (`tags.zig`, shared) and registers a builtin `TagSet` component (`[words]u64`, raw bytes) when the program declares any tag. `lowerWhen` (refactored onto a `LowerWhenCtx`) lowers a `.tag_filter` to a per-entity `TagPredicate` (operand paths → leaf bits; a category namespace expands via `collectUnder`); a positive op (`has_tag`/`has_any_tag`/`has_all_tags`) also emits a `has TagSet` archetype predicate + marks the rule entity-bound, a negative op (`has_no_tag`/`has_no_tags`) adds no archetype predicate (matches entities lacking `TagSet`). `runRule` applies the tag predicates per entity after the field filter. `add_tag`/`remove_tag` enqueue a deferred `PendingTag`; `stepOnce` flushes the queue at the tick boundary — after every rule, never mid-walk. The `.tag_filter` interp fail-loud is replaced; the `.tag_mutation_stmt` execStmt arm enqueues. Test: `add_tag` deferred at tick 1 (no increment), `has_tag` gates a counter at ticks 2-3 → value 2, `TagSet` bit ends set.
  3. **Tier-0 deferred tag command** (`e3b3586`). `World.applyTagMutation(gpa, entity, tagset_id, bit, set)` — the single apply primitive (RMW in place, or add `TagSet` via `addComponentDynamic` when absent+set, no-op on absent+clear). `command_buffer` gains `set_tag`/`clear_tag` (`CommandKind` + `TagCommand` + `setTag`/`clearTag` + `applyOne` → `applyTagMutation`); both `observers.zig` flush switches handle them (tags fire no observers). The interp's `flushPendingTags` now routes through `World.applyTagMutation` (one source of truth → byte-exact by construction). Test: `command_buffer` set_tag adds `TagSet`+sets bit on flush, clear_tag flips it back.
  All green Debug + ReleaseSafe + lint + fmt + hooks each commit. Differentials still 01–42 (codegen can't cook tags yet → 43 is the next commit); EBNF harness 38.

- 2026-06-09 — **Tag-execution interp+Tier-0 VALIDATED on report + allocator-threading ruling (Guy, relayed).** The session close is a coherent boundary; `applyTagMutation` as the shared apply primitive (byte-exact by construction) + the tick-boundary deferred flush are accepted. Structural fact 1 (Row has no entity id → arch-walk + `__entity` for mutations) is local to the tag codegen, GO. Structural fact 2 (`tick(world, gpa)`) is **VALIDATED as the uniform allocator-threading model for ALL of sub-slice C** (heap collections, `string`, `Error`), not just the tag command buffer — no global-allocator magic. **Load-bearing caveat (engraved in the RESUME MARKER as the C codegen contract):** confirm the threaded gpa is **frame/tick-scoped (reset at the boundary), not a persistent leaking gpa**; it is moot for tags (the `CommandBuffer` self-manages its arena per tick) → no STOP for item 1(c), but the frame-scoped-vs-persistent choice is the **first half of the sub-slice-C allocator decision** and **STOP+surfaces** when C's heap-allocating constructs land if non-trivial.

- 2026-06-09 — **Query-ops report VALIDATED + `command_buffer` tag-op ruling DEFINITIVE (Guy, relayed).** The query-operator parse+resolve commit (`658351e`) is accepted: the `.tag_filter` fail-loud across the 4 exhaustive `WhenNodeKind` switches is the right guarantee (a resolved-but-unexecuted filter breaks loudly, never silently dropped); the §12 reconciliation (infix ops, E1212-in-when vs E0830, arity) was read from the specs, not guessed; `parseTagPathExpr` in operand context is the correct `.variant` disambiguation; the parse/table/query-ops boundary is correct. **Definitive ruling (do not revisit):** the `add_tag`/`remove_tag` `command_buffer` touch is VALIDATED as additive — a `set_tag`/`clear_tag` command beside `add_component`/`remove_component`, same deferred-command mechanism + tick-boundary flush, forces no refactor → implement it directly next session, **NO STOP, no separate Review validation**; it appears in the Review E3 diff as a *delivered fact*, not a pending decision. The SOLE STOP+surface of sub-slice A remains `Changed<T>` (new kernel mechanics). The RESUME MARKER item 1(c) is updated accordingly.

- 2026-06-09 — **E3 sub-slice A — tag CODEGEN runtime + differential 43 landed (`de48537`). Tag-execution unit CLOSED (item 1 done).** Executed the code-verified RESUME-MARKER design verbatim; before writing, re-read `etch-abi-zig.md §3` (extern-struct layout), `etch-validation-ecs.md §5/§7`, `etch-resolver-types.md §12`, `engine-ecs-internals.md §5/§6`, the interp tag reference (`tagPredicatesPass`/`flushPendingTags`/`lowerWhen .tag_filter`/`tag_mutation_stmt`), and `command_buffer.zig` (`setTag`/`clearTag`/`flush`) — every structural fact confirmed in the real tree (Row has no entity id `comptime_query.zig` l.52-56; `DynamicArchetype = archetype.Archetype` so `entityIdsConst` is available; `EntityId` identical across archetype/command_buffer, no bitcast). `lower.zig`: emit `TagSet` as `extern struct { bits: [N]u64 = [_]u64{0} ** N }` (N=`tag_table.words()`) + register via `registerComponentRaw` (name "TagSet", no fields) — byte-identical descriptor to the interpreter. `WhenInfo` gains `tag_filters` (op + resolved leaf bits, via a second `collectTagFilters` pass on the shared `tags.zig` table); positive tag filters route through the arch-walk path (`walkWhen` adds "TagSet" to components → `arch.hasComponent(TagSet_id)` predicate; a per-slot `bits[w] & mask` continue guard mirrors `tagPredicatesPass` — has_tag/has_all_tags = all bits set, has_any_tag = any). Tag mutations materialise `const __entity = arch.entityIdsConst(chunk)[slot]` and emit `cmd.setTag`/`clearTag(__entity, idOf("TagSet").?, bit) catch {}`; the rule takes a `*CommandBuffer`, dispatched `&cmd` by `tick`. **Negative tag ops (`has_no_tag`/`has_no_tags`) stay deferred → `UnsupportedConstruct` (interp is the reference, flagged for Review E3)** — they match entities possibly lacking `TagSet`, so the arch-walk `TagSet_arr[slot]` access is undefined.
  **`tick(world)` → `tick(world, gpa)` (the engraved sub-slice-C allocator contract).** A tick-scoped `CommandBuffer` is created in `tick`, flushed after every rule and before `world.tickBoundary()` — byte-exact with the interpreter's `stepOnce` (rules + `flushPendingTags`). The contract rippled to: the `Program.tick` fn-pointer emitted by `tools/etch_cook` (was `*const fn(*World) void`), the differential-harness `Runner.step(self, world)` → `step(self, gpa, world)` (codegen runner threads gpa into `tick`; interp runner ignores it), `src/demo_etch_codegen.zig`, `diff_runner.zig`. **gpa is moot for tags** (the CommandBuffer owns + frees its arena each tick) — a plain gpa is leak-free; the frame-scoped-vs-persistent decision is still the first half of the sub-slice-C allocator question, surfaced when C's heap-allocating constructs land (per the prior ruling). No-tag programs emit `_ = gpa;` (the demo cooks + compiles clean, confirming the no-tags path).
  **Differential 43** (`43_tag_filter_mutation`, registered in `corpus_facade.zig` + `codegen_corpus_build.zig`): `mark` add_tags at the tick boundary, `count` has_tag-gates a `Counter` increment → **value 2 after 3 ticks, byte-exact interpreter ↔ codegen** (tick 1 `count` skips — no `TagSet` yet; ticks 2-3 +1 each). Verified the generated `corpus_codegen.zig` p43 emission matches the design line-for-line (TagSet struct, register, `rule_mark(world, &cmd)` with `__entity`+`cmd.setTag`, `rule_count` with `bits[0] & 0x1` guard, `tick(world, gpa)` with flush). **EBNF harness +1 → 39** (a tags block: declaration + all five query operators incl. `[.a, .b]` list operands + add_tag/remove_tag). All green Debug + ReleaseSafe + `zig build lint` + `zig fmt --check` + hooks. Differentials now run 01–43. **Sub-slice A remaining:** (2) `@on_event` observer (implicit `event` binding, self-style), then (3) STOP+surface `Changed<T>` (the sole STOP of sub-slice A).

- 2026-06-09 — **E3 sub-slice A — `@on_event` observer delivered (2 commits) + pre-existing event-register fix. Item 2 CLOSED.** Surfaced one observer-specific decision before writing (relayed to Guy, ruled): a pure event-based observer (the marker's form — implicit `event`, no entity param) has **no codegen-sound world-state write** — a component needs an iterated entity (an observer has no row), and a resource write is `get_mut(R)` receiver-less, deferred at the E3 gate (D-S3-resource-receiver; resource store is `gpa.dupe(u8)` byte-aligned → `@alignCast(*R)` is refused in ReleaseSafe). **Guy's ruling:** do NOT pull resource-receiver codegen / aligned Tier-0 storage forward — it stays homed at the E3 gate. Deliver the observer complete; the differential is **interpreter-reference**; the byte-exact event differential (emit ↔ consumer write) closes in **sub-slice C** with resource-receiver codegen. Same interp-first / codegen-when-the-infra-lands pattern as every other deferred path.
  1. **Parse + resolve + interp drain + binding** (`7dce932`). `AnnotationKind.on_event` + `onEventAnnotation`/`onEventTypeName` AST helpers (shared resolver↔interp). `event` in expression position parses as the implicit binding (an `.ident` named "event", self-style — the keyword stays the top-level declaration keyword; `parsePrimary` precedent `kw_get`). Resolve: **E1203 OnEventTypeMismatch** added (type-coherence — `@on_event(T)` needs a declared event, no declared `event:` param); `ResolvedType.event_t` + a `lookupFieldType` arm resolve `event.field` against the event decl; `@on_event` applies to `rule`. Interp: `RuleDesc.event_type`; `runObserver` fires the body once per event of type T in the per-tick `EventStore` (emit order, **live-length re-check** so a body `emit` of the same type is delivered too); `execBody` injects `event` self-style (materialised as a `struct_ref` — `EventVal`/`StructVal` share `(type_name, fields)`). Codegen guarded fail-loud until commit 2. Tests: parser, resolver (E1203 on a non-event + malformed `@on_event`, clean observer with `event.field`), interpreter (global producer emits, observer accumulates `event.amount` into a resource → 30 after 3 ticks — the byte-exact reference).
  2. **Codegen bus drain** (`dffd0fd`). **Engraved drain contract (Guy-validated — what the sub-slice-C differential exercises):** `tick(world, gpa)` calls `world.event_bus.drainAtBoundary(.tick)` at the top (= the interpreter's `events.clear()` at `stepOnce` start), then subscribes one `*EventCursor` per observer **at head=0, BEFORE any rule runs/emits**, and threads `&__evcur_<i>` into the observer call; the observer fn `rule_X(world, ev_cursor)` runs its body once per event via `while (world.event_bus.poll(T, ev_cursor) catch null) |event|`. Subscribing at the tick top (not inside the observer fn, where `subscribe` would snap past the producer's emits) is what makes the consumer see same-tick events — byte-exact with the interpreter (producer-before-consumer, emit order). The `event` payload binds as a Zig local of type T (`event.field` lowers verbatim). **No Tier-0 kernel addition** — `subscribe`/`poll`/`drainAtBoundary` all pre-exist. Deferred + fail-loud: the observer body's resource write (`get_mut(R)` → `UnsupportedConstruct` in `emitStmt`) and a combined event+entity form (component `when`/tag filter → `UnsupportedConstruct` in `emitObserverRule`). **Pre-existing producer bug surfaced + fixed:** the `build-obj` Sema check showed `world.event_bus.register(T, 256, .tick)` was missing the leading `gpa` (`register(self, gpa, T, cap, lifetime)`) — the producer's generated `register` never compiled (events had no codegen differential, so it slipped past). Now `register(gpa, T, 256, .tick)`. Validation: a relay observer (`emit B { y: event.x }`) cooks to syntactically-valid Zig (`std.zig.Ast.parse`) with the exact drain lines asserted (`lower_test`), and `zig build-obj` against `weld_core` Sema-compiles it. EBNF harness +1 → **40** (an observer block). All green Debug + ReleaseSafe + lint + fmt + hooks each commit. Differentials still 01–43 (the observer is interp-reference, no numbered differential — its codegen body write is deferred). `recoverToTopLevel` stop-set unchanged (`@on_event` is a rule annotation, not a top-level keyword — verified). **Item 2 done. Next: (3) `Changed<T>` → STOP+surface (the sole STOP of sub-slice A).**

- 2026-06-09 — **E3 sub-slice A — `Changed<T>` STOP+surface (the sole STOP; new kernel mechanics, decision relayed to Guy, awaiting GO).** Reconnaissance done; NOT implemented. Two intertwined decisions + three wiring points surfaced:
  - **Surface (anti-hallucination — not in any attached EBNF).** `etch-grammar.md §6 ecs_condition` and `etch-resolver-types.md §12.2` define ONLY `resource T changed`; the brief's scope line says `Changed<T>`; `engine-ecs-internals.md §5` *illustrates* `when entity has Health and Health changed` (a bare conjunct). No component-changed production exists in the EBNF. Candidate forms: (i) `entity has Health changed` (extend `ecs_condition` with `expression "has" TYPE_IDENT "changed"`, mirroring `resource T changed`); (ii) a `Changed<T>` filter (generic-style, the brief's wording, a new production); (iii) bare `Health changed`. Reco (i) — minimal, mirrors the existing resource form — but the EBNF is the source of truth (not in repo) → needs a Claude.ai ruling/patch, plus the E12xx code.
  - **Mechanism — Tier-0 has the full tick-based machinery, but it is DORMANT.** `World.current_tick` + `World.get_mut(T)` → `arch.markChanged(chunk, col, slot, current_tick)` (writes `changed_tick` + `setDirty`) + `beginFrame` (advances the tick, clears the dirty bitsets) + `archetype.changedTick`/`isChunkClean`/`addedTick` all EXIST (`src/core/ecs/{world,archetype,chunk,change_detection}.zig`). But in the M0.8 runtime: (a) `beginFrame` is never called — `runFor`/`tick` call only `tickBoundary` (resources), so `current_tick` is frozen; (b) the interpreter's component writes (`ecs_bridge.writeComponentField`) AND the codegen's writes (direct `C_arr[slot].field = …`) BYPASS `World.get_mut`, so nothing `markChanged`s; (c) `comptime_query.Row` exposes only component pointers, not `changed_tick`. Options: (A) tick-based per-rule (§5-faithful — advance `current_tick` per tick, route writes through `markChanged`, store per-rule `last_run_tick`, filter `changedTick(slot) > last_run_tick`); (B) dirty-bitset frame-granular (clear per tick, `setDirty` on write, filter `isDirty(slot)` — coarser, most wiring exists). For the M0.8 single-rule-pass-per-tick model (A) and (B) are observationally equivalent; reco (A) (§5-faithful, scheduler-future-proof).
  - **Three wiring points (under reco A), all NEW:** (1) `last_run_tick` per rule — interp: a `RuleDesc.last_run_tick` set after each `runRule`; codegen: a per-rule static; PLUS advancing `current_tick` per tick (call `beginFrame`, or fold the advance into the runtime's tick). (2) `comptime_query` change-tick exposure — `Row` must surface the per-slot `changed_tick` (or the archetype+slot), and the codegen `get_mut` write must `markChanged`. (3) interp dynamic per-component `changed_tick` — the dynamic archetype IS `archetype.Archetype` (so `markChanged`/`changedTick` are available); the gap is the interp's write path calling `markChanged(current_tick)` + tracking `current_tick`/`last_run_tick`. **Load-bearing across both backends: the component WRITE path must mark — today it bypasses the marking entirely, so `Changed` would never fire.** STOP for GO before implementing.

- 2026-06-09 — **E3 sub-slice A — `Changed<T>` GO + delivered byte-exact (2 commits). SUB-SLICE A CLOSE.** Guy ruled both decisions: surface (i) `entity has T changed` (the `Changed<T>` of the brief is a conceptual label, like With/Without = has/not-has — no brief patch); mechanism (A) tick-based per-rule (§5-faithful — (B) frame-granular would force a refactor when the scheduler goes multi-pass/tick in Phase 1+, a design-rule violation; (A) is observationally equivalent for M0.8, zero cost now / zero debt later). No new E12xx code (a `changed` filter on a non-component is just an unknown component — E1210). **Do NOT pull resource-receiver codegen / aligned Tier-0 storage** — unchanged.
  - **EBNF note (anti-hallucination):** Guy patched `etch-grammar.md §6` to `expression "has" TYPE_IDENT "changed"` (exact mirror of `resource T changed`; `changed` already a reserved keyword, §1.3 l.132 — verified). The `Downloads/M0.8-full-grammar-v0.6/etch-grammar.md` copy was NOT re-synced with the patch (still shows only `resource T changed`); the exact patched production was given verbatim by Guy (the authoritative ruling), so it was implemented from his message, not guessed — flagged so a next-session re-read of the stale Downloads copy is not mistaken for the form being absent.
  1. **Parse + resolve + interp** (`9d09d38`). `WhenNodeKind.has_changed` (mirror `resource_changed`); the parser accepts an optional `changed` after `has TYPE_IDENT` (mutually exclusive with `{ filter }`); the resolver folds it into the `has` arm (component must exist — E1210 reused). Interp (gated on `has_changed`, so a `changed`-free program is byte-identical to pre-E3): `RuleDesc.changed_filters` + `last_run_tick`; a component write `markChanged`s via `Bridge.markComponentChanged` in `execAssign` (per-write — same point the codegen marks); `stepOnce` advances `current_tick` (`beginFrame`) at the tick top + records each rule's `last_run_tick` after it runs; `runRule` skips a slot whose `changedTick(T) ≤ last_run_tick`. Tests: parser, resolver (component OK / E1210), interp (two-entity gating — written-each-tick → Counter 3, never-written → Counter 0).
  2. **Codegen + byte-exact differential** (`4a54b73`). **Chose the arch-walk path over exposing `changed_tick` in `comptime_query.Row`** — within the surfaced "(or archetype+slot)" latitude: the marking needs `arch`/`chunk`/`slot`, which the arch walk has, so a `changed` program routes EVERY component rule through it (consistent with the tag path; avoids a core `comptime_query` change + its gate-4 monomorphisation impact). `tick` calls `beginFrame` at the top; a component-field write emits a trailing `arch.markChanged(chunk, <C>_idx, slot, world.current_tick)` **co-located with the assignment** (executes exactly when the write does — byte-exact even under a conditional, by construction, same point as the interp); a `changed` rule keeps a module-level `__last_run_<name>` it reads (the `changedTick > __last_run` per-slot guard) and advances at the fn end. **Also moved the interp's `beginFrame` from `runFor` into `stepOnce`** — the differential harness drives ticks via the runner's `step` (→ `stepOnce`), NOT `runFor`, so the tick must advance there for both drivers to agree (caught by the differential: codegen advanced, interp did not → entity-0 Counter 0 ≠ 3, fixed by the relocation). **Differential `44_changed_filter`** (both facades): entity 0 (`Marked`) Health written each tick → `react` fires → Counter 3; entity 1 (no `Marked`) Health untouched → the `changed` guard skips it → Counter 0 — the gating proof (an always-passing filter would leave it at 3). Byte-exact interpreter↔codegen, Sema-clean via `zig build`. **EBNF harness +1 → 41. Differentials now 01–44.** All green Debug + ReleaseSafe + lint + fmt + hooks each commit. `recoverToTopLevel` stop-set unchanged (`changed` is a `when`-clause keyword, not top-level).
  **SUB-SLICE A IS COMPLETE** — event-producer + tags + `@on_event` observer + `Changed<T>` filter all delivered (observer = interp-reference / codegen drain plumbing, its byte-exact world-state diff homed at sub-slice C; tags + Changed byte-exact both backends). Next: **sub-slice B (async interpretation)** at a fresh-context boundary — see the RESUME MARKER. STOP at the Review E3 gate, not before.

- 2026-06-09 — **SUB-SLICE B IS COMPLETE (async interpretation, T1) — ratified by Guy.** Scope deviation/refinement of the brief's terse "async interpretation (suspend on `await`, resume on wake)" (l.53), the two surfaced Cas-2 decisions ruled by Guy (Option-A task-record; `wait` in TICKS; `await` at statement position; poll in `stepOnce`; T1-only). The full detail + the code-verified design + the sub-slice-C RESUME MARKER are in the matching "Blockers encountered" entry; the **perimeter of record** is:
  - **T1 — DELIVERED** (interp-reference, 3 commits `8dd3465` parse / `5484709` wait / `02c856a` global_event): `async rule` (parameterless, non-entity-bound) + `await`, suspend/resume via the Option-A `AsyncSlot` task-record polled in `stepOnce`; two wake sources — **event-based** `await global_event(T)` (live per-tick EventStore, producer-before-awaiter) and **`await wait(N)` counted in TICKS** (`async_tick` clock gated on `has_async` → sync-only programs byte-identical to pre-B, 44 differentials preserved). EBNF harness 42; differentials still 01–44 (async = interp-reference, no numbered codegen diff).
  - **T2 — DEFERRED:** `async fn` *invocation* (only reachable via `await <async_fn()>` = Future-await, by function coloring §9.3 — `async fn` parses, invocation fail-loud `callFn` l.1212), `Future`-await, `spawn`, `TaskHandle`.
  - **T3 — DEFERRED:** `race` / `sync` / `branch` (+ `spawn`) — reserved-unproduced (`non_s3_keywords`).
  - **Fail-loud bounds (interp the reference, never silently wrong):** `wait_unscaled` (real clock, out of E3); `entity_event` (the global EventStore models no per-entity source — §9-underspecified for M0.8); nested / value `await` (`let x = await …`, await in `if`/`loop`/`match` — the Phase-2 state machine); entity-bound / parameterised `async rule`; heap (non-POD) locals surviving a suspend (store-reset would corrupt them); `await` event-type not resolver-validated (unknown event = silent never-wakes).
  - **Async codegen = Phase 2** (`UnsupportedConstruct`, `emitRule` `is_async` guard + the `await_expr` else arm); **async = interp-reference** throughout (no interp↔codegen differential). Pushed to `origin` as the cross-session archive (not a merge). **Next: sub-slice C (Level-A codegen closure) via a dedicated Guy-relayed prompt (C spec set: abi-zig / stdlib / resolver-types) — NOT started here.** After C: D (7 debts) → Review E3 gate. Merge/tag = Guy at M0.8 close.

- 2026-06-09 — **E3 sub-slice C — GO ruled + tranche 1a (string type + literal + `.len()`) DELIVERED (`2519958`).** Guy's GO on the recon+STOP boundary: **(1) resource storage = Option A** — over-align the Tier-0 `ResourceStore` buffer to `ChunkAlignment` (16) so `@alignCast(*R)` is legal in ReleaseSafe and the codegen forms `*R` natively (ABI pointer-identity), interp+harness+diff intact; **GARDE-FOU: the over-alignment is UNCONDITIONAL** — every resource buffer aligned 16, not just codegen-accessed ones (two alignment regimes by access path would reopen an interp/codegen divergence). Options B (field-by-field) + C (singleton, breaks byte-exact) rejected. **(2) string depth = minimal faithful subset** — literal + `+` + interpolation + `.len()`, runtime string pool in BOTH backends, byte-exact; any §12 String method beyond the minimum → `UnsupportedConstruct` fail-loud, flagged stdlib Phase 1+ (Review E3). **(3) plan/order + frame-arena model confirmed** (not a question). Disciplines re-locked: byte-exact at the same logical point; generics-mono + async stay `UnsupportedConstruct` (NEVER closed M0.8); closures consume `returning` (return-in-closure exits the closure); journal-as-you-go; no `--no-verify`; STOP at Review E3.

  **Tranche 1a delivered (`2519958`, green Debug+ReleaseSafe+lint+fmt+hooks):** `string` is now a first-class expression type. **types.zig:** `BuiltinType.string_` (deliberately NOT in `fromName` → `string` stays rejected as a component/resource field type via `validateFieldsInDecl`, deferred to the Error/tranche-2 layer); `synthExpr` `.string_lit` → `.{ .builtin = .string_ }` (was `.unknown`); `synthMethodCall` builtin-string path — `len` → `int`, any other string method → `E0200` "not in the M0.8 minimal subset (stdlib Phase 1+)". **interp.zig:** `.method_call` switch gains a `.string_id` arm — `len` → byte length (`ast.strings.slice(sid).len`), else `RuntimeFailure`. **lower.zig:** `string_lit` emits `@as([]const u8, "<escaped>")` (new `emitZigStringLiteral` helper — escapes `"`/`\`/control bytes); `inferExprZigType` `.string_lit → "[]const u8"`; `method_call` emit gains a string-receiver branch (`.len` → `@as(i64, @intCast((<recv>).len))`, other → `UnsupportedConstruct`). **Differential `45_string_len`** (both facades): `Acc.out = "hello".len()` → 5, byte-exact (cooked verified line-for-line: `__row[0].out = @as(i64, @intCast((@as([]const u8, "hello")).len));`). Inline resolver test (accepts `.len()`, rejects other string methods). Differentials now **01–45**; no frame-arena change (literal `.len` needs no alloc — the arena lands with tranche 1b concat). **Next within tranche 1: 1b concat `+` + frame arena → 1c interpolation (lexer/parser, the heaviest).**

## Blockers encountered

- 2026-06-06 — **E1 foundations checkpoint — SESSION 1 CLOSE (5/8 verticals, all green & committed). RESUME MARKER for session 2.** This is a continuation of M0.8 on a 2nd Claude Code session — same milestone / branch (`phase-0/etch/full-grammar-v0.6`) / brief, not a new milestone and not a reactive split (M0.8 is the largest Phase 0 milestone; a clean cross-session continuation was the explicit call). Deciding factor: resuming the 3 heaviest tranches (collections / closures / error-union) on an already-saturated context risks post-compaction guessed grammar — declined. **Done in E1:** breaking interface + top-level recovery (`7bc84dd`); 5 S3 debts (`d4a9895` annot-field-access, `700ec91` trivia+doccomment, `2ccbdc3` annot-applicability, `98631c9` resource-receiver); 5 expression/control-flow foundations, each full-vertical (parse→resolve→interpret→codegen) + a differential program — cast `as` (`a566cc1`, diff 21), type-alias + recovery lockstep (`9671ab5`, diff 22), assert (`e2b8da3`, diff 23), match Maranget-lite/E1230 (`57a2619`, diff 24), ranges + for-in (`b16d434`, diff 25). `zig build test` green (exit 0; the 2 plugin_loader/events `failed command` lines are documented spurious noise — `macos-full-suite-flakiness`), `zig fmt --check` + weld-lint clean.

  **Remaining E1 — FIXED resume order (Guy-relayed):** collections → closures → loop/break-value → error-handling → EBNF harness → Review E1 tests. Per-tranche coupling + EBNF references:
  1. **collections `T[N]` / `T[]` / `Map` / `Set` / `Iterator`** — **first** (closures' main consumers are the Iterator methods; the entailed consumers **indexing `arr[i]` + slicing `arr[0..3]`** ship with it, cf. §Notes scope clarification). Heaviest tranche: a heap-managed dynamic array/map/set runtime + the lazy `Iterator<T>` API, plus indexing/slicing as postfix ops. EBNF: `etch-grammar.md` postfix_op `"[" expression "]"` (§425, slice = index with a range), array/map/set literal forms; `etch-reference-part1.md` §3.3 (composite types), §11 (full collection + Iterator API), §5.5 (POD rule — only `T[N]` allowed in components). New runtime: a heap value kind in `value.zig` + container codegen; consult `etch-stdlib.md` for the canonical method set.
  2. **closures `|a| e` / `|a| { block }`** — EBNF `etch-grammar.md` closure_expr (~§523); `etch-reference-part1.md` §6.13 + §5.6 (capture: value types copied; long-lived closures cannot capture ECS refs). Interp: closure value + captured environment; codegen: anonymous-fn / struct lowering. Consumed by Iterator `.map`/`.filter` → depends on collections.
  3. **loop / labeled / `break label value`** — EBNF `etch-grammar.md` loop_stmt (`[loop_label] "loop" block`), break_stmt §632 (`"break" [IDENT] [expression]`), continue_stmt §633; `etch-reference-part1.md` §7.5 + §7.7. Interp: control-flow propagation (break/continue as a signal through the statement executor — a small error set or a control enum) + label matching + loop-as-expression break-value typing. Codegen: Zig `while(true)` + labeled break. Conditional exit uses block-body `match` arms or break-as-expression (no `if`/`while` in E1) — may entail extending `match` arms to blocks.
  4. **error-handling `throws` / `throw` / `try` / `catch`** (assert already done) — `etch-reference-part1.md` §10.2; builtin `Error` struct + `ErrorCode` enum + error-union semantics. `throws` fn marker, `throw Error{...}`, `try { } catch err { }`.
  5. **EBNF harness** — `tests/etch/ebnf_examples_test.zig`: `@embedFile` blocks from `etch-grammar.md` (+ `etch-reference-part*` blocks tagged `<!-- ebnf-example -->`); ≥30 blocks parse without error (brief §E1 + §E7).
  6. **Review E1 tests** — foundations differential + recovery sync-point test + 1 test per debt (per-debt tests already exist), then STOP at the Review E1 gate and present the E1 diff. No E2.

  Specs to re-attach for session 2: `etch-grammar.md`, `etch-reference-part1.md`, `etch-resolver-types.md`, `etch-stdlib.md`. Proven per-construct pattern: lexer → token → AST slab + add-helper → parser → types → interp → codegen → inline tests + a differential program (numbered sidecar in `tests/etch_interp/programs/`, registered in `corpus_facade.zig` + `codegen_corpus_build.zig`). Anti-hallucination (M0.2.1 lesson): verify every new syntactic form is present in the attached EBNF before writing it; STOP and ask if a form is absent. Not the Review E1 gate (E1 incomplete). No code blocker.

- 2026-06-07 — **E2 SESSION CLOSE — block 1 (control-flow completion) done & committed. RESUME MARKER for the next E2 session.** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary, not the Review E2 gate (E2 incomplete: 1 of 5 internal blocks done). Matches the E1 session-1-close discipline: block 2 (call mechanism) + block 3 (declarations) are the heaviest tranches; resuming them on a saturated context risks post-compaction guessed grammar — declined, as in E1. **Done this session:** full reconnaissance of the delivered E1 surface, then E2 block 1 — block expressions (`4021dbe`, diff 32), if/else (`32fcfc8`, diff 33), while (`3cb9af3`, diff 34), match statements + E1 deferral closure (`7548a85`, diffs 35/36/37). All green Debug + ReleaseSafe + lint + fmt; per-tranche `feat(etch)` commits + `docs(brief)` journals. Differentials now run 01–37; next number is **38**. EBNF harness unchanged at 31 (in-repo corpus; spec docs not in repo).

  **Remaining E2 — FIXED internal order (launch-relayed). Per-block coupling + the call-mechanism sequencing analysis (the non-obvious part — read this first next session):**
  2. **Call mechanism** (`etch-resolver-types.md §5`, read it first). The 4-kind dispatch is **strict order**: inherent (`impl Type`) → trait (`impl Trait for Type`) → builtin (wired in resolver) → service (Tier 1, Level B / out of M0.8 core). **Key dependency:** kinds 1–2 need `impl` (block 3); free calls `f(args)` need `fn`. So block 2's testable vertical = **top-level `fn` + free-function calls**: extend the existing `fn_call` path (E1 closures) to a named top-level `fn` callee in resolve/interp/codegen, with a free-call differential (e.g. `fn double(x: int) -> int { x*2 }` + `double(5)` = 10). Postfix **method-call parsing** `recv.method(args)` produces the reserved `method_call` ExprKind (parser-testable in block 2); the **4-kind method dispatch** (resolve/interp/codegen) is exercised in block 3 once `impl` provides methods. `fn` grammar: `etch-grammar.md §5.3` (`[async] fn IDENT [generics] (params) [throws] [-> type] [where] block`); `async fn` parsed (E2), interpreted E3, codegen Phase 2; `throws` marker parsed here (the fn-marker `throws` deferred from E1 lands with `fn`). `return` keyword graduates with `fn` (still in `non_s3_keywords`). **LOCKSTEP — fires in block 2:** adding `kw_fn` to `parseTopLevel` (top-level `fn` is a top-level construct) means `kw_fn` joins the `recoverToTopLevel` stop-set IN THE SAME COMMIT, with a survival test (a broken construct followed by a valid top-level `fn` survives in the AST). The remaining declaration keywords (`kw_struct`/`kw_enum`/`kw_trait`/`kw_impl`) follow the same lockstep as they land in block 3.
  3. **Declaration layer** (`etch-grammar.md §5.3–5.9`, `etch-resolver-types.md §7` coherence): `fn` associated (impl methods), `struct`, `enum`, `trait`, `impl` (incl. `impl Trait for T when ...` conditional + orphan rule §7.4 + conditional-impl proof §7.3). Each vertical + byte-exact differential. **`recoverToTopLevel` lockstep:** `kw_fn` / `kw_struct` / `kw_enum` / `kw_trait` / `kw_impl` join `parseTopLevel` AND the `recoverToTopLevel` stop-set TOGETHER as each lands, each with a survival test (a broken construct followed by a valid `fn`/`struct`/… survives in the AST). This is where the 4-kind method dispatch (inherent/trait, §5.1–5.2) gets exercised. struct/enum bring struct literals + enum variants (and enable richer `match` patterns — but Phase-1 pattern set stays the flat fragment per E1).
  4. **Generics** (`etch-resolver-types.md §6`): parse `<T>` (`§2.4`) + resolve (bounds `E0601`, inference `E0603`/`E0604`, instance keying `§6.2`) + interp (the tree-walker monomorphises trivially at runtime). **CODEGEN MONOMORPHISATION DEFERRED Phase 2** (`§6.3` places it at HIR lowering, absent in M0.8) → the generics codegen path returns `CodegenError.UnsupportedConstruct` (fail loud, like async). **No codegen differential on generics.**
  5. **`Optional T?` + `Error`/`ErrorCode`** builtins (enum family). E2 LIVRE these primitives. **`if let` / `while let`** land here (they unwrap `T?`, reference §7.2/§10.1 — coupled to Optional, deferred from block 1). The **E1-deferred accessors** (`pop()→T?`, error-handling codegen) do **NOT** close here — they close in **E3** with these primitives (Review E1 ruling 1). E2 does not reopen the E1-deferred codegen.

  Specs to re-attach next session: `etch-grammar.md`, `etch-reference-part1.md`, `etch-reference-part2.md`, `etch-resolver-types.md`, `etch-abi-zig.md` (the launch set). The per-construct vertical pattern + the differential-registration sites (`corpus_facade.zig` + `codegen_corpus_build.zig`) are unchanged. Anti-hallucination: every syntactic form verified in the EBNF before writing (caught + fixed a missing match-arm comma this session by following the EBNF, not loosening the parser). **STOP at the Review E2 gate** (after blocks 2–5 are green) — present the full E2 diff for the Claude.ai review. Not reached yet.

- 2026-06-07 — **E2 SESSION CLOSE — block 2 (call mechanism) done & committed. RESUME MARKER for the next E2 session.** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary, not the Review E2 gate (E2 incomplete: 2 of 5 internal blocks done). Matches the E1 + E2-block-1 session-close discipline: block 3 (declarations: `struct`/`enum`/`trait`/`impl` + the 4-kind dispatch + coherence/orphan rules) is the single heaviest E2 tranche and needs fresh reading of `etch-grammar.md §5.7–5.9` + `etch-resolver-types.md §7`; resuming it on a context already saturated by 5 spec reads + a full 7-file reverse-engineering + 2 verticals risks post-compaction guessed grammar — declined, as in E1/block-1. **Done this session:** block-2 plan (`38cd03c`); tranche A — top-level `fn` + free calls + `return` + LOCKSTEP (`6e30e55`, diff 38) + its journal (`ec2ec91`); tranche B — method-call parsing (`e336878`). All green Debug + ReleaseSafe + test-codegen-diff + lint + fmt. Differentials now run 01–38; **next number is 39**. EBNF harness unchanged at 31. `recoverToTopLevel` stop-set now: S3 (`component`/`resource`/`rule`) + `type` + `fn` + `async`.

  **Remaining E2 — FIXED internal order (launch-relayed). Read the named spec section FIRST each block:**
  3. **Declaration layer** (`etch-grammar.md §5.7` struct / `§5.8` enum / `§5.9` trait+impl, `etch-resolver-types.md §7` coherence — read first): `struct`, `enum`, `trait`, `impl` (incl. `impl Type` inherent + `impl Trait for T [when ...]` conditional + orphan rule §7.4 + conditional-impl proof §7.3) + `fn` associated (impl methods, with `self`/`mut self` params §5.9). Each a full vertical + byte-exact differential. **This is where the 4-kind method dispatch (inherent §5.1 → trait §5.2 → builtin §5.3 → service §5.4) gets wired into `synthCall`/interp/codegen for `method_call`** (tranche B left it fail-loud). struct/enum bring struct literals (`T { f: v }` / `.{ ... }`) + enum variants + `.variant` shorthand (and enable richer `match` patterns — but the Phase-1 pattern set stays the flat fragment per E1). **LOCKSTEP:** `kw_struct` / `kw_enum` / `kw_trait` / `kw_impl` join `parseTopLevel` AND `recoverToTopLevel`'s stop-set TOGETHER as each lands, each with a survival test. (`kw_fn`/`kw_async` already in the stop-set from block 2.)
  4. **Generics** (`etch-resolver-types.md §6`): parse `<T>` (`§2.4` — the `<` block 2's `parseFnDecl` currently rejects) + resolve (bounds `E0601`, inference `E0603`/`E0604`, instance keying `§6.2`) + interp (the tree-walker monomorphises trivially at runtime). **CODEGEN MONOMORPHISATION DEFERRED Phase 2** (`§6.3` places it at HIR lowering, absent in M0.8) → the generics codegen path returns `CodegenError.UnsupportedConstruct` (fail loud, like async). **No codegen differential on generics.**
  5. **`Optional T?` + `Error`/`ErrorCode`** builtins (enum family). E2 ships these primitives. **`if let` / `while let`** land here (they unwrap `T?`, reference §7.2/§10.1 — coupled to Optional, deferred from block 1). The **E1-deferred / block-1-deferred codegen paths** (heap collections `T[]`/`Map`/`Set`, capturing closures, closure-block-body, error-handling, async fn, `throws` fn, resource-receiver `get(R)`) do **NOT** close here — they close in **E3** at the "Level A complete in codegen" gate (Review E1 ruling 1). E2 does not reopen them.

  Specs to re-attach next session: `etch-grammar.md`, `etch-reference-part1.md`, `etch-reference-part2.md`, `etch-resolver-types.md`, `etch-abi-zig.md` (the launch set). The per-construct vertical pattern (lexer → token → AST slab + add-helper → parser → types → interp → codegen → inline tests + a numbered differential under `tests/etch_interp/programs/`, registered in `tests/etch_interp/corpus_facade.zig` + `tests/etch_interp/codegen_corpus_build.zig`) is unchanged. Stale-fixture watch: graduating `struct`/`enum`/`trait`/`impl` will obsolete more `E0001_unsupported_*` corpus fixtures / lexer+parser "unsupported" tests (as `fn` did — repointed to `behavior`); repoint them to a still-out-of-scope keyword. Anti-hallucination (M0.2.1 lesson): every syntactic form verified in the attached EBNF before writing it; STOP and ask if a form is absent. **STOP at the Review E2 gate** (after blocks 3–5 are green) — present the full E2 diff for the Claude.ai review. Not reached yet.

- 2026-06-07 — **E2 SESSION CLOSE — block 3 tranche A (struct + inherent impl + dispatch) done & committed. RESUME MARKER for the next E2 session (block 3 tranches B + C).** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary (tranche A of block 3 done), NOT the Review E2 gate (E2 incomplete: block 3 is 1 of 3 tranches done; blocks 4–5 untouched). **Why stop after tranche A** (unlike block 2, which did 2 tranches in one session): block-2's tranche B was a tiny parse-only add; block-3's tranches B (`enum`) and C (`trait` + coherence) are each a *full vertical* (parse→resolve→interp→codegen + differential), and tranche C carries the deep `etch-resolver-types.md §7` coherence/orphan/conditional-impl-proof surface the launch brief explicitly flags as "read it, don't guess". Resuming the heavy §7 work on a context already saturated by 5 spec reads + a full codebase reverse-engineering + a complete tranche-A vertical risks post-compaction guessed grammar — the same call as the E1 session-1 close. **Done this session:** block-3 plan (`7912408`); tranche A feat (`be207ce`, struct decls + inherent `impl` + `self`/`mut self` methods + associated fns + struct literals + the 4-kind dispatch's inherent kind §5.1 wired into `synth`/interp/codegen for `method_call`, + the `no_struct_lit` head-context disambiguation, + LOCKSTEP `kw_struct`/`kw_impl`); tranche A journal (`de06a75`). All green Debug + ReleaseSafe + test-codegen-diff + lint + fmt. Differentials now run 01–39; **next number is 40**. EBNF harness now 32 clean (added a struct/impl block). `recoverToTopLevel` stop-set now: S3 (`component`/`resource`/`rule`) + `type` + `fn` + `async` + `struct` + `impl`.

  **Remaining block 3 — FIXED tranche order. Read the named spec section FIRST each tranche:**
  - **B — `enum` + variants + `.variant` shorthand + enum `match` + LOCKSTEP `kw_enum`** (`etch-grammar.md §5.8` enum_decl/enum_variant + §3.2 patterns l.510-511, `etch-reference-part2.md §6`, `etch-resolver-types.md §12.4` enum exhaustiveness — read first). **C-like enums full vertical:** `enum Difficulty { easy, normal, hard }` → Zig `enum`; the value forms `Difficulty.hard` (a `.path` receiver + `.field` postfix → resolve to an enum value) and `.hard` shorthand (currently a `tag_path` expr — type-driven resolution from the expected/scrutinee enum, `etch-resolver-types.md §3.5`); a new `ExprKind`/`Value` enum value (carry `(type_name, variant_index)`); a new `PatternKind.enum_variant` (extend `parsePattern` + `MatchArm` + `synthMatch` enum-exhaustiveness: all variants covered ⇒ exhaustive, else E1230) + interp match compare + codegen if-else chain on `== Type.variant`. **Differential 40** (e.g. `let d = Difficulty.hard; out = match d { Difficulty.easy => 1, Difficulty.normal => 2, Difficulty.hard => 3 }` → 3; enum is an intermediate local, the byte-exact diff is on the int `out` — avoids enum-in-component-field, which stays out of scope: component fields are builtin scalars). **Data-carrying variants** (`Physical { amount: float }` struct-like, `ok(T)` tuple-like): PARSE the shape so the grammar is accepted, but their *construction* + *destructuring patterns* are DEFERRED (destructuring is the post-Phase-1 advanced pattern set, `etch-reference-part1.md §7.6`) — fail-loud / flag. **Generic enums** → block 4 (`<` rejected, as struct does). **LOCKSTEP:** `kw_enum` joins `parseTopLevel` AND `recoverToTopLevel` together + survival test. Stale-fixture watch: graduating `enum` obsoletes the lexer test `"fn enum behavior"` (line ~413, `enum` currently asserted `error_unknown_keyword`) — repoint to a still-out-of-scope keyword (e.g. `trait` until tranche C, then `behavior`).
  - **C — `trait` + `impl Trait for T [when …]` + trait dispatch (§5.2) + coherence/orphan (§7.4) + conditional-impl proof (§7.3) + LOCKSTEP `kw_trait`** (`etch-grammar.md §5.9` trait_decl/impl_trait_for_type, `etch-reference-part2.md §7-8`, `etch-resolver-types.md §5.2`/`§7.1-7.5` — READ §7 IN FULL, do not guess the coherence resolution). Extend `parseImplDecl` to accept the `impl Trait for Type` form (tranche A rejects `for` with a pointer — flip that to parse it, storing `trait_name`); `trait_decl` with abstract + default-bodied methods (`trait_member = function_signature | function_decl`); wire the trait kind (§5.2) into `synthMethodCall` AFTER inherent (§5.5 strict order inherent→trait→builtin→service); orphan rule §7.4 (Trait OR Type local to the impl's module — single-module in M0.8, so the check is mostly structural); conditional-impl proof §7.3 (`impl Trait for Entity when self has Health` — provable from the calling rule's `when`; E0215 if not). **Differential 41** on a trait method called through a non-generic path. **LOCKSTEP** `kw_trait`. This is the heaviest tranche — give it a fresh context.

  After block 3: **block 4 — generics** (parse `<T>` + resolve bounds + interp monomorphises trivially; codegen `UnsupportedConstruct`) → **block 5 — `Optional T?` + `Error`/`ErrorCode` + `if let`/`while let`** → STOP at the Review E2 gate. Specs to re-attach: `etch-grammar.md`, `etch-reference-part1.md`, `etch-reference-part2.md`, `etch-resolver-types.md`, `etch-abi-zig.md`. The per-construct vertical pattern + differential-registration sites are unchanged. **Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud** (never silently-wrong Zig). Anti-hallucination: verify every form in the EBNF before writing; STOP + ask if absent. Not the Review E2 gate.

- 2026-06-08 — **E2 SESSION CLOSE — block 3 COMPLETE (tranches A struct + B enum + C trait). RESUME MARKER for the next E2 session (blocks 4 + 5).** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary (the full declaration layer + 4-kind dispatch is done), NOT the Review E2 gate (E2 incomplete: blocks 4–5 untouched). **Done this session:** tranche B — enum vertical (`d2b47e6` feat, `7923f55` journal, diff 40); tranche C — trait/impl vertical (`9c48bc2` feat, diff 41) + this journal. Both tranches full verticals (lexer→token→ast→value→parser→resolver→interp→codegen + a differential + inline tests), each with its LOCKSTEP keyword + survival test. All green Debug + ReleaseSafe + test-codegen-diff + lint + fmt. Differentials now run 01–41; **next number is 42**. EBNF harness now **34** clean. `recoverToTopLevel` stop-set now: S3 (`component`/`resource`/`rule`) + `type` + `fn` + `async` + `struct` + `impl` + `enum` + `trait`. **The launch's must-close validation `E0220` is CLOSED** (resolver flags a `mut self` call on an immutable receiver — inherent + trait).

  **Remaining E2 — FIXED internal order. Read the named spec section FIRST each block:**
  4. **Generics** (`etch-resolver-types.md §6` — read first): parse `<T>` generic params (`§2.4`; block-2/3 currently *reject* a `<` on fn/struct/enum/trait/impl — flip those to parse the param list) + resolve (bounds `E0601`, inference `E0603`/`E0604`, instance keying `§6.2`) + interp (the tree-walker monomorphises trivially at runtime). **CODEGEN MONOMORPHISATION DEFERRED Phase 2** (`§6.3` places it at HIR lowering, absent in M0.8) → the generics codegen path returns `CodegenError.UnsupportedConstruct` (fail loud, like async). **No codegen differential on generics** (interp-reference only). Generic enums/structs/traits/impls all unlock here.
  5. **`Optional T?` + `Error`/`ErrorCode`** builtins (the enum family — reuse tranche-B enum machinery) + **`if let` / `while let`** (unwrap `T?`, `etch-reference-part1.md §7.2`/§10.1; deferred from block 1). The **E1-/block-1-deferred codegen paths** (heap collections `T[]`/`Map`/`Set`, capturing closures, closure-block-body, error-handling, async fn, `throws` fn, resource-receiver `get(R)`, `mut self` method codegen, anonymous `.{}` struct literal, enum shorthand-in-value-position) do **NOT** close here — they close in **E3** at the "Level A complete in codegen" gate (Review E1 ruling 1). E2 does not reopen them.

  Specs to re-attach next session: `etch-grammar.md`, `etch-reference-part1.md`, `etch-reference-part2.md`, `etch-resolver-types.md`, `etch-abi-zig.md` (the launch set). The per-construct vertical pattern + the differential-registration sites (`corpus_facade.zig` + `codegen_corpus_build.zig`) are unchanged. **Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud** (never silently-wrong Zig). Anti-hallucination (M0.2.1 lesson): every syntactic form verified in the attached EBNF before writing it; STOP + ask if a form is absent. **STOP at the Review E2 gate** (after blocks 4–5 are green) — present the full E2 diff for the Claude.ai review. Not reached yet.

- 2026-06-08 — **E2 SESSION CLOSE — block 4 (generics) done & committed. RESUME MARKER for the next E2 session (block 5 — the LAST before the Review E2 gate).** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary (block 4 done), NOT the gate (block 5 untouched). **Done this session:** block-4 generics — full parse + resolve (inference E0603/E0604 + bounds E0601) + type-erased interp + codegen fail-loud (`5d66520` feat) + this journal. All green Debug + ReleaseSafe + test-codegen-diff + lint + fmt. Differentials still 01–41 (generics have **no codegen differential** by design — interp-reference only); EBNF harness now **35** clean. `recoverToTopLevel` stop-set unchanged (generics add no top-level keyword). One session decision relayed by Guy and recorded: **EBNF-strict, inference-only** for generic type args (no `f<int>(x)` turbofish — absent from the EBNF; type args inferred per §6.4); flagged for KB reconciliation along with the `impl<T> Range<T>` inherent-target illustration.

  **Remaining E2 — block 5 (the last). Read the named spec section FIRST:**
  5. **`Optional T?` + `Error`/`ErrorCode` + `if let` / `while let`** (`etch-reference-part1.md §3.3`/§7.2/§7.4/§10.1, `etch-grammar.md` §3.2 `if_expr`/`while`-let forms + `optional` type node §256, `etch-resolver-types.md §3.5` none/some — read first). Ship the **`Optional<T>` builtin enum family** (`T?` sugar, `none` / `some(T)` — reuse the tranche-B enum value machinery: a new `Value` optional + `ExprKind` `none_lit`/`some_lit` are reserved already) + the **`Error` struct / `ErrorCode` enum** builtins. Wire **`if let x = opt { … } else { … }`** and **`while let x = it.next() { … }`** (unwrap an `Optional`, bind `x` to the payload in the then/body scope; `etch-reference-part1.md §7.2`/§7.4 — the forms were deferred from block 1). The **E1-/block-1-deferred codegen paths close in E3** (Review E1 ruling 1) — E2 does NOT reopen them; the only Optional-returning accessor to wire is whatever §11 collection method block 5 needs for `while let` (e.g. `pop()`/`next()` as the interp reference — codegen stays deferred). **Differential 42** on a sound Optional path (e.g. `if let` on a `some`/`none` intermediate driving an int field), byte-exact where the codegen path is sound; the heap/closure/error-handling deferrals stay interp-reference. After block 5 green: **STOP at the Review E2 gate** — present the full E2 diff (blocks 1–5) for the Claude.ai review. Specs to re-attach: the launch set. Anti-hallucination: verify every form in the EBNF before writing; STOP + ask if absent. **Codegen-deferred = `UnsupportedConstruct` fail loud.**

- 2026-06-08 — **REVIEW E2 GATE REACHED — E2 (type-declaration layer) COMPLETE. STOP for the Claude.ai review.** All five internal blocks landed across sessions on `phase-0/etch/full-grammar-v0.6`, each a full vertical (lexer→token→AST→parser→resolver→interp→codegen) with inline tests + a numbered byte-exact differential (or interp-reference where codegen is deferred by doctrine), LOCKSTEP `recoverToTopLevel` per new top-level keyword + survival test, Conventional Commits, green on every commit. **Blocks:** (1) control-flow completion — block-expr / if-else / while / match-stmt (diffs 32–37); (2) call mechanism — top-level `fn` + free calls + method-call parsing (diff 38); (3) declarations — `struct` + inherent `impl` (diff 39), `enum` + variants + match (diff 40), `trait` + `impl Trait for T` + 4-kind dispatch §5.5 + coherence/orphan §7.4 + conditional-impl proof §7.3 + E0220 (diff 41); (4) generics — params/bounds/where + `generic_type`, inference E0603/E0604 + bounds E0601, type-erased interp, codegen `UnsupportedConstruct` (no codegen diff by doctrine); §891 alignment (generic inherent impl target); (5) Optional `T?` + none/some + `if let` / `while let` (diff 42). **Differentials 01–42; EBNF harness 37 clean.** `recoverToTopLevel` stop-set: S3 + `type`/`fn`/`async`/`struct`/`impl`/`enum`/`trait`. All green Debug + ReleaseSafe + test-codegen-diff + lint + fmt + pre-push hooks. **Codegen deferrals (all fail-loud `UnsupportedConstruct`, interp is the reference, close at the E3 "Level A complete in codegen" gate):** generics monomorphisation (Phase 2), async fn, `throws` fn + throw/try/catch, `mut self` method, capturing / block-body closures, heap collections `T[]`/Map/Set, resource-receiver `get(R)`, anonymous `.{}` struct literal, enum/optional non-scalar payloads, component/resource instance-method dispatch in interp. **Scope decisions for review:** `Error`/`ErrorCode` builtins deferred to E3 (the error-handling type layer, string/recursion-blocked, inert without E3 codegen — a flagged reduction vs the block-5 marker); EBNF-strict inference-only generics (no fn-call turbofish — KB §6.4 patched to match); §3.7 generic-impl-target reconciled by the §891 patch; `?.`/`??`/`!` + Optional match patterns + Optional-returning accessors deferred. **E3 not started** — awaiting the Review E2 verdict (relayed by Guy) before opening it.

- 2026-06-08 — **E3 sub-slice A — SPEC GAP flagged: the rule/filter spec `etch-resolver-types.md §12` is not attached, blocking the `@on_event` observer + the `Changed<T>` filter (NOT a code blocker for event-producer + tags).** Anti-hallucination stop: two sub-slice-A items are under-specified by the 4 attached docs and need `etch-resolver-types.md §12` (the brief cites "`etch-validation-ecs.md §12`" for the filters, but §12 of the attached doc is `ability`; the real rule/when/filter detail spec is `etch-resolver-types.md §12`, cross-referenced by `etch-validation-ecs.md §7 — NOT attached):
  1. **Component `Changed<T>` filter surface + semantics.** `etch-grammar.md §6 ecs_condition` shows only `resource TYPE_IDENT changed` — no component-`changed` form. The only verbatim example is `engine-ecs-internals.md §5`: `when entity has Health and Health changed` (a bare `TYPE_IDENT changed` conjunct). The exact Etch surface (`Health changed` vs `entity has Health changed`), its archetype-vs-per-entity-tick gating, and the E12xx codes live in resolver-types §12. Also a Tier-0 implication: `comptime_query` does not expose `change_detection` (the tick infra exists but is unused by queries) — wiring `Changed` into the comptime query is a Tier-0 change that should be confirmed before implementing. **Will not guess the surface.**
  2. **`@on_event` observer binding (the event consumer).** The event-handler rule's payload binding is under-specified. Worse, there is a **spec inconsistency**: `event` is a reserved keyword (`etch-grammar.md §1.3`) → graduated to `kw_event` (per the LOCKSTEP directive), but `etch-validation-ecs.md §7.1 E1203` describes the event-based rule param as `event: T`, which cannot be a `regular_param` (`IDENT ":" type`) if `event` is a keyword. The binding form (param named `event` as a contextual keyword? bound by type? a different name?) needs a ruling. Without the observer there is no byte-exact event differential (an `emit` with no consumer has no observable world-state effect).

  **Request:** re-attach `etch-resolver-types.md §12` (rule signatures / when / filters / ECS access — the section the brief points to) for the observer + `Changed<T>` tranche, and resolve the `event`-keyword vs `event:`-param inconsistency. **Not blocking the start:** `event` declaration + `emit` (producer) are landed (`67d0959`); the `tags` vertical (decl + bitfield `TagSet` + tag-op when-conditions + `add_tag`/`remove_tag` deferred mutations) is fully specified by the attached docs and proceeds next. The observer + `Changed<T>` filter close once §12 is attached. No code is blocked for the in-flight work.

- 2026-06-08 — **E3 SESSION CLOSE — sub-slice A: event *producer* done & committed. RESUME MARKER for the next E3 session (tags vertical + the §12-gated observer + `Changed<T>`).** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary, NOT the Review E3 gate (E3 is far from complete: tags + observer + filters + async interp + Level-A codegen closure + 7 debts remain). **Done this session:** reprise + sub-slice-A scoping + the event producer vertical (`67d0959` feat, `5867efa` journal) + the §12 spec-gap blocker flagged above. All green Debug + ReleaseSafe + lint + fmt; the event codegen was Sema-verified by a manual cook + `zig build-obj`. Differentials still 01–42 (event has no differential yet — observer-gated); EBNF harness **38** clean; `recoverToTopLevel` stop-set now: S3 + `type`/`fn`/`async`/`struct`/`impl`/`enum`/`trait` + **`event`**.

  **Why stop here** (matches the E1/E2 session-close discipline): the `tags` vertical is a heavy, grammar-sensitive tranche (hierarchical recursive parse, bitfield encoding + global table, 5 tag operators, **deferred structural mutations needing a Tier-0 command-buffer addition**, 5 new diagnostics) — exactly the kind E1/E2 declined to start on a context already saturated by 4 spec reads + a full reverse-engineering + a completed vertical (post-compaction guessed-grammar risk). And §12 already forces a session boundary for the observer + `Changed<T>` (~half of sub-slice A). The event producer is a committed, green, tested, Sema-verified boundary.

  **RESUME — next E3 session, FIXED order. Read the named EBNF FIRST, verify every form before writing (anti-hallucination):**
  1. **`tags` vertical** (fully specified by the attached docs — no §12 dependency). EBNF: `tags_decl = "tags" "{" {tag_namespace} "}"` (§5.11 l.914); `tag_namespace = IDENT "{" tag_body "}"`; `tag_body = tag_leaf {"," tag_leaf} [","] | tag_namespace {tag_namespace}` (homogeneous — leaves OR sub-namespaces, never mixed; disambiguate by lookahead: after a namespace `{`, `IDENT {` → sub-namespace, `IDENT ,`/`IDENT }` → leaf); `TAG_PATH = "." IDENT {"." IDENT}` (§1.3 l.99, the reserved `tag_path` ExprKind); `tag_op = has_tag | has_no_tag | has_any_tag | has_all_tags | has_no_tags`, `tag_operand = TAG_PATH | "[" TAG_PATH {"," TAG_PATH} "]"` (§3.2 l.531-534); `ecs_condition |= expression tag_op tag_operand` (§6 l.945); `tag_mutation_stmt = expression "." ("add_tag"|"remove_tag") "(" (TAG_PATH | TYPE_IDENT) ")"` (§4.4 l.697-698, `TYPE_IDENT` = a whole category/namespace). Global table (`etch-validation-ecs.md §5.2`): merge all `tags {}` blocks → hierarchy → `bit_index` per leaf, depth-first + declaration order, stable cross-build; `TagSet = struct { bits: [TAG_BITFIELD_WORDS]u64 }` (§5.3), `WORDS = (total+63)/64`, default max 256; `has_any_tag(.namespace)` accepts a category → a mask of all leaves under it (§5.4). Diagnostics: **E0830** TagPathInvalid, **E0831** TagPathConflict (leaf vs namespace), **E0832** TagBitfieldOverflow, **E0833** TagInvalidOperation (tag op on non-Entity). **LOCKSTEP:** `kw_tags` joins `parseTopLevel` AND `recoverToTopLevel`'s stop-set in the delivering commit + a survival test (the tag-op / `add_tag` / `remove_tag` keywords are statement/expression keywords, not top-level — only `kw_tags` needs the stop-set). **Mutations:** `add_tag`/`remove_tag` are deferred structural changes at the tick boundary via the ECS deferred commands (NO structural mutation mid-archetype-walk) — interp: a tag-mutation queue flushed after `stepOnce` / at `world.tickBoundary()`; codegen: a Tier-0 `command_buffer` tag-set/clear command (a small additive Tier-0 change, in-scope "only as required"). **TagSet** is registered in both backends (interp dynamic store + codegen `extern struct` + registry). **Differential 43** (next number): a rule `add_tag`s (deferred), a later-tick rule `has_tag`-queries + mutates a counter → byte-exact. Stale-fixture watch: graduating `tags`/`has_tag`/`has_no_tag`/`has_any_tag`/`has_all_tags`/`has_no_tags`/`add_tag`/`remove_tag` — re-grep the corpus/tests for any identifier collision (none found for `event`/`emit`).
  2. **`@on_event` observer + `Changed<T>` filter** (design unblocked by Guy's rulings — see the "§12 spec-gap blocker RESOLVED" entry above; `etch-resolver-types.md §12` is still re-attached for the `Changed` surface + the E12xx codes + the E1203 type-check details). **Observer:** the event is an **implicit `event` binding** in the observer body (self-style — reuse the block-3 `callMethod` self-injection; `event` has the `@on_event(T)` type `T`, no declared param). The observer is what gives the event vertical its byte-exact differential (next number **43+**). **Changed:** `With`/`Without` already exist (`has`/`not has`); component-`Changed<T>` lowers to `chunk.changed_ticks[T][i] > last_run_tick` (`engine-ecs-internals.md §5`) — needs `comptime_query` to expose change-detection AND the dynamic interp to track a per-component `changed_tick` (Tier-0, in-scope per brief E3).

  **Specs to re-attach next session:** `etch-grammar.md`, `etch-validation-ecs.md`, `engine-ecs-internals.md`, `etch-abi-zig.md` (the sub-slice-A set, for tags) **+ `etch-resolver-types.md §12`** (for the observer + `Changed<T>`). The per-construct vertical pattern (lexer → token → AST slab + add-helper → parser → resolver → interp → codegen → inline tests + a numbered differential under `tests/etch_interp/programs/`, registered in `corpus_facade.zig` + `codegen_corpus_build.zig`) is unchanged. **Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud.** Anti-hallucination: every form verified in the EBNF before writing; STOP + ask if absent. NOT the Review E3 gate.

- 2026-06-09 — **E3 SESSION CLOSE — sub-slice A: `tags` parse + global tag table done & committed (`6343276`, `c3f9d97`). RESUME MARKER for the next E3 session.** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary (the tag-declaration front + the deterministic global table are complete, green, tested), NOT the Review E3 gate (sub-slice A's query ops + mutations + observer + `Changed<T>` remain, then sub-slices B/C/D). **Why stop here** (E1/E2 session-close discipline): the next unit — tag query operators **and** mutations — is a single heavy, grammar-sensitive, semantically-rich vertical (see below) that the journal repeatedly declines to start on a context already loaded by deep reconnaissance + two landed verticals (post-compaction guessed-grammar risk). Parse + table is a committed, green, Sema-clean boundary; `tag_table` is already on the `TypeChecker` waiting for the use sites.

  **RESUME — next E3 session, FIXED order. Re-attach + re-verify every EBNF form before writing (anti-hallucination); the authoritative PATCHED spec set is `Downloads/M0.8-full-grammar-v0.6/`, NOT the stale in-repo knowledge-base copy.**

  1. **Tag query operators + mutations = ONE coupled vertical (full stack), differential 43.** They are coupled: no Etch source can populate a `TagSet` without `add_tag`, so the byte-exact differential ("a rule `add_tag`s deferred at tick N; a tick-N+1 rule `has_tag`-queries and mutates a counter") needs BOTH. Do them together.
     - **Parse.** Graduate the 5 query keywords `has_tag`/`has_no_tag`/`has_any_tag`/`has_all_tags`/`has_no_tags` (infix ops) + `add_tag`/`remove_tag` (post-`.`), each from `non_s3_keywords` to real `kw_*` tokens (stale-fixture re-grep first — none expected). Produce the reserved `tag_path` ExprKind (`TAG_PATH = "." IDENT {"." IDENT}`, §3.2 l.460/§1.3 l.99). Represent the infix `tag_expr = expression tag_op tag_operand` (§3.2 l.531-535): it appears BOTH in general expression position (a Pratt infix; `tag_op` binds like `has`) AND in `ecs_condition` (when-clause, §6 l.945) — add a `WhenNodeKind` tag-filter branched in `parseWhenPrimary` AFTER the entity ident (mirrors the `has` leaf, parser.zig ~l.1422-1427). `tag_operand` = single `TAG_PATH` | `"[" TAG_PATH {"," TAG_PATH} "]"` | `TYPE_IDENT` (category). `tag_mutation_stmt = expression "." ("add_tag"|"remove_tag") "(" (TAG_PATH | TYPE_IDENT) ")"` (§4.4 l.697-698) is a dedicated statement (StmtKind `tag_mutation_stmt` reserved) — handle `.` `kw_add_tag`/`kw_remove_tag` `(` in `continuePostfix` (they are keywords now, NOT idents → not a `method_call`), or detect at statement head. **LOCKSTEP: these are body/expression keywords, NOT top-level → `recoverToTopLevel` UNCHANGED** (only `kw_tags` needed the stop-set, already done in `6343276`).
     - **Resolve.** Resolve each `TAG_PATH` against the already-built `tag_table` (on `TypeChecker`): **E0830 TagPathInvalid** if unknown; `has_tag`/`has_no_tag` require a single leaf; `has_any_tag`/`has_all_tags`/`has_no_tags` accept a leaf-list OR a category namespace (→ `collectUnder` mask). **E0833 TagInvalidOperation** if the receiver is non-Entity. Add E0830/E0833 to the catalogue (E0831/E0832 already added in `c3f9d97`).
     - **SEMANTIC DESIGN — confirmed, implement (NOT a surface-decision; that is reserved for `Changed<T>`).** `TagSet` is "a builtin component on each entity having tags" (`etch-validation-ecs.md` §5.1/§5.3, `bits: [words]u64`). So `entity has_tag .x` = entity has `TagSet` AND bit `bit_index(.x)` set (false if no `TagSet`). **`add_tag` on an entity lacking `TagSet` ADDS the `TagSet` component (archetype transition) then sets the bit — THIS is precisely why mutations are "deferred structural" (§4.4).** interp: a tag-mutation queue on the `Interpreter`, flushed after `stepOnce` / at `world.tickBoundary()` (l.915), NO mutation mid-archetype-walk; `TagSet` is a dynamic component via the ECS bridge. codegen: emit `TagSet` as an `extern struct { bits: [N]u64 }` (N = `tag_table.words()`) + register it; `has_tag` → `(tagset.bits[w] & mask) != 0`; `add_tag`/`remove_tag` → a deferred `command_buffer` tag op. **The command_buffer tag op is the in-scope additive Tier-0 touch the marker pre-decided ("small additive"):** `CommandKind {spawn,despawn,add_component,remove_component}` has no field/bit mutation, but `World` exposes `dynamicLocation` + `dynamicArchetype` (mutable archetype byte access) + `addComponentDynamic`, so a new `set_tag`/`clear_tag` command (apply: ensure `TagSet` via `addComponentDynamic` if absent, then RMW the bit in the archetype bytes) is genuinely additive — confirm the archetype mutable-byte accessor before writing, but do NOT treat this as the surface-decision (that is `Changed<T>` only).
     - **Differential 43** (next number; register in `corpus_facade.zig` + `codegen_corpus_build.zig`) + EBNF harness +1 (a tags-with-ops block). Codegen-deferred (if any heap/path edge needs out-of-path infra) = `UnsupportedConstruct` fail loud, interp the reference.
  2. **`@on_event` observer** (design unblocked, see the "§12 spec-gap blocker RESOLVED" entry above): the event is an **implicit `event` binding** in the observer body, self-style — reuse the block-3 `callMethod` self-injection; `event` carries the `@on_event(T)` type `T`, NO declared param; **E1203 OnEventTypeMismatch** is a type-coherence check. Closes the event vertical's byte-exact differential (producer landed `67d0959`). `etch-resolver-types.md §12` re-attached for the E1203 details.
  3. **`Changed<T>` filter — STOP + SURFACE the Tier-0 structural decision (Cas 2 → Claude.ai) BEFORE implementing.** The mechanism is known (`chunk.changed_ticks[T][i] > system.last_run_tick`, `get_mut` increments `changed_tick`, `engine-ecs-internals.md` §5 l.527-600) but the STRUCTURE is not pre-decided: where the per-rule `last_run_tick` lives interp-side; how `comptime_query` (`src/core/ecs/comptime_query.zig`) exposes the chunk change-ticks to codegen (currently unused by queries); how the dynamic interp tracks a per-component `changed_tick`. `etch-resolver-types.md §12` re-attached for the `Changed<T>` Etch surface + E12xx codes. **Do NOT guess the kernel structure.**

  After sub-slice A: B (async interpretation, no codegen) → C (Level-A codegen closure of the E1/E2 deferrals — heap collections / string / Error+error-handling / Optional ops / mut-self / closures / resource-receiver / `.{}` — carries the allocator + aligned-resource-storage decisions, to surface) → D (the 7 debts) → STOP at the Review E3 gate. Close the session at a coherent boundary if context saturates. **Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud. Merge/tag = Guy.**

- 2026-06-09 — **E3 SESSION CLOSE — sub-slice A: tag query operators (parse + resolve) done & committed (`658351e`). RESUME MARKER for the next E3 session.** Specs re-attached this session resolved the earlier §12 gap; the surface details are now reconciled (infix ops, E1212 when-context code, operator arity — see the journal entry above). Same milestone / branch / brief; a clean cross-session continuation, NOT the Review E3 gate. **Tags progress so far:** declaration parse (`6343276`) + global table (`c3f9d97`) + query-operator parse/resolve in `when` clauses (`658351e`). The query ops resolve (E1212) but DO NOT execute yet (interp/codegen fail loud on `.tag_filter`).

  **RESUME — next E3 session, FIXED order. The two prior RESUME-MARKER design notes (TagSet-on-entity semantics, `add_tag` = deferred structural mutation, the additive `command_buffer` tag op, the Pratt/`continuePostfix` sites, E0830/E0833) STILL HOLD — re-read them above before writing.**
  1. **Tag mutations + the tag RUNTIME (interp + codegen) + differential 43 — the tag-execution unit.** This is the coupled vertical that makes tags run end-to-end (queries currently fail loud). Steps: (a) **mutations parse + resolve** — graduate `add_tag`/`remove_tag` to `kw_*`; parse `tag_mutation_stmt = expression "." ("add_tag"|"remove_tag") "(" (TAG_PATH | TYPE_IDENT) ")"` (§4.4 l.697-698) — `.kw_add_tag`/`.kw_remove_tag` after `.` need handling in `continuePostfix` (they are keywords, not idents → not a `method_call`); produce the reserved `tag_mutation_stmt` StmtKind; resolve path validity (E0830) + E0833 non-Entity. (b) **interp runtime** — a dynamic `TagSet` component in the ECS bridge; `has_tag` (the `.tag_filter` when-node, replace its fail-loud) reads the bit; `add_tag`/`remove_tag` enqueue a deferred mutation flushed at `world.tickBoundary()` (l.915) — ensure `TagSet` (add the component if absent, structural) then set/clear the bit; NO mutation mid-archetype-walk. (c) **codegen runtime** — emit `TagSet` as `extern struct { bits: [tag_table.words()]u64 }` + register; `has_tag` → bit test; `add_tag`/`remove_tag` → the **additive `command_buffer` tag op** (`CommandKind` is `{spawn,despawn,add_component,remove_component}` today — add a `set_tag`/`clear_tag` command whose apply ensures `TagSet` via `addComponentDynamic` then RMWs the bit through `dynamicLocation`/`dynamicArchetype`. **VALIDATED additive — definitive ruling, Guy 2026-06-09 (do not revisit):** `set_tag`/`clear_tag` sits beside `add_component`/`remove_component`, same deferred-command mechanism, same tick-boundary flush, forces NO refactor → **implement it directly, NO STOP, no separate Review validation**; mention it in the Review E3 diff as a *delivered fact*, not a pending decision. The ONLY STOP+surface in sub-slice A is `Changed<T>` (item 3, new kernel mechanics)). (d) **differential 43** — `add_tag` deferred at tick N, a tick-N+1 rule `has_tag`-queries + mutates a counter → byte-exact interp↔codegen (register in both facades) + EBNF harness +1. **Also flagged-deferred:** the general-expression infix `tag_expr` (bare `TAG_PATH` primary) + the bare-`TYPE_IDENT` category operand (decide at Review E3 whether to add or leave deferred).
  2. **`@on_event` observer** (implicit `event` binding self-style — see item 2 of the prior marker, unchanged). Closes the event differential.
  3. **`Changed<T>` filter — STOP + SURFACE the Tier-0 structural decision (Cas 2) BEFORE implementing** (see item 3 of the prior marker, unchanged). `etch-resolver-types.md §12` for the surface + E12xx.

  **Specs to re-attach:** the sub-slice-A set (`etch-grammar.md`, `etch-validation-ecs.md`, `engine-ecs-internals.md`, `etch-abi-zig.md`) **+ `etch-resolver-types.md §12`** (observer + `Changed<T>`). Per-construct vertical pattern + differential facades unchanged. **Codegen-deferred = `UnsupportedConstruct` fail loud.** Anti-hallucination: verify every form in the EBNF before writing; STOP + ask if absent. NOT the Review E3 gate.

- 2026-06-09 — **E3 SESSION CLOSE — tag-execution: parse+resolve + interp runtime + Tier-0 deferred command done & committed (`244e283`, `1b31b98`, `e3b3586`). RESUME MARKER for the next E3 session (codegen runtime + differential 43, then observer, then `Changed<T>` STOP).** Same milestone / branch / brief — a clean cross-session continuation at a coherent, green boundary, NOT the Review E3 gate. **Why stop here** (the locked E1/E2/E3 session-close discipline): tag execution is COMPLETE in the interpreter (the reference backend) and the Tier-0 deferred command mechanism is in place + tested; what remains is **one heavy, codegen-sensitive vertical** (`tag-execution` item 1(c): TagSet emission + arch-walk tag filters with entity-id materialization + a `tick`-signature change to thread a gpa for the CommandBuffer + the differential). The reconnaissance found two structural facts that make this exactly the "don't start on a saturated context" tranche — they are recorded below so the next session executes without re-discovery.

  **RESUME — next E3 session, FIXED order. Re-verify every EBNF form before writing (anti-hallucination); the authoritative PATCHED spec set is `Downloads/M0.8-full-grammar-v0.6/`.**

  1. **Tag CODEGEN runtime + differential 43 — closes the tag-execution unit.** Codegen still fails loud on `.tag_filter` (4 sites: `emitArchPredicate` l.1001, `walkWhen` l.2447, `collectComponents` l.2502, all in `zig_codegen/lower.zig`; the interp `lowerWhen` one is already replaced) and on `.tag_mutation_stmt` (`emitStmt` `else`). Design, fully worked out + code-verified this session:
     - **TWO structural facts (verified, do not re-derive):** (a) **`comptime_query.Row` exposes only component pointers — NO entity id** (`src/core/ecs/comptime_query.zig` l.52-56). So a tag **mutation** (which needs the entity id for `cmd.setTag`) CANNOT use the comptime-query path → route any rule with a tag mutation through the **arch-walk path** (`emitRuleAsArchWalk`), where `arch.entityIdsConst(chunk)[slot]` is available; emit `const __entity = arch.entityIdsConst(chunk)[slot];` in the slot loop and resolve the body's `entity` ident to it. (b) **The generated `tick(world)` has NO allocator** (l.2342), but a `CommandBuffer` needs `gpa`. → change the generated `tick(world)` → `tick(world, gpa)` AND the differential-harness `Runner.step(self, world)` contract → `step(self, gpa, world)`: `tests/etch_interp/diff_runner.zig` l.134 (`runner.step(gpa, &world)`), `runner_codegen.zig` l.46 (`self.program.tick(world, gpa)`), `runner_interp.zig` l.64 (ignore gpa), plus the two other `tick` callers `src/demo_etch_codegen.zig` l.37 and any `synth_100`/bench caller (re-grep `\.tick(`). `register(world, gpa)` already takes gpa — `tick(world, gpa)` is symmetric.
     - **ALLOCATOR CONTRACT (Guy 2026-06-09, load-bearing — engraved): `tick(world, gpa)` is THE allocator-threading model for ALL of sub-slice C** (heap collections `T[]`/`Map`/`Set`, `string`, `Error` — every generated construct that allocates), not just the tag command buffer. Uniform explicit threading, **no global-allocator magic**. Freezing `tick(world, gpa)` here freezes the C contract — so when implementing it, **confirm the NATURE of the threaded gpa**: it must behave as a **FRAME/TICK allocator reset at the tick boundary**, NOT a persistent gpa that leaks — transient per-tick collection/string allocations (sub-slice C) must be freed at the boundary. **For TAGS this is moot** (the `CommandBuffer` owns + frees its arena each tick via `defer cmd.deinit()` + `flush`'s reset, and `applyTagMutation`'s add-buf is `defer gpa.free`'d), so any gpa is leak-free for the tag codegen → **no STOP for item 1(c)**; thread a plain gpa, the tag path self-manages. But the frame-scoped-vs-persistent choice is the **first half of the sub-slice-C allocator decision, surfaced early via the command_buffer**: when C's heap-allocating constructs land, if establishing the frame-scoped nature is non-trivial → **STOP+surface (Cas 2)**. Do not silently thread a persistent gpa into C's transient allocations.
     - **Uniform tag handling in the arch-walk path** (keeps the comptime-query path untouched; gate-4 monomorphisation count unaffected, as for the existing or/not arch-walk): route a rule through arch-walk when its when-clause has a tag filter OR its body has a tag mutation. `WhenInfo` gains `tag_filters` (op + resolved leaf bits, from a tag table built in `generateFile`). `walkWhen` populates it (+ adds `"TagSet"` to `components` for **positive** ops); `emitArchPredicate` for a positive `.tag_filter` → `arch.hasComponent(TagSet_id)`; the per-slot bit test is a `continue` guard in the arch-walk slot loop reading `TagSet_arr[slot].bits[w] & mask` (ensure `TagSet` in `body_used` so `_arr` is emitted). `emitStmt` `.tag_mutation_stmt` → `cmd.setTag(__entity, world.registry.idOf("TagSet").?, <bit>)` / `clearTag`. `tick` creates `var cmd = CommandBuffer.init(gpa, world); defer cmd.deinit();`, passes `&cmd` to tag-mutating rules (`rule_x(world, cmd)`; non-mutating rules stay `rule_x(world)`), `cmd.flush()` before `world.tickBoundary()`. Emit `TagSet` as `pub const TagSet = extern struct { bits: [N]u64 = [_]u64{0}**N }` (N = `tag_table.words()`) + register via `registerComponentRaw` (size N*8, align 8, no fields) when `leaf_count>0`.
     - **NEGATIVE tag-op codegen DEFERRED → `UnsupportedConstruct`, interp is the reference, flag for Review E3.** `has_no_tag`/`has_no_tags` match entities that may LACK `TagSet`, so the arch-walk `TagSet_arr[slot]` access is undefined for them — the clean cut is to fail loud (the differential uses the positive `has_tag`). The interp already handles negatives.
     - **Differential 43** (`tests/etch_interp/programs/43_*.etch` + sidecar, register in BOTH `corpus_facade.zig` and `codegen_corpus_build.zig`): mirror the interp test — `mark` rule `add_tag`s at tick N (deferred), `count` rule `has_tag`-queries + increments a `Counter` at N+1 → byte-exact (value 2 after 3 ticks). Entities may start WITHOUT `TagSet` (both backends add it via `applyTagMutation` — gpa is available in the interp flush AND the cmd flush). EBNF harness +1 (a tags-with-ops-and-mutation block).
  2. **`@on_event` observer** (design unblocked, "§12 spec-gap RESOLVED" entry above): the event is an **implicit `event` binding** in the observer body, self-style — reuse the block-3 `callMethod` self-injection (inject `event: T` from `@on_event(T)`, NO declared param); **E1203 OnEventTypeMismatch** is a type-coherence check. Closes the event vertical's byte-exact differential (producer landed `67d0959`). `etch-resolver-types.md §12` re-attached for E1203.
  3. **`Changed<T>` filter — STOP + SURFACE the Tier-0 structural decision (Cas 2) BEFORE implementing.** Mechanism known (`chunk.changed_ticks[T][i] > last_run_tick`, `engine-ecs-internals.md` §5) but the STRUCTURE is not pre-decided: where the per-rule `last_run_tick` lives interp-side; how `comptime_query` exposes change-ticks to codegen (currently unused by queries); how the dynamic interp tracks a per-component `changed_tick`. The ONLY STOP+surface of sub-slice A. `etch-resolver-types.md §12` for the surface + E12xx.

  After sub-slice A: B (async interpretation) → C (Level-A codegen closure of the E1/E2 deferrals — carries the allocator + aligned-resource-storage decisions, to surface) → D (the 7 debts) → STOP at the Review E3 gate. **Specs to re-attach:** the sub-slice-A set + `etch-resolver-types.md §12`. **Codegen-deferred = `UnsupportedConstruct` fail loud. Merge/tag = Guy.** NOT the Review E3 gate.

- 2026-06-09 — **E3 SESSION CLOSE — sub-slice A item 1 (tag CODEGEN runtime + differential 43) done & committed (`de48537` feat, `b478ba1` journal). RESUME MARKER for the next E3 session (item 2 `@on_event` observer, then item 3 `Changed<T>` STOP).** Same milestone / branch / brief — a clean cross-session continuation at a coherent, green boundary (the tag-execution unit is fully CLOSED in both backends), NOT the Review E3 gate. **Why stop here** (the locked E1/E2/E3 session-close discipline): the next unit — the `@on_event` observer — is one full grammar-sensitive vertical (parse `@on_event(T)` + mark the rule event-based + resolve E1203 + interp drain + codegen drain + the `event`-binding self-injection + a differential), and this session has already landed one full heavy vertical (the tag codegen) plus its deep reconnaissance; starting a second heavy vertical on a saturated context risks post-compaction guessed grammar/kernel detail (the differential demands byte-exact precision). Item 1 is committed, green (`zig build` / `test` / `lint` / `fmt` + hooks), differentials 01–43, EBNF 39. The generated `corpus_codegen.zig` p43 was eyeball-verified to match the design line-for-line.

  **RESUME — next E3 session, FIXED order. Re-verify every EBNF form + signature before writing (anti-hallucination); the authoritative PATCHED spec set is `Downloads/M0.8-full-grammar-v0.6/`.**

  1. **`@on_event` observer — the design is RESUME-engraved above (item 2 + the §12-RESOLVED entry l.438): implicit `event` binding, self-style; reuse the block-3 `callMethod` self-injection (inject `event` of type `T` from `@on_event(T)` into the rule frame, NO declared param); `E1203 OnEventTypeMismatch` is a type-coherence check, not a name mandate.** Reconnaissance done this session (so the next executes without re-discovery):
     - **`@on_event` is NOT parsed yet** — only the event *producer* landed (`67d0959`: `event` decl + `emit`, full stack); every `@on_event`/observer reference in the tree is a comment marking the deferral. So this is net-new: recognise `@on_event(T)` (an annotation — the `@annotation(arg)` parse infra exists; `AnnotTarget` already has `.event` for `@networked` on event decls, but `@on_event` applies to a **rule**) and classify the rule as **event-based** (the 4th rule form — read `RuleDesc`/`runRule` classification + `etch-reference-part2.md §12` four forms + `etch-resolver-types.md §12.1`).
     - **Interp drain (reference):** the per-tick `EventStore` already exists (`interp.zig` ~l.297): `list: []EventVal { type_name: StringId, fields }` + a `count(type_name)` helper; cleared at `stepOnce` start (`Lifetime.tick`), NOT reset at the body boundary (events cross to observers). The observer drains by iterating `list` filtered by `type_name == T`, running the body once per matching event with `event` bound to that event's `fields` (a struct-shaped value; field access reuses the struct-field path). **Ordering caveat to settle:** an observer must see events emitted *this* tick — confirm the producer rule runs before the observer drains within `stepOnce`, and that the clear-at-tick-start cadence does not drop same-tick events (the producer enqueues during the tick; observers drain after; clear is at the NEXT tick start).
     - **Codegen drain — the read path EXISTS, no new kernel API (key finding):** `World.event_bus.subscribe(comptime T) → EventCursor` (`src/core/events/bus.zig` l.178) + `poll(...) → ?T` (l.194, CONFIRM the exact arg list — likely `poll(comptime T, cursor: *EventCursor)`). The observer codegen emits roughly `var __cur = world.event_bus.subscribe(T) catch return; while (world.event_bus.poll(T, &__cur) catch null) |event| { <body with event.field …> }`. `event` is the polled `T` value; `event.amount` is direct field access. Verify subscribe/poll signatures + error sets before emitting.
     - **Differential (closes the event vertical):** a producer rule `emit DamageDealt { amount: … }` + an `@on_event(DamageDealt)` observer that reads `event.amount` and accumulates into a `Counter`/resource → byte-exact interp ↔ codegen. Mind the same-tick vs next-tick visibility decided above. EBNF harness +1 (an `@on_event` observer block).
     - **Specs to re-read first:** `etch-resolver-types.md §12` (E1203 + rule forms), `etch-reference-part2.md §12` (the 4 rule forms), `engine-ecs-internals.md §8` (observers concept), and the `67d0959` producer internals (`EventStore`, `emit` codegen `world.event_bus.emit`, the register-pass `world.event_bus.register(T, 256, .tick)`).
  2. **`Changed<T>` filter — STILL the SOLE STOP + SURFACE of sub-slice A (Cas 2), AFTER the observer.** Unchanged from item 3 above: mechanism known (`chunk.changed_ticks[T][i] > last_run_tick`) but the Tier-0 STRUCTURE is not pre-decided (per-rule `last_run_tick` interp-side; `comptime_query` change-tick exposure to codegen; dynamic-interp per-component `changed_tick`). New kernel mechanics → return to Claude.ai, do not guess.

  After sub-slice A: B (async interpretation) → C (Level-A codegen closure — allocator frame-scoped + aligned-resource-storage decisions to surface) → D (the 7 debts) → STOP at the Review E3 gate. **Codegen-deferred = `UnsupportedConstruct` fail loud. Merge/tag = Guy.**

- 2026-06-09 — **E3 SESSION CLOSE — sub-slice A COMPLETE (observer + `Changed<T>` delivered). RESUME MARKER for sub-slice B (async interpretation).** Same milestone / branch / brief — a clean cross-session continuation at a coherent boundary (sub-slice A closed: event-producer + tags landed earlier; `@on_event` observer + `Changed<T>` filter this session), NOT the Review E3 gate (sub-slices B/C/D remain). **Done this session:** observer `@on_event` — interp drain + `event` binding (`7dce932`), codegen bus-drain + register-gpa fix (`dffd0fd`), journal (`55149b8`); `Changed<T>` STOP+surface + GO (`73dc7e1`), interp (`9d09d38`), codegen + differential 44 (`4a54b73`). All green Debug + ReleaseSafe + lint + fmt; differentials now **01–44**; EBNF harness **41**; `recoverToTopLevel` stop-set unchanged (`@on_event` is a rule annotation, `changed` a when-clause keyword — neither top-level). Pushed to `origin` as the boundary archive (not a merge).
  **Why stop here (not chain B):** sub-slice B = **async interpretation** (the tree-walker as its own runtime: suspend on `await`, resume on wake — `async fn` was parsed at E2 `6e30e55`; its interpretation lands here, codegen is Phase 2 / out of scope). It is a fresh, heavy tranche with its own spec surface (async/await/`race`/`sync`/`spawn`/`branch` semantics + the effects algebra) — resuming it on a context already saturated by the observer + the `Changed<T>` surface/implementation risks post-compaction guessed grammar, the same call as every prior session close (E1/E2 tranches).
  **Sub-slice B — to do next session:** request + re-read the async specs FIRST (the boundary is set by `etch-ast-ir.md §9.4` per the brief; the async surface lives in `etch-grammar.md` + `etch-reference-part1.md`; the async effect in `etch-resolver-types.md §9` — confirm exact sections on attach, do not guess). The brief scopes it: **async interpretation only** (state-machine-free — the tree-walker suspends/resumes directly), **async codegen is OUT of scope** (`UnsupportedConstruct` fail loud, Phase 2 per `etch-ast-ir.md §11`). Forward note (Review E2): a block-body closure's call boundary must consume `returning` and decide "a `return` inside a closure exits the closure, not the enclosing fn" — if block-body closures land alongside async, mind the unified-signal frontier (mirrors `callFn`/`callMethod`). Per-construct vertical pattern + differential-registration sites (`corpus_facade.zig` + `codegen_corpus_build.zig`) unchanged.
  After B: **C** (Level-A codegen closure — the E1/E2/observer-deferred codegen: heap collections, `string`, capturing/block-body closures, `mut self`, resource-receiver `get(R)` + the observer's resource write, `Error`/error-handling, Optional; surfaces the frame-scoped-vs-persistent allocator decision (2nd half) + aligned-resource-storage) → **D** (the 7 debts: 5×S4 + 2×S5) → STOP at the Review E3 gate. **Anti-hallucination: form/code absent → STOP + ask. Byte-exact differential interp↔codegen, interp the reference. Codegen-deferred = `UnsupportedConstruct` fail loud. Merge/tag = Guy.**

- 2026-06-09 — **E3 sub-slice B — async suspend/resume model + B perimeter: STOP + SURFACE (two Cas-2 design decisions, relayed to Guy, awaiting GO). Reconnaissance done; NOT implemented.** Reprise on `1a4d829` (clean); read the sub-slice-B RESUME MARKER (l.603-606); re-read `etch-reference-part1.md §9` (async semantics), `etch-grammar.md` (async surface — §4.2 `await`/`race`/`sync`/`branch`/`spawn` stmts, §5.3 `[async] fn`, §7 `[async] rule`), `etch-ast-ir.md §5.3` (await→state-machine desugaring = HIR lowering, M0.8 has no HIR), `etch-memory-model.md §5.7/§7.1` (async state struct = persistent-heap, refcounted via `TaskHandle`); every claim below grounded in the interp code.

  **Code-verified surface (do not re-derive next session):** (a) the tick model runs **every rule to completion inside one `stepOnce`** (`interp.zig` l.675-700: optional `beginFrame` → `events.clear` → each rule `runRule` to completion → `flushPendingTags`); there is **no partial-run / suspended-task / wake lifecycle** anywhere. (b) the control-flow signal model is flag-unwind (`control`/`returning`/`thrown`, l.400-416) consumed at the fn boundary — an `await` suspend would need a 4th signal but, unlike return/throw, it must be **resumable** (re-enter the body at the await point a later tick). (c) `async fn`/`async rule` **fail loud today** (`callFn` l.1212, `callMethod` l.1256 `if (is_async) return error.RuntimeFailure`); `async rule` is **rejected by the parser** (l.368-374, "async rules land in E3"), no `RuleDecl.is_async`. (d) `await`/`race`/`sync`/`branch`/`spawn` keywords are **reserved-unproduced** (`token.zig non_s3_keywords` l.262-266); their **AST slabs exist** (`ast.zig` StmtKind `await_stmt`/`race_stmt`/`sync_stmt`/`branch_stmt`/`spawn_stmt` l.190-194, ExprKind `await_expr` l.238) — reserved exactly like event/tags were pre-E3. (e) `value.zig` has **no Task/Future/TaskHandle kind** (l.58-129) — representing a suspended task / handle needs a new `Value` variant or store.

  **POINT 1 — the suspend/resume model is NOT pinned for the M0.8 tree-walker (new kernel mechanics, like `Changed<T>`).** `part1 §9.1` pins the INTENDED model as "compiled **state machines** (no fibers/threads), the ECS **scheduler** resumes the rule on worker threads at wake"; `memory-model §5.7/§7.1` describes the heap **state struct** allocated at spawn. **Both describe the codegen/Phase-2 representation** (state machine = HIR lowering `ast-ir §5.3`; async codegen is OUT of scope). For the *tree-walker* — a recursive AST walker with no compiled state machine and no real multi-thread scheduler (1 rule-pass/tick) — §9 does **not** specify how a body suspends at `await` and resumes mid-way N ticks later. Options:
    - **(A) interp-level poll / task-record (RECO).** A persistent (cross-tick) task store on the `Interpreter`: `AsyncTask { body ref, locals snapshot, resume cursor, WakeCond }`. A 4th `suspended` control-signal unwinds the execStmt stack like `returning`, carrying the await target; the driver records `(cursor, locals, wake)`. `stepOnce` polls live tasks; a fired wake resumes (restore locals, run from cursor to the next await or completion). Faithful to §9.1's "struct that remembers its state between suspensions" **at the tree-walk level** — no fibers, no Zig-level state machine. Implies three sub-rulings: **(i) time base** — `wait(2.0s)` needs a tick↔seconds map; M0.8 `runFor` has no dt/clock. Reco: a fixed-timestep const, or restrict the B differential to event-based awaits + tick-counted waits. **(ii) await granularity** — clean resume needs `await` at **statement position** in straight-line bodies (+ simple `if`/`loop`); `await` inside an arbitrary mid-evaluated expression is precisely what the Phase-2 state machine is for → constrain M0.8, fail loud beyond it. **(iii) placement** — task store as an `Interpreter` field + a poll loop in `stepOnce` (before/after rules — TBD with the same-tick event-visibility ordering, mirrors the observer drain).
    - **(B) stackful fibers/threads — REJECTED:** §9.1 explicitly excludes fibers/threads; adds a runtime M0.8 lacks.
    - **(C) synchronous-only (resolve awaits inline within the tick) — REJECTED:** cannot honor `wait(d)` cross-tick suspension; under-delivers the brief's "suspend on `await`, resume on wake".

  **POINT 2 — perimeter: bound to `async fn`/`async rule` + `await`, DEFER `race`/`sync`/`branch`/`spawn` (RECO).** Parsed today = the `async fn` modifier ONLY; everything else is reserved-unproduced (see (d)). The brief E3 scope line (l.53) reads "async interpretation (suspend on `await`, resume on wake)" — **await-centered, race/sync/branch/spawn not named**; the marker says "async interpretation only"; the launch prompt cautions "ne sur-livre pas". `race`/`sync`/`branch`/`spawn` (`part1 §9.5-9.8`) each need **multiple concurrent live tasks + cancellation + join + a `TaskHandle` value** — a substantially larger concurrency runtime than suspend/resume. Tiered:
    - **T1 (RECO for M0.8 B):** `async fn`/`async rule` + `await` — produce the `await_stmt`/`await_expr` slabs, the `async rule` parse path (+ `RuleDecl.is_async`), the Option-A runtime. Await targets scoped per the time-base ruling (event-based `global_event`/`entity_event` are natural — the per-tick `EventStore` from sub-slice A already exists; `wait` per (i); `Future`-await spans into T2).
    - **T2:** `Future` await (one async fn awaiting another) + `spawn` + `TaskHandle`.
    - **T3:** `race` / `sync` / `branch` (parallel branches, cancellation, join).
    Reco: deliver **T1**, defer **T2/T3** → `race`/`sync`/`branch`/`spawn` stay reserved-unproduced (or parse + `UnsupportedConstruct`), flagged for Review E3.

  **LOCKSTEP check (per the directive):** `kw_async` is already top-level + in the `recoverToTopLevel` stop-set (l.340); `kw_rule` likewise. `await`/`race`/`sync`/`branch`/`spawn` are statement/expression keywords, **not top-level** → no `recoverToTopLevel` change for them (confirmed). **Forward note (Review E2, still live):** if block-body closures land alongside async, the closure call boundary must consume `returning` ("a `return` in a closure exits the closure, not the fn") — the same unified-signal frontier the `suspended` signal joins. **Awaiting Guy's GO on the T1/T2/T3 line + the three Point-1 sub-rulings before implementing. NOT the Review E3 gate.** Anti-hallucination: every form verified in the EBNF / code before writing. Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud (async codegen INCLUDED — Phase 2). Merge/tag = Guy.

- 2026-06-09 — **E3 sub-slice B — async interpretation (T1: `async rule` + `await`) DELIVERED in 3 commits (GO ruled both surfaced points). RESUME MARKER for sub-slice C.** Same milestone / branch / brief — a clean cross-session boundary (T1 suspend/resume complete + green + tested), NOT the Review E3 gate (sub-slices C/D remain). Guy ruled: **Option A** (interp-level task-record; the 4th `suspended`-style signal unwinds `execStmt` like `returning` — no fibers, no state machine); **`wait` counted in TICKS** (no ticks↔seconds map — `runFor` has no clock); **`await` at STATEMENT position** (cursor = top-level statement index, straight-line bodies; nested/value await fails loud — the Phase-2 state machine covers the general case); **poll in `stepOnce`** at the async rule's rule-order position (same-tick event visibility mirrors the observer drain); **perimeter T1 only** (`race`/`sync`/`branch`/`spawn` deferred T2/T3).

  **Delivered (3 commits, each green Debug+ReleaseSafe+lint+fmt+hooks):**
  1. **Parse layer** (`8dd3465`): `await` graduated to `kw_await`; `async rule` parsed (`RuleDecl.is_async`, `parseTopLevel` `.kw_async` → fn|rule); `await <target>` → an `.await_expr` node (`AwaitExpr` slab + `AwaitTargetKind {wait, wait_unscaled, entity_event, global_event, future}`) — the four builtin sources recognised by lexeme after `await`, `future` the `await <expr>` fall-through; `entity_event` payload-filter body rejected. LOCKSTEP no-op (`kw_async`/`kw_rule` already in the stop-set; `await` is expr-level). Interp fail-loud (else→RuntimeFailure) + codegen `UnsupportedConstruct` (`emitRule` `is_async` guard l.834 + await else l.1701). 2 parser tests.
  2. **Wait runtime** (`5484709`): Option-A machinery — `WakeCond {wait_until, global_event}`, `AsyncSlot {state, cursor, wake, locals}` parallel to `rule_descs`, an `async_tick` clock gated on `has_async` (a sync-only program is byte-identical to pre-B). `runAsyncRule`/`driveAsyncBody`/`finishAsync`/`bareAwaitExpr`/`evalAwaitTarget`/`asyncWakeFired` (after `runObserver`). `await wait(N)` (N an int = N ticks) suspends; resumes when `async_tick >= spawn_tick + N`. Interp test: n=1,1,2,2 across 4 ticks.
  3. **global_event runtime + harness** (`02c856a`): `await global_event(T)` resumes the tick after a producer (earlier in rule order) emits T (live EventStore check). Interp test + an async EBNF block (harness **42**). Differentials unchanged 01–44 (async = interp-reference, no numbered codegen differential).

  **Code-verified design (do not re-derive):** the tick model runs each rule to completion in one `stepOnce`; async rules are dispatched by index at their loop position, driven by their `AsyncSlot` (which persists across ticks — the by-value `rd` in `runRule` can't hold state). The cursor is the next top-level body-statement index; a bare top-level `await` statement (an expr-stmt wrapping `.await_expr`) is the only suspension point — `driveAsyncBody` evaluates its target to a `WakeCond`, stores `(cursor=i+1, wake)`, returns; resume re-enters from the cursor with the retained `slot.locals`. The rule-arena store resets (`collections`/`structs`/…) are NOT run for async bodies → POD-only locals (see bounds).

  **Bounds — fail loud, flagged for Review E3 (interp is the reference; deliver less, never silently wrong):**
  - **`async fn` interpretation = T2, not T1.** Function coloring (§9.3) makes an async fn's sole call path `await <async_fn()>` = a Future-await, which is T2. So `async fn` *parses* (E2) but its *invocation* stays fail-loud (`callFn` l.1212). T1's observable surface is `async rule`. (A structural consequence of coloring + the T2 deferral, not an under-delivery.)
  - **await targets:** `wait_unscaled` (needs a real clock, out of E3), `entity_event` (the global EventStore models no per-entity event source — its entity-association is §9-underspecified for M0.8), `future` (T2) → `evalAwaitTarget` RuntimeFailure.
  - **await placement:** nested (in `if`/`loop`/`match`) or value (`let x = await …`) await → `evalExpr`'s `await_expr` arm fail-louds (the path-cursor + value-threading is the Phase-2 state machine; e.g. the §7.5 `loop { await … }` pattern is deferred).
  - **async rule shape:** parameterless + non-entity-bound (a resource-only `when` is fine); an entity-bound / parameterised async rule → fail-loud in `runAsyncRule` (one-task-per-matching-entity lifecycle deferred).
  - **heap locals across suspend:** POD-only — a heap collection/struct local would be wiped by another rule's store reset between suspend and resume (the persistent-heap state struct of `memory-model §5.7` is not modelled at the tree-walk level).
  - **same-tick event semantics:** a task suspends at `await global_event(T)` without consuming a T present the same tick it spawned; it resumes the next tick T is present, and the producer must precede the awaiter in rule order (the single-pass analogue of the observer drain).
  - **resolver:** `await`'s event type is not validated (an unknown event = silent never-wakes); `await_expr` resolves to `.unknown`. Deferred.
  - **`race`/`sync`/`branch`/`spawn`:** reserved-unproduced (still in `non_s3_keywords`), T2/T3.

  **Why stop here (locked E1/E2/E3 session-close discipline):** sub-slice C is the heaviest remaining tranche — the Level-A codegen closure of every E1/E2/observer-deferred path (heap collections `T[]`/`Map`/`Set`, `string`, capturing/block-body closures, `mut self`, resource-receiver `get(R)` + the observer's resource write, `Error`/error-handling, Optional ops, anonymous `.{}`) — and it **carries the two surfaced decisions**: the frame-scoped-vs-persistent allocator choice (2nd half — the `tick(world, gpa)` contract from sub-slice A is the threading model; confirm the gpa is **frame/tick-scoped, reset at the boundary** when C's heap constructs land → STOP+surface (Cas 2) if non-trivial) + **aligned-resource-storage** (the resource store is `gpa.dupe(u8)` byte-aligned → `@alignCast(*R)` is refused in ReleaseSafe; needed for resource-receiver codegen + the observer's resource write). Starting C on a context already saturated by the recon + STOP-surface + 3 verticals risks post-compaction guessed grammar — the same call as every prior session close.

  **RESUME — next session = sub-slice C.** Re-attach `etch-abi-zig.md` (§3 ABI, §11 error-propagation), `etch-grammar.md`, `etch-reference-part1.md` (§5 memory, §10 errors, §11 collections), `etch-resolver-types.md`. The deferred-codegen inventory is enumerated in the **Review-E2-gate entry** (the "Codegen deferrals" list) + the block-5 markers. **The two allocator/storage decisions STOP+surface (Cas 2) before implementing if non-trivial.** After C: **D** (the 7 debts: 5×S4 + 2×S5) → STOP at the Review E3 gate. **Codegen-deferred = `CodegenError.UnsupportedConstruct` fail loud. Async differential = interp-reference. Anti-hallucination: form/code absent → STOP + ask. Merge/tag = Guy.** NOT the Review E3 gate.

- 2026-06-09 — **E3 sub-slice C OPENED — reprise + recon + the SOLE C STOP+surface (resource-storage alignment, reco relayed to Guy) + allocator-model ruling APPLIED (not surfaced).** Reprise on `921cf1d` (clean tree, branch up to date). Re-read the sub-slice-C RESUME MARKER (l.632-653) + the full Acted-deviations deferred-codegen inventory + the Review-E2-gate "Codegen deferrals" list (l.420-425). Re-read the re-attached PATCHED specs: `etch-abi-zig.md` §3.1-3.6 (POD layout-identical Etch↔Zig; non-POD pass-by-handle `String`/`DynArray`/`HashTable`/`ClosureStruct` extern structs; §3.6 string = `*const String`, immutable) + §5.5/§5.6/§6.5 (frame-arena return alloc, signature validation); `etch-memory-model.md` §3 (frame arena = bump, reset at tick boundary), §5 (layouts), §6.5 (iterator-chain alloc in the caller arena), §7 (durable lifetimes); `etch-stdlib.md` §11-16 (Duration/Time/String/Array/Map/Set/Optional APIs + §16.2 `??`/`?.`/`!`); `etch-reference-part1.md` §3 (Optional/arrays/maps/sets) + §10 (`Error { message, code: ErrorCode, source: Error? }` + throws/throw/try/catch/assert); `etch-resolver-types.md` §5 (4-kind dispatch) + §16 (operator overloading — `string + string` = `Add` concat, `==`/`<` = `Eq`/`Ord`, builtin-impl lowered inline).

  **Recon (verified in code, not from a map — anti-hallucination):** codegen tree = `src/etch/zig_codegen/{root,emit,lower,type_map,errors,cache}.zig` (`lower.zig` is the 3123-line engine). Deferred-codegen fail-loud sites confirmed in `lower.zig`: string-lit expr `:1829`; heap empty/dynamic array `:1888`; map for-in `:1586`; capturing closure `:1932`; anonymous `.{}` `:1980`; resource receiver-less `get(R)`/`get_mut(R)` `:1720`+`:1877`; `mut self` method `:429`; throws/async fn `:690`/`:689`; observer-with-entity-iteration `:790`. Optional today = scalar `?T`/`null`/`@as(?T,x)` only (`:1839`) — no `?.`/`??`/`!`, no `some`/`none` match patterns. `tick(world, gpa)` already emitted (`:2736`); rule fns are `fn rule_NAME(world)` / `(world, *CommandBuffer)` / `(world, *EventCursor)` — `gpa` not yet threaded into rule fns (only the tag `CommandBuffer.init(gpa, world)` consumes it).

  **Allocator-model RULING — APPLIED, not surfaced (per launch directive).** `etch-memory-model.md` §3 + `etch-abi-zig.md` §5.6/§6.5 settle it: transient non-POD (`string`/`T[]`/`Map`/`Set`) lives in a **frame arena mounted on the threaded `gpa`, reset at the tick boundary**; components are POD-only, so collections are transient locals with no persistence case in M0.8. Wiring is NOT under-specified: the emitted `tick(world, gpa)` gains `var __frame = std.heap.ArenaAllocator.init(gpa); defer __frame.deinit();` when the program heap-allocates, and `__frame.allocator()` threads into the rule fns that need it (a conditional `, fa: std.mem.Allocator` param). Arena-per-tick = reset-at-boundary by construction; the interp's per-body `CollectionStore` reset is finer-grained but observably identical (collections never enter the POD world-state → the byte-exact diff is unaffected). No STOP — the model is specified and the wiring is clear.

  **SOLE C STOP+SURFACE — resource-storage alignment (Cas 2, kernel; reco relayed, awaiting GO before the resource-receiver codegen).** Code-grounded: the Etch codegen registers + accesses resources through the **byte `ResourceStore`** (`src/core/ecs/resources.zig` — `gpa.dupe(u8)`, 1-byte-aligned) via `world.addResource(gpa, R_id, asBytes(&default))` (`lower.zig:593`) + `world.resources.isDirty/contains` guards; the interp reads/writes the SAME store field-by-field at byte offsets (`ecs_bridge.read/writeResourceField`, no cast); the differential harness verifies the SAME store (`diff_runner verifyResources → world.resources.getResource`). To emit `get_mut(R).field = v` natively the codegen must form `*R = @ptrCast(@alignCast(bytes.ptr))`, but `@alignCast(*R)` on a 1-byte-aligned `dupe` buffer **panics in ReleaseSafe**. Options:
    - **(A) Align the Tier-0 resource buffer — RECO.** `ResourceStore.addResource` allocates the byte buffer over-aligned (fixed 16-byte = `ChunkAlignment`, ≥ every POD/extern-struct field alignment — max POD align is 8 per abi-zig §3.1). Generated Zig then does `@ptrCast(@alignCast(world.resources.getMutResource(R_id).?.ptr))` → `*R` natively (zero-cost, faithful to abi-zig §3.1 "pointer identity, no serialization"). Interp unchanged (byte-offset access works on an over-aligned buffer); harness unchanged (reads bytes); byte-exact preserved (the resource is an `extern struct`, the same layout the registry/interp use). ~3-line Tier-0 edit + the codegen `register` passes the alignment. Exactly the "aligned resource storage on the Tier-0 side" anticipated by Review-E1 ruling 3 (l.288).
    - **(B) Field-by-field byte-level emission — no Tier-0 change.** Generated Zig never forms `*R`; it reads/writes each accessed field at its byte offset via unaligned helpers (`std.mem.bytesToValue`/`writeInt`/`@memcpy`-through-tmp), mirroring the interp `ecs_bridge`. Zero kernel change, but verbose, per-field-type, replicates field-offset logic, and diverges from the ABI pointer-identity model (more byte-exact-drift surface).
    - **(C) Route through the M0.2 aligned singleton-entity path (`src/core/resources/api.zig`) — REJECTED on recon.** That subsystem stores resources as singleton entities in 16-byte-aligned chunks (`world.get(T,eid)` `@alignCast` sound), but the Etch interp + the differential harness both read/write `world.resources` (the byte store); migrating only the codegen to `singleton_resources` would diverge the written store from the read/verified store → byte-exact breaks. Migrating interp + harness too is out of sub-slice-C scope.
    **Reco = (A).** Smallest change, ABI-faithful, leaves the interp + harness + byte-exact diff untouched, registry already tracks per-component alignment. Awaiting Guy's GO before the Tier-0 edit + resource-receiver codegen + the observer resource-write + the complete event differential.

  **Sub-slice-C plan (internal order; dependencies respected; resource-gated items last).** Unblocked (next working session): (1) **string** + frame arena (String extern struct + literal + concat `+` + interpolation; the differential routes the observable through a POD scalar, e.g. `s.len()` → component, since strings can't sit in POD components). **Scope nuance verified in code:** the interp *reference* itself only has the string **literal** (`interp.zig:1512` `string_lit → string_id`); `binaryArith` (`:2032`) has no `string_id` case (`string + string` → `RuntimeFailure`), and no string methods exist. So "string" is a full **interp+codegen** vertical (runtime string pool for concat/interp results + concat + `.len()`-class methods + interpolation in BOTH backends), not a codegen-only fail-loud swap — a depth question (minimal faithful subset vs the whole §12 API) flagged for Guy. (2) **Error/ErrorCode + error-handling** codegen (throw/try/catch desugared to a flag+branch; `Error.message: string` depends on (1)); (3) **heap-collections** `T[]`/`Map`/`Set` + methods via §5 dispatch (frame-arena allocated); (4) **Optional ops** `?.`/`??`/`!` + patterns `some(v)`/`none` + Optional-returning accessors; (5) **mut self** method codegen (pointer receiver `self: *T`); (6) **capturing + block-body closures** (capture-struct lowering; the closure call boundary consumes `returning` — a `return` in a closure exits the closure, not the enclosing fn — Review-E2 forward note l.425); (8) **anonymous `.{}`** (bidirectional inference; not resource-gated, small, folded near the end). Resource-gated (after GO): (7) **resource-receiver** `get(R)`/`get_mut(R)` codegen + the observer resource-write + the **complete event differential** (observer writes a resource). Each construct = replace its fail-loud with real emission + a byte-exact interp↔codegen differential at the SAME logical point of both backends. Codegen-deferred-forever (generics monomorphisation + async) stays `CodegenError.UnsupportedConstruct`. Merge/tag = Guy.

  **Session boundary (this entry).** Reprise + full spec re-read + code recon + the allocator ruling + the resource STOP-surface = the coherent boundary (NOT implemented — same shape as the sub-slice-B opening session l.615). Awaiting Guy: **(1) GO on the resource-storage STOP (reco = Option A);** **(2) confirmation of the plan + the "string depth" question** (minimal faithful subset — literal + concat + `.len()` + interpolation — vs the full §12 String API). The next working session executes string → … → resource with both settled, on a fresh context. STOP-surface pushed to `origin` (not a merge).

- 2026-06-09 — **E3 sub-slice C SESSION CLOSE — tranche 1a delivered (string type + literal + `.len()`). RESUME MARKER for tranche 1b (concat `+` + frame arena).** Same milestone / branch / brief — a clean cross-session boundary at a green, committed, pushed vertical (`2519958` feat, `bcf8d5d` journal), NOT the Review E3 gate (1b/1c + tranches 2–8 remain). **Why stop here** (locked E1/E2/E3 discipline + Guy's "clos à frontière cohérente, C multi-session"): tranche 1b is a **cross-cutting frame-arena vertical** (tick emission + conditional rule-fn signatures + dispatch + `LocalCtx` + binary-emit + a new interp `string_run` Value variant + per-body string store + reset) — starting that plumbing at the tail of a recon+STOP+1a session risks a half-wired arena; it executes cleaner fresh. This session: reprise + full spec re-read + code recon + allocator ruling + the resource STOP-surface (→ Guy GO Option A) + tranche 1a green. Differentials **01–45**; EBNF harness 42; `recoverToTopLevel` unchanged.

  **RESUME — next session = tranche 1b (concat `+` + frame arena), code-verified sites (do not re-derive):**
  - **Resolver** (`types.zig`): `synthBinary` (`:2271`) `.add` arm — add `string + string → string` BEFORE the numeric check (only `.add`; `sub`/`mul`/`div`/`rem` on strings stay errors). String `Eq`/`Ord` are NOT in the minimal subset (defer).
  - **Interp** (`interp.zig`): add `Value.string_run: u32` (`value.zig`) + a per-body string store (mirror `collections`/`structs`/`optionals`, owned `[]u8`, **reset at the body boundary** in `execBody`); a `stringBytes(v) []const u8` helper (`.string_id` → `ast.strings.slice`, `.string_run` → store). Intercept `.add` on two strings in the **`.binary` `evalExpr` arm** (`:1577` — NOT `binaryArith`, which is a free fn with no `self`/store) → concat into the store → `string_run`. Generalize the `.method_call` `.string_id` arm (added 1a) to also accept `.string_run` for `.len()` (via `stringBytes`). `value.zig` `eql` `.string_run => false` (string equality not exercised in the minimal subset).
  - **Codegen** (`lower.zig`): a `ruleNeedsArena(ast, rule)` body-walk (1b: body contains a string concat = a binary `.add` whose operand `inferExprZigType == "[]const u8"`); in `tick` (`:2736`) emit `var __frame = std.heap.ArenaAllocator.init(gpa); defer __frame.deinit();` when any rule needs it (reconcile with the existing `_ = gpa;` no-alloc path and the tag `CommandBuffer.init(gpa, world)` path — gpa backs both); arena-needing rule fns gain a conditional `, fa: std.mem.Allocator` param (exactly like the tag `*CommandBuffer` / observer `*EventCursor` conditional params at `:863`/`:792`); the tick dispatch (`:2772`) passes `__frame.allocator()`; thread the arena var name through `LocalCtx` so the binary-`.add` emit on strings → `std.mem.concat(fa, u8, &.{ <lhs>, <rhs> }) catch unreachable` (returns `[]u8`, coerces to `[]const u8`). Differential **46**: `("ab" + "cd").len()` → 4.
  - **Then tranche 1c — interpolation (heaviest; lexer/parser).** `token.zig:34` = "simple-quote only, no interpolation"; `lexer.zig:277 lexString` lexes `"..."` as ONE `string_literal` token (`{`/`}` are plain `lbrace`/`rbrace`, literal bytes today). Approach (a): split `{expr}` at **parse** time (sub-parse the embedded expressions, keep the lexer single-token), AST interpolation node = alternating literal-segment `StringId`s + embedded expr `NodeId`s; resolve each expr (Display/int/string for minimal); interp formats into the string store; codegen `std.fmt.allocPrint(fa, "<fmt>", .{ <args> }) catch unreachable`. Differential **47**.
  - **Resource Option A (GO'd) — lands with the resource-receiver tranche (7), not now:** over-align `ResourceStore.addResource` (`src/core/ecs/resources.zig`) to **16 (ChunkAlignment), UNCONDITIONALLY** (every resource buffer, no two-regime divergence — Guy's garde-fou); then resource-receiver codegen forms `@ptrCast(@alignCast(world.resources.getMutResource(R_id).?.ptr))` → `*R`. Orphan until its consumer (tranche 7) exists → bundle it there with the resource-receiver differential + the observer resource-write + the complete event differential.
  - **Order after 1 (string a/b/c):** 2 Error/error-handling → 3 heap-collections → 4 Optional ops/patterns/accessors → 5 mut-self → 6 closures (capt./block-body; call boundary consumes `returning`) → 8 `.{}` → 7 resource-receiver (+ Option A + observer-write + event-diff). Disciplines: byte-exact same-logical-point; generics-mono + async stay `UnsupportedConstruct` (NEVER closed M0.8); anti-hallucination (form absent → STOP+ask); no `--no-verify`; STOP at Review E3. Merge/tag = Guy.

## Closing notes

- **What worked**:
- **What deviated from the original spec**:
- **What to flag explicitly in review**:
- **Final measures** (parse, cold/incremental compile, hot-reload, monomorphizations):
- **Residual risks / tech debt deliberately left**:
