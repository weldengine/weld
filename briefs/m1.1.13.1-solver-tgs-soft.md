# M1.1.13.1 — Rigid solver port: SI + NGS → TGS Soft substepping

> **Status:** ACTIVE
> **Phase:** 1.1
> **Branch:** `phase-1/forge/solver-tgs-soft`
> **Planned tag:** `v0.11.13-solver-tgs-soft`
> **Dependencies:** M1.1.13 (`v0.11.13-sensors-triggers`, main @ `4a35343`)
> **Opened:** 2026-08-12
> **Closed:** —

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

The rigid contact solver moves from the big-step Sequential Impulses + NGS pair (M1.1.6–M1.1.7) to TGS Soft substepping — soft constraints + per-substep relax, restitution as a dedicated pass — per the decision in `engine-physics-forge.md` §1.6 and the rewritten normative model in `engine-physics-solver.md` §1.7. The reference implementation is pinned: `erincatto/box2d @ 56edae79f2949d86142b03450d5d60f63bcf5a6f` (main snapshot, project version 3.2.0 — no official v3.2.0 tag exists; latest release tag is v3.1.1). This milestone runs before M1.1.14 so cross-platform determinism validates the definitive solver exactly once. The E0 recon findings against the pinned source are frozen in the Notes below; the §1.7 recon markers are re-frozen in the KB at closure.

## Scope

- One substepped contact solver in `rigid/solver.zig` executing normative cycle step 6 and step 7: per substep — `integrateVelocities(h)` (tick accelerations + gravity + damping, `h = dt / substep_count`) → warm-start application → biased solve (normal points only) → `integratePositions(h)` → relax (normal points unbiased + friction); after the loop — restitution pass, then `storeContacts` (step 9). Exactly 1 biased sweep and 1 relax sweep per substep (compile-time constants, no config knobs).
- Warm start split into two named operations: cache seeding once per tick at `prepare` (cache lookup, world-tangent reprojection then clamp to `μ·λₙ`, hit/miss telemetry, `total_normal_impulse := 0`), and accumulator application every substep (current live `λₙ`, `λ_t1`, `λ_t2` values, including growth from earlier substeps of the same tick).
- `contact_constraint.zig` `prepare` extended: soft coefficients (`b2MakeSoft` form) from `(effective_hertz, contact_damping_ratio, h)`, effective-hertz clamp `min(contact_hertz, 0.125 · substep_count / dt)`, static-contact stiffening (2× hertz when either body is static — static only), per-point `total_normal_impulse` bookkeeping, cache seeding absorbed from the deleted `velocity_solver.warmStart`.
- `SolverConfig` rewritten: `substep_count = 4` (≥ 1 by type and assertion; 1 legal as degenerate big-step for A/B; 0 asserts), `contact_hertz = 30.0`, `contact_damping_ratio = 10.0`, `contact_push_max_speed = 3.0`, `restitution_threshold = 1.0` (unchanged), `penetration_slop = 0.005` (unchanged, consumed as dead-zone offset — see Notes N2). Removed: `velocity_iterations`, `position_iterations`, `max_penetration_correction`, `position_correction_factor`. Validation: all `Real` fields finite; `contact_hertz > 0`; `contact_damping_ratio ≥ 0`; `contact_push_max_speed ≥ 0`; `restitution_threshold ≥ 0`; `penetration_slop ≥ 0`.
- Restitution as a dedicated post-loop pass, per island: apply iff `v_n⁻ ≤ −restitution_threshold` AND `total_normal_impulse ≠ 0` (see Notes N6).
- NGS position pass removed: `position_solver.zig` deleted; no position authority inside the loop; penetration recovery paced by `contact_push_max_speed` only.
- Island lockstep preserved: solve, relax, warm-start application and restitution remain range-shaped `[from, to)` entries, one call per island interval per stage per substep; islands advance in lockstep. Telemetry sister entry reports substeps executed, solve/relax sweeps, minimum observed separation (§1.8.2, `get_solver_iterations_stats` surface).
- Test migration RED-first: solver oracles rewritten against the new model; the five-box stack at `substep_count = 4` replaces the 16-iteration floor; rest overlap re-measured and recorded for the closure KB patch (§1.7.2, replaces the SUPERSEDED `5.9e-7`).

## Out of scope

- Manifold-aggregated friction and torsional friction (open design item; per-point two-axis model with circular clamp is unchanged).
- Speculative inter-frame contacts: narrowphase margins and manifold generation untouched. The `s > 0` branch handles intra-tick separation only. The slop dead zone (N2) is the interim persistence mechanism.
- Kinematic-contact stiffening: the pinned source stiffens static-only. Extending 2× to kinematic bodies is a named open item, not implemented here.
- Joints, vehicles (M1.1.16+); engine-level `step()` orchestrator (born at M1.1.15 — the loop lives in `rigid/` plus the test harness).
- Rolling resistance and `tangentSpeed` (conveyor) — v3.2 features, not ported.
- CCD, contact events, any change to the warm-start cache format (frozen: key `(pair_key, subshape_id, feature_id)`, value `λₙ` + world-space tangent `Vec3`).
- Any solver knob on the module public surface (`root.zig` re-exports `SolverConfig` for in-module and harness use only, as today; nothing crosses `src/modules/forge/api/`).
- CI matrix changes: the missing `-Dphysics_f64=true` CI leg stays a tracked gap (M1.1.14 territory). The f64 oracle legs run locally (see CI).

## Specs to read first

1. `engine-physics-solver.md` — §1.7 (normative cycle, steps 1–11 + 10 bis, step 8 retired at frozen number), §1.7.1 (constraint, tangent basis, cache, anchors, coefficients, config), §1.7.2 (relax, restitution, paced recovery, NGS-removal trace), §1.8.1 (island seams), §1.8.2 (substep budget, telemetry), §1.8.5 (wake envelope — must stay green).
2. `engine-physics-forge.md` — §1.6 (the two-axis decision; do not re-debate), §1.2 (branch layout), §1.10 (`get_solver_iterations_stats`, `get_warm_start_data` surfaces).
3. `engine-invariants.md` — `ARCH-017` (in-tree discipline) and any determinism entries touching `forge_3d` iteration order.
4. `engine-phase-1-plan.md` — M1.1 context and the M1.1.13.1 line (before M1.1.14).

## Files to create or modify

- `src/modules/forge/forge_3d/rigid/solver.zig` — create — the single substepped solver: per-substep loop, warm-start application, biased solve, relax, restitution pass, range-shaped island entries (`solveRange`-style `void` entry + telemetry sister entry).
- `src/modules/forge/forge_3d/rigid/velocity_solver.zig` — delete — superseded by `solver.zig`.
- `src/modules/forge/forge_3d/rigid/position_solver.zig` — delete — no position pass exists in the new model.
- `src/modules/forge/forge_3d/rigid/solver_config.zig` — modify — field set and validation per Scope; doc header re-pointed to `engine-physics-solver.md` §1.7 (the current header cites `engine-physics-forge.md` §1.7 — stale since the M1.1.12 file split).
- `src/modules/forge/forge_3d/rigid/contact_constraint.zig` — modify — `prepare` gains soft coefficients, effective-hertz clamp, static selection, cache seeding, `total_normal_impulse`; local anchors and local inverse inertias (M1.1.7) change consumer, not shape.
- `src/modules/forge/forge_3d/rigid/root.zig` — modify — re-exports follow the new file set.
- `src/modules/forge/forge_3d/tests/solver_test.zig` — modify — `World.step` harness reshaped to the normative cycle; oracles migrated RED-first; absorbs the rest-overlap oracle.
- `src/modules/forge/forge_3d/tests/position_solver_test.zig` — delete — NGS-specific tests die with the model; the rest-overlap measurement re-lands in `solver_test.zig`.

Explicitly untouched: `rigid/contact_cache.zig`, `rigid/island_manager.zig`, `pipeline/integration.zig` (call sites now pass `h = dt / substep_count`; function bodies unchanged), broadphase, narrowphase, queries, `character.zig`, `pipeline/sensor.zig`, `pipeline/sleep.zig`, `body*.zig`, `src/modules/forge/api/`.

## Acceptance criteria

### Tests

- `tests/solver_test.zig` — `test "five-box stack comes to rest at substep_count=4"` — the discriminating oracle; runs and passes at f32 and f64 (`-Dphysics_f64=true`), Debug and ReleaseSafe; replaces the 16-iteration floor; measured rest values recorded in the LIVING closing notes for the §1.7.1 closure patch.
- `tests/solver_test.zig` — `test "rest overlap at equilibrium is measured and bounded"` — measured value recorded for the §1.7.2 closure patch (replaces SUPERSEDED `5.9e-7`); expected order: `penetration_slop` plus spring sag under the dead zone.
- `tests/solver_test.zig` — `test "resting body arms no restitution from one tick of gravity"` — `v_n⁻` captured pre-gravity at `prepare`.
- `tests/solver_test.zig` — `test "restitution predicate: <= threshold boundary arms; zero total impulse skips"` — includes a speculative point that never generated impulse.
- `tests/solver_test.zig` — `test "speculative branch limits approach without attraction"` — a point whose separation turns positive mid-tick receives `bias = s/h`, `mass_scale = 1`, `impulse_scale = 0`, in solve and in relax.
- `tests/solver_test.zig` — `test "substep_count=1 degenerate big-step runs"` — A/B lever; effective hertz clamps to `0.125/dt` (7.5 Hz at 60 Hz) — soft behavior expected, stack depth not asserted.
- `tests/solver_test.zig` — `test "island lockstep equivalence per stage per substep"` — seam re-baseline of the existing equivalence oracle.
- `tests/solver_test.zig` — `test "telemetry reports substeps and sweeps"` — 4 substeps, 4 solve + 4 relax sweeps at defaults.
- Friction scheduling revalidation (relax-only is a measurable scheduling change, not a neutral refactor): slope hold/slide threshold, friction isotropy under basis flip, stack shear — existing behaviors re-baselined where values shift, semantics preserved.
- Inline `solver.zig` / `contact_constraint.zig` tests — soft coefficients match the closed form for known `(f, ζ, h)`; clamp form `min(hertz, 0.125 · substep_rate)`; static softness selected iff either body is static (kinematic side receives ordinary softness); `total_normal_impulse` bookkeeping across warm applies and clamped deltas.
- Re-baseline budget: the migration set is the two solver files' inline tests (14 + 7), the two harness files (11 + 7), plus touched `contact_constraint`/`solver_config` inline tests (9 + 1 max). All suites outside the solver stay green with zero re-baseline — one exception process in Notes N8.

### Benchmarks

- None gated. Constraint-sweep count is asserted via telemetry (test above), not a wall-clock benchmark.

### Observable behavior

- `zig build test` filtered to `forge_3d` at `substep_count = 4` then `1`: telemetry line from the sister entry on the five-box scene showing substeps, solve/relax sweeps, and minimum separation for both settings.
- Recorded measurements (stack rest values f32/f64, rest overlap) in the LIVING closing notes — inputs to the closure KB patches.

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (Debug + ReleaseSafe)
- `zig build test -Dphysics_f64=true` green locally, Debug + ReleaseSafe, results recorded (no new CI leg this milestone)
- `zig fmt --check` green
- `commit-msg` hook green on every commit of the branch

## Conventions

- **Branch:** `phase-1/forge/solver-tgs-soft`
- **Final tag:** `v0.11.13-solver-tgs-soft`
- **PR title:** `Phase 1 / Forge / Rigid solver port to TGS Soft substepping`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`); squash title `feat(forge): port rigid solver to TGS Soft substepping (M1.1.13.1)`

## Notes

**N1 — Pinned reference and per-substep order (read on source, never deduced).** `erincatto/box2d @ 56edae79f2949d86142b03450d5d60f63bcf5a6f`. Per substep (`src/solver.c`, substep loop): integrate velocities (gravity + forces + damping, per substep with `h`) → warm-start application → biased solve (`useBias = true`, 1 sweep) → integrate positions (`h`) → relax (`useBias = false`, 1 sweep). After all substeps: restitution, then store impulses. Warm start is inside the loop, every substep, after velocity integration.

**N2 — Normal solve math (per point).** Separation `s` is re-derived each substep from the local anchors at the current poses against the frozen world normal (§1.7.1 anchors model). Three branches:

```
if s > 0:                                   # speculative — active in solve AND relax
    velocity_bias = s / h
    mass_scale    = 1
    impulse_scale = 0
else if biased pass (solve):
    error         = min(s + penetration_slop, 0)        # WELD DIVERGENCE: slop dead zone
    velocity_bias = max(softness.mass_scale * softness.bias_rate * error,
                        -contact_push_max_speed)
    mass_scale    = softness.mass_scale
    impulse_scale = softness.impulse_scale
else (relax, s <= 0):
    velocity_bias = 0 ; mass_scale = 1 ; impulse_scale = 0

delta = -normal_mass * (mass_scale * v_n + velocity_bias) - impulse_scale * lambda_n
lambda_n_new = max(lambda_n + delta, 0)                  # accumulated clamp: push, never pull
applied = lambda_n_new - lambda_n
total_normal_impulse += applied                          # algebraic, post-clamp
```

The source uses raw `s` (no dead zone); the `min(s + penetration_slop, 0)` offset is a documented Weld divergence: it preserves the frozen role of the 5 mm slop — a stationary overlap that keeps manifold and warm-start cache persistent — which the source obtains instead from speculative manifold margins (`4·slop`), out of scope here. Setting the offset to 0 restores exact source parity the day speculative contacts land. `mass_scale`/`impulse_scale` still apply throughout the dead zone (only the bias term is zeroed). Note the clamp floor sits outside `mass_scale` in the source form: the effective recovery cap is exactly `contact_push_max_speed`.

**N3 — Soft coefficients, clamp, static stiffening.** Once per tick at `prepare` (per-shape overrides do not exist in Weld):

```
f_eff = min(contact_hertz, 0.125 * substep_count / dt)   # 30 Hz exactly at 60 Hz × 4
omega = 2π·f_eff ; a1 = 2ζ + h·omega ; a2 = h·omega·a1 ; a3 = 1/(1 + a2)
bias_rate = omega/a1 ; mass_scale = a2·a3 ; impulse_scale = a3
static side present (either body_type == static): coefficients from (2·f_eff, ζ, h)
```

Kinematic bodies receive ordinary softness — source parity (the source keys on "no solver body", which only static bodies lack). `contact_hertz > 0` is validated, so the source's zero-hertz branch is unreachable and not ported. A/B gotcha: at `substep_count = 1` and 60 Hz, `f_eff = 7.5 Hz` — visibly soft contacts are the expected behavior of the lever, not a defect.

**N4 — Warm start: two operations, one hazard.** Seeding (once per tick, at `prepare`, absorbing the deleted `velocity_solver.warmStart`): cache lookup by full key, binary search point by point; world-space tangent impulse reprojected onto the new tangent plane THEN clamped to `μ·λₙ` (reproject before clamp, never the reverse); hit/miss telemetry counted here and only here; `total_normal_impulse := 0`. Application (every substep, immediately after `integrateVelocities`): inject `P = λₙ·n̂ + λ_t1·t̂1 + λ_t2·t̂2` from the CURRENT accumulators — including growth from earlier substeps — and add `λₙ` (current) to `total_normal_impulse`. HAZARD: re-calling a seeding-style function per substep would re-read the cache, reset `λ` to last-frame values and multiply hit/miss counters. The two operations must be distinct functions.

**N5 — Friction: relax-only, normals-first, mechanics unchanged.** In the pinned source, friction (and rolling resistance) runs only in the relax pass, after all normal points of the constraint. Ported as-is: biased solve = normal points only; relax = all normal points (unbiased), then all friction points. This is a measurable scheduling change (Gauss-Seidel points are coupled through body velocities), hence the revalidation entries in Acceptance. Weld's 3D friction mechanics are unchanged: two independent tangent axes driven to zero, circular clamp `‖(λ_t1, λ_t2)‖ ≤ μ·λₙ` against the point's CURRENT accumulated normal impulse, vector rescale never per-axis, no friction bias. The M1.1.7 "normal-first" divergence note retires at closure: the pinned source is normal-first too.

**N6 — Restitution pass (step 7, per island, once after the loop).**

```
apply iff  v_n_pre <= -restitution_threshold  AND  total_normal_impulse != 0
delta = -normal_mass * (v_n + e * v_n_pre)
lambda_n accumulate + clamp >= 0 ; total_normal_impulse += applied delta
```

`v_n_pre` (= `v_n⁻`) is captured at `prepare`, pre-gravity (source parity: the source captures `relativeVelocity` in prepare, before the first in-loop velocity integration). The `<=` follows the source and the closed-interval family convention; it supersedes §1.7.2's strict `<` wording at closure. `total_normal_impulse` is exactly: 0 at prepare; `+= λₙ` (current) at each per-substep warm application; `+= applied` (algebraic, post-clamp) at each solve/relax/restitution update. It is neither the final `λₙ` nor a boolean, and it is not stored to the cache (format frozen). `storeContacts` (step 9) runs after restitution, so cached impulses include the restitution correction — source parity.

**N7 — Frozen-at-prepare vs re-derived.** Jacobian arms `r_a`/`r_b`, world inverse inertias, effective masses (normal + both tangent axes) and the tangent basis are computed at `prepare` and frozen for the tick — source parity. Only the separation scalar is re-derived per substep, from local anchors at current poses (§1.7.1; worldspace noise class `floatEps(Real)·|x|` documented there; the f64 comptime flag is the escape hatch — the source's delta-tracking scheme is NOT ported). Do not "improve" by re-rotating Jacobian arms per substep: arms and frozen effective masses must stay consistent.

**N8 — Suites outside the solver.** Expected green with zero re-baseline: sleep/island wake-envelope tests assert clause bounds denominated in `penetration_slop`, which the dead zone preserves at the same order. If an exact-value SI+NGS transient baseline is discovered inside them, it joins the migration set with an execution-log entry (fix-as-you-go); it does not widen the envelope.

**N9 — Closure KB patches (produced by Claude.ai at closure; inputs = LIVING measurements).** §1.7 step 6 reworded: seed once at prepare, apply accumulators every substep (replaces "first substep only"); §1.7.1 solve formulas re-frozen per N2 (mass_scale inside the clamp; dead-zone divergence noted), clamp `0.125 · substep rate`, static 2× re-worded static-only, config table gains the `penetration_slop` row, five-box measured values re-recorded; §1.7.2 predicate `<=` + `total_normal_impulse` definition + re-measured rest overlap (replaces `5.9e-7` SUPERSEDED); `engine-physics-forge.md` §1.6 sweep count corrected `2n+1 → 3n+1` (13 at defaults, of which n are pure applications); M1.1.7 friction-order divergence note retired. Abort rule: if the acceptance gates fail, the branch is abandoned — the big-step solver stays, `main` untouched.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone. The log is not a marketing report: it serves review and post-mortem debugging.*

## Specs read

*Check before writing any production code. Confirms the spec was ingested in full, not merely skimmed.*

- [x] `engine-physics-solver.md` — read in full (400 lines), 2026-08-12 22:28. §1.7 rewritten model, §1.7.1 constraint/anchors/soft coefficients/config, §1.7.2 relax + restitution + paced recovery + NGS-removal trace, §1.8.1 island seams, §1.8.2 substep budget + telemetry, §1.8.5 wake envelope, §1.13 sensors (must stay green).
- [x] `engine-physics-forge.md` — read in full (1230 lines), 2026-08-12 22:29. §1.1/§1.2 branch layout, §1.5 scope, §1.6 the two-axis decision, §1.10 `PhysicsDebugProvider` (`get_solver_iterations_stats`, `get_warm_start_data`), §2 components, §3 manifold contract (`position` = midpoint, `penetration`), §4 material combine rules.
- [x] `engine-invariants.md` — read in full (902 lines), 2026-08-12 22:15. `ARCH-017` in-tree discipline, `ARCH-016` module isolation, `ARCH-022` coordinate conventions, `ARCH-023` SI storage, `ARCH-013` tier model.
- [x] `engine-phase-1-plan.md` — read in full (475 lines), 2026-08-12 22:30. M1.1 context, the M1.1.13.1 line (before M1.1.14), M1.1.15 ownership of `step()` + the typed-bus bridge, M1.A placement constraints.

## Execution log

*One entry per logical unit of work (typically: an objective met, a green test, a blocker). Chronological. Short — 1 to 3 lines per entry.*

- **2026-08-12 — Gate A opened.** Branch cut from `main` @ `4a35343`; brief copied verbatim; the four specs read in full before any production line. Baseline measured before touching anything: `zig build test-forge-3d` = **532/532**, exit 0.
- **2026-08-12 — Soft-coefficient forms (additive).** `contact_constraint.zig` gains `Softness`, `makeSoft`, `effectiveContactHertz`, `static_hertz_factor`, `usesStaticSoftness`, plus `ConstraintPoint.total_normal_impulse` whose `= 0` default makes the per-tick reset at `prepare` STRUCTURAL rather than assigned. No signature moved, no existing test touched.
- **Design note — the Gate A/B seam.** The coefficient FORMS land here; the two `Softness` triples are constraint-INDEPENDENT, so the substepped solver derives them once per tick (Gate B) and `prepare` SELECTS between them via `usesStaticSoftness`. That is why `build` needs no `dt`/config parameter at this gate and `contact_constraint.zig` stays free of solver tuning.
- **Test defect, self-reported.** The first substep-count ladder asserted STRICT monotonicity of `effectiveContactHertz` and failed at `n = 16`: the clamp SATURATES at the authored 30 Hz from `n = 8` at 60 Hz. The estimate was wrong, the code was not. The ladder is now non-strict, with strict growth asserted only on the sub-ladder where the bound genuinely binds — so it cannot pass by being flat.
- **Gate A green.** 532 → **537** (+5 inline tests), zero re-baseline. Four corners all 537/537: f32 and `-Dphysics_f64=true`, Debug and ReleaseSafe. `zig fmt --check` and tree-wide `zig build lint` clean. Language audit over the branch diff: 198 added lines, zero French.
- **2026-08-12 — Gate A arbitrations relayed, both journalled as clarifications.** (1) The A/B seam takes the STRICT letter: the orchestration derives the two `Softness` triples once per tick from the `contact_constraint.zig` helpers, `prepare` receives the PAIR as a parameter, selects with `usesStaticSoftness` and STORES the chosen triple on the constraint (source form `constraint->softness`) — so no bool→triple re-branch runs in the hot sweeps, and `build` still carries no `dt`/config. (2) The `0.125` factor is CONFIRMED on the pinned source `56edae7`: `src/physics_world.c:912` `b2MinFloat(world->contactHertz, 0.125f * context.inv_h)` with `inv_h = subStepCount * inv_dt` (`:898-899`, i.e. the substep rate), second occurrence at `src/contact_solver.c:1688` (per-shape path). Closed — the value I implemented at Gate A stands unchanged.
- **2026-08-12 — Gate B STOPPED before its first implementation line (blocker B1).** Calling `integrateVelocities` once per substep would deliver `1/substep_count` of any accumulated force's impulse, because that function CLEARS the accumulators it applies. Scope requires the per-substep call, the file list requires `pipeline/integration.zig` unchanged, and §1.7 step 3 sides with Scope. Recommendation and two rejected alternatives in Blockers § B1.
- **Transient, recorded rather than hidden.** One run failed with NO test count at all — crash-shaped, not an assertion — immediately after a fresh compile, and did not reproduce in six subsequent runs including two forced recompiles. Same family as the `windows-2025 / ReleaseSafe` hang already tracked in `CLAUDE.md`. Flagged, not diagnosed.

## Recorded deviations

*Changes to the FROZEN SECTION made mid-milestone after a Claude.ai round-trip. Each deviation references the commit that records it. If empty at milestone end: nominal case.*

## Blockers encountered

*Blocking points that required a return to Claude.ai (cf. `engine-development-workflow.md §2.4`). If 2+ distinct blockers: re-scope signal.*

### B1 — `integrateVelocities` cannot be called once per substep as it stands: the force accumulator would deliver `1/substep_count` of its impulse

**Opened 2026-08-12, Gate B, before any implementation line.**

Scope says the substep loop calls `integrateVelocities(h)`, and the file list says
`pipeline/integration.zig` is untouched with "function bodies unchanged". Those two cannot both
hold, and the reason is arithmetic rather than stylistic.

`integrateVelocities` fuses three operations: it READS the force/torque accumulators, APPLIES them,
and CLEARS them (`integration.zig:91` reads `forces[i]`, `:109-110` zero both columns for every live
body). Called `n` times per tick with `h = dt/n`:

- **gravity** is applied `n` times over `h` ⇒ total `g·dt`, which is exactly what §1.7 step 3 asks
  for ("une force constante sur le tick produit la même impulsion totale, appliquée en
  `substep_count` tranches");
- **damping** becomes `(1 − d·h)^n` instead of `(1 − d·dt)` — substep-count-sensitive, anticipated
  by M1.1.5's own doc and wanted by §1.7 step 6, so this one is an intended change;
- **an accumulated force `F`** is applied on the FIRST substep only and cleared there, so the tick
  delivers `F/m·h` instead of `F/m·dt` — **a quarter of the intended impulse at the default
  `substep_count = 4`**.

This is not a reading of mine that needs a probe: `tests/integration_test.zig:201` already pins it,
in these words — "Second tick with no re-apply: no force contribution ⇒ velocity unchanged".

§1.7 step 3 settles the SEMANTICS and contradicts the file list, not the Scope: the accumulators are
read into the tick's accelerations and **reset at step 3**, once, uniformly over live bodies, while
the APPLICATION is per substep at step 6. `integrateVelocities` as written does the reset at step 6,
`n` times.

`addForce`/`addTorque` have no production consumer today (only `integration_test.zig` and
`body_manager_test.zig`), so the defect would be LATENT — which is precisely why it is worth
stopping for rather than discovering at M1.1.15 with the orchestrator on top of it.

**Recommended resolution, and it costs no re-baseline.** Decompose in `pipeline/integration.zig`,
purely additively:

- `integrateVelocitiesNoReset(bm, h, gravity)` — the current body minus the two clears;
- `resetForceAccumulators(bm)` — the uniform §2 clear over live bodies, dead slots untouched;
- `integrateVelocities` becomes their exact composition, so its behaviour is bit-identical and
  **every M1.1.5 pin stays green with zero re-baseline** — the same move M1.1.5 itself made when it
  split `integrate` into `integrateVelocities`/`integratePositions`, in this very file.

`rigid/solver.zig` then calls `resetForceAccumulators` once at step 3 and `integrateVelocitiesNoReset`
per substep. N8 is honoured exactly; what changes is the frozen "Explicitly untouched" clause for one
file, which is why this is not a call I make alone.

Two alternatives, both rejected and recorded so the choice is visible: keeping the file literally
untouched and accepting `1/n` of the force impulse (a silent physics error); or having `solver.zig`
snapshot and restore the accumulator columns around each substep (correct, but it duplicates the §2
reset rule in a second place and needs per-tick scratch).

**Status: awaiting arbitration. No implementation line written for Gate B.**

## Closing notes

*Fill in at Status → CLOSED, just before opening the PR.*

- **What worked:**
- **What deviated from the original spec:**
- **What to flag explicitly in review:**
- **Final measurements** (perf, binary size, compile time, whatever is relevant to the milestone):
- **Residual risks / tech debt left intentionally:**
