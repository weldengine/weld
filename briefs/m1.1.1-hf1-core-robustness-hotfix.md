# M1.1.1-HF1 — Core robustness hotfix: scene load, ECS release, asset I/O, IPC send

> **Status:** CLOSED
> **Phase:** 1.1
> **Branch:** `phase-1/core/robustness-hotfix`
> **Planned tag:** `v0.11.2-core-robustness-hotfix`
> **Dependencies:** M1.1.1 (`v0.11.1-broadphase-bvh`)
> **Opened:** 2026-07-13
> **Closed:** 2026-07-13

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

An external full-repo review (Codex, 2026-07-13) surfaced 6 P1 and 1 P2 defects, all verified against `main` at `v0.11.1-broadphase-bvh` by direct source inspection. They are transverse (core/scene, core/ecs, core/ipc, asset_pipeline) and none touches forge. This standalone hotfix fixes all 7 before M1.1.2 opens, per the zero-deferred-debt rule. Defects are referenced below as D1–D7 in the order of the review.

## Scope

- **D3 — `EntityIdentityStore.release` atomicity** (`src/core/ecs/entity.zig`): reserve free-list capacity (`ensureUnusedCapacity(gpa, 1)`) *before* mutating the slot; append with `appendAssumeCapacity` *after*. Contract: on `OutOfMemory`, no observable mutation — the slot stays alive, `liveCount` unchanged, the call is retryable.
- **D7 — `World.despawn` purges extensions** (`src/core/ecs/world.zig`): remove the entity's `entity_extensions` entry, freeing the owned name slices and the list, in the same call that releases the identity.
- **D4 — `Registry` release atomicity** (`src/modules/asset_pipeline/registry/Registry.zig`): same no-mutation-on-error pattern. When `refcount == 1`, reserve free-list capacity first; only then decrement, invalidate, and append. On `OutOfMemory` the slot keeps `refcount == 1` and stays alive. `Loader.release` then never propagates an error after the slot died, so the payload-free path in `Loader.release` is reachable exactly when it must be — no change needed there beyond what the registry fix implies.
- **D6 — `.bin` validation** (`src/modules/asset_pipeline/format/runtime_bin.zig` + `loader/Loader.zig`): add a `RuntimeHeader.validate(bin_len: usize)` check called from `readBin` after `read`: `version == current_version`; `data_offset >= header_size`, `data_offset + data_size <= bin_len` (overflow-safe arithmetic); same bounds for the metadata section; `hash == hash.u64Of(data section)`. Any failure → new `error.MalformedAsset` added to `LoadError`. `LoadError` is a pinned named set (C0.5): bump `WELD_ASSET_PIPELINE_PROTOCOL_VERSION` from 1 to 2 in `src/modules/asset_pipeline/root.zig`.
- **D5 — `sendWithHandles` short-write handling** (`src/core/ipc/transport_posix.zig`): capture the `sendmsg` return value; when `0 <= n < bytes.len`, send the remainder `bytes[n..]` through the same short-write loop `send()` already implements (factor that loop into a shared helper rather than duplicating it). Ancillary data (fds) travels with the first successful segment; the remainder carries none. POSIX backend only — the Windows backend's `sendWithHandles` is `error.Unimplemented` by contract until Phase 3 (`engine-ipc.md` §4.7).
- **D1 — scene loader string ownership** (`src/core/scene/loader.zig`): interned resource strings are allocated **refcounted** (refcount 1, owned by the written `StringSlot`), not immortal. The loader adopts the `ecs_bridge` write discipline: read the old slot → write the new slot → `decref` the old block. `LoadResult.resource_strings` and its `deinit` destroy-loop are **removed**; `LoadResult` shrinks to `spawned` + `uuid_to_entity` + `mmap`. Teardown parity with interp-written strings — no new mechanism.
- **D2 — scene load rollback** (`src/core/scene/loader.zig`): `loadFromBytes` becomes loader-transactional. Before any resource field write, snapshot the full resource value bytes. On any error after the first spawn: (a) `decref` every string block the loader installed and restore the snapshotted resource bytes; (b) despawn every spawned entity in reverse spawn order (which, post-D7, also purges their extension entries). The contract covers the loader's own mutations only; side effects of user `on_attach` / `on_spawned` hooks that ran before the failure are explicitly outside it. Rollback is best-effort under `OutOfMemory` (log and continue); the guaranteed path targets non-OOM failures (malformed scene, unknown component, resolver failure, hook error).
- **CLAUDE.md §3.4** updated within the milestone PR: État courant table, +1 Tags row, Last updated date.

## Out of scope

- Hot-reload `asset_type` re-validation against the slot's `type_tag` — already a traced Phase-0 limitation in `Loader.zig`; unchanged.
- Windows `sendWithHandles` implementation (Phase 3, `DuplicateHandle`-based, per `engine-ipc.md` §4.7).
- Generation-wraparound guard in `EntityIdentityStore` (traced, future-phase).
- Transactional undo of user hook side effects during scene load (unbounded; excluded by the D2 contract).
- Any forge / broadphase / narrowphase work (that is M1.1.2).
- `weld scene validate` CLI surface — validation here is runtime-loader-side only.

## Specs to read first

1. `engine-scene-serialization.md` — §4 (cooked format + loader algorithm), resource section semantics.
2. `engine-ipc.md` — §4 transport contract, §4.7 handle passing.
3. `engine-asset-pipeline.md` — runtime `.bin` format and header fields.
4. `engine-ecs-internals.md` — entity identity store, free list, generation discipline.
5. `engine-zig-conventions.md` — error-handling and allocator conventions (no-mutation-on-error patterns).

## Files to create or modify

- `src/core/ecs/entity.zig` — modify — D3 atomic `release`; inline test.
- `src/core/ecs/world.zig` — modify — D7 extension purge in `despawn`; inline test.
- `src/modules/asset_pipeline/registry/Registry.zig` — modify — D4 atomic `freeSlot`/`release`; inline test.
- `src/modules/asset_pipeline/format/runtime_bin.zig` — modify — D6 `RuntimeHeader.validate`; inline tests.
- `src/modules/asset_pipeline/loader/Loader.zig` — modify — D6 call site, `LoadError += MalformedAsset`; inline tests.
- `src/modules/asset_pipeline/root.zig` — modify — `WELD_ASSET_PIPELINE_PROTOCOL_VERSION` 1 → 2.
- `src/core/ipc/transport_posix.zig` — modify — D5 shared short-write loop, remainder send.
- `src/core/scene/loader.zig` — modify — D1 refcounted strings + `LoadResult` shrink, D2 rollback; inline tests.
- `CLAUDE.md` — modify — §3.4 per protocol.
- `briefs/m1.1.1-hf1-core-robustness-hotfix.md` — create — this brief, first commit of the branch.

No other file may be touched without approval.

## Acceptance criteria

### Tests

- `src/core/ecs/entity.zig` — `test "release under OOM leaves the slot alive and retryable"` — `std.testing.FailingAllocator` fails the free-list append; assert `isLive` still true, `liveCount` unchanged, then a retry with a working allocator succeeds.
- `src/core/ecs/world.zig` — `test "despawn removes the entity's extension entry"` — attach an extension, despawn, assert no `entity_extensions` entry remains; `std.testing.allocator` leak detection proves the names were freed.
- `src/modules/asset_pipeline/registry/Registry.zig` — `test "release at refcount 1 under OOM leaves the slot alive and retryable"` — same FailingAllocator pattern; assert `refCount == 1` after the failed call, retry succeeds and recycles the slot.
- `src/modules/asset_pipeline/format/runtime_bin.zig` — `test "validate rejects out-of-bounds sections"`, `test "validate rejects an unsupported version"`, `test "validate rejects offset+size overflow"` — crafted 40-byte headers; no panic in any build mode.
- `src/modules/asset_pipeline/loader/Loader.zig` — `test "a malformed .bin loads as error.MalformedAsset"` — corpus: truncated data section, oob metadata, wrong hash, bad version.
- `src/core/scene/loader.zig` — `test "a failed load leaves the world unchanged"` — inject a failure after successful spawns (unknown component in a later archetype block, or a failing extension resolver); assert entity count returns to the pre-load value and touched resources hold their prior bytes.
- `src/core/scene/loader.zig` — `test "resource strings outlive LoadResult.deinit"` — load, `deinit` the `LoadResult`, read the resource string field, assert intact bytes.
- `src/core/scene/loader.zig` — `test "loading over an existing resource string releases the previous block"` — two successive loads writing the same string field under `std.testing.allocator`; leak detection proves the decref.
- All pre-existing tests stay green (notably the IPC handle round-trip suite for D5).

### Benchmarks

- N/A — defect-fix milestone; no perf-sensitive surface changes. Existing broadphase benches must not regress (spot-check only).

### Observable behavior

- Full `zig build test` green in Debug and ReleaseSafe on both CI OSes — this suite, including the new malformed-`.bin` corpus and rollback tests, is the observable artifact.

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (debug + ReleaseSafe)
- `zig fmt --check` green
- `commit-msg` hook green on every commit of the branch

## Conventions

- **Branch:** `phase-1/core/robustness-hotfix`
- **Final tag:** `v0.11.2-core-robustness-hotfix`
- **PR title:** `Phase 1 / Core / Robustness hotfix — scene load, ECS release, asset I/O, IPC send`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

**Gate plan (STOP+GO, one push + review per gate):**
- **E1 — ECS** : D3 + D7.
- **E2 — Asset pipeline** : D4 + D6 (registry atomicity, header validation, `LoadError` + protocol version bump).
- **E3 — IPC posix** : D5.
- **E4 — Scene loader** : D1 + D2, then Étape 4 (CLAUDE.md §3.4, audits, brief closure, PR).

E4 comes last deliberately: the rollback path relies on D7 (despawn purges extensions) and D3 (release semantics) being merged into the branch first.

**D1/D2 write discipline (normative):** for each resource the loader touches: (1) snapshot the full resource value bytes; (2) per string field: read old slot → alloc refcounted block, copy → write new slot; (3) after the whole load succeeds: decref each old slot block (`decref` is a no-op on immortal defaults). On rollback: decref each *new* block, then memcpy the snapshot back — the old blocks were never decreffed, so no incref is needed and no double-free is possible. This ordering is load-bearing; do not reorder.

**D2 aggravation being fixed (context for review):** on current `main`, the `errdefer` over `res_strings` destroys blocks that `loadResources` may already have installed into live `StringSlot`s — the error path dangles even before any `LoadResult.deinit`. The D1 ownership change removes that `errdefer` entirely.

**D5 testability:** a partial `sendmsg` on a blocking `SOCK_STREAM` socket cannot be forced portably in a unit test (it occurs on signal interruption after a partial transfer). Acceptance for D5 is therefore: the remainder loop is the *same shared helper* as `send()` (no duplicated loop), plus the existing handle round-trip tests staying green. Do not add a flaky signal-based test.

**D6 hash cost:** verifying `hash.u64Of` over the data section adds one linear pass per asset load. Accepted: the scene loader already verifies its post-header hash (`verifyHash`), and asset loads are I/O-bound; consistency wins.

**D3/D4 pattern name for commit messages:** "reserve-then-mutate" — reserve every fallible resource before the first observable mutation.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone. The log is not a marketing report: it serves review and post-mortem debugging.*

## Specs read

- [x] `engine-scene-serialization.md` (§4) — read 2026-07-13 19:00
- [x] `engine-ipc.md` (§4, §4.7) — read 2026-07-13 19:00
- [x] `engine-asset-pipeline.md` (runtime `.bin` format) — read 2026-07-13 19:00
- [x] `engine-ecs-internals.md` (identity store) — read 2026-07-13 19:00
- [x] `engine-zig-conventions.md` (error handling) — read 2026-07-13 19:00

## Execution log

- 2026-07-13 19:00 — Setup: read the 5 specs in full; branch `phase-1/core/robustness-hotfix` off latest `main` (27480e0); brief copied verbatim; status → ACTIVE.
- 2026-07-13 19:13 — E1/D3 (`entity.zig`): `EntityIdentityStore.release` is now reserve-then-mutate — `free_indices.ensureUnusedCapacity(gpa, 1)` *before* touching the slot, `appendAssumeCapacity` after. On `OutOfMemory` the slot stays alive, `liveCount` is unchanged, the call is retryable. Inline test drives the OOM with `std.testing.FailingAllocator`. Note: `entity.zig` carries a `//! FROZEN` header (C0.5, M0.9) that protects the `EntityId` 64-bit wire layout; the D3 change touches only `release`'s internal ordering and leaves that layout + its comptime asserts untouched — authorized by the brief's Files-to-modify list.
- 2026-07-13 19:13 — E1/D7 (`world.zig`): `despawn` purges the entity's `entity_extensions` entry (new private `purgeEntityExtensions`: `fetchRemove` → free each owned name copy → `deinit` the list) right before `identity.release`, so a despawned entity's names are freed here instead of stranded until `World.deinit`. Inline test spawns a real entity, attaches two extensions, despawns, asserts the entry is gone (the testing allocator would flag any leak).
- 2026-07-13 19:13 — E1 verification: `zig build test` (Debug) and `zig build test -Doptimize=ReleaseSafe` both exit 0, 0 assertion failures across 1149 tests; both new test-name strings confirmed embedded in the compiled core test binary. `zig fmt --check` + `zig build lint` green. One transient Debug exit-1 on an earlier run was the documented macOS `--listen` flakiness (the flaky step set moved run-to-run and hit only known-flaky modules — plugin_loader/hot-reload/query-filters — never ECS core). Gate E1 pushed; pre-push (build / test / test-release / test-tsan-wayland) green. GO received.
- 2026-07-13 19:40 — E2/D4 (`registry/Registry.zig`): `release` + `freeSlot` are now reserve-then-mutate. `release` at refcount 1 delegates to `freeSlot`, which `ensureUnusedCapacity(gpa, 1)` on the free list *before* touching the slot; on `OutOfMemory` the slot stays alive at refcount 1, retryable. refcount > 1 is a plain infallible decrement. `unload` (force) gets the same atomicity for free (it already called `freeSlot`). Inline test drives OOM with `FailingAllocator`. `Loader.release` unchanged — the registry fix makes its payload-free path reachable exactly when the slot actually dies (per brief). `Registry.zig` carries `//! FROZEN` (C0.5, handle/slot layout); D4 touches only internal ordering, not the layout — brief-authorized.
- 2026-07-13 19:40 — E2/D6 (`format/runtime_bin.zig` + `loader/Loader.zig` + `root.zig`): added `RuntimeHeader.validate(bin_len) ValidateError!void` — version + section-bounds with u64-widened overflow-safe arithmetic; 3 inline tests (oob sections, bad version, offset+size overflow). `readBin` now calls `validate` then verifies `header.hash == hash.u64Of(data section)`; both failures → new `LoadError.MalformedAsset`. Loader inline test drives a 4-case malformed corpus (bad version / truncated data / oob metadata / wrong hash) + a positive control, via a tmpDir and hand-assembled `.bin`s. `WELD_ASSET_PIPELINE_PROTOCOL_VERSION` bumped 1 → 2 in `root.zig` (the sanctioned tracked-migration mechanism for widening the pinned `load`/`reload` error sets — not a freeze violation). **D6 realization note:** the brief lists the data-hash equality under `validate`'s bullets, but the literal signature `validate(bin_len: usize)` cannot compute a hash from a length; the hash check is therefore realized in `readBin` (which owns the bytes), while `validate` does the length-only structural checks. This matches the acceptance-test split verbatim — the wrong-hash case lives in `Loader.zig`'s malformed corpus, the structural cases in `runtime_bin.zig`'s `validate` tests. Faithful realization of the stated signature, not a scope change.
- 2026-07-13 19:40 — E2 verification: Debug + ReleaseSafe suites exit 0, 0 compile/assertion failures; the 5 new test-name strings embedded in the compiled asset_pipeline test binaries; `zig fmt --check` + `zig build lint` green. Gate E2 pushed; pre-push green. GO + `docs(brief): record e2 design resolution` (53ff9a1) committed.
- 2026-07-13 19:54 — E3/D5 (`core/ipc/transport_posix.zig`): `sendWithHandles` now handles a `sendmsg` short write. The partial-write loop in `send` is factored into a shared private `writeAll` helper; `send` delegates to it, and after `sendmsg` succeeds `sendWithHandles` flushes any remainder `bytes[sent..]` through the same `writeAll` — the fds ride out with the first segment, the remainder carries no ancillary data. No new test (a partial `sendmsg` is not portably forceable; per brief the acceptance is the shared helper + the existing IPC round-trip suite green — `zig build test-ipc` exit 0, no duplicated loop). **n == 0 refinement:** the brief says "when 0 <= n < bytes.len, send the remainder"; taken literally, n == 0 would resend `bytes[0..]` with no ancillary and silently drop the fds. Since a `sendmsg` of a non-empty payload on a blocking `SOCK_STREAM` socket never returns 0 (it blocks until ≥1 byte, which carries the fds), I treat n == 0 as `error.BrokenPipe` — mirroring `writeAll`'s own n == 0 handling and never losing the fds. Faithful, defensive realization on an unreachable boundary; flagged for E3 review.
- 2026-07-13 19:54 — E3 verification: `zig build test-ipc` exit 0; full Debug + ReleaseSafe suites exit 0, 0 compile/assertion failures; `zig fmt --check` + `zig build lint` green. Gate E3 pushed; pre-push green. GO received.
- 2026-07-13 22:25 — E4/D1 (`core/scene/loader.zig`): loaded resource `string` fields are now interned as **refcounted** blocks (`persistent.alloc`, not `allocImmortal`) owned by the resource `StringSlot`, following the `ecs_bridge` read-old→write-new→decref-old discipline. `LoadResult` shrank to `spawned + uuid_to_entity + mmap` (the `resource_strings` field + its `deinit` destroy-loop removed). Teardown parity: the blocks are reclaimed by the resource owner's deinit (the interp's resource-string decref loop, `interp.zig`) exactly like interp-written strings — no new mechanism. Verified against `interp.zig`'s scene-load test (full interp teardown) and the two bare-World inline tests (which simulate the owner teardown via `decrefResourceStrings`).
- 2026-07-13 22:25 — E4/D2 (`core/scene/loader.zig`): `loadFromBytes` is loader-transactional. A per-resource journal records each touched resource's pre-write snapshot (null for a freshly-added resource). On success `commitResources` decrefs the replaced old blocks (immortal defaults no-op); on any post-first-spawn error a `committed`-guarded `errdefer` runs `rollbackResources` (decref the new blocks + restore the snapshot / remove the fresh resource) then despawns every spawned entity in reverse order (D7 purges their extension entries). The old `errdefer` over `res_strings` (which destroyed blocks already installed into live `StringSlot`s — the dangling-on-error bug) is gone. Best-effort under OOM (despawn logs + continues). The spawned slice is `dupe`d before the irreversible commit so a dupe OOM still rolls back.
- 2026-07-13 22:25 — E4 orphan patch (`tests/scene/load_resources_test.zig`, **not in the brief file list**): D1 removed `LoadResult.resource_strings`, which this test read (compile break) and relied on (`result.deinit`) to free the interned string (would leak post-D1). Patched per the §3.6.1 drift audit: dropped the `.resource_strings.len` assertion, released the slot's refcounted block at teardown (owner parity), refreshed the doc comment. Justified here per the Étape-3 file-touch rule. Blast radius confirmed minimal — it is the only test building a resource `string_` field; all other scene tests load string-free scenes or run under a full interp teardown.
- 2026-07-13 22:25 — E4 D2 test injection note: `test "a failed load leaves the world unchanged"` injects the post-spawn failure via a trailing collection-field resource (the loader's `CollectionResourceFieldUnsupported` guard), which fires after the spawn + the Settings string write — exercising entity despawn AND resource restore in one scene. Chosen over the brief's two example injections (unknown-component-in-a-later-block resolves pre-spawn in `buildSchemaRemap`; a failing extension resolver needs a hand-built Entity Extensions Table) purely for buildability with the Tier-0 scaffolding; it is an equivalent post-first-spawn failure.
- 2026-07-13 22:25 — E4 verification: Debug + ReleaseSafe suites exit 0, 0 compile/leak/double-free/assertion failures (the refcount transaction is exactly what a leak/double-free would expose); the 3 new test-name strings embedded in the compiled core test binary; `zig fmt --check` + `zig build lint` green. §3.6.1 audits: drift (LoadResult / resource_strings / WELD_ASSET_PIPELINE_PROTOCOL_VERSION / LoadError / sendWithHandles) shows no orphans; language audit (French tool-words + accents on the branch diff) empty. Gate E4 pushed for review.
- 2026-07-13 22:48 — E4 review correction (STOP E4, Guy): `rollbackResources` iterated the journal FORWARD, but an undo-log must replay LIFO. A `.bin` can carry two resource entries of the same cid (constructible, hash-valid); forward undo restores the old block for edit 0 then edit 1 reads the just-restored live block and wrongly decrefs it → refcount corruption → teardown double-free / use-after-free. Fixed to reverse iteration (`while (k > 0) { k -= 1; … }`, body unchanged), mirroring the reverse despawn. `commitResources` is unaffected — it operates on independent snapshot copies, not the aliased live slot, so its order is immaterial (left forward). Added `test "rollback restores across duplicate resource entries"` (two same-cid Settings entries "a"/"b" + trailing collection failure; asserts the slot holds the pre-load "pre", zero leak). **Proof:** the test was run against the forward code first — it FAILED with a DebugAllocator leak of the "a" block (`loader.zig:614`); with the reverse fix it passes, Debug + ReleaseSafe exit 0.

## Recorded deviations

- 3bd3775 — E2 review: hash-equality check realized in Loader.readBin; RuntimeHeader.validate keeps the stipulated length-only signature (brief-internal inconsistency resolved, approved in E2 review).

## Blockers encountered

- <blocker summary> — resolved by <commit SHA> or <Claude.ai conversation reference>

## Closing notes

- **What worked:** All 7 Codex-reported defects fixed across the 4-gate STOP+GO plan (E1 ECS → E2 Asset → E3 IPC → E4 Scene, E4 last so it builds on merged D3/D7). The **reserve-then-mutate** invariant (D3/D4) generalised cleanly: `ensureUnusedCapacity` before any observable mutation, `appendAssumeCapacity` after — proven with `std.testing.FailingAllocator` (slot alive, count unchanged, retryable). D1's refcount ownership hand-off reused the existing interp teardown loop verbatim (teardown parity, no new mechanism). D2's per-resource journal made the load transactional with a `committed`-guarded errdefer. Both §3.6.1 audits (drift + language) were clean on the first pass.
- **What deviated from the original spec:** (1) D6 — the hash-equality check listed under `validate`'s bullets is realised in `Loader.readBin` (which owns the bytes); `RuntimeHeader.validate(bin_len)` keeps the stipulated length-only signature and does the structural checks (approved at E2 review; see Recorded deviations). (2) D2 regression-test injection — used a trailing collection-field resource (`CollectionResourceFieldUnsupported`) rather than the brief's two example injections, purely for buildability with the Tier-0 scaffolding (equivalent post-first-spawn failure). (3) D5 — `n == 0` from `sendmsg` is surfaced as `BrokenPipe` rather than the literal "resend `bytes[0..]`", which would drop the fds (an unreachable boundary on a blocking stream socket; consistent with `writeAll`'s own `n == 0`).
- **What to flag explicitly in review:** The E4 review caught the load-bearing bug — `rollbackResources` must replay **LIFO**; forward undo of two same-cid resource edits corrupts the refcount into a teardown double-free (fixed, regression test proven red against the forward code). `entity.zig` / `Registry.zig` / the asset-pipeline surfaces carry `//! FROZEN` headers; D3/D4 touch only internal ordering (layout untouched), and the `WELD_ASSET_PIPELINE_PROTOCOL_VERSION` 1→2 bump is the sanctioned migration for the widened pinned `LoadError`. One orphan test outside the brief file list was patched under the §3.6.1 drift-audit allowance (`tests/scene/load_resources_test.zig`, justified in the Execution log).
- **Final measurements** (perf, binary size, compile time, whatever is relevant to the milestone): N/A — defect-fix milestone, no perf-sensitive surface changed. `zig build test` green in Debug **and** ReleaseSafe on macOS dev (0 assertion/leak/double-free failures across 1149+ tests); the pre-push gate (build / test / test-release / test-tsan-wayland) green on every gate push. 8 new inline/acceptance tests added (D3, D4, 3× `validate`, malformed-`.bin` corpus, 3× scene D1/D2 + the dup-cid regression). No new benchmark surface; broadphase benches untouched.
- **Residual risks / tech debt left intentionally:** Windows `sendWithHandles` stays `error.Unimplemented` (Phase 3 `DuplicateHandle`, explicitly out of scope). D2 rollback is best-effort under `OutOfMemory` (despawn logs + continues) — the guaranteed path targets non-OOM failures, per the D2 contract; user `on_attach`/`on_spawned` hook side effects that ran before a failure are outside the rollback contract (unbounded). Hot-reload `asset_type` re-validation remains a traced Phase-0 limitation (unchanged). Generation-wraparound guard in `EntityIdentityStore` remains future-phase (unchanged).
