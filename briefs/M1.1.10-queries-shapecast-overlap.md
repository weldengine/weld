# M1.1.10 — Forge 3D queries: shapecast, overlap, point query, closest point

> **Status:** IN PROGRESS
> **Phase:** 1
> **Branch:** `phase-1/forge/queries-shapecast-overlap`
> **Planned tag:** `v0.11.10-queries-shapecast-overlap`
> **Dependencies:** M1.1.0 (shapes, `BodyManager`), M1.1.1 (broadphase BVH), M1.1.2 (GJK cores + support maps), M1.1.8 (sleep state), M1.1.9 (query family frozen, ray traversal, `containsPoint`)
> **Opened:** 2026-07-26
> **Closed:** —

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

Eleventh core sub-milestone of the M1.1 rigid arc, and the second and last that interrogates the world instead of advancing it. M1.1.9 implemented the raycast and froze the complete family; this milestone replaces the five `@panic` bodies it left behind. The normative model for the four non-ray families was authored for this milestone and is `engine-physics-forge.md` §1.11.10 through §1.11.14, appended to the §1.11 series so that nothing renumbers.

Three things are settled here that are larger than the five bodies.

First, **a shape cast is not expressible over the ray kernels.** The cast of A along `d` against B is a raycast against the Minkowski difference of the two cores, inflated by `r_a + r_b` (§1.11.11). A sphere cast against a box is therefore a ray against a *rounded* box — exactly the shape `rayShape` rejects with `error.UnsupportedShape`. The cast gets its own kernel, and because that kernel covers every bounded convex through its support map, it has no shape to reject and needs no error channel. The frozen signature, which carries none, is evidence for the design rather than a constraint on it.

Second, **the solver-side entries of the whole family move to the solver scalar.** M1.1.9 left the five stubs typed on the `f32` public aggregates while the implemented raycast trio takes `Real`, and its own pin test records that one of the two halves would have to move here or at the freeze. It moves here. An entry typed `f32` inside the solver would force the conversion *into* the solver — under `-Dphysics_f64`, a time of impact and a contact point narrowed to `f32` before leaving the kernel and widened again at the interface tier: two conversions, one of them invisible, and the loss of precisely what the flag buys. The public `f32` surface of `engine-tier-interfaces.md` §1 and `api/types.zig` is **not touched**; it is wrapped in one place, at M1.1.15, for all eight entries at once. `engine-physics-forge.md` §1.11.8 carries the corollary.

Third, **`BodyId` cannot order a query result.** It is a slot index: it encodes creation order. The three overlap entries have no distance to rank by, so an unranked truncation had no invariant key at all; and the ray family's `(distance, BodyId)` key is already wrong on an exact distance tie. Measured on `main` at `deeabe7`, `f32`: two unit spheres on either side of the ray at a perpendicular offset of 0.5 give the bit-identical distance `19.133974`, and swapping the two creation orders changes the entity returned — for the closest hit as much as for the answer truncated to one slot. The key becomes `(distance, entity, BodyId)` across the family, `BodyId` surviving only as the final tie-break. `engine-physics-forge.md` §1.11.14 is normative on this.

## Scope

- **`src/foundation/math/aabb.zig`** — `Aabb(T).inflate(half_extents: Vec3T) Self`: the box grown by `half_extents` on every axis. Added here under the first-consumer rule, the `surfaceArea()` (M1.1.1) and `rayInterval()` (M1.1.9) precedents. An addition, not a threshold: no constant appears.
- **`Bvh(T).queryCast(ray, extent, collector) u32`** and **`Broadphase(T).queryCast(ray, extent, collector) u32`** in `pipeline/broadphase.zig` — the swept traversal of §1.11.10. Additive on `queryRay`: the same duck-typed collector contract including `maxDistance()` and `shouldStop()`, the same near-first descent, the same slab test unchanged, the same visited-node return. The one difference is that each node's stored box is inflated by `extent` before the slab test. `queryRay` is re-expressed as `queryCast` with a zero extent and its behaviour must stay **bit-identical**: the existing M1.1.9 traversal suite is the proof and must not be weakened to accommodate the refactor.
- **`src/modules/forge/forge_3d/pipeline/narrowphase/shapecast.zig`** (create) — the GJK cast kernel: a raycast from the origin against the Minkowski difference of the two cores, inflated by the radius sum, in the core + inflation-radius convention and never a second one. Returns the time of impact, the witness point on B, and the separating axis. Reuses `Simplex(T)`'s Voronoi closest-origin solver and `support.zig` (`SupportShape`, `RelativePose`, `minkowskiSupport`); it does **not** call `gjk()`. Dependency discipline identical to `raycast.zig` and `fast_paths.zig`: imports `foundation` and the sibling `support.zig` only — never `gjk.zig`'s loop, `manifold.zig`, `epa.zig`, `weld_forge`, `body*.zig`, `config.zig` or `broadphase.zig`. A named iteration ceiling, sibling of `max_gjk_iterations`, whose exhaustion returns a hit at the current parameter (§1.11.11).
- **`src/modules/forge/forge_3d/query/`** (create as a package; `query.zig` moves into it) — `root.zig` is the façade carrying the eight public entries, the shared `Filter`, `prepare`, the solver-side mirror types and the ordering key; `ray.zig` holds the three ray collectors moved unchanged from `query.zig`; `cast.zig` and `overlap.zig` hold the new collectors and per-candidate evaluation. `forge_3d/root.zig` re-exports the package at the same public path, so `forge_3d.query.raycast` is unchanged for every caller. The move is a `git mv` plus a split, not a rewrite: the ray code that moves must move **textually unchanged** except for the ordering-key edit of E1, so the diff is reviewable.
- **The five bodies, at `Real`** — `shapeCast`, `overlapShape`, `overlapAabb`, `pointQuery`, `closestPoint`, with solver-side mirror types `CastQuery`, `CastHit`, `OverlapRequest`, `ClosestPointHit` alongside the existing `RayQuery` / `RayHit`, and `[]BodyId` outputs. `overlapAabb` gains `store: *const ShapeStore`, without which its exact kernel is unreachable (see Notes). Every entry keeps taking `*const BodyManager`: a query cannot wake anything, structurally.
- **`BodyManager` adapters** — `entity(id) ?EntityId` (the column exists on `Body` and was never exposed); `castShapeBody`, `overlapShapeBody`, `containsPointBody`, `closestPointBody`, each resolving the body's pose and support shape via `store` and running the exact kernel, mirroring `raycastBody` / `gjkPair` / `collidePair`. Each returns null on a stale handle or a stale shape.
- **The ordering key** — `(distance, entity, BodyId)` for the distance-ranked entries, `(entity, BodyId)` for the three overlaps, with the retained set on overflow being the best `out.len` under that key by the replace-worst pattern of `AllCollector`. This **modifies M1.1.9-delivered code** (`hitLess`, `ClosestCollector.add`) under the no-deferred-debt rule; the justification is measured, not argued.
- **`bench/forge_3d_shapecast.zig`** (create) — cast and overlap throughput over a static scene, **measured and reported**, with no pre-registered numeric envelope.

## Out of scope

- **Analytic swept fast paths.** One generic kernel. Per-pair analytic sweeps are the M1.1.4 pattern — a separate body of work carrying a geometric-equivalence obligation against the generic path — and doubling the acceptance surface in one session is what the one-milestone-one-session rule exists to prevent. Additive after profiling.
- **The deepest point at a zero time of impact.** It requires EPA; it is purely additive (a defaulted field, zero call sites), on the model of the `treat_convex_as_solid` toggle, and the reference itself gates it behind a setting.
- **A bounded nearest-neighbour descent for `closestPoint`.** `max_distance` is a required parameter, so the caller already bounds the candidate set. Additive.
- **Widening the public surface.** The `f32` boundary is engine-wide; widening is one decision over `BodyDescriptor`, the interface pose, the query results and the ECS `Transform` together, and belongs to M1.1.15. Moving the *solver-side* entries to `Real` is not that decision and must not be presented as it: `engine-tier-interfaces.md` §1 and the public types of `api/types.zig` are untouched.
- **Plane, MeshShape and every shape beyond sphere/box/capsule** (M1.1.11), and therefore back-face handling, vacuous on a solid convex. Note what the kernel does and does not buy: it extends to any *bounded* convex through its support map, so ConvexHull (M1.1.19) is nearly free, but an infinite half-space has an **unbounded** support map and a non-convex MeshShape needs a per-triangle traversal. Neither is free and neither is here.
- **`error.UnsupportedShape` on the cast path.** It does not exist there, by construction. The ray path keeps its own, and it stays structurally unreachable end to end until M1.1.11 — dated unreachability, recorded in `CLAUDE.md`, not debt.
- **CCD and speculative contacts.** A cast interrogates the current state along a given translation; it advances no body and appears in no step of the cycle. Do not let the scope slide.
- Far-field conditioning beyond what §1.11.4 bis mandates. It is characterised and **not** fixed; a compensated intermediate is roughly twice the cost of the hot-path dot products and belongs to M1.1.15.
- `step()` / `PhysicsWorld` / `PhysicsModule` instantiation / ECS `Transform` sync (M1.1.15), the Tier 1 `physics_query` service wiring and its Etch wrappers, and the entity-level deduplication that service owes (§13).
- The authoring source of `collision_layer` on the ECS side (M1.1.13); character controller (M1.1.12); sensors and triggers (M1.1.13); joints (M1.1.16–18); compounds and `subshape_id` beyond the constant 0 (M1.1.20); `forge_2d` and the 3D↔2D `closest_point` divergence recorded in §13; editor overlays; `src/c-api/`.
- The Tier 0 IPC bounded receive and the Jolt friction-model port: neither is a physics-milestone item and neither is here.

## Specs to read first

1. `engine-physics-forge.md` — **§1.11 in full**, and in particular the sections authored for this milestone: §1.11.10 (swept traversal), §1.11.11 (shape cast internal model, including the termination table), §1.11.12 (overlaps), §1.11.13 (closest point), §1.11.14 (results not ranked by distance). Then §1.11.1 to §1.11.9 including §1.11.4 bis, then §1.1, §1.2, §3 in full (the three GJK regimes and the contact margin), §4, §13 in full, §17.
2. `engine-tier-interfaces.md` — §1, the Queries block of the `assertFn` list and its types. These are the **public** frozen shapes and this milestone does not touch them.
3. `engine-phase-1-criteria.md` — C1.1, in particular the query list.
4. `engine-phase-1-plan.md` — the M1.1.10, M1.1.11 and M1.1.19 rows.
5. `engine-physics-forge-2d.md` — §7, the symmetric 2D surface. Read for symmetry only; the 3D form is authoritative for 3D and nothing 2D is touched.
6. `engine-zig-conventions.md` — naming, unmanaged-first allocation, `root.zig` convention, the comptime interface-check pattern, the file-length guideline.

## Files to create or modify

- `src/foundation/math/aabb.zig` — modify — `inflate` + inline tests.
- `src/modules/forge/forge_3d/pipeline/broadphase.zig` — modify — `queryCast` on `Bvh(T)` and `Broadphase(T)`, `queryRay` re-expressed at zero extent, collector contract in the file header.
- `src/modules/forge/forge_3d/pipeline/narrowphase/shapecast.zig` — create — the GJK cast kernel.
- `src/modules/forge/forge_3d/pipeline/narrowphase/root.zig` — modify — re-export the kernel.
- `src/modules/forge/forge_3d/query.zig` — delete (moved).
- `src/modules/forge/forge_3d/query/root.zig` — create — façade, `Filter`, `prepare`, mirror types, ordering key.
- `src/modules/forge/forge_3d/query/ray.zig` — create — the three ray collectors, moved.
- `src/modules/forge/forge_3d/query/cast.zig` — create — cast collector and per-candidate evaluation.
- `src/modules/forge/forge_3d/query/overlap.zig` — create — overlap / point / closest-point collectors.
- `src/modules/forge/forge_3d/body_manager.zig` — modify — `entity` getter and the four exact-kernel adapters.
- `src/modules/forge/forge_3d/root.zig` — modify — re-exports at `Real` and the comptime test pin for the new suites.
- `src/modules/forge/forge_3d/tests/raycast_test.zig` — modify — import path, the ordering-key RED tests, the frozen-signature pin rewritten onto the `Real` forms.
- `src/modules/forge/forge_3d/tests/shapecast_test.zig` — create — the cast acceptance suite.
- `src/modules/forge/forge_3d/tests/overlap_test.zig` — create — overlap, point query and closest point.
- `src/modules/forge/forge_3d/tests/broadphase_test.zig` — modify — swept traversal tests plus the zero-extent bit-identity proof.
- `bench/forge_3d_shapecast.zig` — create — throughput measurement.
- `build.zig` — modify — bench target wiring only if the existing pattern requires it.
- `CLAUDE.md` — modify — the stale `Current state` table and the §3.4 patch, in the closing gate, content supplied by Claude.ai.

## Acceptance criteria

### Tests

RED-first on every pinnable behaviour. Every expectation is a **closed form computed in the comment above it**, never a value read back from the implementation.

**`src/foundation/math/aabb.zig`** (inline)
- `test "inflate grows every axis and is exact at zero"` — the zero extent returns a box that compares equal on all six bounds.

**`tests/broadphase_test.zig`**
- `test "queryRay at zero extent is bit-identical to M1.1.9"` — visited-node counts and collected sets identical to the pre-refactor behaviour on the existing scenes. This is the refactor's proof; it may not be weakened.
- `test "queryCast collects every proxy the swept box crosses"` — against a brute-force scan over all leaves.
- `test "queryCast prunes with the collector bound"` — with the mandatory **discrimination guard**: assert the tightening and non-tightening runs really visited different node counts, otherwise the test proves nothing.
- `test "queryCast visits all four layer trees"`.
- `test "queryCast is empty on an empty tree"`.

**`tests/shapecast_test.zig`** (create)
- `test "sphere cast against a sphere matches the closed form"` — the time of impact is a root of an analytic equation, at three obliquities including one that is **not** axis-aligned.
- `test "sphere cast against a box does not error"` — the discriminating test for the whole design: the same geometry through `rayShape` is a rounded box and returns `error.UnsupportedShape`; assert **both** facts in the same test, so the claim that the cast covers what the ray kernel rejects is proven and not asserted.
- `test "box cast against a box on a face, an edge and a vertex"` — closed-form time of impact per case.
- `test "capsule cast against a box and against a capsule"`.
- `test "the normal at the time of impact is the outward normal of the hit body"` — unit length asserted **tight**, and `normal · direction <= 0` on every hit. The sign is pinned by a closed-form oracle, never transcribed from the reference.
- `test "initial contact returns a witness on the hit body"` — two shapes already intersecting at the start pose: distance 0, and the returned position lies **on or inside the hit body**, which the cast shape's origin need not. Construct the case so that the cast origin is demonstrably outside the hit body, and assert that too — otherwise the test cannot tell the correct rule from the refuted one.
- `test "max_distance is a closed interval"` — a contact exactly at `max_distance` counts; the same at `max_distance` minus one ulp does not.
- `test "max_distance zero degenerates to an overlap test at the start pose"`.
- `test "a zero direction returns nothing"` — including a direction whose squared length underflows, at both precisions.
- `test "the iteration ceiling returns a hit at the current parameter"` — with a **discrimination guard** proving the ceiling was actually reached; a ceiling never observed to bind is a comment with syntax.
- `test "an oblique cast in the far field keeps a unit normal"` — the §1.11.4 bis obligation. The norm is asserted tight and everywhere; only the orientation carries a distance-dependent bound, and that bound is applied only inside the envelope where the scalar still resolves the geometry.

**`tests/overlap_test.zig`** (create)
- `test "overlapShape returns the overlapping bodies and nothing else"` — a scene with bodies inside, touching and clear of the query shape.
- `test "overlapAabb rejects a body inside the fat margin but outside the tight box"` — with a **discrimination guard** asserting the candidate's fat box really did overlap the query box, so the test proves the margin was rejected rather than never encountered.
- `test "overlapAabb is face-inclusive"`.
- `test "pointQuery is solid with the boundary included"` — for each of the three shapes, a point strictly inside, exactly on the boundary, and just outside.
- `test "closestPoint on a point inside the solid returns zero and the point itself"`.
- `test "closestPoint in the shallow band returns a positive surface distance"` — a point just outside a surface, with a **discrimination guard** asserting GJK really classified the pair `.shallow`; treating `.shallow` as an interior is the defect this test exists to catch.
- `test "closestPoint projects onto the inflated surface"` — closed-form surface point and distance for a sphere and for a capsule, where the core distance and the surface distance differ by the radius.
- `test "closestPoint selects among several candidates"`.
- `test "closestPoint returns nothing beyond max_distance"`.

**Shared across every family** (in the suite that owns each entry)
- `test "the object mask filters"` and `test "exclusions are honoured"` — one case per entry, including the empty and full masks.
- `test "a sleeping body answers and stays asleep"` — drive the published `World` harness until an island sleeps, assert `isSleeping`, run the query, assert the result, assert the body is **still asleep**.
- `test "a caller buffer that overflows keeps the best set under the ordering key"` — for each multi-result entry.
- `test "the result is invariant under creation-order permutation"` — the same scene built in several orders yields a bit-identical result **including the identity of the bodies returned**, which the M1.1.9 test deliberately did not assert. The visited-node count is explicitly not asserted.
- `test "two identical runs are bit-identical"`.

**Ordering key** (`tests/raycast_test.zig`, RED-first on `main`)
- `test "an exact distance tie is broken by entity, not by creation order"` — two unit spheres on either side of the ray at a perpendicular offset of 0.5, whose distances are bit-identical; assert the equality **first**, then assert that the closest hit and the answer truncated to one slot both return the same entity under both creation orders. This test must be seen **red on `main`** before the fix: it is red there today, measured.
- `test "two bodies on the same entity fall back on BodyId"` — the named residual of §1.11.14, pinned rather than left implicit.

The whole suite green at `f32` **and** under `-Dphysics_f64=true`, debug and ReleaseSafe.

### Benchmarks

- `bench/forge_3d_shapecast.zig` — sphere / box / capsule casts and shape overlaps over a static scene of 10 000 bodies, reported at `f32` ReleaseFast. **Reported, not gated.** No numeric envelope is pre-registered: no baseline exists. The leak-checking allocator must have `safety` **forced true** — its default is `std.debug.runtime_safety`, false in ReleaseFast, and a check whose failure has never been observed is worthless (M1.1.9 finding). Prove the check binds by reintroducing a leak deliberately, once, and reporting that it fired.
- Every inherited envelope quantity of M1.1.5 to M1.1.9 re-measured against `main` in both precisions, and the movement reported digit for digit.

### Observable behavior

- `zig build test-forge-3d` green, and green again with `-Dphysics_f64=true`.
- The bench runs and prints its measurement; the figures go in the closing notes.

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (debug + ReleaseSafe)
- `zig fmt --check` green
- `zig build lint` green
- `commit-msg` hook green on every commit of the branch
- The full forge suite green under `-Dphysics_f64=true`

## Conventions

- **Branch:** `phase-1/forge/queries-shapecast-overlap`
- **Final tag:** `v0.11.10-queries-shapecast-overlap`
- **PR title:** `Phase 1 / Forge / Queries: shapecast, overlap, point query, closest point`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

**Threshold discipline.** A cast is pure geometry with one extra temporal parameter, which is twice the ground on which this discipline is usually lost. Every guard is at **true zero**. In particular the "no longer approaching" test: the reference compares the axis-direction product against `-1.0e-18f` so that the following division cannot overflow, but at true zero that division is never reached, and a denormal negative denominator overflows the step to infinity, hence the parameter past `max_distance`, hence a miss through the existing bound test. No NaN is reachable on that path, the numerator being strictly positive at that branch. Write that argument next to the guard. Same substitution as §1.11.2 against the absolute parallel guard of `RayAABox.h`. No geometric epsilon anywhere; distances compared exactly, including in the tie-break.

**The iteration ceiling is normative, not an implementation choice.** §1.11.11 carries the table. Returning a miss on exhaustion fabricates a false negative, therefore tunneling; returning the current parameter fabricates a contact announced **early**, never a missed one, which is the safe failure direction for the character controller of M1.1.12. The parameter grows from zero and is at every step a lower bound of the true time of impact, which is what makes the early answer conservative rather than merely wrong.

**Reference lineage, verified on source at `jrouwe/JoltPhysics@master`, `Jolt/Geometry/GJKClosestPoint.h`.** `CastShape` builds the Minkowski difference of B and the transformed A and calls its own `CastRay`, credited to *Ray Casting against General Convex Objects with Application to Continuous Collision Detection*, Gino van den Bergen. That `CastRay` has **no iteration ceiling at all** (`for (;;)`), and both of its non-convergence exits — the parameter failing to progress, and the simplex reduction failing after the single permitted restart — break out as a **hit at the current parameter**, the second with the comment that a state too close to resolve is returned as a hit. The radius-carrying `CastShape` overload keeps the support points on A and B separate, reconstructs `outPointB` from the simplex barycentrics **in every case including the full simplex**, and computes A's witness separately only when the parameter is zero — beyond that the two coincide and only B's is computed, as it carries fewer terms. The separating axis falls back to the **previous iteration's** axis when the radius sum is zero and the current axis collapses at convergence. In `Jolt/Physics/Collision/CollideShape.h`, `mPenetrationAxis` is documented as the direction to move shape 2 out of collision and the recommended contact normal is its negated normalisation — that is, the outward normal of the hit body, which is the Weld convention. Verify any further claim about the reference on the reference, never on a comment in this repository.

**Why `position = cast.origin` is refused at a zero time of impact.** The analogy with the ray does not hold. A ray whose origin is inside a shape has its origin *in the hit body* by construction; two overlapping volumes can have the cast shape's centre entirely outside the hit body. Returning the origin would yield a point that is neither a contact point nor a point of the collider, contradicting §13's public surface and the normal convention. The witness on B comes out of the same loop; no EPA is involved.

**Why `overlapAabb` needs `store`, and why the fat boxes are not the answer.** The trees store boxes fattened by `BroadphaseConfig.margin`, default `0.1 m`. Returning the candidate set would report bodies that do not overlap the query box, and — the decisive point — the error would be a function of a **tuning constant**: changing the margin would change a query's answer. The exact kernel is therefore the body's tight world AABB, which `BodyManager.bodyAabb` computes from the shape and the pose and which consequently needs `store`. The M1.1.9 signature was declared without it on the belief, recorded in that milestone's tag annotation, that the entry stops at the broadphase. It stops at AABB *granularity*, which is not the same thing (§1.11.12).

**`.shallow` is not an interior.** `GjkResult.closest_a` / `closest_b` are valid for `.separated` **and** `.shallow`, and `.shallow` means the cores are disjoint beyond the contact margin while the inflated surfaces touch or overlap — a real separation absorbed by the numeric margin. Collapsing it with `.deep` would return a zero distance for a measurably external point. Solidity is decided by `containsPoint`, upstream of any classification, and the surface projection then runs uniformly over both valid regimes (§1.11.13).

**`BodyId` encodes creation order, and this was measured, not argued.** On `main` at `deeabe7`, at `f32`, two unit spheres at `(20, ±0.5, 0)` against a ray from the origin along `+X` both return `19.133974`, bit-identical because the squared perpendicular offset is `0.25` on both sides; the closed form is `20 − √0.75 = 19.133974596`, whose nearest `f32` is `19.133974075`. Swapping the two creation orders changes the entity returned by `raycast` and by `raycastAll` truncated to one slot. A symmetric scene is not a measure-zero case: mirrored cover or props on a grid produce it. The named residual stands: two bodies carrying the **same** entity at an exactly equal distance fall back on `BodyId` and are not invariant.

**The traversal starts at the AABB centre, not at the shape's position.** They coincide for the three shapes in the store, whose local AABB is centred on the origin, but that is a property of those shapes and not of the model; a decentred volume would break the equality. Write it on the centre, with the line saying why the two agree today. The returned parameter is the same either way, the sweep being a pure translation.

**Do not reach for a single swept AABB.** Enclosing the whole sweep in one box and calling the overlap traversal loses the bound entirely and turns a sub-linear query into one linear in the proxies of the swept corridor.

**Do not write analytic swept fast paths.** Out of scope, and the reason is the M1.1.4 precedent: a fast path owes a geometric-equivalence proof against the generic path, which is a body of work of its own.

**`subshape_id` and `cast_subshape_id` stay 0.** One shape is one body until compounds (M1.1.20). The material is derived at service level from the sub-shape identity and never carried in the solver result (§1.11.7).

**File length.** The `query/` package exists so that no file in it approaches the 500-line review guideline of `engine-zig-conventions.md`. `broadphase.zig` and `epa.zig` are already conscious brief-driven overages; a third avoidable one is not justified.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone.*

## Gates

Seven gates, each ending in a push and a STOP+GO verdict from Guy. No two gates
are chained without a GO.

| Gate | Content |
|---|---|
| E1 | Ordering key `(distance, entity, BodyId)` — `BodyManager.entity`, `hitLess` and `ClosestCollector` corrected, the two RED tests seen **red on `main`** first and the redness reported |
| E2 | `Aabb(T).inflate` + `Bvh(T).queryCast` / `Broadphase(T).queryCast`, `queryRay` re-expressed at zero extent with bit-identity proven on the M1.1.9 suite, swept traversal tests with the discrimination guard |
| E3 | `pipeline/narrowphase/shapecast.zig` — the GJK cast kernel, named ceiling with the §1.11.11 fallback, closed-form oracles, `raycast.zig` dependency discipline |
| E4 | `BodyManager` adapters — `castShapeBody`, `overlapShapeBody`, `containsPointBody`, `closestPointBody`, stale-safe, mirroring `raycastBody` |
| E5 | `query/` package — the move split reviewably, the five bodies at `Real` with their mirror types, `overlapAabb` gaining `store`, the frozen-signature pin rewritten onto the `Real` forms |
| E6 | `tests/shapecast_test.zig` + `tests/overlap_test.zig` — the full acceptance suite, RED-first, green at `f32` and under `-Dphysics_f64=true`, debug and ReleaseSafe |
| E7 | `bench/forge_3d_shapecast.zig` measured and reported with the leak check proven to bind, inherited envelopes re-measured, §3.6.1 local audit, language audit, `CLAUDE.md` `Current state` + §3.4 patch, closing notes, PR |

## Specs read

Read in the order the FROZEN § Specs to read first prescribes, on 2026-07-26.

| # | Document | Scope read | Done |
|---|---|---|---|
| 1 | `engine-physics-forge.md` | §1.11 in full (§1.11.1–§1.11.9 including §1.11.4 bis, then the sections authored for this milestone: §1.11.10 swept traversal, §1.11.11 cast internal model + termination table, §1.11.12 overlaps, §1.11.13 closest point, §1.11.14 results not ranked by distance), then §1.1, §1.2, §3 in full, §4, §13 in full, §17 | 2026-07-26 |
| 2 | `engine-tier-interfaces.md` | §1 — the `assertFn` Queries block and its types (`PhysicsQueryFilter`, `RaycastQuery`, `ShapeCastQuery`, `OverlapQuery`, `RaycastHit`, `ShapeCastHit`, `ClosestPointResult`, `Transform`) | 2026-07-26 |
| 3 | `engine-phase-1-criteria.md` | C1.1 including the query list (whole document read) | 2026-07-26 |
| 4 | `engine-phase-1-plan.md` | the M1.1.10, M1.1.11 and M1.1.19 rows, in the M1.1 table | 2026-07-26 |
| 5 | `engine-physics-forge-2d.md` | §7 (whole document read) — symmetry only, nothing 2D touched | 2026-07-26 |
| 6 | `engine-zig-conventions.md` | §1 naming (file-struct vs file-namespace), §2 `root.zig` convention, §3 unmanaged-first + the Tier-1 root exception, §15 formatting, §16 comptime strategy-interface pattern, §19 file-length guideline (≤ 500 lines, Review) | 2026-07-26 |

**Relay verified before any work.** The copy read carries §1.11.10 through §1.11.14
— §1.11.14 « Résultats multiples non ordonnés par distance » at line 633 — plus the
§1.11.8 structural corollary on the solver-side entries and the corrected
`physics_overlap_aabb` comment in §13 ("s'arrête à la granularité AABB (§1.11.12) —
pas à l'ensemble candidat"). No relay hole; no STOP.

**§1.11.6 patch — opened at E2, CLOSED at E3.** At the E1 GO a corrected
`engine-physics-forge.md` of 1584 lines was designated; at E2 every copy reachable
from this repo was still the unpatched 1582-line version, byte-identical at md5
`0b0943457221e6c8e7bb554e2d61b76b`, so the read list was not re-attached and E2
proceeded on §1.11.10, which the patch does not touch. The patched file arrived for
E3 at `Devlab/weldengine/weld-spec/engine-physics-forge.md` — 1584 lines, md5
`37a014f8eadad217804747b58e90c8e8`, carrying the decisive witness string « fait
autorité sur ce paragraphe » exactly once. **Read and re-attached** (2026-07-27): the
Départage paragraph now states `(distance, entité, BodyId)` and defers to §1.11.14 as
authoritative over itself, a second paragraph keeps `BodyId` as the engine's total
order wherever creation order does not enter the answer, and the retired formulations
are gone. It agrees with what E1 implemented; no repo change follows from it.

**Location note, worth carrying forward.** The stale copy under
`Downloads/M1.1.10-queries-shapecast-overlap/` was NOT updated and still reads 1582
lines. For this milestone the fresh mirror is `Devlab/weldengine/weld-spec/`. Compare
mtimes and line counts before trusting either.

## Execution log

### E1 — the ordering key `(distance, entity, BodyId)`

RED-first, in the four prescribed steps.

**Base.** Branched from `main` at `dd7fa1f`, pushed to `origin` before the first
commit. Note for the record: the FROZEN section cites `main` at `deeabe7`, which is
the **annotated-tag object** of `v0.11.9-queries-raycast`; it dereferences to commit
`dd7fa1f`, which is `main`'s tip. The two designate the same state — nothing to
correct.

**Step 1 — the two tests, run against intact production code.** Both were written
using only the M1.1.9 surface (no `BodyManager.entity`, which does not exist yet), so
they compile and run against unmodified production code. The scene is two unit
spheres at `(20, ±0.5, 0)` and a ray from the origin along `+X`, with the entity
indices **crossed against creation order** — the low entity at `−0.5`, the high one
at `+0.5` — and the scene built in both orders, so the `BodyId`-only rule and the
entity rule disagree on it by construction.

That RED state is a COMMIT (`1096ab6`, tests alone) rather than only a paste in this
log: `git checkout 1096ab6 && zig build test-forge-3d` reproduces it. The commit that
follows adds, on top of the fix, four read-back assertions that need the new getter —
`bm.entity` cross-checked against the descriptors, and the entity carried by each
returned hit. The assertion that was RED is unchanged by that addition; only
assertions were added, never relaxed, and the diff shows it.

RED confirmed, at BOTH precisions, on `an exact distance tie is broken by entity, not
by creation order`:

```
+- run test 259 pass, 1 fail (260 total)
error: 'tests.raycast_test.test.an exact distance tie is broken by entity, not by creation order' failed:
       expected 1, found 0
       .../tests/raycast_test.zig:1622: try testing.expectEqual(want, hit.body);
```

`expected 1` is the low-entity body (`BodyId` 1, created second); `found 0` is the
first-created body. That is the defect verbatim: the answer followed creation order.
The three preconditions **passed** in the same run, which is what makes the failure
mean something — the two `BodyId`s are ordered by creation (`first < second`), the two
kernel distances are bit-identical, and both equal the closed form asserted EXACTLY
(no tolerance).

The second test, `two bodies on the same entity fall back on BodyId`, was GREEN on
intact code and stays green: with entities equal the fallback *is* the `BodyId`, so
both rules agree there. It pins §1.11.14's named residual, including the
non-invariance itself — the two creation orders answer with bodies on opposite sides
of the ray, asserted rather than hidden.

**Guy's measurement reproduced, not presupposed.** An isolated probe evaluating the
kernel's own operations (`−(o·d) − √(r² − |w|²)` with `o·d = −20`, `r² = 1`,
`|w|² = 0.25`, every operand exact in binary):

| | value | bits |
|---|---|---|
| f32 | `19.133974`, widened `19.133974075317` | `0x41991261` |
| f64 | `19.133974596215563` | `0x4033224C28BD3D9B` |
| exact real | `19.133974596215562…` | — |
| f32 ulp at that magnitude | `1.907349e-6` | — |

The brief's `19.133974075` is confirmed digit for digit, and the f32 value sits
0.27 ulp below the real one. Because every operand is exact, the test asserts the
closed form with `expectEqual` rather than a tolerance.

**Step 2 — `BodyManager.entity(id) ?EntityId`.** The column has existed since M1.1.0
and was never exposed. Stale-safe like every sibling getter (`alloc.validate` →
`null`).

**Step 3 — the key corrected.** `hitLess` becomes `(distance, entity, BodyId)` over a
`entityKey` helper, and `ClosestCollector.add` now *calls* `hitLess` instead of
re-deriving the comparison inline — so `closest` and an `all` truncated to one slot
cannot disagree structurally, rather than by two hand-written comparisons agreeing.
Two design points, both inside the FROZEN scope's mirror-type mandate:

- **The solver-side `RayHit` gains `entity: EntityId`.** The alternative was passing
  the `BodyManager` into the comparator, which would turn each of
  `AllCollector`'s replace-worst comparisons into a store lookup. The public
  `RaycastHit` carries the entity too, so the mirror is not widened by holding it,
  and the key stays a pure function of two hits.
- **`entityKey` is `index` major, `generation` minor, and deliberately not
  `@bitCast`.** `EntityId` is a `packed struct(u64)` whose `index` occupies the LOW
  32 bits, so a bitcast would order by generation first — a layout accident. Both
  orders satisfy what §1.11.14 requires (a function of the entity identity alone);
  this one also reads the way entities are named. `EntityId.dead` sorts last with no
  special case.

**Both senses of the discrimination, measured.** Beyond the RED→GREEN transition, the
key was reverted to `BodyId`-only as a counter-factual with everything else in place:

```
+- run test 258 pass, 2 fail (260 total)
error: 'tests.raycast_test.test.equal distance is broken by the owning entity' failed:
error: 'tests.raycast_test.test.an exact distance tie is broken by entity, not by creation order' failed:
```

Both ordering tests bite; `two bodies on the same entity fall back on BodyId` stays
green, correctly, since the two rules coincide when entities are equal. `query.zig`
was then restored byte-for-byte and re-verified green.

**Step 4 — literal grep over the whole tree.** Live code and tests: **0 remaining**
occurrences claiming the tie-break is on `BodyId` alone. Six were corrected in place
— `query.zig` header, the `raycastAll` doc, the `ClosestCollector` and `AllCollector`
comments, the E6 section header of `raycast_test.zig`, and the M1.1.9 test
`equal distance is broken by the smaller BodyId`. That last one was **retargeted, not
deleted**: with entities `0` and `1` attached to creation order it passed under either
rule and so discriminated nothing — a control never seen to fail. Its entity indices
are now attached to the POSITIONS instead, which makes its radius-1.5 scene a second,
independent witness of the entity key, and the counter-factual above proves it now
bites. It is renamed `equal distance is broken by the owning entity`.

Two live mentions of `BodyId` in an ordering context survive, and both are true as
written rather than exempted: `raycast_test.zig` "Entities equal ⇒ the smaller
`BodyId` decides", which is the residual itself, and `rigid/island_manager.zig`
"Rank is the smallest member `BodyId`", which is the island partition's rank under
§1.8.1 and has nothing to do with a query.

Three occurrences remain, all reported rather than silently patched:

| Occurrence | Status |
|---|---|
| `briefs/M1.1.9-queries-raycast.md` (3 lines: acceptance, gate E4, the defect-injection table) | **Correct as history, not patched.** A closed brief records what its own milestone did; the precedent is M1.1.9's own ruling not to retro-patch the closed M0.7 acceptance line. |
| `CLAUDE.md` line 68, the `v0.11.9-queries-raycast` row: "the exact-distance tie-break on the smaller `BodyId`" | **Correct as history, superseded by M1.1.10.** The FROZEN § Files to create or modify assigns `CLAUDE.md` to the closing gate with content supplied by Claude.ai, so E1 does not touch it. Carried to E7. |
| `engine-physics-forge.md` §1.11.6 (KB, out of repo): "le plus petit `BodyId` gagne … `all` trie sur la même clé composite `(distance, BodyId)`" | **Superseded in the same document.** §1.11.14 states in as many words that without it §1.11.6 is false on an exact distance tie, so the normative text is not self-contradictory — but §1.11.6 carries no forward pointer to §1.11.14. Flagged for the E7 KB reconciliation. |

**Gates.** f32 and `-Dphysics_f64=true`, debug and ReleaseSafe: 260/260 in all four
legs. `zig build` clean, `zig build lint` green, `zig fmt --check src/ bench/ tests/`
green.

### E2 — `Aabb(T).inflate` and the swept-volume traversal

**`Aabb(T).inflate(half_extents)`** — `min − e`, `max + e`. An addition, no constant.
Its doc states the load-bearing property and an inline test pins it in both
directions: `inflate` is the Minkowski sum of the box with `[−e, e]`, hence exactly
the set of CENTRES at which a box of half-extents `e` overlaps it. That equivalence is
what makes the traversal exact rather than approximate, so it is asserted over a 13³
lattice of centres straddling every face, edge and corner, with counters proving both
verdicts occurred — an all-true sweep would satisfy the equality and prove nothing.

**`Bvh(T).queryCast(ray, extent, collector)` and `Broadphase(T).queryCast`** —
additive in the strict sense: the collector contract, the near-first descent, the
visited accounting and `Aabb.rayInterval` itself are untouched; the only change is
that `castInterval` hands the slab test `node.aabb.inflate(extent)`. `queryRay` is now
one line, `queryCast` at `Vec3T.zero`, on both types.

The `extent`/`c₀` contract is written on `queryCast`, since the traversal is where the
precondition has to be stated and E5 is where it will be satisfied: `extent` is the
half-extents of the cast shape's initial world AABB, `ray.origin` is the CENTRE of
that AABB and not the shape's position, and the box is a constant of the query because
the sweep is a pure translation. The line saying why the two agree today is there too
— the three shapes the store builds have a local AABB centred on the origin, which is
a property of those shapes and not of the model; a decentred volume breaks it.

**The `−0.0` argument, written at the re-expression site** rather than left for
review: `min − 0` is exact and sign-preserving, `max + 0` maps a `−0.0` bound to
`+0.0`, and nothing downstream distinguishes the two zeros — the parallel-slab test
compares with `<`/`>`, `@min`/`@max` treat `±0.0` as equal, the `enter > exit`
rejection and `accepts`'s two comparisons are sign-of-zero-blind, the near-first
`e1 <= e2` compares values that are equal, and the `0 · inf` NaN repair is
unconditional either way.

**The refactor's proof rests on counts measured BEFORE it.** Baselines were read off
the pre-refactor traversal through a throwaway probe, on the two existing scenes:

| Scene | ray | all: visited / collected | closest: visited / collected |
|---|---|---|---|
| 800-box cloud, seed `0x2A11D0C5`, margin 0 | `(−50,0,0)→+X` | 91 / 1 | 77 / 1 |
| | `(−50,−50,−50)→(1,1,1)` | 181 / 3 | 125 / 1 |
| | `(0,0,0)→(1,2,−3)` | 115 / 2 | 89 / 1 |
| | `(−50,100,0)→+X` (misses) | 1 / 0 | 1 / 0 |
| | `(50,0,0)→−X` | 91 / 1 | 91 / 1 |
| 200-box line, far-to-near | `(−10,0,0)→+X` | 399 / 200 | 17 / 1 |

The 399 / 17 of the line are the figures the M1.1.9 pruning test already records in
its own comment — **re-read off the running code, not copied from the comment**. These
are the only exact visited-count literals in the suite, and they are legitimate here
for one reason: the claim is "unchanged by this refactor" on a scene whose creation
order is fixed on purpose, which is not what §1.11.6 forbids asserting.

**A count and a set were NOT enough, and that was measured.** The first version of the
bit-identity pin compared counts and collected sets only. Counter-factual (b) below —
delegating at an extent of `0.001` instead of zero — left every count and every set
unchanged, because the cloud's boxes are far apart and the line's are spaced 4; the
M1.1.9 suite caught it, on its one ulp-sensitive assertion, and my pin did not. The
pin now carries that sensitivity itself: one box entered at exactly `t == 10`, driven
through both entries at a bound of `10` and at `10 − 1 ulp`, plus the exact entry
parameter. Re-running the counter-factual then failed the pin as well.

**Three counter-factuals, each isolating what it was written for.** Every one was run;
none is argued.

| Counter-factual | Result |
|---|---|
| (a) `castInterval` drops the inflation | 263/266 — fails `collects every proxy the swept box crosses`, `the extent widens the candidate set by exactly the Minkowski sum`, `visits all four layer trees`. The bit-identity pin correctly stays green: at a zero extent there is no inflation to drop |
| (b) `queryRay` delegates at extent `0.001` | 264/266 — fails `queryRay treats the collector bound as closed` (M1.1.9) **and** `queryRay at zero extent is bit-identical to M1.1.9`. Before the strengthening above, only the first fired |
| (c) both `collector.maxDistance()` reads replaced by `inf` | 261/266 — fails `queryCast prunes with the collector bound` (the mandatory discrimination guard, `visited_closest < visited_all`, **bit**), `queryRay prunes with the collector bound`, `queryRay treats the collector bound as closed`, `queryRay node count grows logarithmically`, and the bit-identity pin |

The two new `Aabb` tests were likewise proven to be COLLECTED and not merely written:
breaking `inflate` fails both (`33 pass, 2 fail` in the foundation module), so the
inline-test wiring is observed rather than assumed.

The extent test is two-sided by construction rather than by a large-extent
demonstration: a unit box whose Y span misses the ray by exactly 1.5 is admitted at
`e = 1.5` (face-inclusive, like every other `Aabb` predicate) and `1.6`, refused at
`0` and `1.4`, and inflation on an axis the gap is not on changes nothing — the
widening is per-axis, not a scalar radius.

**Gates.** 266/266 (260 + 6) in all four legs — f32 and `-Dphysics_f64=true`, debug
and ReleaseSafe. Full `zig build test` exit 0, zero `error:` lines. `zig build` clean,
`zig build lint` green, `zig fmt --check src/ bench/ tests/` green. The tests are
written on `Bvh(f32)`, as the M1.1.9 traversal suite already was, so the baked counts
are the same figure in both precision legs by construction.

**Carried in, per the E1 GO.** Brief header `Status` → `IN PROGRESS`. The `deeabe7`
line was already in the E1 log at the E1 signal and is unchanged: it names the value
as the annotated-tag object of `v0.11.9-queries-raycast` and gives the commit it
dereferences to, so no future reader hunts for a commit that does not exist.

**File lengths, reported not acted on.** `broadphase.zig` 898 → 981 lines (an
already-documented conscious brief-driven overage), `tests/broadphase_test.zig`
1048 → 1506. The `query/` package of E5 is where the 500-line guideline is actually
honoured, per the FROZEN Notes.

### E3 — the GJK cast kernel

**`pipeline/narrowphase/shapecast.zig`, 490 lines.** A ray march from the
configuration-space origin along `−d` against `C = core_A ⊖ core_B`, stopping when
the ray point comes within `r_sum` of `C`. `A ⊖ B` rather than `B ⊖ A` is a
deliberate choice and not a coin flip: it is the difference `minkowskiSupport`
already computes, so the `Vertex{w, support_a, support_b}` semantics — and with them
the witness reconstruction `Σ λ_i · support_b_i` — are reused exactly as the rest of
the narrowphase has them, at the price of one negation on the ray direction.

**`gjk.zig` IS imported, for `Simplex(T)` and nothing else.** The FROZEN scope says
the kernel "reuses `Simplex(T)`'s Voronoi closest-origin solver" and, one sentence
later, that it imports "`foundation` and the sibling `support.zig` only — never
`gjk.zig`'s LOOP". `Simplex(T)` lives in `gjk.zig`, so the two clauses cannot both be
read literally; the operative one is the enumerated item, which forbids the LOOP. The
repo settles it: `epa.zig` already declares "imports `foundation` (math) + the
sibling `support.zig` / `gjk.zig` ONLY" and uses `gjk_mod.Simplex(T)` without ever
calling `gjk()`. This file follows that precedent verbatim. Verified by grep: the
only `gjk_mod.` reference is `Simplex(T)`, and none of `manifold`, `epa`,
`weld_forge`, `body*`, `config` or `broadphase` appears.

**A DEFECT the oracles found, not a design choice defended after the fact.**
§1.11.11's row reads "Échec de réduction du simplexe après **l'unique restart
autorisé**", and I first implemented it literally: one restart per CALL. That is
measurably wrong. For a POINT core the Minkowski difference is a single point, so
EVERY support sample repeats and the march is nonetheless a perfectly healthy
plane-offset iteration; spending the budget once per call truncated a
sphere-against-sphere cast whose closed form is 7 at **6.952526**, after two
iterations, exiting `restart_exhausted`. The budget is therefore refreshed at each
ADVANCE — one restart per ray point, the reference's placement — and
`restart_exhausted` now fires only on a genuine stall: two repeats with no advance
between them. The same scene then converges 6.214835 → 6.952526 → 6.999764 →
7.000001 in four iterations. The measurement is in the code comment at the branch,
not only here.

**The interval row and the bound row of §1.11.11 conflict, and the conflict is
resolved in favour of the interval** — see Recorded deviations.

**`CastExit` is §1.11.11's termination table, one variant per row**, so no row is
unobservable and therefore none is untestable — `epa.zig`'s `EpaDiagnostics` model.
The zero-direction exit is a distinct eighth variant explicitly labelled as belonging
to the DOMAIN table and firing before the march begins, precisely so the seven
termination variants stay a faithful mirror, neither padded nor short.

**The ceiling is observable.** `castShapeBounded` exposes the ceiling and the
diagnostics for one reason: on the three cores the store builds, 32 is unreachable —
the hardest scene here converges in 4. A guard never seen to fire is a comment with
extra syntax, so the test drives ceilings 1, 2 and 3 through the SAME code path and
asserts the cap fired, that a HIT came back rather than a miss, that its parameter is
strictly below the converged one, and that the three are monotone increasing — the
lower-bound property the whole fallback rests on. Only the constant differs between
that run and production.

**Oracles.** Every expectation is a closed form derived in the comment above it.
Sphere/sphere at three obliquities including one whose direction is on no axis
(`d̂ = (3,4,0)/5`, `c = (6,8,4)`, `r_sum = 5` → `t = 7`), with the normal AND the
witness pinned and the coincidence of A's and B's witnesses asserted rather than
assumed. Box/box on a face (`t = 8`), an edge (B rotated 45° about Z → `t = 9 − √2`,
witness on the vertical edge) and a vertex (B rotated so its local diagonal faces −X
→ `t = 9 − √3`, a UNIQUE witness pinned in all three coordinates). Capsule against a
box, against a parallel capsule (non-unique witness, only its X pinned) and against a
crossed one (unique witness, all three pinned). The closed interval at exactly 8 and
at 8 − 1 ulp — exact comparisons, no tolerance, on the one scene whose advance lands
on the answer in a single step. `max_distance == 0` both ways: a miss on a separated
pair and a hit at distance 0 on an overlapping one. A zero direction, a DENORMAL one
whose square underflows (asserted underflowing) and a huge one whose square overflows
(asserted overflowing), the last two agreeing with the unit direction bit for bit on
the distance. And the discriminating test of the whole design: the same rounded-box
geometry returns `error.UnsupportedShape` through `rayShape` and a correct answer
through `castShape`, both asserted in the same test.

**Far field, §1.11.4 bis.** An OBLIQUE sweep — the aligned one proves nothing, the
cancellation being identically zero there — over 1e2…1e8 with a construction whose
answer is distance-independent (`t = D − √0.75`, `normal · d̂ = −√0.75` at every D).
The NORM is asserted tight at every distance without exception; the ORIENTATION only
inside the envelope where the scalar still resolves the geometry, with a
distance-dependent bound, and the count of orientation checks is asserted non-empty
so that half really ran. A constructed hit that misses fails hard naming the
distance — a `continue` would let the false negative through the test written to
catch it.

**Seven counter-factuals, all run, all bit.**

| Counter-factual | Result |
|---|---|
| The restart budget spent once per CALL (the literal reading) | Found the real defect: sphere/sphere answered 6.952526 for a closed form of 7 |
| Inflation dropped from the plane offset | 270/275 — sphere/sphere, capsule, ceiling, the rounded-box test and the far-field sweep |
| Bound test made inclusive (`>=`) | 274/275 — `max_distance is a closed interval` alone |
| Ceiling returns a miss instead of a hit | 274/275 — `the iteration ceiling returns a hit at the current parameter` alone |
| Normal left as A's outward (the `.neg()` dropped) | 269/275 — all six hit-bearing tests |
| Witness inflation offset dropped | 273/275 — sphere/sphere and capsule, the two with a pinned witness |
| Zero-direction guard removed | 271/275 + 1 CRASH — the domain test aborts on the assert, three others fail |

**Gates.** 275/275 (266 + 9) in all four legs — f32 and `-Dphysics_f64=true`, debug
and ReleaseSafe. Full `zig build test` exit 0, zero `error:` lines. `zig build`
clean, `zig build lint` green, `zig fmt --check src/ bench/ tests/` green.
`shapecast.zig` 490 lines, `tests/shapecast_test.zig` 511 — the kernel stays under
the 500-line review guideline.

### E4 — the `BodyManager` adapters

Four adapters, no new geometry: `castShapeBody`, `overlapShapeBody`,
`containsPointBody`, `closestPointBody`. Each resolves a body's pose and support
shape through `store` and calls the exact kernel, mirroring `raycastBody` /
`gjkPair` / `collidePair`.

**`*const BodyManager` on all four**, verified by grep on the four signatures rather
than by intent. A query cannot wake anything and that is now structural, not a
convention someone has to remember.

**Two ways to be gone, both checked.** Every adapter validates the HANDLE and then
the SHAPE, and answers null for either. A body whose handle is live but whose shape
has been destroyed is a different failure from a removed body, and an adapter that
validated only the first would read a freed shape.

**The core + inflation-radius convention at the wiring point.** The body's `Shape`
becomes a `SupportShape` through the one shared `shape_mod.supportShape`, the same
translation `gjkPair`/`collidePair`/`raycastBody` use, and nothing on this path adds
a margin of its own. `overlapShapeBody` in particular introduces NO threshold: the
predicate is "the GJK regime is not `separated`", whose contact margin is GJK's own
— a second margin proper to queries is exactly what §1.11.12 forbids.

**Frames, and why the cast adapter differs from `raycastBody`.** `raycastBody`
returns the BODY's local frame, which differs per candidate; the cast kernel's frame
is the QUERY shape's, a constant of the whole traversal, so returning it would hand
the caller a contract in a frame that has nothing to do with the body it just asked
about. The cast and closest-point adapters therefore return WORLD space, through two
small dedicated types — `BodyCastHit`, `BodyClosestPoint` — rather than reusing the
kernel's `CastHit`, whose own doc says "A's frame". Two frames, two types, no quiet
reinterpretation.

**`closestPointBody` follows §1.11.13 in its order.** Solidity FIRST, by the same
membership predicate the point query uses, upstream of any classification: an
interior point — boundary included — is at distance 0 and its closest point is
itself. Outside, GJK between a degenerate point core and the body, then
`distance = max(0, core − r_b)` and
`position = closest_b + r_b · normalize(closest_a − closest_b)`. A hard core takes no
projection at all (it IS its own surface), so no normalisation is even reached there;
where one is, it is guarded at true zero because `foundation`'s `normalize` is
unguarded — documented as unreachable after the membership gate, kept because the
alternative to answering is a NaN position. `.deep` after that gate is likewise
unreachable and likewise handled, the two tests sitting on opposite sides of GJK's
noise band.

`entity()` was not redeclared; it has existed since E1.

**Four counter-factuals, and the two that matter read together.**

| Counter-factual | Result |
|---|---|
| The KERNEL broken (time of impact nudged by 0.1 %) | 270/280 — **all three E4 adapter tests fall with it**, alongside the seven E3 oracles. The adapters do not survive a broken kernel |
| The ADAPTER perturbs the kernel's answer by ONE ULP | 279/280 — **only** `the adapters call the kernel, bit for bit` falls. Nothing else notices |
| `containsPointBody` skips the stale-SHAPE check (returns `false` instead of null) | 279/280 — the stale test alone, so the shape check is load-bearing and distinguishable from the handle check |
| An adapter wakes the body it is asked about (`@constCast` + `wakeBody`) | 279/280 — the sleeping test alone |

The first two are the pair Guy asked for and they say different things. Breaking the
kernel proves the adapters really run it. Perturbing the adapter by a single ulp
proves the differential detects a REIMPLEMENTATION at the finest possible grain — and
note that it is deliberately blind to a uniformly wrong kernel, since it compares the
adapter against that same kernel; catching that is E3's oracles' job, and they do.

**Tests.** The bit-for-bit differential runs all four adapters against direct kernel
calls in a scene arranged so the frame conversions are the IDENTITY, which is what
makes bit-equality the right assertion instead of an optimistic one — plus an
independent closed form beside it. The conversions themselves are then exercised by
two rotated scenes: a 3×1×1 box rotated 90° about Z (whose −X face is at 9, not 7,
and whose membership answers flip unrotated), and a capsule cast rotated so its
Y-segment core lies along world X (8.5 upright → 6.5 laid → 5.5 with a unit origin
offset). The sleeping test drives the published harness to a real island sleep and
checks `isSleeping` before AND after each of the four adapters, then one more tick.

**Fix-as-you-go, caught by the f64 leg.** A cast rotation is at the SOLVER scalar
(`Quatr`) while a descriptor rotation is `f32` by design (§1.11.8). The two coincide
at f32 and not at f64, so one test built a cast rotation with `Quatf` and compiled in
one leg only. The comment now says which is which and why.

**Gates.** 280/280 (275 + 5) in all four legs. Full `zig build test` exit 0, zero
`error:` lines. `zig build` clean, lint and fmt green.

## Recorded deviations

### RD-1 — §1.11.11's bound row is applied STRICTLY — RESOLVED IN THE SPEC

**Status: resolved.** `engine-physics-forge.md` was patched to **1588 lines**
(md5 `54b8a4f7d64e8e2ca33152ea5dc585a7`, decisive witness string `dépassement
strict`, absent from both the 1584 and the 1582). §1.11.11's table now reads
« dépasse » and a paragraph beneath it carries the argument: the lower-bound
property, the consistency with §1.11.4's closed interval, the reachability of the
equality, and the assumed divergence from the reference's `>=` — "La référence teste
`>=` et son intervalle est donc semi-ouvert ; Weld ne la suit pas ici". The kernel
needed no change: it was written strict. What follows is the record of the
divergence as it stood when the code was written.

§1.11.11 states two rules that cannot both hold at the parameter's boundary. Its
parameterisation table: "Intervalle **fermé** `[0, max_distance]` ; un contact
exactement à `max_distance` compte". Its termination table: "Le paramètre **atteint
ou dépasse** `max_distance` → miss". A contact whose time of impact is exactly
`max_distance` satisfies the first and is refused by the second.

The kernel implements `next > max_distance → miss`, strictly. The reasons, in order
of weight:

1. The parameter is a LOWER BOUND of the true time of impact, so `>` is the only test
   that proves anything: exceeding the bound proves the contact is out of range,
   whereas reaching it proves nothing yet. Left to run, such a march either converges
   at `max_distance` — a hit, which the closed interval requires — or advances
   strictly past it, a miss.
2. The advance can land EXACTLY on the answer, so the equality case is reachable and
   not measure-zero prose: the box-against-box face scene does it in one step, which
   is what the closed-interval test exploits.
3. The brief's own acceptance criteria mandate `test "max_distance is a closed
   interval"` — "a contact exactly at `max_distance` counts". An inclusive bound test
   fails it, as the counter-factual above confirms.

The deviation was therefore from one row of the spec's termination table, in favour
of the interval rule stated in the same section and of the brief's own required test.
Nothing else in the table is touched — the other six rows are implemented as written,
one `CastExit` variant each. The 1588-line patch cited above closed it.

### RD-2 — the FROZEN scope's import set excludes where `Simplex(T)` lives

The FROZEN § Scope bullet for `shapecast.zig` states two things that cannot both hold
literally. Quoted verbatim, and NOT mutated:

> Reuses `Simplex(T)`'s Voronoi closest-origin solver and `support.zig`
> (`SupportShape`, `RelativePose`, `minkowskiSupport`); it does **not** call `gjk()`.
> Dependency discipline identical to `raycast.zig` and `fast_paths.zig`: imports
> `foundation` and the sibling `support.zig` only — never `gjk.zig`'s loop,
> `manifold.zig`, `epa.zig`, `weld_forge`, `body*.zig`, `config.zig` or
> `broadphase.zig`.

`Simplex(T)` is declared in `gjk.zig` (`gjk.zig:51`), re-exported by the package
façade as `root.zig:38`. Reusing it therefore REQUIRES importing `gjk.zig`, which the
"imports … only" clause excludes. The two clauses are inconsistent as written.

**Resolution.** The operative restriction is the enumerated item, which forbids
`gjk.zig`'s **loop** and not the module: `raycast.zig` and `fast_paths.zig` are named
as the discipline's model, and neither needs a simplex solver, so the "only" clause
describes their import set rather than legislating one for a file that does. The repo
settles it independently: `epa.zig` already declares "Imports `foundation` (math) +
the sibling `support.zig` / `gjk.zig` ONLY" and consumes `gjk_mod.Simplex(T)` at four
sites without ever calling `gjk()`. `shapecast.zig` follows that precedent verbatim,
and its header states the same thing in the same words.

**Verified, not asserted.** Grep over the file: the imports are exactly `std`,
`foundation`, `support.zig`, `gjk.zig`; the only `gjk_mod.` reference is
`Simplex(T)`; and none of `manifold`, `epa`, `weld_forge`, `body*`, `config` or
`broadphase` appears anywhere.

**The formulation that would have avoided this**, for the next brief: forbid the
`gjk()` LOOP, not the `gjk.zig` module — "never calls `gjk()`; imports `gjk.zig` for
`Simplex(T)` alone, as `epa.zig` does". A simplex solver is not a GJK descent, and
the import list should not have to pretend otherwise.

## Closing notes
