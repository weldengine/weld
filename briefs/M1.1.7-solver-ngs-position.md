# M1.1.7 — Forge 3D: NGS position solver

> **Status:** CLOSED
> **Phase:** 1
> **Branch:** `phase-1/forge/solver-ngs-position`
> **Planned tag:** `v0.11.7-solver-ngs-position`
> **Dependencies:** M1.1.0–M1.1.6 (base = HEAD of `main` at `v0.11.6-solver-sequential-impulses`)
> **Opened:** 2026-07-25
> **Closed:** 2026-07-25

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

Eighth core sub-milestone of the M1.1 rigid arc (M1.1.0–15, `PhysicsModule` freeze at M1.1.15). Delivers the **NGS position solver** — the position-correction answer the spec mandates for the rigid branch (`engine-physics-forge.md` §1.1, §1.7, §1.7.2) and the **first consumer** of the per-point `penetration` the M1.1.6 constraints carry unused.

M1.1.6 is a velocity solver with **no positional bias of any kind** (no Baumgarte, no split impulse); its accepted interim artifact is a resting body whose penetration is bounded at its at-impact value and never resorbed. M1.1.7 removes that artifact. The plan's acceptance criterion is stacking stability (`engine-phase-1-plan.md`, M1.1.7 line: "Empilement de boxes stable (pas de jitter/enfoncement)").

The design is the Jolt lineage (`ContactConstraintManager::sSolvePositionConstraint`, `AxisConstraintPart::SolvePositionConstraint`, `PhysicsSystem::JobSolvePositionConstraints`), cross-checked against Box2D's position solver. Pseudo-impulses are integrated straight into the pose and the velocity change is discarded (Catto, GDC 2007 — the Baumgarte term is split out of the velocity solver precisely so it adds no momentum). The KB spec section §1.7 was authored and re-uploaded **before** this brief; it is normative for everything below.

## Scope

- **`SolverConfig` relocation + additive fields (E1).** `SolverConfig` moves from `rigid/velocity_solver.zig` to a new `rigid/solver_config.zig`: one struct shared by two solvers belongs in neither. `velocity_solver.zig` imports it; `rigid/root.zig` re-exports from the new owner. Call sites use `rigid.SolverConfig` and are untouched. Additive fields, spec §1.7.2 table: `position_iterations: u32 = 3`, `penetration_slop: Real = 0.005`, `position_correction_factor: Real = 0.2`, `max_penetration_correction: Real = 0.2`. Existing fields, defaults and semantics unchanged (`velocity_iterations = 8`, `restitution_threshold = 1.0`). Entry validation on the position path, same debug-assert discipline as M1.1.6's `restitution_threshold`: all four finite, `penetration_slop >= 0`, `max_penetration_correction >= 0`, `position_correction_factor` in `[0, 1]` (a factor above 1 over-relaxes and can diverge).
- **Surface anchors in `prepare` (E1).** `ConstraintPoint` gains `local_anchor_a: Vec3r` and `local_anchor_b: Vec3r`; `ContactConstraint` gains `local_inv_inertia_a: Mat3r` and `local_inv_inertia_b: Mat3r`. `prepare` reconstructs the two surface points from the manifold point and its penetration — `surface_a = position + 0.5 * penetration * n`, `surface_b = position - 0.5 * penetration * n` (exact: the manifold point IS the midpoint of the two surface points, `pipeline/narrowphase/manifold.zig` `ContactPoint` doc + the clipping and point-core producers) — and stores each in its own body's local frame (`conj(q) * (surface - x)`). `local_inv_inertia_*` is copied from `MotionProperties.local_inv_inertia` (pose-invariant), so the position pass never calls `motionProperties` in its inner loop. The existing world lever arms `r_a`/`r_b`, effective masses and `v_n⁻` capture are untouched — the velocity solver's inputs do not change. `penetration`'s doc comment updates: consumed here, at `prepare`, to derive the anchors; never re-read from the frozen manifold during the iterations.
- **Pose mutators on `BodyManager` (E1).** Additive `setPosition(id, Vec3r)` / `setRotation(id, Quatr)`, stale-safe (`isValid` guard), mirroring the M1.1.5 velocity setters. **INTERNAL** to `forge_3d` — like `addTorque`/`setAngularVelocity`, they are not part of the day-1 `PhysicsModule` surface; flagged for the M1.1.15 freeze review.
- **`rigid/position_solver.zig` (E2).** New sibling file. `solvePositionRange(bm: *BodyManager, constraints: []ContactConstraint, from: usize, to: usize, cfg: SolverConfig) PositionSolveResult`. Same range-shaped form as `solveRange` — the M1.1.8 island seam (contract (d)). Import discipline identical to the sibling files: `foundation`, `weld_forge`, `../config.zig`, `../body_manager.zig`, `contact_constraint.zig`, `solver_config.zig`. Never `broadphase.zig`, never any `pipeline/` file.
  - **Normal FIXED in world for the whole pass.** `c.normal` is read as-is and never re-rotated. Normative reason (spec §1.7.2): the frozen manifold does not carry the reference-face owner, and that owner is **not** necessarily A — `manifold.zig` selects it by alignment with the contact axis (`a_is_ref = align_a >= align_b`; for a sphere against a box the reference is the box). Attaching the normal to a body would require extending the frozen `ContactManifold`.
  - **Per iteration, per point, in the velocity solver's order** (constraints ascending by pair key as `build` sorted them, points in manifold order): world anchors from the CURRENT poses; `sep = max((p_b - p_a) · n + penetration_slop, -max_penetration_correction)`; `if (sep >= 0) continue`; lever arms from the midpoint of the two current anchors (`p = 0.5 * (p_a + p_b)`); world inverse inertia rebuilt from the **current** rotation (`R * local_inv_inertia * Rᵀ`) per body; `lambda = -position_correction_factor * sep / k(n, r_a, r_b)` with `k` the normal effective mass at the current geometry; `P = lambda * n` applied immediately to both poses (Gauss-Seidel). The clamp is on the **error** in, not on the correction out (Jolt `mMaxPenetrationDistance` semantics); the `sep >= 0` gate replaces any upper clamp.
  - **Orientation:** `dtheta = ±I⁻¹ * (r × P)` applied by the first-order linearized rule `q <- normalize(q + 0.5 * (dtheta_quat ⊗ q))`, `dtheta` world-space on the LEFT — the same rule as `integratePositions`. No `|dtheta|` division, therefore no absolute guard (M1.1.4 threshold discipline). Deliberate divergence from Jolt's exact axis-angle `SubRotationStep`, which needs an absolute `|dtheta| > 1e-6` guard because that routine is shared with kinematic pose driving (large deltas) — a regime absent from a position solver, where the angular delta is bounded by `max_penetration_correction` over the lever arm. Do NOT factor a shared helper with `pipeline/integration.zig`: `rigid/` must not import `pipeline/` outside the narrowphase facade, and the integrator has a flagged additive option (exact non-linearized orientation integration) the position solver would not follow.
  - **True-zero pose-write guards.** Static and kinematic bodies zero the correction naturally (`inv_mass == 0`, `local_inv_inertia == 0`) — no `body_type` branch. The pose write is nevertheless guarded at EXACT zero (position delta and rotation delta each strictly non-zero) because `normalize` on an already-unit quaternion is not bit-neutral: a non-dynamic body must come out **bit-unchanged**.
  - **Termination.** An iteration that applies no correction at all ends the pass (Jolt semantics: `if (!applied_impulse) break`). No convergence-threshold constant of any kind.
  - **`PositionSolveResult { iterations_run: u32, min_separation: ?Real }`.** `min_separation` is `null` when no point was evaluated. Telemetry only — never a control input. Feeds `get_solver_iterations_stats` (`PhysicsDebugProvider`, spec §1.9); no other consumer is claimed at M1.1.7.
- **Cycle insertion (E3).** The test harness's normative per-tick cycle gains step (6) between `integratePositions` and `storeContacts`, per spec §1.7: `... (5) integratePositions -> (6) solvePositionRange -> (7) storeContacts + endTick -> (8) proxy updates`. The harness header's normative cycle comment is updated to the spec's numbering. Production `step()` stays M1.1.15.
- **M1.1.6 acceptance-test tightening (E3).** `tests/solver_test.zig`, `test "box dropped on a static ground comes to rest without sinking (e = 0)"`: the "penetration bounded by its at-impact value, absolute non-recovery accepted" statement is now obsolete. Replace it by a **recovery** statement — resting penetration `<= penetration_slop` — and tighten the envelope to `y_final` in `(1.0 - 2 * penetration_slop, 1.0 + 1e-4)` (analytic rest is `ground_top 0.5 + box_half 0.5 = 1.0`; NGS converges to `1.0 - penetration_slop` from below). RED-first: write the tightened assertion before the solver exists and watch it fail. **If the converged value falls outside that envelope, that is a STOP requiring analysis — never a widened envelope.** Any other existing test whose envelope must move is reported in its gate verdict with the measured before/after, never silently adjusted.
- **Additivity seam pin (E3).** `position_iterations = 0` performs no correction whatsoever (early return) and therefore reproduces the M1.1.6 resting behaviour exactly — penetration bounded at its at-impact value, unrecovered. Pinned by a dedicated test, so the M1.1.6 contract stays observable after M1.1.7.
- **Acceptance suite (E4).** New `forge_3d/tests/position_solver_test.zig`, pinned in `forge_3d/root.zig`'s comptime block (no `build.zig` change — `zig build test-forge-3d` picks it up through the module).

## Out of scope

- Island manager, sleep detection, parallel solving (M1.1.8). The range-shaped entry and the per-range termination are the seams; nothing island-aware is built here. **Documented consequence:** at M1.1.8 each island range terminates independently, which changes the aggregate result versus one global range — that is the intended semantics (Jolt iterates position steps per island) and a global termination would break per-island equivalence.
- Joints (M1.1.16–18). Jolt's per-island position-step count raised by constraints is an additive seam, not built here.
- `PhysicsWorld` / `step()` orchestration / `PhysicsModule` instantiation / ECS Transform sync (M1.1.15).
- Raycast / shapecast / overlap / point queries (M1.1.9–10). CCD / speculative contacts.
- Shapes beyond sphere/box/capsule (M1.1.11+). `forge_2d`.
- Per-shape / per-face `PhysicsMaterial` implementation (precedence already recorded and now specified in §4; implementation lands with per-shape materials).
- **The M1.1.4 clip-recentring precision boundary — examined and NOT absorbed here.** `sep` is a difference of two nearly-equal world points, so its absolute noise is `~floatEps(Real) * |x|`: at f32 and 10 km that is ~1.2 mm against a 5 mm slop (margin ~4x, positive). This is the same structural worldspace limit the narrowphase already documents and answers with the f64 comptime flag (spec §1.5, §1.7.2) — not a new class. It is **characterized in the spec and pinned by a test** (far-from-origin stack, below); the clip-recentring fix stays a future precision hotfix upstream in `generateManifold`.
- Any reintroduction of a positional bias in the velocity solver. A Baumgarte term or split impulse found in `velocity_solver.zig` is a **STOP** (contract (a)).
- Benchmarks. Absolute perf targets land with the full `step` (M1.1.3/4/5/6 precedent).

## Specs to read first

1. `engine-physics-forge.md` — **§1.7 in full** (normative per-tick cycle, §1.7.1 velocity pass, §1.7.2 NGS: anchors, fixed normal, iteration, orientation, true-zero guards, termination, island seam, config table, slop rationale, worldspace precision characteristic), §1.2 (`rigid/` vs `pipeline/`), §1.5 (scope, f64 flag), §1.9 (`PhysicsDebugProvider`, `get_solver_iterations_stats`), §3 (manifold contract: `position` is the midpoint of the two surface points; `penetration` continuity; `feature_id` frame-stability through `collidePair`), §4 (combine rules, domains, body-vs-material precedence).
2. `engine-phase-1-plan.md` — M1.1 table: the M1.1.7 line and the downstream boundaries M1.1.8 / M1.1.9–10 / M1.1.11 / M1.1.14 / M1.1.15 / M1.1.16–18.
3. `engine-zig-conventions.md` — §13 (tests, lazy-analysis guard), naming and formatting.

## Files to create or modify

- `src/modules/forge/forge_3d/rigid/solver_config.zig` — **create** — `SolverConfig` (both halves) + the position-path entry validation.
- `src/modules/forge/forge_3d/rigid/velocity_solver.zig` — modify — import `SolverConfig` from its new owner; **no behavioural change**, no positional bias, existing inline tests untouched.
- `src/modules/forge/forge_3d/rigid/contact_constraint.zig` — modify — `local_anchor_a/b` on `ConstraintPoint`, `local_inv_inertia_a/b` on `ContactConstraint`, filled in `prepare`; `penetration` doc updated. Purely additive: no existing field, order or value changes.
- `src/modules/forge/forge_3d/rigid/position_solver.zig` — **create** — `solvePositionRange`, `PositionSolveResult`, the local first-order rotation helper.
- `src/modules/forge/forge_3d/rigid/root.zig` — modify — re-export `SolverConfig` from its new owner, `PositionSolveResult`, `solvePositionRange`; comptime pins for the two new files.
- `src/modules/forge/forge_3d/body_manager.zig` — modify — stale-safe `setPosition` / `setRotation` (INTERNAL).
- `src/modules/forge/forge_3d/root.zig` — modify — comptime pin for `tests/position_solver_test.zig`.
- `src/modules/forge/forge_3d/tests/solver_test.zig` — modify — cycle step (6), normative-cycle header, tightened resting assertions.
- `src/modules/forge/forge_3d/tests/position_solver_test.zig` — **create** — acceptance suite.
- `CLAUDE.md` — modify — §3.4 patch (closing gate; content authored by Claude.ai).
- `briefs/M1.1.7-solver-ngs-position.md` — **create** — this brief, committed verbatim as the first commit.

## Acceptance criteria

### Tests

Inline in `rigid/contact_constraint.zig`:

- `test "prepare stores body-local surface anchors that reproduce the penetration"` — RED-first. Reconstructing the world anchors at the prepare-time pose gives `(p_b - p_a) · n == -penetration` within tolerance; the stored anchors are pose-invariant (re-pose both bodies rigidly by the same transform, re-prepare, assert the local anchors are unchanged).
- `test "prepare caches the pose-invariant local inverse inertias"` — matches `MotionProperties.local_inv_inertia`; zero for static and kinematic bodies.

Inline in `rigid/solver_config.zig`:

- `test "solver config position defaults"` — `3` / `0.005` / `0.2` / `0.2`, and the velocity half unchanged (`8` / `1.0`).

Inline in `rigid/position_solver.zig`:

- `test "position solve resorbs penetration down to the slop"` — two overlapping spheres, penetration well above the slop; after a bounded number of passes the re-derived separation is within `[-penetration_slop, 0]`.
- `test "position solve applies no correction beyond the slop"` — a pair penetrating by strictly less than the slop: `iterations_run == 1` (first pass applies nothing, the pass terminates) and both poses are **bit-unchanged**.
- `test "position solve leaves every accumulated impulse bit-unchanged"` — `normal_impulse`, `tangent1_impulse`, `tangent2_impulse` on every point compared bit-exactly before/after a converging pass.
- `test "position solve leaves a static body bit-unchanged"` — dynamic against static, deep penetration: the static body's position and all four quaternion components are bit-identical after the pass; the dynamic body moved.
- `test "the contact normal is not attached to body A"` — sphere A against box B, so the reference face is B's (`a_is_ref == false`): after `prepare`, rotate A by a large angle (a sphere's geometry is rotation-invariant, so the physical contact is unchanged), then solve — the correction stays along the stored world normal. Discrimination guard: assert that a normal re-rotated with A would have tilted the correction by more than the tolerance.
- `test "world inverse inertia follows the current rotation during the pass"` — one iteration on a rotated box with a lever-heavy contact, checked against a closed-form oracle computed in the test from the **current** rotation. Discrimination guard: the frozen-inertia oracle differs from the observed result by more than the tolerance.
- `test "solvePositionRange corrects only the given constraint index range"` — mirror of the velocity solver's range test: two independent pairs, solve `[0, 1)`, the second pair's poses are bit-unchanged.

In `tests/position_solver_test.zig` (harness-level, full pipeline in tests):

- `test "a stack of five boxes is stable"` — five unit boxes stacked flush on a static ground, 600 ticks. Each box's final height is within a named envelope of its analytic rest height (cumulative sink at most `N * penetration_slop` plus a named margin); the maximum speed over the last 60 ticks is below a named threshold (no jitter); lateral drift is bounded by a named margin; two identical runs are **bit-exact**.
- `test "a heavy box resting on a light box does not sink through"` — 10:1 mass ratio, 600 ticks, named envelope. (The property spec §1.6 claims for SI + NGS.)
- `test "a stack far from the origin along the contact normal still settles"` — ground + three boxes at `y ~ 5000` (the offset is along the contact normal: this is the configuration that actually exercises the `(p_b - p_a) · n` cancellation; an offset perpendicular to the normal does not), f32, named envelope. Documents the worldspace precision characteristic of spec §1.7.2.
- `test "a tilted anisotropic box resorbs penetration without lateral drift"` — half-extents `(2, 0.1, 0.5)`, tilted, settling on the ground: normal-direction recovery within the slop envelope, tangential displacement bounded by a named margin.
- `test "reference face carried by B still resorbs penetration"` — small sphere against a large static box (`a_is_ref == false` by construction, asserted indirectly via the manifold's normal orientation): penetration resorbed to the slop envelope.
- `test "BodyId order permutation converges to the same poses"` — the same scene built with the two bodies added in swapped order: converged heights equal within a named tolerance.
- `test "position_iterations = 0 reproduces the velocity-only resting behaviour"` — the M1.1.6 statement preserved: penetration bounded at its at-impact value across 300 ticks, no recovery.

In `tests/solver_test.zig` (modified):

- `test "box dropped on a static ground comes to rest without sinking (e = 0)"` — tightened as specified in Scope: resting penetration `<= penetration_slop`, `y_final` in `(1.0 - 2 * penetration_slop, 1.0 + 1e-4)`, still at rest.
- All other existing tests green. `"small hop within the fat margin keeps the contact pair alive"`, the incline hold/slide pair, the friction isotropy pair and the determinism run must pass **unmodified**; any envelope that has to move is reported with measured before/after in the gate verdict.

### Benchmarks

- None mandated (M1.1.3/4/5/6 precedent: absolute perf targets land with the full `step`).

### Observable behavior

- Harness scenario (test-runnable): a five-box stack placed on a static ground settles within a few tenths of a second and holds — per-box heights within one slop of analytic rest, no visible sink, no jitter, bit-exact across runs. Contrast against `position_iterations = 0` on the same scene, where the stack retains its at-impact penetration.

### CI

- `zig build` clean, zero warnings, configured matrix.
- `zig build test` and `zig build test-forge-3d` green — debug **and** ReleaseSafe, f32 **and** `-Dphysics_f64=true`.
- `zig fmt --check` green; `commit-msg` hook green on every commit.

## Gates

- **E1** — `solver_config.zig` (relocation + four additive fields + entry validation); `prepare` anchors + cached local inverse inertias; `BodyManager.setPosition`/`setRotation`. RED-first on the anchor round-trip test.
- **E2** — `position_solver.zig`: `solvePositionRange` + `PositionSolveResult`, fixed world normal, per-iteration re-derivation (anchors, lever arms from the current midpoint, world inverse inertia at the current rotation), first-order orientation, true-zero pose-write guards, no-correction termination. Inline unit tests including both discrimination-guarded oracles.
- **E3** — Harness cycle step (6) + normative-cycle header; M1.1.6 resting-test tightening (RED-first); `position_iterations = 0` seam pin.
- **E4** — Acceptance suite (`tests/position_solver_test.zig`): stack, mass ratio, far-from-origin along the normal, tilted anisotropic box, reference-on-B, BodyId permutation. Determinism run.
- **E5** — f64 + ReleaseSafe matrix runs, `CLAUDE.md` §3.4 patch, closing notes.

## Conventions

- **Branch:** `phase-1/forge/solver-ngs-position`
- **Final tag:** `v0.11.7-solver-ngs-position`
- **PR title:** `Phase 1 / Forge / NGS position solver`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md` §4.3); squash title `feat(forge): add ngs position solver (M1.1.7)`
- **Merge strategy:** squash-and-merge (Guy merges and tags; Claude Code never merges or tags)

## Notes

- **Inherited M1.1.6 contracts, binding on this design.** (a) The velocity solver carries no positional bias — reintroducing one is a STOP. (b) Contact points come from `collidePair` (canonical `BodyId` order, frame-stable `feature_id`), never from bare `collide`. (c) `SolverConfig` extension is additive. (d) `solveRange`'s range shape is the island seam; the position entry mirrors it. (e) The resting-penetration artifact is what this milestone removes. (f) The per-tick cycle is normative in the spec, not guessable. (g) Candidate pairs are retained while the fat AABBs overlap; the harness keeps every emitted pair (a conservative superset). (h) The tangent basis is discontinuous at a dominant-axis flip — never assume basis stability.
- **Design points verified against the reference implementations** (read, not recalled): normal fixed in world for the whole pass (`ContactConstraintManager.cpp`, `ws_normal` read once before the point loop); two distinct body-local anchors with `separation = (p2 - p1) · normal`, no initial-penetration term; world inverse inertia rebuilt inside the point loop from the current rotation (`Body::GetInverseInertia` = `GetInverseInertiaForRotation(current rotation)`); lever arms from the midpoint of the two current anchors; clamp on the error in, `if (separation < 0)` gate; termination on "no impulse applied", per island. Box2D's position solver was the cross-check; where the two diverge, Jolt is followed (the project's declared physics reference), with the exception noted below.
- **Alternatives examined and rejected.** Normal attached to body A (the frozen manifold does not carry the reference-face owner and it is often B — this was corrected during scoping). Frozen world inverse inertia during the pass (unjustified approximation; the pass rotates the bodies it reads). Two identical midpoint anchors plus a stored `penetration_0` scalar (algebraically within `O(theta * penetration)` of the two-anchor form, but it stores an extra scalar and is not the reference discretization). Box2D's `min_separation >= -3 * slop` termination (introduces a magic multiplier; Jolt's "no impulse applied" needs no constant). Box2D's clamp on the correction rather than on the error. Jolt's exact axis-angle rotation step (needs an absolute `1e-6` guard, imported for a regime that does not exist here). A slop derived from a scale — body-size-derived makes the resting overlap pair-dependent (non-uniform rest offset in a stack), contact-`coordScale`-derived makes it world-position-dependent. Jolt's `0.02` slop (coupled to its speculative-contact distance, which is out of scope — the number without its reason). A separate `PositionSolverConfig` struct (M1.1.6 named `velocity_iterations` explicitly for a `position_iterations` sibling in the same struct).
- **Threshold discipline (M1.1.4).** `penetration_slop` and `max_penetration_correction` are absolute lengths in metres and that is **assumed and motivated**: they are physical tolerances of the internal unit scale (`engine-units.md`), of the same class as M1.1.6's `restitution_threshold`, and they classify nothing and guard no numerical degeneracy. Every other guard in this milestone fires at true zero. No new geometric epsilon anywhere.
- **Determinism by construction (anticipates M1.1.14).** Iteration order identical to the velocity solver (constraints ascending by pair key, points in manifold order); no hash container on the path; every constant named in `SolverConfig`; the termination decision is a pure function of the range's state.
- **Spec status.** `engine-physics-forge.md` §1.7 / §1.7.2 / §4 / §1.9 were authored and re-uploaded to the KB **before** this brief and are normative. Any deviation discovered in implementation is a Recorded deviation here and triggers a corrective re-upload at close (Claude.ai-produced) — never a silent divergence.
- **No deferred debt.** Any finding surfaced at any point in this milestone is fixed inside this milestone. One milestone, one Claude Code session.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone.*

## Specs read

- [x] `engine-physics-forge.md` (§1.2, §1.5, §1.7, §1.9, §3, §4) — read in full 2026-07-25 01:24. §1.7 present (§1.7.1 velocity pass, §1.7.2 NGS) — the normative source of this milestone, no STOP.
- [x] `engine-phase-1-plan.md` (M1.1 table) — read in full 2026-07-25 01:22.
- [x] `engine-zig-conventions.md` (§13) — read in full 2026-07-25 01:26.

## Execution log

### E1 — config relocation, surface anchors, pose mutators

- **RED-first on the anchor round-trip** (`test "prepare stores body-local surface anchors that reproduce the penetration"`). Step 1: the test written against the not-yet-existing fields fails to compile (`no field named 'local_anchor_a'`). Step 2 (discrimination proof): with the fields added but filled from the REJECTED single-midpoint alternative (`surface_a = surface_b = p.position`), the suite compiles and the test fails on the round-trip — `actual 0, not within absolute tolerance 0.00001 of expected -0.100000024`. The assertion therefore discriminates the anchor CONSTRUCTION, not merely the field's existence. Step 3: the two-surface form (`position ± ½·penetration·n`) turns it green.
- **`rigid/solver_config.zig` created.** `SolverConfig` moved out of `velocity_solver.zig` verbatim for the velocity half (`velocity_iterations = 8`, `restitution_threshold = 1.0`; the iteration default was later raised to 16 at E4b — RD-2) plus the four spec §1.7.2 position fields (`position_iterations = 3`, `penetration_slop = 0.005`, `position_correction_factor = 0.2`, `max_penetration_correction = 0.2`), and `assertPositionDomain` (finiteness, `slop ≥ 0`, `max ≥ 0`, factor ∈ `[0, 1]`; `position_iterations` is a `u32`, so its domain holds by type). `velocity_solver.zig` now imports it as a private const — no public re-export from there, so `SolverConfig` has exactly one owner; `rigid/root.zig` re-exports from the new owner and pins the file. Zero call-site change (`rigid.SolverConfig`), zero behavioural change in the velocity pass; its existing inline tests (including `"solver config defaults"`) are untouched.
- **`prepare` now fills the position pass's inputs.** `ConstraintPoint.local_anchor_a/b` = `conj(q)·(surface − x)` per body, from `surface_{a,b} = position ± ½·penetration·n` (the manifold point is the midpoint of the two surface points); `ContactConstraint.local_inv_inertia_a/b` = a straight copy of `MotionProperties.local_inv_inertia` (pose-invariant, so the position pass never calls `motionProperties` in its inner loop). Purely additive: no existing field, order or value changed, and `penetration`'s doc now records that `prepare` is its single consumer.
- **`BodyManager.setPosition`/`setRotation`** added next to the velocity setters, same `isValid` discipline, documented INTERNAL and flagged for the M1.1.15 freeze review. Covered by an inline test (live write + stale-handle no-op) placed in `body_manager.zig` itself rather than `tests/body_manager_test.zig` — that file is outside the brief's file list, and the mutators' whole contract is this file's handle validation.
- **Gate E1 green:** 166/166 `test-forge-3d` in debug f32, debug `-Dphysics_f64=true`, and ReleaseSafe f32; `zig build`, `zig build lint`, `zig fmt --check` clean.

### E2 — `rigid/position_solver.zig`

- **`solvePositionRange(bm, constraints, from, to, cfg) PositionSolveResult`** implemented exactly as §1.7.2 prescribes: normal read from the constraint and never re-rotated; per iteration, per point, in the velocity pass's order — world anchors from the current poses, `sep = max((p_b − p_a)·n + slop, −max_penetration_correction)`, `sep >= 0` gate, lever arms from the midpoint of the two current anchors, world inverse inertia rebuilt from the CURRENT rotation, `lambda = −factor·sep / k`, `P = lambda·n` applied immediately to both poses (Gauss-Seidel). Entry calls `solver_config.assertPositionDomain`. `position_iterations == 0` early-returns before any evaluation (`iterations_run = 0`, `min_separation = null`).
- **Orientation** by the local `rotateByDelta` — `q ← normalize(q + ½·(Δθ_quat ⊗ q))`, Δθ world-space on the LEFT, no `|Δθ|` division and therefore no absolute guard. Not factored with `pipeline/integration.zig` (import boundary + the integrator's flagged additive exact-orientation option).
- **True-zero pose-write guards**: the position delta and the rotation delta are each written only when strictly non-zero, so a static/kinematic body — whose correction is exactly zeroed by `inv_mass == 0` and a zero local inverse inertia — never goes through `normalize` and comes out bit-unchanged. The same predicate defines "applied", so the no-correction termination cannot be fooled by a zero-magnitude correction (it also makes `position_correction_factor = 0` terminate after one iteration instead of spinning).
- **`min_separation`** is the raw geometric `(p_b − p_a)·n`, before the slop and before the error clamp — the worst residual penetration, telemetry only (Box2D tracks the same quantity; Jolt tracks none).
- **Measured** (temporary diagnostics, removed before commit): the converging two-sphere case settles at `separation = −0.00511760` against a 5 mm slop — a 1.2e-4 residue, matching the `(1 − factor)ⁿ` contraction and inside the named `convergence_residue = 1e-3`. The current-rotation inertia oracle reproduces the implementation **bit-for-bit** (`|observed − expected| = 0`), while the frozen-inertia oracle is off by 5.3e-3 on its worst quaternion component — 5× the discrimination guard, and identical at f32 and f64, so the gap is geometric, not float noise. The extra post-`prepare` rotation was widened 0.4 → 0.9 rad to buy that margin.
- **Gate E2 green:** 173/173 `test-forge-3d` across the four configurations (debug/ReleaseSafe × f32/f64); `zig build`, `zig build lint`, `zig fmt --check` clean. No existing test envelope moved.

### E3 — cycle step (6), M1.1.6 tightening, seam pin

- **RED-first on the tightened resting test.** The recovery assertion was written into `tests/solver_test.zig` BEFORE step (6) existed and observed failing on exactly that line (`resting_penetration <= world.cfg.penetration_slop`, 172/173). Wiring `solvePositionRange` into the harness turned it green.
- **Harness cycle.** Step (6) `solvePositionRange` inserted between `integratePositions` and `storeContacts`, and the header's normative-cycle comment renumbered to the spec's eight steps (§1.7), including the pair-retention step the harness already implemented but did not number. `World` gained `position_result` (last tick's NGS telemetry).
- **Harness published instead of copied.** `World` and the `vr`/`av3`/`groundAndBox` helpers became `pub` so `tests/position_solver_test.zig` drives the SAME harness. Duplicating the cycle in the new suite would let the two copies drift on the one thing that must not drift; the alternative (a third shared file) is outside the brief's file list.
- **Measured rest** (temporary diagnostics, removed before commit): `resting_penetration = 0.00500059`, `y_final = 0.99499947`, 4 contact points, 3 iterations run per tick, `min_separation = −0.00500059` at f32; `0.00500000` / `0.99500000` at f64. The imposed envelope `y_final ∈ (1.0 − 2·slop, 1.0 + 1e-4) = (0.99, 1.0001)` holds with a 5 mm margin.
- **Named margin on the recovery statement (reported, not silent).** The rest sits exactly at the NGS fixed point `separation = −penetration_slop`, and a geometric contraction never crosses its fixed point, so `resting_penetration <= penetration_slop` cannot hold as a bare inequality in floating point: the measured excess is 5.9e-7 at f32 (≈ 5 ULP of the 1 m contact coordinate) and 0 at f64. The assertion is written `<= penetration_slop + 16·floatEps(Real)·coord_scale` — the M1.1.4 `k·floatEps·coordScale` noise form, 3.2× the measured f32 residue and 500× tighter than the slop itself. The physical content is untouched: the ≈ 7 cm at-impact penetration is resorbed to 5 mm.
- **Seam pin.** `tests/position_solver_test.zig` created (pinned in `forge_3d/root.zig`) with `test "position_iterations = 0 reproduces the velocity-only resting behaviour"`: the pass early-returns (`iterations_run == 0`, `min_separation == null`) and the box keeps its at-impact penetration across 300 ticks. Discrimination guard: that rest is measurably below the envelope the same scene satisfies with the pass enabled.
- **Gate E3 green:** 174/174 `test-forge-3d` across the four configurations. Every other existing test passes UNMODIFIED — the small-hop retention test, the incline hold/slide pair, the friction isotropy pair and the determinism run included. No existing envelope moved.

### E4 — acceptance suite

- **Six scenes added to `tests/position_solver_test.zig`**, all driving the shared harness: five-box stack (600 ticks, per-box height envelope + jitter + lateral bound + a bit-exact replay run), 10:1 heavy-on-light, three boxes on a ground at `y = 5000` (the offset is ALONG the contact normal — the configuration that exercises the `(p_b − p_a)·n` cancellation), a tilted anisotropic box `(2, 0.1, 0.5)`, a sphere against a large static box with the sphere holding the LOWER BodyId so the reference face is B's, and a BodyId-order permutation.
- **Scene-order correction.** The reference-on-B scene was first written with the box added first, which gave it the lower BodyId and made the BOX the canonical A (`normal = +Y`) — the opposite of what the test claims. Adding the sphere first makes the pair `(sphere = A, box = B)` and the normal `(0, −1, 0)`, i.e. the box top face's normal negated; a sphere carries no face, so that axis could not come from an A-owned reference. The assertion is now the one the brief describes.
- **Envelopes named from measurement**, all with the worst case over the four build configurations and their headroom recorded in the file: `rest_margin = 10 mm` (worst 6.1 mm), `lateral_bound = 0.15 m` (worst 0.081 m), `creep_bound = 1 mm` (measured 0), `order_tolerance = 1e-4`. `settle_speed` is PHYSICAL rather than fitted — `g·dt`, the speed a free-falling body picks up in one tick: below it no body is in sustained free fall, which is what "not bouncing" means (worst measured 0.075, 2.2× inside).
- **Gate E4 green:** 180/180 `test-forge-3d` across the four configurations; full `zig build test` green; `zig fmt --check`, `zig build lint` clean.

#### Finding — the five-box stack creeps laterally at the default iteration budget

Heights and non-penetration are correct everywhere; what the five-deep chain does NOT do is come fully to rest. Measured at tick 600 (f32 / f64): top box lateral 0.071 / 0.081 m, residual speed 0.047 / 0.075 m/s. Over 30 s the creep is roughly linear at ≈ 5 mm/s, while the heights stay put (top box 4.97 ± 0.01 against an analytic 5.0 − 5·slop = 4.975).

Attribution, measured (five-box stack, 1200 ticks, top-box lateral / residual speed):

| `velocity_iterations` | `position_iterations` | lateral | speed | top `y` |
|---|---|---|---|---|
| 8 | 0 | 0.075 | 0.0002 | 4.883 (no recovery — M1.1.6) |
| 8 | 1 | 0.086 | 0.068 | 4.967 |
| 8 | **3 (default)** | 0.131 | 0.067 | 4.971 |
| 8 | 8 | 0.298 | 0.072 | 4.972 |
| 16 | 3 | 0.022 | **0.000** | 4.984 |
| 64 | 3 | 0.002 | 0.000 | 4.998 |

Reading: the chain depth is what triggers it (1, 2 and 3-box stacks stop dead — 2e-6, 5e-4 and 4e-3 m of lateral offset with zero residual speed). Gauss-Seidel visits the four points of a face manifold in sequence, so each position pass leaves a small residual tilt; the velocity pass at 8 iterations does not re-equilibrate a five-deep chain, and the tilt walks the stack. Raising the velocity budget to 16 removes it entirely (zero residual speed), which shows the position pass is not unstable on its own — the two passes are simply not converged together at this depth and budget.

**Not fixed at E4, reported for Guy's ruling** — and the ruling amended the scope at E4b (RD-2, RD-3). Two of the three arguments in this section were wrong and are corrected here rather than deleted: (1) attributing the creep to a joint convergence deficit is refuted by the same table, since it grows monotonically with `position_iterations`, which means something is injected, not under-converged; (2) "M1.1.8's sleep would freeze it" is false — Jolt's `mPointVelocitySleepThreshold` is 0.03 m/s and the sustained residual is 0.047 (f32) to 0.075 (f64), so this stack would never fall asleep. The argument would have parked a defect, which is what the no-deferred-debt rule forbids. What survives: the depth dependence, the measurements, and the fact that raising the velocity budget removes the symptom.

### E4b — scope amendment: iteration default, attribution fix, stack envelopes

- **`velocity_iterations` 8 → 16** in `solver_config.zig`, with the per-island rationale and the M1.1.8 floor seam in the field's doc (RD-2). `penetration_slop` and `position_iterations` untouched. The slop's doc lost its stale "coherent with `velocity_iterations = 8`" parenthetical: the slop sets where the position pass STOPS, not how fast either pass converges — the two are independent.
- **False attribution removed** from `velocity_solver.zig` (RD-3): normal-then-friction is now documented as a deliberate divergence from Jolt/Box2D, with no unmeasured motive claimed.
- **Stack test now asserts the STOP, not slowness.** Run extended 600 → 1200 ticks with a mid-run sample; three statements replace the old pair: `lateral_bound` 0.15 → **0.04 m** (measured worst 0.0219), a new `lateral_creep_bound = 1 mm` on the GROWTH of that offset over the second half (measured −6e-8, i.e. a decrease; the rejected regime gained 0.10 m), and a new `rest_speed = 1 mm/s` immobility criterion (measured 1.2e-7 f32, 0.0 f64) kept DISTINCT from `settle_speed = g·dt`, which stays as the anti-bounce ceiling and is still asserted. `rest_margin` also tightened 10 → 5 mm, since the stack now settles inside `n · slop`.
- **Every other envelope in the suite held unchanged** at the new default — 180/180 across the four configurations, full `zig build test` green, `zig fmt --check` and `zig build lint` clean. The only other edit the default forced is factual: `test "solver config defaults"` asserts 16.

## Recorded deviations

### RD-1 — the resting-penetration acceptance criterion is unreachable as frozen (E3)

**Frozen text** (Scope, "M1.1.6 acceptance-test tightening"): *"Replace it by a **recovery** statement — resting penetration `<= penetration_slop`"*.

**Why it cannot hold.** The `sep >= 0` gate makes `penetration == penetration_slop` the FIXED POINT of the pass, and the pass is a geometric contraction toward it (`separation + slop` is multiplied by `1 − position_correction_factor` per correction). A contraction never crosses its fixed point, so the resting penetration approaches the slop **from above** and reaches it only in the limit — never in finitely many floating-point steps. The bare inequality is a boundary the model cannot attain; it is a property of the model, not of the test.

**Form retained.** `resting_penetration <= penetration_slop + 16·floatEps(Real)·coord_scale` — the M1.1.4 noise class, same `k = 16` as the narrowphase's accumulated-rounding band, `coord_scale = 1 m` (the contact's height). A TEST tolerance only: no epsilon of any kind was added to production code, and the imposed `y_final ∈ (1.0 − 2·slop, 1.0 + 1e-4)` envelope is untouched and met with a 5 mm margin.

**Measured.** f32: `resting_penetration = 0.00500059`, `y_final = 0.99499947` — an excess of 5.9e-7 over the slop, ≈ 5 ULP of the 1 m contact coordinate, and 3.2× inside the named margin. f64: `0.00500000` / `0.99500000` — the excess is below the printed 1e-8 resolution. The physical content of the criterion is intact: the ≈ 7 cm at-impact penetration is resorbed to 5 mm, a factor of ~14.

**Also stronger than asked.** The penetration is measured on the MANIFOLD (`ContactPoint.penetration` of the resting constraint), not re-derived from the anchors — it observes what the narrowphase reports about the physical overlap rather than what the solver's own state says.

**Spec follow-up at close.** `engine-physics-forge.md` §1.7.2 needs one line: the attainable resting bound is `penetration_slop + O(floatEps·coordScale)`, not `penetration_slop` — the slop is the fixed point, approached from above. This belongs with the §1.7 KB re-upload (Claude.ai-produced, Guy re-uploads).

### RD-2 — `velocity_iterations` default raised 8 → 16 (E4b, Guy's ruling)

**Change.** `SolverConfig.velocity_iterations` 8 → 16. A CONFIG DEFAULT, not solver behaviour: no code path changed, no call site touched, the STOP boundary on the velocity pass is not crossed. `position_iterations` stays 3, `penetration_slop` stays 5 mm.

**Motive.** 8 comes from the Box2D tuning family, which is 2D — a contact patch is two points and a rotation is one scalar. In 3D a face patch carries four points and a five-deep stack carries forty; 8 is under-provisioned at that depth. Measured at 8: a five-box stack never comes to rest — two distinct quantities, not to be conflated when this entry is re-read at M1.1.8: the top box's lateral OFFSET grows at a rate of ≈ 5 mm/s (0.071 m at tick 600 → 0.174 m at tick 1800, f32), while its INSTANTANEOUS residual speed sits at 0.047 m/s (f32) to 0.075 m/s (f64) and does not decay. At 16 the stack stops dead on both counts. Jolt does not use a global constant at all — it derives the budget PER ISLAND (`PhysicsSystem.cpp`, `steps_calculator.GetNumVelocitySteps()`), so a deep stack automatically gets more iterations.

**M1.1.8 seam.** When the island manager lands, the budget becomes island-derived and this field reverts to a FLOOR under it. Recorded in the field's doc comment so the follow-up cannot be lost.

**Measured at 16/3** (five-box stack, top box, lateral offset / max speed over the last second of each window):

| tick | f32 lateral | f32 speed | f64 lateral | f64 speed |
|---|---|---|---|---|
| 600 | 0.02184822 | 5.5e-7 | 0.00818421 | 4.0e-6 |
| 1200 | 0.02184819 | 1.8e-7 | 0.00818428 | 0.0 |
| 1800 | 0.02184813 | 1.2e-7 | 0.00818428 | 0.0 |

The stack is frozen at both precisions and holds to 1800 ticks; the lateral offset DECREASES by 6e-8 over the second half instead of growing. Sinks also improve (top box 16.4 mm at f32 / 8.2 mm at f64, against the 25 mm `5·slop` budget; it was 27.7 / 31.1 mm at 8 iterations).

**Suite re-validated at the new default:** 180/180 across the four configurations, full `zig build test` green. **No existing envelope had to move.** The two edits the change forced are factual, not widenings: `test "solver config defaults"` now asserts 16, and the stack test's envelopes were re-derived downward (see RD-3's neighbour entry in the E4b log). The most iteration-sensitive inherited test is unchanged in value: `"box dropped on a static ground comes to rest without sinking"` measures `resting_penetration = 0.00500059`, `y_final = 0.99499947` at f32 — identical to the 8-iteration measurement — with `v_final` now exactly 0.

**Spec line to correct at close:** the `velocity_iterations` default in `engine-physics-forge.md` §1.7.1, with the per-island rationale and the M1.1.8 seam.

### E5 — closing

- **`CLAUDE.md` §3.4 patched**: `Current milestone` and `Last released tag` were still on M1.1.6 / `v0.11.5` and are corrected to M1.1.7 / `v0.11.6-solver-sequential-impulses`; active branch updated; `Next planned milestone` now names M1.1.8 as the owner of the island-derived velocity budget. New Tags row for `v0.11.7-solver-ngs-position`. Four Open-decisions entries added: the M1.1.7 scope boundary, the `velocity_iterations` 8 → 16 default as a MEASURED FLOOR with the M1.1.8 seam and an explicit "do not treat as closed", the open design item on the whole-model friction port pointing at RD-3, and the three-line KB spec patch pending.
- **RD-2 wording fixed** (Guy, E5): the entry now separates the lateral-offset RATE (≈ 5 mm/s) from the instantaneous residual speed (0.047 m/s f32 / 0.075 f64) instead of conflating them — the entry will be re-read during M1.1.8.
- **Final matrix**: 180/180 `test-forge-3d` in debug and ReleaseSafe × f32 and `-Dphysics_f64=true`; full `zig build test` green; `zig build`, `zig fmt --check`, `zig build lint` clean; language and drift audits clean.

### RD-3 — three friction-model fragments measured and rejected (E4b, Guy's ruling)

**Context.** The five-box creep at 8 iterations was first attributed by me to a joint convergence deficit; Guy refuted that from my own table (the creep grows monotonically with `position_iterations`, so something is injected, and raising `velocity_iterations` absorbs rather than removes it) and identified a false attribution in M1.1.6: the comment claiming "Jolt order" for normal-then-friction states the opposite of Jolt (`ContactConstraintManager.cpp` `sSolveVelocityConstraint`: friction first, non-penetration last, cone clamped with the previous iteration's λₙ) and of Box2D (`b2_contact_solver.cpp:322`).

**Measured, in a scratch build, never committed** (five-box stack at 8/3, top-box lateral offset / max speed over the last second of each window):

| variant | f32 @600 | f32 @1200 | f32 @1800 | f64 @600 | f64 @1200 | f64 @1800 |
|---|---|---|---|---|---|---|
| V0 as shipped (normal first, per-point cone) | 0.071 / 0.047 | 0.131 / 0.067 | 0.174 / 0.063 | 0.081 / 0.079 | 0.116 / 0.039 | 0.128 / 0.015 |
| V1 friction first, cone on the previous λₙ | 0.086 / 0.041 | 0.179 / 0.129 | 0.285 / 0.031 | 0.093 / 0.068 | 0.159 / 0.060 | 0.246 / 0.052 |
| V2′ per-manifold aggregated friction (sequential, shared cone) | 0.081 / 0.136 | 0.170 / 0.089 | 0.246 / 0.086 | 0.023 / 3.7e-5 | 0.023 / 0.0 | 0.023 / 0.0 |
| V3 = V1 + V2′ | 0.212 / 0.055 | 0.258 / 0.040 | 0.314 / 0.044 | 0.056 / 0.042 | 0.105 / 0.081 | 0.127 / 0.039 |

A first aggregation attempt — per-point candidates from one velocity state (Jacobi inside the patch) then a joint rescale — DIVERGED (stack at −64 m, 41 m/s). That is a flaw in my design of the trial, not a verdict on aggregation; V2′ is the sequential Gauss-Seidel form with a shared cone budget and is stable.

**Ruling (Guy).** None of the three fragments — friction-first ordering, per-manifold aggregation, twist friction — is adopted: they are pieces of a model that is only coherent whole, and as fragments they measure worse at f32. A faithful port of the reference friction model (order + aggregation + twist together) becomes an OPEN DESIGN ITEM for a dedicated milestone, with this table attached so it is not re-litigated blind.

**Done here (documentation only).** The false attribution is removed from `velocity_solver.zig`: the comment now reads as a deliberate divergence from Jolt/Box2D, states that swapping the order was measured and did not help, and claims no performance motive we have not measured.

**Still a hypothesis, not a conclusion:** the ≈ 7 cm settling-transient offset present even at `position_iterations = 0` is attributed to the absence of twist friction. Nothing in these measurements confirms it.

**Spec line to correct at close:** §1.7.1's "impulsion normale d'abord, friction ensuite (ordre Jolt)" — the parenthetical attribution is wrong in the same way the code comment was.

## Blockers encountered

## Closing notes

**Delivered.** The NGS position pass (`rigid/position_solver.zig`) resorbs contact penetration by correcting poses only, exactly as `engine-physics-forge.md` §1.7.2 prescribes: normal fixed in world for the whole pass, anchors and lever arms and world inverse inertia re-derived from the current poses at every point of every iteration, clamp on the error going in with a `sep >= 0` gate, first-order linearized orientation step with no absolute guard, true-zero pose-write guards, termination on the first iteration that applies nothing. `SolverConfig` moved to its own file and gained the four position fields; `prepare` stores the two body-local surface anchors and the pose-invariant local inverse inertias; `BodyManager` gained internal pose mutators. The M1.1.6 interim artifact is gone: a box dropped from 2 m now rests one slop under the analytic height instead of keeping its ≈ 7 cm impact penetration, and `position_iterations = 0` reproduces the old behaviour exactly, pinned by a dedicated test.

**Validation.** 180/180 in `test-forge-3d` across debug × ReleaseSafe × f32 × `-Dphysics_f64=true`; full `zig build test` green; `zig build`, `zig fmt --check`, `zig build lint` clean. RED-first was observed and reported on both pinnable behaviours (the anchor round-trip, including a discrimination step where the rejected midpoint-anchor alternative fails the assertion; and the tightened M1.1.6 resting test). Both oracles in the position solver's unit tests carry discrimination guards, and the current-rotation inertia oracle reproduces the implementation bit-for-bit.

**What we do NOT know — the iteration budget.** The five-box stack came out of the milestone stable, but the reason it is stable is a measurement, not an account. We know that at `velocity_iterations = 8` the stack never settles and that at 16 it stops dead at both precisions and holds through 1800 ticks. We do **not** know why 16 rather than 12 or 20: there is no first-principles derivation, no model of how the required budget scales with chain depth, patch size or mass ratio. 16 is a measured floor for the worst case this milestone accepts, and it is a global constant standing in for something that should not be global at all. The real answer is the per-island budget Jolt derives (`PhysicsSystem.cpp`, `steps_calculator.GetNumVelocitySteps()`), which lands with the island manager at **M1.1.8** and demotes this field to a floor. Anyone reading the green stack test should not conclude the subject is closed.

**Second open thread.** The reference friction model — friction solved first with the cone clamped on the previous iteration's λₙ, per-manifold aggregation, twist friction — is NOT implemented, and the three parts were each measured to make things worse when adopted alone (RD-3, with the f32/f64 tables at 600/1200/1800 ticks). It needs a dedicated milestone and must be ported whole. The ≈ 7 cm settling-transient lateral offset that exists even with the position pass disabled is hypothesized to come from the missing twist friction; that remains a hypothesis, and nothing measured here confirms it.

**Deviations, all recorded, none silent.** RD-1: `resting penetration <= penetration_slop` is unreachable as frozen, the slop being the pass's fixed point approached from above; the assertion carries an M1.1.4-form noise margin and the physical content (7 cm resorbed to 5 mm) is intact. RD-2: `velocity_iterations` default 8 → 16, a config default with the M1.1.8 floor seam recorded. RD-3: the three friction fragments measured and rejected. Two E4 conclusions of mine were wrong and are corrected in place rather than deleted — the "joint convergence deficit" reading, refuted by the monotonic degradation with the NGS budget, and "M1.1.8 sleep would freeze it", refuted by Jolt's 0.03 m/s sleep threshold against a 0.047–0.075 m/s residual.

**Three spec lines for the KB re-upload at close** (Claude.ai produces the file, Guy re-uploads): §1.7.2 the attainable resting bound; §1.7.1 the friction/normal ordering attribution; §1.7.1 the `velocity_iterations` default with its per-island rationale.

**Files touched outside the brief's list:** none. Two placements are justified in the Execution log rather than the file list: the pose-mutator coverage is inline in `body_manager.zig` (its contract is that file's handle validation) and the acceptance suite imports the harness from `solver_test.zig` instead of copying it (the normative per-tick cycle must have exactly one definition).
