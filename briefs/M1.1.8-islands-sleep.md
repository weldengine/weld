# M1.1.8 — Forge 3D: island manager + sleep detection

> **Status:** ACTIVE
> **Phase:** 1
> **Branch:** `phase-1/forge/islands-sleep`
> **Planned tag:** `v0.11.8-islands-sleep`
> **Dependencies:** M1.1.0–M1.1.7 (base = HEAD of `main` at `v0.11.7-solver-ngs-position`)
> **Opened:** 2026-07-25
> **Closed:** —

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

Ninth sub-milestone of the M1.1 rigid arc (M1.1.0–15, `PhysicsModule` freeze at M1.1.15). The contact solver landed range-shaped on purpose: `solveRange` and `solvePositionRange` resolve `[from, to)`, and this milestone is what fills those ranges with meaning. It partitions the simulated bodies into independent islands, drives both solver passes per island, and puts a resting island to sleep so that it stops costing narrowphase, integration and solve work. It also settles the iteration-budget question M1.1.7 assigned here — by refuting its premise (see § Notes, inherited finding (d)).

The plan's acceptance line for M1.1.8 asks for independent groups and for a group at rest to fall asleep. The normative model is `engine-physics-forge.md` §1.8, authored and re-uploaded to the KB before this brief.

## Scope

- `pipeline/island.zig` — branch-neutral island core: union-find over **opaque** body indices (path compression + union by size), seeded per body so an unlinked body is a singleton group. Imports `std` only — no `weld_forge`, no `config.zig`, no `rigid/` type, mirroring the broadphase/narrowphase dependency discipline.
- `pipeline/sleep.zig` — `SleepConfig` (`allow_sleeping = true`, `point_velocity_threshold = 0.03` m/s, `time_before_sleep = 0.5` s, derived `maxDisplacement()` = product) with entry-asserted domain, plus the per-body sleep-window sweep and the eligibility predicate.
- Per-body sleep state on the SoA store: `BodyFlags.can_sleep` + `BodyFlags.sleeping` (two bits taken from the existing `_reserved: u7`, byte size unchanged), and four columns `sleep_time`, `sleep_ref_position`, `sleep_ref_rotation`, `sleep_radius`.
- `BodyDescriptor.can_sleep: bool = true` — closes the gap where the public `RigidBody` component carries `can_sleep` while the descriptor and the internal body drop it (same class as the `friction`/`restitution` drop closed at M1.1.6).
- Write-intent separation on `BodyManager` (§1.8.4): `setLinearVelocity`/`setAngularVelocity`/`setPosition`/`setRotation` are **non-activating by contract** (they are the solver's own write path); `addForce`/`addTorque`/`addImpulse` are **activating** (wake + reset the window even if the body was already awake); `wakeBody(id)` and `setCanSleep(id, …)` are the explicit primitives.
- `rigid/island_manager.zig` — the rigid adapter: seeds the core with every awake dynamic body, links dynamic↔dynamic contact constraints only, ranks islands by minimum member `BodyId`, sorts constraints by the composite key `(island rank, pair_key)` into contiguous ranges, records per island whether it touches a moving non-member, then arbitrates activation (W2, W3) and, at the end of the tick, the sleep transition (AND over members).
- Fixpoint in `contact_constraint.build`: skip a candidate pair whose two endpoints are both non-awake (recording it in a deferred list), wake a sleeping endpoint as soon as a manifold appears, re-scan the deferred list while a pass wakes at least one body. `build` takes `bm: *BodyManager` instead of `*const BodyManager` (no call site text changes).
- Sleep skips: `integrateVelocities`/`integratePositions` skip sleeping bodies (the force/torque accumulator reset stays uniform over all live bodies, §2); the harness skips the broadphase proxy update of a sleeping body.
- `velocity_solver.solveRangeReport(bm, constraints, from, to, cfg) VelocitySolveResult` — the sibling entry that returns `iterations_run`, plus the early-out: an iteration that applies no impulse (true-zero `Δλ` test) ends the pass. `solveRange` keeps its exact signature and becomes `_ = solveRangeReport(…)`.
- Test harness (`tests/solver_test.zig`) upgraded to the 11-step normative cycle of §1.7, driving both solver passes per island, and `allow_sleeping = false` on every inherited convergence test.
- Corrections carried by this milestone: the `solver_config.zig` doc comment repeating the refuted Jolt claim; the `position_solver.zig` §1.9 → §1.10 spec reference.

## Out of scope

- Job-parallel island solving, large-island splitting (§1.8.8). The partition makes it possible; the scheduler is not this milestone.
- Per-body iteration-count override channel (§1.8.2) — additive, no consumer until an authoring surface exists.
- `step()` / `PhysicsWorld` / `PhysicsModule` instantiation / ECS `Transform` sync (M1.1.15), including the production persistent candidate-pair set and the W4 wiring (removal / static teleport). W4 is proven at harness level here; its production call site is M1.1.15.
- `PhysicsDebugProvider` implementation, `IslandDebugInfo` shape (§1.10), editor overlays.
- Joints (M1.1.16–18) and the per-island position-step count they would raise; queries (M1.1.9–10); CCD and speculative contacts; shapes beyond sphere/box/capsule (M1.1.11+); `forge_2d`; per-shape/per-face `PhysicsMaterial`.
- Faithful port of the Jolt friction model (order + per-manifold aggregation + twist friction) — open design item, whole and never in fragments.
- M1.1.4 clip-recentring precision boundary — future precision hotfix upstream in `generateManifold`.
- Sensors never sleeping (Jolt rule): sensors land at M1.1.13; the rule is additive then.

## Specs to read first

1. `engine-physics-forge.md` — §1.8 **in full** (the normative model of this milestone), then §1.7 (the 11-step cycle), §1.7.1, §1.7.2, §1.2, §1.5, §2, §1.10. If §1.8 is absent from the file you read, the KB was not refreshed: **STOP and report**.
2. `engine-phase-1-plan.md` — the M1.1 table (M1.1.8 line and the downstream numbering).
3. `engine-zig-conventions.md` — §3 (unmanaged-first), §13 (lazy-analysis comptime pin), §16 (extern POD components).
4. `engine-development-workflow.md` — §3.4 (`CLAUDE.md` update in the closing PR), §3.6.1 (end-of-milestone local audit, including the language criterion).
5. `CLAUDE.md` — § Current state, § Tags (the `v0.11.7` row), § Open / deferred decisions (the `velocity_iterations` entry and the two M1.1.6/M1.1.7 "KB spec patch pending" entries — both are now settled in the KB and are retired by this milestone's §3.4 patch).

## Files to create or modify

- `src/modules/forge/forge_3d/pipeline/island.zig` — create — branch-neutral union-find island core over opaque indices.
- `src/modules/forge/forge_3d/pipeline/sleep.zig` — create — `SleepConfig` + domain assert + per-body window sweep + eligibility predicate.
- `src/modules/forge/forge_3d/rigid/island_manager.zig` — create — rigid adapter: partition, ranks, contiguous constraint ranges, activation and sleep arbitration.
- `src/modules/forge/forge_3d/body.zig` — modify — `BodyFlags` gains `can_sleep` + `sleeping` (from `_reserved`); `Body` gains the four sleep columns.
- `src/modules/forge/forge_3d/body_manager.zig` — modify — store the new columns in `addBody` (including `sleep_radius` from the shape's local AABB); `isSleeping`/`sleepTime`/`canSleep` getters; `wakeBody`/`setCanSleep`; activating behaviour on `addForce`/`addTorque`/`addImpulse`; non-activating contract documented on the four setters.
- `src/modules/forge/api/types.zig` — modify — `BodyDescriptor.can_sleep: bool = true`.
- `src/modules/forge/forge_3d/pipeline/integration.zig` — modify — both passes skip sleeping bodies; the accumulator reset stays uniform.
- `src/modules/forge/forge_3d/rigid/contact_constraint.zig` — modify — `build` becomes the wake fixpoint and takes `bm: *BodyManager`.
- `src/modules/forge/forge_3d/rigid/velocity_solver.zig` — modify — `VelocitySolveResult`, `solveRangeReport`, early-out at true zero; `solveRange` unchanged in signature.
- `src/modules/forge/forge_3d/rigid/solver_config.zig` — modify — correct the `velocity_iterations` doc comment (refuted Jolt claim) and point at §1.8.2.
- `src/modules/forge/forge_3d/rigid/position_solver.zig` — modify — spec reference §1.9 → §1.10 only.
- `src/modules/forge/forge_3d/rigid/root.zig` — modify — re-export the island manager surface + `VelocitySolveResult`/`solveRangeReport`; comptime pin.
- `src/modules/forge/forge_3d/root.zig` — modify — re-export `island`/`sleep` at `Real`; comptime pins for the two new test files.
- `src/modules/forge/forge_3d/tests/solver_test.zig` — modify — 11-step cycle, per-island driving, sleeping-body proxy skip, `allow_sleeping = false` on inherited convergence tests, envelopes re-measured.
- `src/modules/forge/forge_3d/tests/island_test.zig` — create — neutral core in isolation + rigid partition acceptance.
- `src/modules/forge/forge_3d/tests/sleep_test.zig` — create — window, write-intent separation, sleep/wake scenarios.
- `CLAUDE.md` — modify — §3.4 patch, applied on the branch in the closing steps (content supplied by Claude.ai).

## Acceptance criteria

### Tests

`tests/island_test.zig`

- `test "island core seeds singletons and links only seeded indices"` — an added-but-unlinked index is its own group; linking is idempotent and symmetric.
- `test "island core grouping is invariant under link-order permutation"` — same partition for any permutation of the link sequence (purity of the core).
- `test "two separated stacks partition into two islands"` — disjoint contact groups, two ranges, every constraint assigned.
- `test "a static ground does not link the bodies resting on it"` — two boxes resting apart on one ground stay in two islands, and each ground contact belongs to its dynamic body's island (no constraint left unassigned).
- `test "a moving kinematic platform does not link but forces its islands awake"` — no fusion through the platform (W3 activation asserted separately in `sleep_test.zig`).
- `test "a dynamic body with no constraint is a singleton island"` — present in the island table with an empty constraint range.
- `test "island ranks are ordered by minimum member BodyId"` — ranks strictly increasing in min member `BodyId`; no tie possible.
- `test "constraint ranges are contiguous and pair-key ascending inside each island"` — the composite ordering holds without relying on sort stability.
- `test "per-island solve equals global solve bit-exactly on disjoint islands"` — both passes, two disjoint stacks, bit-for-bit equality of every body's pose and velocity against a single global range on the same constraint array.
- `test "island partition is invariant under body add-order permutation"` — the partition (the sets of bodies grouped together) is identical; bit-exactness is **not** asserted, because permuting add order changes `BodyId`s hence pair keys hence solve order.

`tests/sleep_test.zig`

- `test "the sleep window is a displacement bound, not an instantaneous velocity"` — a body kept within `maxDisplacement()` over the window becomes eligible; one crossing it resets the reference pose and the window.
- `test "rotation alone resets the window through the sleep radius"` — pure angular motion with zero linear displacement exceeds the bound and prevents eligibility.
- `test "solver writes never rearm the sleep window"` — a box resting on the ground reaches sleep **while both solver passes write its velocity and pose every tick**. This test fails if any solver write path is treated as an external mutation. RED-first: it must be shown failing against an activating variant of the four setters before the contract is in place.
- `test "can_sleep false never sleeps"` and `test "allow_sleeping false never sleeps"` — each independently.
- `test "a resting island sleeps and stops costing"` — after ~`time_before_sleep`, the constraint array is **empty** for that island and every member's position and rotation are bit-frozen over the following 60 ticks.
- `test "falling asleep zeroes both velocities exactly"` — exact zero, not an approximate comparison.
- `test "an external impulse wakes a sleeping body and resets its window"` — `addImpulse` on a sleeping body; `addForce` likewise.
- `test "a non-activating pose write does not wake, an explicit wake does"` — `setPosition` alone leaves `sleeping` set; `wakeBody` + `setPosition` clears it.
- `test "an impact wakes the whole sleeping stack in one tick with its internal contacts"` — five-box stack asleep, one dynamic body dropped on top: after **one** tick all five members are awake **and** the constraint count is back to its pre-sleep value plus the impactor's contacts. Penetration inside the woken island stays `<= 5 * penetration_slop` during the wake tick and the five following ticks, and returns to `<= penetration_slop + O(floatEps(Real)·coordScale)` within 30 ticks (envelope pre-registered here, §1.8.5 — not to be widened after measurement).
- `test "removing the support under a sleeping body wakes it"` — W4 at harness level (the harness owns the retained pair set).
- `test "sleeping and waking is bit-deterministic across two identical runs"` — two worlds built identically, same tick count, bit-identical poses, velocities, sleep flags and windows.

`tests/solver_test.zig` (inherited, modified)

- Every convergence test (five-box stack, resting box, mass ratio) runs with `allow_sleeping = false`. Envelopes are re-measured and reported with before/after values; the expectation, motivated but to be verified, is **no envelope movement** — the stack plus its ground is a single island, therefore a single range, therefore the same termination as today. Any movement is reported with both numbers.

### Benchmarks

No numeric target. Absolute physics performance lands with the full `step()` (M1.1.3/M1.1.4/M1.1.5 precedent). The cost claim of this milestone is asserted structurally instead: an empty constraint array and a bit-frozen pose for a sleeping island (see the test above).

### Observable behavior

- `zig build test-forge-3d` green at f32 **and** `-Dphysics_f64=true`, debug **and** ReleaseSafe; `zig build test` green.
- Reported as numbers in the execution log: (1) the wake-transient penetration figures against the pre-registered envelope; (2) the five-box stack at `velocity_iterations = 8` **with sleeping enabled** — does it fall asleep while still creeping? The answer is expected to be yes (a ~5 mm/s creep is 2.5 mm per 0.5 s window against a 15 mm bound) and is the measured justification for the `allow_sleeping = false` rule on convergence tests. Report the measurement whichever way it comes out.
- Reported as counts: iterations actually run by the velocity pass with the early-out, on a resting island versus at impact, at both precisions.

### CI

- `zig build` clean, zero warnings, configured matrix.
- `zig build test` and `zig build test-forge-3d` green — debug **and** ReleaseSafe, f32 **and** `-Dphysics_f64=true`.
- `zig fmt --check` green; `commit-msg` hook green on every commit.
- §3.6.1 local audit reported in the PR: literal `grep -rn` for the terms this milestone changes (`steps_calculator`, "global constant", `§1.9`, `§1.10`, `can_sleep`, `solveRange`) across `src/`, `CLAUDE.md` and the brief, with the count of remaining occurrences and their status; plus the language criterion (zero French word or identifier in the milestone diff and in this brief).

## Gates

Mandatory STOP+GO between gates. One verdict per gate, on the real pushed diff.

- **E1** — `pipeline/island.zig`: the neutral core and its standalone tests (no physics type involved). Purity under link-order permutation is RED-first.
- **E2** — Body-side sleep state and write-intent separation: `BodyFlags`, the four columns, `BodyDescriptor.can_sleep`, `sleep_radius` at `addBody`, getters, `wakeBody`/`setCanSleep`, activating `add*` versus non-activating setters, plus `pipeline/sleep.zig` (config, domain assert, window sweep, eligibility). RED-first on `"solver writes never rearm the sleep window"` and on the window tests.
- **E3** — `rigid/island_manager.zig`: partition (seeding, dynamic-only linking, ranks by min member `BodyId`, composite-key ranges, moving-non-member flag) and activation arbitration (W2, W3). Partition tests green.
- **E4** — `build` wake fixpoint, integration skips, `solveRangeReport` + early-out. The bit-exact equivalence of the early-out is asserted (same final state with the early-out reached versus the full iteration count).
- **E5** — Harness: 11-step cycle, per-island driving of both passes, sleeping-body proxy skip, `allow_sleeping = false` on inherited convergence tests, envelopes re-measured with before/after values. `"per-island solve equals global solve bit-exactly"` green.
- **E6** — Full acceptance suite (sleep/wake scenarios, one-tick stack wake with the pre-registered envelope, determinism), f64 × ReleaseSafe matrix, the two doc-comment corrections, §3.6.1 audit, `CLAUDE.md` §3.4 patch, closing notes.

## Conventions

- **Branch:** `phase-1/forge/islands-sleep`
- **Final tag:** `v0.11.8-islands-sleep`
- **PR title:** `Phase 1 / Forge / Island manager + sleep detection`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md` §4.3); squash title `feat(forge): add island manager and sleep detection (M1.1.8)`
- **Merge strategy:** squash-and-merge (Guy merges and tags; Claude Code never merges or tags)

## Notes

- **Inherited contracts, binding on this design.** (a) `solveRange` and `solvePositionRange` keep their exact signatures — the range shape is the seam this milestone consumes, and the iteration telemetry arrives through the sibling `solveRangeReport`, not through a changed return type. (b) `build` sorts constraints by ascending pair key and points are in manifold order; island ranks, membership and constraint order must be a **pure function** of the awake dynamic set and that sorted array (anticipates M1.1.14). (c) NGS termination is evaluated per range, never globally; for disjoint islands this is bit-exactly equivalent to a global range, which is what the equivalence test asserts. (d) **The premise of the inherited iteration-budget item is refuted.** M1.1.7 recorded that Jolt derives the velocity budget per island so a deep stack automatically gets more iterations, and designated this milestone as the owner of that mechanism. Read on the source: `PhysicsSettings.h` carries global defaults (`mNumVelocitySteps = 10`, `mNumPositionSteps = 2`) and `CalculateSolverSteps.h` aggregates a **maximum over author-supplied per-body/per-constraint overrides**, falling back on the global default — there is no topology-derived heuristic of any kind. What lands here instead is the mechanism that makes a high global floor affordable: the velocity-pass early-out (`PhysicsSystem.cpp`, `if (!applied_impulse) break;`). The override channel is recorded as additive with no Phase-1 consumer. (e) The warm-start cache evicts implicitly at `endTick`, so a woken island cold-starts for one tick — accepted, with the numeric envelope pre-registered in the acceptance criteria and in §1.8.5. If a measurement leaves the envelope, the answer is selective cache retention at `endTick` **inside this milestone**, not a widened envelope. (f) Candidate-pair retention is a correctness condition, not an optimisation: it is the persistent wake graph (§1.8.7). The harness must keep retaining every emitted pair. (g) The per-tick cycle is normative (§1.7, eleven steps) — activation at step 5, sleep transition and velocity zeroing at step 11, nowhere else. (h) `PositionSolveResult` stays telemetry and is never a sleep criterion. (i) Sleep thresholds are physical constants carried by the config (m/s and s), the same class as `restitution_threshold` and `penetration_slop`; `maxDisplacement()` is their named product, never a literal; every numerical guard stays at true zero. (j) Statics and kinematics never join an island.
- **Design points verified against the reference implementation** (read, not recalled): islands are built from the **active bodies** list, then constraints are attached (`IslandBuilder.cpp`, `Finalize(inActiveBodies, …)` → `BuildBodyIslands`); bodies are linked **only if both are dynamic**, with the stated reason that a constraint cannot affect the velocity of a kinematic body, and each contact is linked to its **first dynamic body** (`ContactConstraintManager.cpp`); a sleeping dynamic body is activated at constraint creation (`ActivateBodies` in the same function); the sleep test is a per-body **displacement** bound over a window — three tracked points, growing spheres, `max_movement = mPointVelocitySleepThreshold * mTimeBeforeSleep` — and never an instantaneous velocity (`Body.cpp`, `UpdateSleepStateInternal`; `PhysicsSettings.h`); the island sleeps only if **every** member can sleep (`PhysicsSystem.cpp`, `CheckSleepAndUpdateBounds`, `all_can_sleep &= …`), checked after the solve; island dispatch order is a job-balance heuristic on constraint count (`IslandBuilder.cpp`, `SortIslands`), unrelated to island identity.
- **Consequence of (j) plus the displacement criterion — the anti-masking rule.** A displacement-bounded criterion lets a slowly creeping body sleep. The M1.1.7 conclusion that a 0.047–0.075 m/s instantaneous residual was "above the Jolt threshold" and therefore could never have been masked by sleep compared an instantaneous velocity against a threshold that is not applied to an instantaneous velocity. It is false. Every convergence measurement in this repository runs with `allow_sleeping = false`, and this milestone measures and reports the 8-iteration case with sleeping enabled to document the masking explicitly.
- **Alternatives examined and rejected.** A persistent sleeping-group structure recorded at sleep time (re-persists an island identity §1.8.3 refuses to attach, needs invalidation on body removal, static teleport and shape change, and saves nothing — rebuilding a woken group's internal contacts goes through the narrowphase either way; the retained pair set already is the adjacency graph). Moving the island manager into `rigid/` because its first input is a rigid type (both `engine-physics-forge.md` §1.2 and `engine-directory-structure.md` place `island.zig` in the shared pipeline; the union-find core is genuinely neutral and the adapter is thin). Waking on the broadphase delta (the fat margin is 0.1 m, so a proxy re-emits only every 0.1 m of travel — a 1 m/s mover would emit once every six ticks, and mere fat-AABB proximity would wake sleepers spuriously). An instantaneous velocity criterion (resets on a single noisy tick, and never sleeps a jittering body — which is one of the things sleeping exists to kill). Jolt's three sleep-test spheres (a dozen accumulated scalars per body and a drifting sphere centre, for the same semantics a reference pose plus a radius gives without accumulation). Changing `solveRange`'s return type (a signature change on a frozen seam; Zig would reject the ignored value at every call site). A separate angular threshold in rad/s (a second constant with no unit relation to the first; the radius-scaled bound covers rotation in metres). Materialising a link array for the neutral core (the adapter calls `link()` directly, no allocation, no copy).
- **`build` signature change, authorised.** `bm: *const BodyManager` → `*BodyManager`, because waking a sleeping endpoint at constraint creation is intrinsically the pair-set consumer's business (Jolt does it in `CreateConstraint`). No call site changes textually: every existing call passes an address of a mutable `BodyManager`. This is not a range-seam change.
- **Determinism by construction (anticipates M1.1.14).** No hash container anywhere on the path. Union-find with path compression and union by size, seeded and linked in the sorted constraint order. Island ranks by min member `BodyId` — unique by disjointness, so no tie-break exists. Constraint order by the composite key `(rank, pair_key)`, so ordering never depends on the sort algorithm's stability. The wake fixpoint scans in sorted pair order and `build` re-sorts its output. Per-island solving preserves bit-exactness against the global range, and will still preserve it when islands run in parallel (disjoint body writes, merge in island order).
- **Spec status.** `engine-physics-forge.md` §1.8 (new), §1.7 (eleven-step cycle), §1.7.1 (the refuted claim corrected), §1.7.2, §1.4, §16, §17 and the §1.8→§1.9→§1.10 renumbering, plus `engine-directory-structure.md` (the `pipeline/` lines and the `PhysicsDebugProvider` reference), were authored and re-uploaded to the KB **before** this brief and are normative. Any deviation discovered in implementation is a Recorded deviation here and triggers a corrective re-upload at close (Claude.ai-produced) — never a silent divergence. The frozen `briefs/M1.1.7-solver-ngs-position.md` still contains the refuted Jolt claim in RD-2: a closed brief is immutable and is **not** patched; the correction lives in the spec, in `solver_config.zig` and in `CLAUDE.md`.
- **No deferred debt.** Any finding surfaced at any point in this milestone is fixed inside this milestone. One milestone, one Claude Code session.

---

# LIVING SECTION

*Maintained by Claude Code during the milestone.*

## Specs read

- [x] `engine-physics-forge.md` — read in full (§1.8 present and complete, so the STOP condition on a stale KB does not apply; §1.7 eleven-step cycle, §1.7.1, §1.7.2, §1.2, §1.5, §2, §1.10, plus §16/§17 for the debug and comparison tables) — 2026-07-25 10:36
- [x] `engine-phase-1-plan.md` — read in full (M1.1 table: M1.1.8 line "Island manager + sleep detection", downstream numbering M1.1.9-15 then the additive completion M1.1.16-24) — 2026-07-25 10:35
- [x] `engine-zig-conventions.md` — read in full (§3 unmanaged-first and the seven-struct allocator whitelist, §13 lazy-analysis comptime pin, §16 extern POD components, plus §8 assertions and §19 rule summary) — 2026-07-25 10:37
- [x] `engine-development-workflow.md` — read in full for §2.4 (blocking protocol), §3.1-§3.4 (brief format, status, `CLAUDE.md` update), §3.6.1 (end-of-milestone local audit and the language criterion), §4.3/§4.4/§4.6/§4.7 (commits, PR, squash and tag) — 2026-07-25 10:37
- [x] `CLAUDE.md` — read at session start (Current state, Tags incl. the `v0.11.7` row, Open / deferred decisions incl. the `velocity_iterations` entry and the two M1.1.6/M1.1.7 "KB spec patch pending" entries) — 2026-07-25 10:34

## Execution log

**2026-07-25 — setup.** Branch `phase-1/forge/islands-sleep` off `main` at `8517d8d`
(tag `v0.11.7-solver-ngs-position`), brief committed verbatim. The STOP condition on
§1.8 does not fire: `engine-physics-forge.md` carries
`### 1.8 Îlots et sommeil — modèle normatif` at line 234, §1.8.1 through §1.8.9
complete. Specs read in the brief's order, brief set to `ACTIVE`.

**2026-07-25 — E1 recon.** Read the surfaces the milestone consumes before writing
anything: `body.zig` (`BodyFlags._reserved: u7`, the SoA `Body` row), `body_manager.zig`
(the stale-safe getter/setter discipline, `addBody`), `contact_constraint.zig`
(`build`/`prepare`, the canonical pair key, the true-zero skip), `velocity_solver.zig`
+ `position_solver.zig` + `solver_config.zig` (the two range-shaped entries),
`contact_cache.zig`, `pipeline/integration.zig` (the split passes), `slot_alloc.zig`
(`isAliveIndex`, the non-compacting store), `tests/solver_test.zig` (the published
harness), `api/types.zig`.

**2026-07-25 — E1 RED-first, purity of the island core.** The permutation invariance of
a union-find holds by construction, so a test written green proves nothing about the
test — it had to be shown discriminating first. `link` was therefore implemented naively
(`parent[b] = a`, no root resolution) and the acceptance test run against it. Two
distinct failures came out.

The full file hangs: `link(1, 3)` then the symmetry check `link(3, 1)` closes a parent
cycle (`parent[3] = 1`, `parent[1] = 3`) and `find` spins forever — the test binary sat
at 99.2 % CPU until killed.

Filtered to the permutation test alone, the failure is a clean partition diff at the
first permutation (`zig test --test-filter "invariant under link-order permutation"`):

```
1/1 tests.island_test.test.island core grouping is invariant under link-order permutation...slices differ. first difference occurs at index 1 (0x1)

============ expected this output: =============  len: 8 (0x8)
[0]: 0
[1]: 0
[2]: 0
[3]: 3
...
============= instead found this: ==============  len: 8 (0x8)
[0]: 0
[1]: 1
[2]: 1
[3]: 3
...
FAIL (TestExpectedEqual)
```

Element 1 is the second endpoint of two different links, so the naive merge strands
whichever group linked first — `{0}, {1,2}` instead of `{0,1,2}`, and a different
stranded group per order. `link` was then implemented root-to-root with union by size
(smaller root index on a size tie) and both tests pass.

**2026-07-25 — E1 implementation.** `pipeline/island.zig` created: `UnionFind` over
opaque `u32` element indices, `std`-only import (no `weld_forge`, no `config.zig`, no
`rigid/` type — the broadphase/narrowphase dependency discipline), unmanaged, with
`seed`/`count`/`find`/`link`. `seed(n)` resets to `n` singletons retaining capacity, so
an element that is never linked stays a singleton group (§1.8.1) and the partition is
rebuilt per tick — island identity is deliberately not persistent (§1.8.3). No hash
container, no address-dependent enumeration. `forge_3d/root.zig` re-exports it as the
scalar-free `island` namespace (it carries no `Real`) and pins `tests/island_test.zig`
for the §13 lazy-analysis guard. That the pin works is not assumed: the naive run above
hung the binary that `zig build test-forge-3d` itself launched, so the build target
really does collect and execute the new file — a silent skip would have exited clean.

**2026-07-25 — E1 gate.** Green on the full matrix: `zig fmt --check`,
`zig build test-forge-3d` in debug and ReleaseSafe at f32 and under
`-Dphysics_f64=true` (four runs), `zig build`, `zig build test`, `zig build lint`.

**2026-07-25 — E1 review fixes (two, both from the gate verdict).** (1) `link`'s doc
comment claimed symmetry in the endpoint order — same partition AND same tree shape —
and nothing tested it: the block labelled "Symmetric" re-linked an ALREADY-merged pair,
which re-asserts idempotence, not symmetry. The description was made true (the assertion
is unchanged, its comment now says idempotence in the other endpoint order) and a
dedicated `"island core link is symmetric in endpoint order"` test added, comparing the
two instances element-wise on `parent` AND `size` — shapes, not partitions, since a
canonical-label comparison would not catch a shape divergence. Not staged RED-first, and
deliberately so: it pins a property already claimed by the code, it did not drive the
design. It passes against the production code unchanged. (2) `find` documented "the
representative index" without saying the representative depends on the link SEQUENCE and
not just the link set — true of any union by size (`link(0,1); link(1,2)` roots at 0,
the reverse order roots at 1), and a direct trap for the E3 adapter, which owes a rank
by minimum member `BodyId`. The invariant now lives on `find` itself and in the header's
Purity bullet, where the consumer reads it, instead of only being implicit in a test
helper. No production behaviour changed by either fix.

**2026-07-25 — E1 gate verdict, split confirmed (no deviation opened).** Ranking by
minimum member `BodyId` and group labelling stay OUT of the neutral core — that is the
brief's own split, `island.zig` exposes only `find`. `seed(n)` rather than an explicit
`addBody(index)` is accepted, the header documenting the dense-projection contract. Two
constraints this places on E3, recorded here so they are not rediscovered: the adapter's
root → rank table is computed from MEMBERSHIP (the minimum member `BodyId`), never from
a root index; and the reverse direction `BodyId` → dense index goes through NO hash
container — a scratch array indexed by slot index (`bm.alloc.validate(id)`) is the
expected form, O(1) and deterministic, with any other choice justified here.

**2026-07-25 — E2 RED-first, the window criterion.** `updateWindows` was implemented
first with the criterion §1.8.3 explicitly rejects — an instantaneous linear speed
against `point_velocity_threshold` — and the suite run against it. Three tests failed
(194/197):

```
error: 'tests.sleep_test.test.the sleep window is a displacement bound, not an instantaneous velocity' failed:
       sleep_test.zig:88: try testing.expect(bm.sleepTime(box).? > 0);
error: 'tests.sleep_test.test.rotation alone resets the window through the sleep radius' failed:
       expected 0, found 0.016666668
       sleep_test.zig:139: try testing.expectEqual(@as(Real, 0), bm.sleepTime(box).?);
error: 'tests.sleep_test.test.a non-activating pose write does not wake, an explicit wake does' failed:
       sleep_test.zig:243: try testing.expect(bm.sleepTime(box).? > 0);
```

The two criteria agree on steady motion — a constant speed under the threshold can never
travel more than threshold × time — so the discriminating cases had to be built
deliberately. They are: a body JITTERING 1 mm per tick, whose instantaneous speed is
0.06 m/s (twice the threshold) while it never leaves a 1 mm neighbourhood, and PURE
ROTATION, whose linear speed is exactly zero while its material points sweep 43 mm per
tick. The velocity criterion refuses the first and accepts the second; the displacement
bound does the opposite, which is the point. The third failure was not predicted and is
a bonus discriminator: the velocity criterion also breaks the wake semantics, since a
residual velocity left by an earlier write keeps resetting the window after an explicit
`wakeBody`. The displacement bound `|Δx| + 2·r·|vec(Δq)|` then made all three pass.

**2026-07-25 — E2 RED-first, write-intent separation.** `"solver writes never rearm the
sleep window"` was shown failing against an activating variant of the four setters, as
the brief requires: with `wakeIndex` temporarily wired into `setLinearVelocity`,
`setAngularVelocity`, `setPosition` and `setRotation`, the suite goes 193/197 and the
mandated test fails at its own assertion —

```
error: 'tests.sleep_test.test.solver writes never rearm the sleep window' failed:
       sleep_test.zig:216: try testing.expect(sleep.isEligible(&bm, box, cfg));
```

— the resting box never becoming eligible because the velocity pass rewrites its
velocity every single tick (gravity keeps adding `−9.81·dt`, the normal solve keeps
cancelling it). The probe was removed and the four lines verified gone. The test drives
the REAL passes, not a stand-in: `integrateVelocities` → `build` → `warmStart` →
`solveRange` → `integratePositions` → `solvePositionRange` → `storeContacts` →
`updateWindows`, over 300 ticks, and it asserts the contact is live and its normal
impulse positive before concluding — otherwise it would prove nothing about solver
writes.

**2026-07-25 — E2 implementation.** `BodyFlags` takes `can_sleep` + `sleeping` out of
`_reserved` (`u7` → `u5`, byte size unchanged); `Body` gains the four sleep columns;
`computeSleepRadius` (in `body.zig`, next to `computeMotion`) derives the radius from
the shape's local AABB by the component-wise max of `|min|`/`|max|`, which is the
furthest corner without enumerating all eight. `BodyDescriptor.can_sleep = true` closes
the gap where the public `RigidBody` component carried the flag and the descriptor
dropped it. `BodyManager` gains `isSleeping`/`sleepTime`/`canSleep`/`sleepRadius`
getters, `wakeBody`/`setCanSleep` (the latter waking on `false`, W1), a private
`wakeIndex` shared by the primitives and the activating mutators, activating
`addForce`/`addTorque`/`addImpulse`, and the non-activating contract documented on the
four setters and in the file header. `pipeline/sleep.zig` carries `SleepConfig` +
`maxDisplacement()` + `assertDomain` + `updateWindows` + `isEligible` + `putToSleep`.

Three placement decisions, none of them scope changes, all recorded here rather than
left implicit:

- `putToSleep` lives in `sleep.zig`, not in the island manager. The brief gives
  `island_manager.zig` the ARBITRATION ("the sleep transition (AND over members)"); the
  mechanism — raise the flag, zero both velocities — belongs next to the window it
  freezes and the eligibility it consumes. Without it at E2 the write-intent contract's
  "does not wake" half would have no sleeping body to observe, and that contract is E2's
  headline deliverable.
- A `sleepRadius` getter was added beyond the three the brief names, so the fourth
  column is observable and the stored value provable against the shape rather than
  re-derived by the test from a hardcoded number.
- `updateWindows` skips static and kinematic bodies (they never join an island, §1.8.1,
  so their window is never read) and saturates `sleep_time` at `time_before_sleep`
  instead of growing without bound — the window's whole meaning is "still long enough",
  and an unbounded accumulator on a `can_sleep = false` body would only collect float
  error in a value nothing reads past the comparison and the debug overlay.

**2026-07-25 — E2 review fix: the aliasing limit of a pose-sampled criterion.** The
header claimed `d` over-estimates the displacement of every material point and that a
radius bound "sees every rotation". True of the NET displacement from the reference
pose, false read as a bound on MOTION: `2·|vec(Δq)| = 2·sin(θ/2)` is a CHORD — it peaks
at θ = π and returns to zero at θ = 2π. A body completing an exact revolution per tick
therefore samples as motionless and can sleep while spinning at 2π/dt ≈ 377 rad/s, and
the regime is not measure-zero (at r = 0.866, ±0.1 rad/s around a multiple of 377 stays
under 15 mm for the whole window). A wheel in the air on a stopped vehicle is exactly
that configuration, and vehicles are Phase 1 (M1.1.21).

This is NOT a design defect to fix: the aliasing is inherent to any pose-sampled
criterion, Jolt's three tracked points alias identically, and the only "correction"
would be to read ω — reintroducing the instantaneous quantity §1.8.3 rejects by name,
plus a second threshold in rad/s with no unit relation to the first. The defect was that
it went unwritten while the header asserted the opposite. Fixed by documentation only —
the aliasing paragraph in the header, the Jolt-divergence sentence bounded by it — plus
a limit test, `"a rotation aliased to a whole turn per tick is invisible to the window
(documented limit)"`, which asserts the REAL behaviour and says so in its name. It
carries its own sanity assertion (the sampled chord really is under the bound at every
tick) and a discrimination guard (the identical loop at a HALF turn per tick sweeps
2·r ≈ 1.73 m and never becomes eligible), so what is pinned is the aliasing of a
rotation that genuinely happened, not a test that rotates nothing. No RED-first staging:
it pins an existing regime and drives no design. **Zero production lines changed** —
`updateWindows` is untouched. Owed to `Closing notes` at close, under residual risks.

**2026-07-25 — E4 trap recorded now, not discovered later.** It follows directly from
the semantics just set: a static body NEVER has `sleeping == true`, because the window
sweep skips non-dynamic bodies entirely. So `build`'s skip filter cannot be written
`if (a.sleeping and b.sleeping) skip`. The §1.8.6 predicate is NON-AWAKE = a sleeping
dynamic body OR a non-dynamic body with zero velocity. Written naively, a sleeping stack
resting on the ground would keep narrowphasing every one of its ground pairs and the
milestone's whole saving would not exist. `"a resting island sleeps and stops costing"`
would catch it at E6, but it is cheaper to know it at E4.

**2026-07-25 — E3 implementation.** `rigid/island_manager.zig` created: `collectMembers`
(awake dynamic bodies, ascending `BodyId`) → `core.seed` → `linkConstraints`
(member↔member only) → `assignRanks` → `groupMembers` → `orderConstraints` →
`markMovingNonMembers` → `applyW3`, plus `sleepEligibleIslands` for step 11. Ranks come
out ordered by minimum member `BodyId` without ever comparing a root: members are sorted
ascending by handle, so walking them ascending and numbering roots on first sight IS
that order — which is also why the root's non-canonicity documented at E1 never leaks
in. The reverse `BodyId` → dense direction is the scratch array indexed by slot the
E1 verdict asked for, read through `alloc.validate` so a stale handle cannot resolve to
whatever body has since refilled its column. Members are grouped by counting sort,
ascending by handle inside each island.

`IdAllocator.idAtIndex` was added to `slot_alloc.zig`, a file outside the brief's list —
justification: an index-ascending sweep sees columns, a column carries no generation,
and the partition ranks by `BodyId`. Rebuilding the handle belongs where the packing
lives; the alternative was `body_manager.zig` reaching into `alloc.slots` and duplicating
knowledge of the bit layout.

Also delivered here rather than left homeless: `sleepEligibleIslands`, the step-11
arbitration. Gate E5 is harness-only and gate E6 is acceptance, so the AND over members
has no other gate to land in, and E5's cycle needs it to exist.

**2026-07-25 — E3 finding, caught by its own test: the permutation convention.**
`applyPermutation` was written with the textbook in-place loop while the sort keys
carried the SOURCE convention ("position i takes from `source_index`"). That loop
implements the DESTINATION convention ("what is at s goes to `perm[s]`"), and fed a
source permutation it does not fail — it silently produces a different, wrong order.
Worse, it swapped the KEYS along with the constraints, so the sorted key array was
scrambled and the range scan that reads ranks from it ran off the end.

Both showed up as the same crash, on `"island ranks are ordered by minimum member
BodyId"` and `"constraint ranges are contiguous and pair-key ascending inside each
island"` (204/207, three crashes):

```
thread panic: reached unreachable code
   island_manager.zig:362: std.debug.assert(cursor == self.keys.items.len);
   island_manager.zig:173: try self.orderConstraints(gpa, bm, constraints);
   island_test.zig:392: try rig.partitionAll(gpa);
```

Fixed by inverting explicitly into a `destination` scratch array before permuting, which
also leaves `keys` sorted so the range scan is reading what it thinks it is reading. The
convention trap is now named in `applyPermutation`'s doc comment — it is the kind of
error that produces plausible output rather than a crash, and it only crashed here
because the corrupted keys tripped the tiling assert. The composite-key test was built
to discriminate: creation order `0,3,4,1,2` gives a chain `{0,3,4}` of rank 0 owning the
LARGEST pair key of the scene, and a far pair `{1,2}` of rank 1 whose key sits strictly
between the chain's two — so pair-key order and rank order genuinely disagree, and the
test asserts that ordering explicitly rather than trusting sort stability.

**2026-07-25 — E4 implementation.** `build` becomes the wake fixpoint and takes
`bm: *BodyManager` (no call site changed textually, as the brief predicted). Three
pieces: a pair with neither endpoint awake is skipped and its INDEX recorded in a
deferred list (indices, not keys, and the list stays unallocated in a fully awake
scene); `emitPair` wakes any sleeping endpoint the moment a manifold exists; the
deferred list is re-scanned while a pass wakes at least one body. Termination is
bounded by the number of sleepers, the output is re-sorted by pair key, and at rest
the second pass never happens.

The awake predicate lives in `pipeline/sleep.zig` as `isAwake`, with `isMoving`
extracted alongside it — `island_manager.zig` had grown its own copy of the true-zero
velocity test at E3 and the two would have drifted. `contact_constraint.zig`'s
import-discipline header was updated to admit `../pipeline/sleep.zig`; the rigid branch
already depended on that file through the island manager.

Both points the E3 verdict traced are honoured by construction rather than
rediscovered. The wake on manifold fires for a sleeping endpoint whatever the other
endpoint is — the loop walks both endpoints and asks only "is this one sleeping". And
`isAwake` is written on MOTION, not on the flag: a static body never carries `sleeping`,
so "both flags set" would have left a sleeping stack narrowphasing all its ground pairs;
a kinematic platform at non-zero velocity counts as awake, which is what keeps the
sleeper-on-a-conveyor pair narrowphased so the fixpoint can wake it. Both are pinned by
tests, including the platform-at-rest control (zero constraints, box still asleep). W4
(a teleported support) is untouched — out of scope, M1.1.15.

Integration: both passes skip sleepers, the force/torque reset stays uniform over every
live body. Pinned by a test that leaves a pending force on a sleeper and proves it is
cleared rather than deferred — waking the body afterwards produces no velocity.

`solveRangeReport` + `VelocitySolveResult` added; `solveRange` is now
`_ = solveRangeReport(…)` with its signature untouched. The early-out tests at TRUE
ZERO: `solveNormalPoint`/`solveFrictionPoint` return whether they applied anything.

**2026-07-25 — E4 finding: a vacuous discrimination guard, caught at once.** The
bit-exactness test's guard was a three-box stack all driven at the same velocity — so
every relative normal velocity was zero, the pass applied nothing and early-outed at
iteration 1 instead of reaching the ceiling: `expected 4, found 1`. The guard was
asserting nothing. Rebuilt on a static ground carrying three boxes driven downward, so
the impulse has to propagate up the chain; the ceiling is now genuinely reached at a
budget of 4 and the velocity at 4 differs from the velocity at 16 — which is what makes
the 16-versus-64 bit-identity evidence of the early-out being exact rather than evidence
that the iteration count does not matter.

**2026-07-25 — E4 finding: an over-specified iteration count, caught by the f64 leg.**
The approaching-contact case asserted `iterations_run == 2` on the reasoning that a
sphere pair with its lever arm parallel to the normal solves in exact arithmetic. Green
at f32, RED at both f64 runs — `expected 2, found 3`. The reasoning was wrong: an exact
count of a float convergence is not a contract, it is last-bit behaviour, and it
legitimately differs by precision. Re-anchored on the contract itself: the pass stops
short of its budget, and once stopped the state is a FIXED POINT — solving the same
range again spends exactly ONE iteration, the one that finds nothing to apply, and
leaves every velocity bit-unchanged. That statement is precision-independent and says
more than the count did. The separating case keeps its exact `== 1`, which is structural
(the Catto clamp pins λₙ at zero and the friction cone has zero width) rather than
arithmetic.

**2026-07-25 — E4 review fix: the first pass was discarding its own wake signal.**
`build`'s first pass wrote `_ = try emitPair(…)`, throwing away the return value that
IS the fixpoint's progress signal, and compensated with a pessimistic
`var woke_someone = true`. Correct results, but it made a resting tick — the exact state
this milestone exists to make cheap — run one empty round: the loop condition was true,
every deferred pair was re-tested with `bothAsleep`, nobody woke, exit. So each sleeping
pair paid the awake predicate twice per tick, two `validate` calls plus the flag/type/
velocity reads each time, while the doc comment claimed "the second pass does not
happen: zero cost". The wrong place for a false claim, that one being the milestone's
central promise.

Fixed by carrying the signal instead of guessing it: the first pass sets
`woke_someone`, the loop starts from `false`. Safe because `bothAsleep` can only change
through a wake — if pass one woke nobody, no deferred pair became processable. Better on
both axes at once, and at rest the loop now does not run. The cost model in the doc
comment was rewritten to state what a resting tick actually pays (one awake test per
candidate pair, one `u32` per deferred pair, that list's allocation) against what the
skip removes (the narrowphase and `prepare` of all of them), with the per-tick buffer
flagged as movable to the orchestrator's scratch at M1.1.15 — `build` owns no state, so
it cannot be reused from here. No signature change.

**2026-07-25 — E5 harness.** `tests/solver_test.zig`'s `World` now runs the eleven
normative steps of §1.7 in order, with both solver passes driven ONCE PER ISLAND RANGE,
the broadphase proxy update skipping sleepers, and step 11 (window sweep, then the sleep
transition) as the only place a body falls asleep. Telemetry is aggregated over islands
into `velocity_result`/`position_result` — for a one-island scene that is the island's
own result, which is what the inherited tests read — plus `slept_last_tick`.

Sleeping is enabled by default in the harness; the eighteen inherited measurements moved
to a named constructor, `World.initNoSleep`, rather than eighteen repeated assignments.
The name states the load-bearing fact at each call site and the reason lives once, on the
constructor: §1.8.3 makes it normative, and beyond that several of those tests would stop
testing anything — a resting body that sleeps is pose-frozen, so a no-sinking or no-drift
assertion goes vacuous, and `"restitution is captured before warm start"` would break
outright, since it restarts the box with the NON-activating `setLinearVelocity`.

**2026-07-25 — E5 envelope re-measurement: before/after, and they are identical.** Not
"the assertions still pass" — the quantities themselves were measured on both sides. A
temporary probe was installed on this branch and, through a `git worktree`, on `main` at
`8517d8d` (M1.1.7), running the same four scenes; the branch measures with
`allow_sleeping = false`, which is the same configuration as `main`, where sleeping does
not exist. Both legs at f32 and f64, then the probe and the worktree removed.

| Quantity | f32 before → after | f64 before → after |
|---|---|---|
| resting-box penetration | 0.005000591 → 0.005000591 | 0.005000000 → 0.005000000 |
| resting-box y | 0.994999470 → 0.994999470 | 0.995000000 → 0.995000000 |
| resting-box speed | 0.000000001 → 0.000000001 | 0.000000000 → 0.000000000 |
| five-box stack penetration | 0.004999259 → 0.004999259 | 0.004466091 → 0.004466091 |
| five-box stack lateral offset | 0.021848189 → 0.021848189 | 0.008184278 → 0.008184278 |
| five-box stack late speed | 0.000000184 → 0.000000184 | 0.000000000 → 0.000000000 |
| five-box stack top y | 4.983634500 → 4.983634500 | 4.991796190 → 4.991796190 |
| mass-ratio y light | 0.995369430 → 0.995369430 | 0.995070054 → 0.995070054 |
| mass-ratio y heavy | 1.992647800 → 1.992647800 | 1.990892443 → 1.990892443 |
| far-from-origin penetration | 0.003886328 → 0.003886328 | 0.002205241 → 0.002205241 |
| far-from-origin top y | 5002.996000000 → 5002.996000000 | 5002.997340642 → 5002.997340642 |

Zero movement on all eleven, digit for digit, at both precisions — the brief's motivated
expectation, now verified rather than argued. It holds for a structural reason: each of
these scenes is one stack plus its ground, therefore ONE island, therefore one range
identical to the global one; the wake fixpoint degenerates to the old scan when nothing
sleeps; and the velocity early-out only fires on an iteration that applies nothing, so it
cannot move a result.

**2026-07-25 — E5 per-island equivalence.** `"per-island solve equals global solve
bit-exactly on disjoint islands"` green at both precisions. Two identically-built,
identically-partitioned worlds — so the constraint ARRAY and its order are the same, and
the only variable is whether the passes are driven per range or over the whole array. The
test asserts that the two strategies really ran different iteration counts
(`fastest_island < global_velocity`) BEFORE asserting the states match, so the equality is
evidence about the early-out rather than about the two runs being one computation.

The first attempt at that guard was too weak, and the f64 leg caught it: two box stacks of
2 and 3 boxes both ran to the iteration ceiling at f64, so `fastest_island` equalled the
global count and the guard failed. Rebuilt on islands separated by CONSTRUCTION rather
than by degree — a sphere pair, whose single contact point settles in a couple of
iterations at either precision, against a three-box stack whose face-face manifolds run to
the ceiling. Two scenes chosen to differ, not one scene hoped to.

**2026-07-25 — E6, the two doc-comment corrections.** `solver_config.zig`'s
`velocity_iterations` doc no longer repeats the refuted claim: it now states what
§1.8.2 established on the source — Jolt carries global defaults
(`mNumVelocitySteps = 10`, `mNumPositionSteps = 2`) and its per-island aggregation is a
MAXIMUM over author-supplied overrides with no topology heuristic anywhere, so a deep
stack receives exactly the global default — and names the early-out, not a derived count,
as what makes a high floor affordable. `position_solver.zig`'s `get_solver_iterations_stats`
reference moved §1.9 → §1.10.

**2026-07-25 — E6 measurements (the three the brief asks for as numbers).** Produced with
a temporary probe, both precisions, removed afterwards.

*(1) Wake transient* — see the Blockers entry for the full table and the two controls.
Clause 1 of the envelope passes with 3×+ margin (0.006984 f32 / 0.007964 f64 against
0.025); clause 2 is the open point.

*(2) Five-box stack at `velocity_iterations = 8` with sleeping ENABLED — does it fall
asleep while creeping?* **Yes, decisively, and this is the measured justification for the
`allow_sleeping = false` rule.**

| | f32 | f64 |
|---|---|---|
| tick the island fell asleep | 118 | 79 |
| instantaneous speed just before sleeping | 0.061 m/s | 0.015 m/s |
| lateral offset frozen at sleep | 0.0177 | 0.0281 |
| same stack, sleeping OFF: lateral at t=600 | 0.0709 | 0.0808 |
| same stack, sleeping OFF: lateral at t=1800 | 0.1737 | 0.1284 |
| same stack, sleeping OFF: speed at t=1800 | 0.0133 m/s | 0.0014 m/s |

At f32 the stack goes to sleep while moving at 0.061 m/s — TWICE
`point_velocity_threshold` — and freezes its lateral offset at 0.0177, where the same
stack left awake walks on to 0.1737. Sleep does not merely mask the non-convergence, it
hides an order of magnitude of it. This also settles the M1.1.7 reasoning that a
0.047–0.075 m/s residual was "above the Jolt threshold" and therefore unmaskable: the
threshold is not applied to an instantaneous velocity, and here a 0.061 m/s body sleeps.

*(3) Velocity iterations actually run with the early-out* (five-box stack, ceiling 16):

| | f32 | f64 |
|---|---|---|
| at impact (first 20 ticks, max) | 16 | 16 |
| at rest (tick 600) | 11 | 16 |
| position pass at rest | 1 | 1 |
| tick after an external impulse | 16 | 16 |

Reported as it came out, not as hoped: the early-out saves 5 of 16 at rest at f32 and
NOTHING at f64, where a resting five-box stack still applies a non-zero `Δλ` on every one
of the 16 iterations. The position pass is where the saving is unambiguous — 1 iteration of
3 at rest, at both precisions, because it reaches the `sep >= 0` gate on the first pass. The
early-out is exact, but on this scene it is not the cost win the §1.8.2 argument implies;
that argument holds for genuinely quiescent contacts, which a 40-point five-box chain at
f64 is not.

## Recorded deviations

**RD-1 — the wake-transient envelope's clause 2 is REPLACED, not widened.** Claude.ai
round-trip, 2026-07-25, on the measurements below and the two controls in the Blockers
entry.

The frozen criterion had two clauses. Clause 1 — worst penetration `<= 5 · penetration_slop`
over the wake tick and the five following — was anchored on an existing measurement (the
budget the M1.1.7 stack test already used) and passes with 3× margin. Clause 2 — return to
`penetration_slop + O(floatEps·coordScale)` within 30 ticks — was anchored on nothing: it
was pre-registered without measuring what the baseline does, that baseline being an island
that never slept. The controls show that baseline fails the same clause HARDER, never
returning within 200 ticks at either precision with a live warm cache. A clause a
sleepless scene fails worse never measured the wake; it measured the position pass's fixed
point on a six-contact chain.

Widening the bound is what the brief forbids. Keeping it would pin a solver characteristic
under the name of a wake criterion. So it is replaced by the criterion it should have been
— a DIFFERENTIAL — plus an extinction check:

- **(2a) Differential.** Over the transient window, the woken island's worst penetration
  is `<=` that of a never-slept control on the same scene and the same window, plus a
  named tolerance of one `penetration_slop`. The control is built inside the test with
  `allow_sleeping = false`. This asks the question the cold start actually raises — does
  it cost anything? Measured: no, it does better at f32 (0.006984 vs 0.010330), because
  `putToSleep` zeroed the velocities so the island meets the impact from exact rest while
  the control still carries its settling creep. The tolerance covers f64, where the
  control is better by 1.7e-4, three per cent of one slop. The cold start itself is
  asserted rather than assumed: on the wake tick `cache.hits == 0` and `misses > 0`, while
  the control is warm-starting (`hits > 0`).
- **(2b) Extinction.** The transient dies out and the whole island settles back to sleep
  within 200 ticks — a generous ceiling by design, whose job is to catch a wake that
  DESTABILISES the stack rather than to tune a threshold. No bound on the absolute
  penetration reached: the scene now carries six contacts where it carried five, so its
  fixed point is not the pre-sleep one and demanding that value back would be asking for
  something false.

The pre-registered remedy — selective retention of sleeping pairs' cache entries at
`endTick` — was NOT applied, and the reason is the measurement rather than a preference:
the control's cache is already live and it fails the clause harder, so retention would
change the cache lifecycle and not move the number.

**RD-2 — the NGS resting fixed point grows with chain length. Pre-existing, EXPOSED here,
not introduced and not a design boundary of this milestone.** The proof of the
classification is the never-slept control failing identically: the characteristic has been
there since M1.1.7.

M1.1.7's RD-1 established that `penetration_slop` is a fixed point the position pass
approaches from above, so the attainable rest is `slop + O(floatEps·coordScale)`; it
calibrated that on a single box at 1 m, measuring an excess of 5.9e-7 at f32. Measured here
on a chain: a five-box stack settles at a deepest penetration of 0.004999 (f32) / 0.004466
(f64), BELOW `slop`; adding a sixth box puts it above `slop + 16·floatEps·6`, and it does
not come back within 200 ticks — with or without sleep. The mechanism is that each tick
re-injects penetration under gravity which three NGS iterations at factor 0.2 only partly
resorb, and the per-tick injection grows with the load the deepest contact carries.

Not fixed here, deliberately. It lives in the position pass, whose relaxation and slop model
are M1.1.7's deliverable; retouching it inside an islands-and-sleep milestone would be scope
drift, not debt paid. "No deferred debt" governs findings produced by this milestone's work,
not an upstream characteristic one of its tests reveals — the distinction set at the M1.1.1
hotfix. Owner: to be characterised BEFORE joints (M1.1.16), which lengthen chains. Recorded
as an open decision in the `CLAUDE.md` §3.4 patch.

**RD-3 — the §1.8.2 early-out claim was over-sold in two doc comments, corrected on the
measurement.** `solver_config.zig` and `velocity_solver.zig` both said the early-out makes
`velocity_iterations` "a rarely-reached ceiling rather than a fixed cost, which is what
makes a high global floor affordable". Measured on a resting five-box stack (40 contact
points, ceiling 16): 11 iterations at f32 and all 16 at f64. The cause is structural and is
now written down — the predicate is a TRUE-ZERO test, so it fires only once `Δλ` underflows
to exactly zero, which happens sooner in coarser precision; the yield is therefore a
function of the type's granularity, not of the scene alone. The predicate is unchanged: an
epsilon would break the bit-exact equivalence and the threshold discipline both. Only the
claim changed. The unambiguous saving is the position pass — 1 iteration of 3 at rest, at
both precisions. Claude.ai re-issues §1.8.2 and §1.8.5 in the corrective KB re-upload at
close; the spec is not edited from here.

## Blockers encountered

**2026-07-25 — E6, the wake-transient envelope's second clause. RESOLVED the same day by
a Claude.ai round-trip: the clause is REPLACED, see Recorded deviation RD-1. The
measurement and the controls below are what it was resolved on, and are kept as the
record. Implementation was stopped on this point only and resumed after the ruling.**

The pre-registered envelope (§1.8.5 and this brief's acceptance criteria) has two clauses.
The first passes comfortably. The second does not, and the measurement shows the
pre-registered remedy is aimed at a cause the data excludes — so I am not applying it, and
I am not widening the envelope either.

Scene: a five-box stack settles and falls asleep, then a sixth box is placed on top,
overlapping by one slop, and one tick is run. Measured at both precisions, plus two
controls built to separate the causes.

| Case | worst penetration, wake tick + 5 | ticks to return `<= slop + 16·floatEps·6` |
|---|---|---|
| woken island, impactor at 3 m/s (f32) | 0.006984 | 80 |
| woken island, impactor at 3 m/s (f64) | 0.007964 | 52 |
| woken island, impactor AT REST (f32) | 0.006984 | never within 200 |
| woken island, impactor AT REST (f64) | 0.007964 | 32 |
| CONTROL — never slept, impactor at 3 m/s (f32) | 0.010330 | never within 200 |
| CONTROL — never slept, impactor at 3 m/s (f64) | 0.007796 | never within 200 |

Envelope clause 1 is `<= 5 · penetration_slop` = 0.025. Every row passes it with a margin
of 3× or more, and the woken island is BETTER than the awake control at f32 (0.006984 vs
0.010330) — `putToSleep` zeroed its velocities, so it takes the disturbance from exact
rest, whereas the awake control carries M1.1.7's residual creep. The cold start does not
degrade the transient.

Envelope clause 2 is where it fails, and three facts say the wake is not the cause:

1. The awake control has a fully WARM cache — `misses = 0` measured — and fails the same
   clause harder, never returning within 200 ticks at either precision.
2. The woken island recovers FASTER than the awake control, not slower.
3. Removing the impact energy entirely (impactor at rest) does not help at f32; it is the
   added LOAD of a sixth box, not the impact, that moves the steady state.

So the clause is not bounding the wake. What it is bounding is the M1.1.7 RD-1
characteristic — `penetration_slop` is a fixed point the position pass approaches from
above, so the attainable rest is `slop + O(floatEps·coordScale)` — evaluated on a SIX-deep
chain, where it was calibrated on a single box at 1 m. The steady-state deepest penetration
of a 6-deep chain under gravity sits above `slop + 16·floatEps·6`, because each tick
re-injects penetration that three NGS iterations at factor 0.2 only partly resorb, and the
per-tick injection grows with the load carried by the deepest contact. Measured for
reference: the five-box awake stack settles at 0.004999 (f32) / 0.004466 (f64), i.e. BELOW
the bound — the sixth box is what pushes it over.

**Why I did not apply the pre-registered remedy.** The brief's instruction is that a
measurement leaving the envelope is answered by selective retention of sleeping pairs' cache
entries at `endTick`, inside this milestone. Fact 1 excludes it: the awake control's cache is
already warm, with zero misses, and it fails the clause harder. Retaining cache entries
across sleep would make the woken island's first tick warm — which the measurement says is
not what delays recovery. Implementing it would add a merge-instead-of-swap to `endTick`,
change the cache lifecycle, and not move the number.

**What is in the tree right now.** The acceptance test asserts clause 1 (which passes) and,
in place of clause 2, asserts the substance of "the island recovers": the disturbance dies
out and the whole island settles back to sleep within 200 ticks. The unasserted clause is
named in the test body with a pointer to this entry, so it cannot be mistaken for a dropped
criterion. Nothing is widened.

**What I need decided.** Three options I can see, and I am not choosing between them:
(a) re-scale clause 2's bound for a multi-contact chain, e.g. `slop + k·floatEps·coordScale`
with `k` a function of chain depth, or a stated absolute allowance; (b) re-scope clause 2 to
the scenario it was calibrated on — a single body or a shallow island — and record the
6-deep chain figure as a measured characteristic of the position pass rather than of the
wake; (c) something else. Options (a) and (b) both amount to editing a FROZEN acceptance
criterion, which is a Claude.ai round-trip.

## Closing notes

### §3.6.1 local audit

Run AFTER the `CLAUDE.md` §3.4 patch was applied, so these are final counts.

**Drift.** Literal `grep -rn` per term this milestone changes, across `src/`, `CLAUDE.md`
and this brief. Counts are occurrences, not files.

| Term | `src/` | `CLAUDE.md` | brief | Status |
|---|---|---|---|---|
| `steps_calculator` | 0 | 0 | 3 | Clean. Gone from `solver_config.zig`, and the `CLAUDE.md` open-decision orphan was replaced by the §3.4 patch. Brief hits are RD-3 and this audit quoting it to refute it. |
| "global constant" | 1 | 0 | 2 | The `src/` hit is the CORRECTION text itself (`solver_config.zig`, "that Jolt uses no global constant … is false"), not the claim. |
| `§1.9` | 0 | 1 | 7 | `src/` clean — `position_solver.zig` corrected to §1.10. The single `CLAUDE.md` hit is the `v0.11.7` TAG ROW, a historical record of what that milestone delivered under the old numbering; the §3.4 patch deliberately leaves it and carries the correction in the adjacent `v0.11.8` row, which states the refutation explicitly. |
| `§1.10` | 2 | 1 | 10 | Both `src/` hits are the corrected references (`position_solver.zig`, `velocity_solver.zig`); the `CLAUDE.md` hit is the new tag row. |
| `can_sleep` | 13 | 1 | 12 | All `src/` hits are this milestone's new surface (`BodyFlags`, `BodyDescriptor`, getter, setter, tests). None stale. |
| `solveRange` | 29 | 4 | 12 | The seam is unchanged in signature, so every reference stays valid; `solveRangeReport` is a sibling, not a replacement. |

Both orphans reported at the STOP are resolved: the `CLAUDE.md:129` open decision was
replaced wholesale by the patch, and the `v0.11.7` tag row keeps its historical wording
with the refutation stated one row below. The frozen
`briefs/M1.1.7-solver-ngs-position.md` still carries the refuted claim in its RD-2 and is
deliberately NOT patched — a closed brief is immutable; the correction lives in the spec,
in `solver_config.zig` and in `CLAUDE.md`.

**Language.** `grep -E '\b(le|la|les|une|des|pour|avec|dans|être|cette|où|donc|ainsi|car)\b'`
over the added lines of `git diff main...HEAD` — **1 hit**, and over the committed brief —
**1 hit**: the same line, this audit paragraph QUOTING ITS OWN PATTERN. Every other line of
19 files and +4000 lines is clean, and `CLAUDE.md` is at 0. No French word or identifier in
the milestone.

### Residual risks

Four, each with what is not pinned and who owns it.

1. **Pose-sampling aliasing of the sleep window.** The criterion measures NET displacement
   from a reference pose, and `2·sin(θ/2)` is a chord: it peaks at θ = π and returns to
   zero at θ = 2π. A body completing an exact revolution per tick samples as motionless and
   sleeps while spinning at ≈ 377 rad/s, and the regime is not measure-zero (at r = 0.866,
   ±0.1 rad/s around a multiple of 377 stays under the 15 mm bound for a whole window). A
   wheel in the air on a stopped vehicle is that configuration, and vehicles are Phase 1
   (M1.1.21). Inherent to ANY pose-sampled criterion — Jolt's three tracked points alias
   identically — so it is documented, not fixed: the only correction would be to read ω,
   reintroducing the instantaneous quantity §1.8.3 rejects by name plus a second threshold
   in rad/s with no unit relation to the first. Pinned by
   `"a rotation aliased to a whole turn per tick is invisible to the window (documented
   limit)"`. Exposure is bounded by the decision being per ISLAND — any moving member keeps
   the group awake. Escalation, should it ever be observed: an ADDITIVE angular-rate guard.
2. **The wake fixpoint's round count is not pinned.** The suite proves the fixpoint's
   CORRECTNESS (a three-deep sleeping chain woken in one `build`, controls at rest) but
   nothing observes how many rounds it takes. The E4 defect — a first pass discarding its
   wake signal, costing one empty round per resting tick — changed no result and was
   therefore invisible to every test; it was found by review, not by red. Pinning it needs
   a round counter returned from `build`, whose signature was deliberately stabilised at
   E4, and the milestone's cost claim is already carried by an observable (empty constraint
   array, bit-frozen pose). Owner: **M1.1.15**, the `step()` orchestrator, which owns both
   the per-tick scratch and the per-tick telemetry. Regression possible without a red test.
3. **`build`'s per-tick deferred buffer.** The fixpoint allocates a `u32` list each tick in
   which any pair is deferred — that is, in exactly the resting scene the milestone makes
   cheap. It stays unallocated in a fully awake scene. It cannot be reused from `build`,
   which owns no state. Owner: **M1.1.15**, same reason as (2) — it moves to the
   orchestrator's scratch.
4. **The NGS resting fixed point grows with chain length** (RD-2). Pre-existing since
   M1.1.7, EXPOSED by this milestone's wake test, neither introduced nor a design boundary
   here — the proof is that a never-slept control fails identically. M1.1.7 calibrated
   `slop + O(floatEps·coordScale)` on a single box at 1 m (excess 5.9e-7 at f32); measured
   here, a five-box stack settles at 0.004999 (f32) / 0.004466 (f64), below the bound, and
   a sixth box puts it above `slop + 16·floatEps·6` with no return inside 200 ticks, sleep
   or no sleep. Not fixed here: it lives in the position pass, whose relaxation and slop
   model are M1.1.7's deliverable, and retouching it under an islands-and-sleep milestone
   would be scope drift. Owner: to be characterised **before joints (M1.1.16)**, which
   lengthen chains. Carried as an open decision in the `CLAUDE.md` §3.4 patch.
