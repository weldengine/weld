# M1.1.2 — Narrowphase: GJK convex intersection

> **Status:** CLOSED
> **Phase:** 1.1
> **Branch:** `phase-1/forge/narrowphase-gjk`
> **Planned tag:** `v0.11.2-narrowphase-gjk`
> **Dependencies:** M1.1.1 (`v0.11.1-broadphase-bvh`)
> **Opened:** 2026-07-13
> **Closed:** 2026-07-18 (re-closed after three post-delivery correction cycles — the third recalibrating the contact margin and noise floor to pure float-noise thresholds)

---

# FROZEN SECTION

*Produced by Claude.ai. Not modifiable by Claude Code outside a Claude.ai round-trip (cf. § Recorded deviations).*

## Context

Third sub-milestone of M1.1 (Forge 3D rigid branch, comptime core arc M1.1.0–15). Delivers the first narrowphase layer: distance-based GJK detection between convex shapes (sphere/box/capsule), consuming the candidate pairs produced by the M1.1.1 broadphase. GJK runs on the shapes' convex **cores** (point / segment / box) with an inflation radius carried alongside — the Jolt convex-radius architecture — so the plan's touch/no-touch acceptance is `distance(cores) ≤ r_a + r_b`. EPA + contact manifold is the NEXT sub-milestone (M1.1.3); pair-type fast paths are M1.1.4. Algorithm scope is Jolt-inspired (GJK closest-point, Voronoi-region simplex solver), not a clone.

## Scope

- **E1 — Support shapes + support functions.** `src/modules/forge/forge_3d/pipeline/narrowphase.zig` created, generic over `comptime T: type`, importing `foundation` (math) only (same dependency discipline as `broadphase.zig`). `SupportShape(T)` = `{ core: Core, radius: T }` with `Core = union(enum) { point, segment: T, box: Vec3 }` (see Notes for the exact freeze). Local support function `support(core, dir) Vec3` per core variant with fixed tie-breaks (see Notes). Relative-pose precompute: GJK evaluates in the frame of shape A — `rot_rel = conj(rot_a)·rot_b`, `pos_rel = conj(rot_a)·rotate(pos_b − pos_a)` — computed once per pair; A's support needs no rotation. Unit tests: extremal points per core on cardinal and oblique directions, and through non-trivial relative rotations.
- **E2 — Simplex solver (Voronoi regions).** Origin-closest-point on point / segment / triangle / tetrahedron as pure functions (Ericson-style case analysis), each returning the closest point, the barycentric coordinates, and the reduced supporting feature (which simplex vertices survive). Simplex vertices are triplets `(w = a − b, support_a, support_b)` so closest points on A and B are reconstructible from the barycentrics. Fixed region-evaluation order (deterministic tie-breaks). Exhaustive unit tests per case: interior / face / edge / vertex regions, plus degenerate inputs (duplicated vertices, collinear triangle, coplanar tetrahedron).
- **E3 — GJK loop + result.** Iterative descent on the Minkowski difference of the two cores: `support_{A−B}(d) = support_A(d) − support_B(−d)` (B's support through the relative pose). Bounded iterations (named constant, 32) and a named relative termination tolerance; termination on lack of progress, never on exact float equality. `GjkResult(T)` with `status: enum { separated, shallow, deep }`: `separated` (core distance strictly greater than `r_a + r_b`) and `shallow` (cores disjoint, inflated shapes touching or overlapping) both carry the core distance and the closest points on both cores re-expressed in **world** space; `deep` (cores intersect) carries the terminal origin-enclosing simplex (EPA seed for M1.1.3) and no distance. The plan's touch/no-touch = `status != .separated`. Tests: all 6 type combinations (ss, sb, sc, bb, bc, cc) in all 3 regimes with non-trivial rotations; distances checked against analytic values; returned closest points consistent (their separation equals the returned distance; each lies on its core); determinism (two runs, identical results); adversarial convergence case (near-parallel box faces) terminates within the iteration bound.
- **E4 — forge_3d integration.** `shape.zig`: `supportShape(shape: Shape) SupportShape(Real)` conversion (sphere → point + r, capsule → segment(half_height) + r, box → box(half_extents) + 0). `body_manager.zig`: safe getter `rotation(id) ?Quatr` (symmetric to `position`), and `gjkPair(store: *const ShapeStore, a: BodyId, b: BodyId) ?GjkResult` — resolves both bodies' pose + support shape and runs GJK; null on any stale handle. `root.zig` re-exports at `Real` (`SupportShape`, `GjkResult`, plus what naming conventions require) + comptime test pins. Integration test: bodies via `BodyManager`, proxies via `Broadphase(Real)` (M1.1.1 pattern), `computePairs` → unpack `user_data` as `BodyId` → `gjkPair` per candidate; fat-AABB false positives (AABBs overlap, shapes don't) come back `.separated`. Full suite green at f32 and under `-Dphysics_f64=true` (local). `CLAUDE.md` updated per §3.4.

## Out of scope

- EPA, penetration depth, contact normal, manifold — M1.1.3. In particular, do NOT compute `depth = r_a + r_b − dist` or a contact normal for `.shallow`: report status + distance + closest points only; the contact interpretation is M1.1.3.
- Pair-type fast paths (sphere/sphere, sphere/box, capsule/capsule, box/box) — M1.1.4.
- Speculative-contact distance thresholds, collision margins beyond the core radius, box convex radius (box radius is 0 in M1.1.2).
- `collision_layer` consumption / `CollisionConfig` matrix wiring (standing M1.1.1 debt, later milestone).
- Stepping, scheduler, `PhysicsWorld`, `PhysicsModule`/`ModuleContext` instantiation (freeze is M1.1.15).
- Raycast/shapecast/point queries (M1.1.9–10).
- Shapes beyond the three delivered primitives (Plane, Mesh, Cylinder, ConvexHull… — M1.1.11/19+); `SupportShape.Core` must stay additively extensible (new union case), nothing more.
- Batched/SIMD pair processing (`engine-simd.md` §7.3 targets are later).
- `forge_2d` (M1.8).

## Specs to read first

1. `engine-physics-forge.md` — §1 in full (pipeline, narrowphase GJK + EPA + fast paths, §1.5 V1 scope and phasing).
2. `engine-phase-1-plan.md` — M1.1 table (the exact M1.1.1 → M1.1.2 → M1.1.3 → M1.1.4 boundaries).
3. `engine-phase-1-criteria.md` — C1.1 (paths, isolation, determinism bar).
4. `engine-directory-structure.md` — forge section (`pipeline/narrowphase.zig` placement).
5. `engine-tier-interfaces.md` — §1 (collision-facing surface `PhysicsModule` will eventually expose — design awareness only, do not instantiate).
6. `engine-zig-conventions.md` — in full (naming, doc comments, §13 test pins).
7. `engine-coordinate-system.md` — axes, units, quaternion conventions.

## Files to create or modify

- `src/modules/forge/forge_3d/pipeline/narrowphase.zig` — create — `SupportShape(T)`, support functions, simplex solver, GJK loop, `GjkResult(T)`
- `src/modules/forge/forge_3d/shape.zig` — modify — `supportShape()` conversion
- `src/modules/forge/forge_3d/body_manager.zig` — modify — `rotation(id) ?Quatr` safe getter; `gjkPair(store, a, b) ?GjkResult`
- `src/modules/forge/forge_3d/root.zig` — modify — narrowphase re-exports at `Real` + comptime test pins
- `src/modules/forge/forge_3d/tests/narrowphase_test.zig` — create — acceptance suite (see Tests)
- `src/modules/forge/forge_3d/tests/body_manager_test.zig` — modify — `rotation` getter round-trip + stale-handle coverage
- `briefs/M1.1.2-narrowphase-gjk.md` — create — this brief
- `CLAUDE.md` — modify — §3.4 update (E4)

## Acceptance criteria

### Tests

- `tests/narrowphase_test.zig` — `test "support functions return extremal points"` — per core: cardinal and oblique directions, known analytic extremal points; box per-component sign select; capsule endpoint selection; all through at least one non-trivial relative rotation.
- `tests/narrowphase_test.zig` — `test "support tie-breaks are fixed"` — direction orthogonal to the capsule axis and zero box-direction components resolve per the frozen conventions (see Notes), byte-stable across runs.
- `tests/narrowphase_test.zig` — `test "closest point on segment/triangle/tetrahedron regions"` — every Voronoi region per case (vertex/edge/face/interior), barycentrics sum to 1, reduced feature correct.
- `tests/narrowphase_test.zig` — `test "simplex solver handles degenerate inputs"` — duplicated vertices, collinear triangle, coplanar tetrahedron: no NaN, sane reduced feature.
- `tests/narrowphase_test.zig` — `test "gjk separated pairs report analytic distance"` — the 6 type combinations, rotated poses; distance within named tolerance of the analytic value; closest-point separation equals distance; each point on its core.
- `tests/narrowphase_test.zig` — `test "gjk shallow pairs are detected"` — the 6 type combinations, rotated poses; cores disjoint, inflated shapes overlapping (and the exact-touch boundary `dist == r_a + r_b` → `.shallow`, see Notes); distance + points valid. [Post-delivery: aligned on line 25's "all 6 combinations in all 3 regimes" — the ss+sb-only phrasing was a 25↔70 internal contradiction; Claude.ai-authorized frozen-section edit, see § Recorded deviations.]
- `tests/narrowphase_test.zig` — `test "gjk deep pairs enclose the origin"` — cores intersecting for all 6 combinations; `.deep`; terminal simplex encloses the origin (verified via the solver itself or containment check).
- `tests/narrowphase_test.zig` — `test "gjk is deterministic and iteration-bounded"` — fixed input set run twice → identical statuses/distances/points bit-for-bit; adversarial near-parallel box-face case terminates under the iteration constant.
- `tests/body_manager_test.zig` — `test "rotation getter round-trips and rejects stale handles"`.
- `tests/narrowphase_test.zig` — `test "broadphase pairs filtered by gjkPair"` — mixed scene through `BodyManager` + `Broadphase(Real)`; `computePairs` candidates classified by `gjkPair`; a constructed fat-AABB false positive comes back `.separated`; stale-handle pair → null.
- f64 instantiation: full suite green under `-Dphysics_f64=true` (local run; CI stays on the default matrix).

### Benchmarks

- None at this gate. Fast-path speedup comparisons are M1.1.4; C1.1 perf targets land with stepping (M1.1.5+).

### Observable behavior

- `zig build test` green (default) and green with `-Dphysics_f64=true` (local).
- The integration test demonstrably rejects a broadphase false positive (log or assertion output showing a candidate pair classified `.separated`).

### CI

- `zig build` clean, zero warnings, on the configured matrix
- `zig build test` green (debug + ReleaseSafe)
- `zig fmt --check` green
- `zig build lint` green
- `commit-msg` hook green on every commit of the branch

## Conventions

- **Branch:** `phase-1/forge/narrowphase-gjk`
- **Final tag:** `v0.11.2-narrowphase-gjk`
- **PR title:** `Phase 1 / Forge / Narrowphase: GJK convex intersection`
- **Commit convention:** Conventional Commits (cf. `engine-development-workflow.md §4.3`)
- **Merge strategy:** squash-and-merge (cf. `engine-development-workflow.md §4.6`)

## Notes

- **Dependency discipline.** `pipeline/narrowphase.zig` imports `foundation` (math) only — never `weld_forge`, never `body*.zig`, never `config.zig`, never `broadphase.zig`. The scalar arrives as the comptime parameter `T`; forge_3d instantiates at `config.Real`. The `Shape → SupportShape` conversion and the `BodyId`-level `gjkPair` adapter live on the forge_3d side (`shape.zig`, `body_manager.zig`), mirroring the broadphase `user_data` discipline.
- **Distance-based GJK, not boolean GJK.** The boolean variant (origin-enclosure only) uses a simplified simplex walk that never computes closest points — upgrading later means rewriting the core. EPA (M1.1.3) seeds from the terminal simplex, the manifold and the character controller (M1.1.12) need closest points/distance, so the simplex stores `(w, support_a, support_b)` triplets from day one.
- **Cores + inflation radius (Jolt convex-radius architecture).** GJK never sees a curved surface: sphere = point, capsule = segment, box = full box (radius 0). Touch test = `dist(cores) ≤ r_a + r_b`. Fast convergence, no simplex degeneracy near contact, and it makes EPA rarely needed in M1.1.3 (shallow contacts resolve from distance + closest points).
- **`SupportShape(T)` freeze:**
  ```zig
  pub fn SupportShape(comptime T: type) type {
      return struct {
          core: Core,
          /// Inflation radius around the core (0 for box in M1.1.2).
          radius: T,
          pub const Core = union(enum) {
              /// Sphere core: the local origin.
              point,
              /// Capsule core: Y-axis segment, ±half_height.
              segment: T,
              /// Box core: half-extents (full box, no shrink).
              box: math.Vec(3, T),
          };
      };
  }
  ```
  Future shapes (cylinder, convex hull — M1.1.19) are new `Core` cases, purely additive.
- **Simplex solver: Voronoi-region case analysis (Ericson), not Johnson, not signed-volumes.** Johnson's subalgorithm has known determinant degeneracies; signed-volumes (Montanari 2017) has a thin f32 validation corpus. Explicit closest-point-to-origin on point/segment/triangle/tetrahedron with feature reduction is the Jolt lineage: each case is a pure, unit-testable function. Region evaluation order is fixed (first matching region wins) — a determinism tie-break.
- **Computation in the frame of A.** B is pre-transformed relative to A once (`rot_rel = conj(rot_a)·rot_b`, `pos_rel = rotateByConj(rot_a, pos_b − pos_a)`); A's support runs untransformed. Better precision far from the world origin (avoids large-coordinate cancellation), half the per-iteration transforms. Frozen NOW because changing the computation frame after the M1.1.14 determinism freeze would break validated bit-exactness. Closest points are re-expressed in world space on output.
- **`GjkResult` regimes.** `separated` iff `dist² > (r_a + r_b)²` strictly — exact touch (`dist == r_a + r_b`) is `.shallow`, consistent with the face-inclusive `Aabb.overlaps` convention (touching counts). `.deep` carries the terminal simplex only; any depth/normal computation is M1.1.3.
- **Determinism by construction (anticipates M1.1.14).** No hash containers, no trigonometry (dot/cross only), bounded iterations (`max_gjk_iterations = 32`, named constant), named relative tolerance on the squared-distance progress test, fixed support tie-breaks: capsule direction with `dir.y == 0` → the `+Y` endpoint; box direction component `== 0` → the `+half_extent` corner component. Termination is progress-based (distance no longer decreases beyond tolerance, or the new support vertex does not move the simplex closer), never an exact float equality.
- **Trap: normalization of the search direction.** Do not normalize `d` between iterations — GJK only needs directions, and normalizing adds a sqrt per iteration plus a divide-by-near-zero hazard when the origin is close to the simplex. Work with squared distances; take a single sqrt at the end for the reported distance.
- **Trap: initial direction.** Seed `d` from the relative position (`pos_rel`), falling back to a fixed axis if degenerate (zero vector) — fixed fallback = determinism.
- **Trap: `Quatr` rotate-by-conjugate.** `foundation` exposes `Quat.conjugate()` and `Quat.rotateVec3()`; inverse-rotating a direction is `q.conjugate().rotateVec3(v)` (unit quaternions). Do not call `inverse()` on the hot path (it divides by the squared norm).

---

# LIVING SECTION

*Maintained by Claude Code during the milestone. The log is not a marketing report: it serves review and post-mortem debugging.*

## Specs read

*Check before writing any production code. Confirms the spec was ingested in full, not merely skimmed.*

- [x] `engine-physics-forge.md` (§1) — read 2026-07-17 21:02
- [x] `engine-phase-1-plan.md` (M1.1) — read 2026-07-17 21:02
- [x] `engine-phase-1-criteria.md` (C1.1) — read 2026-07-17 21:02
- [x] `engine-directory-structure.md` (forge section) — read 2026-07-17 21:02
- [x] `engine-tier-interfaces.md` (§1) — read 2026-07-17 21:02
- [x] `engine-zig-conventions.md` — read 2026-07-17 21:02
- [x] `engine-coordinate-system.md` — read 2026-07-17 21:02

## Execution log

- 2026-07-17 21:02 — Branch `phase-1/forge/narrowphase-gjk` off `main` (in sync with `origin/main`). Brief copied verbatim; all 7 specs read in full; Status PLANNED → ACTIVE.
- 2026-07-17 21:10 — E1: created `pipeline/narrowphase.zig` (`foundation` math only, generic over `T`). `SupportShape(T)` — frozen `Core` union (point / segment: T / box: `math.Vec(3,T)`) + inflation `radius` — with `support(dir)` on the core (fixed tie-breaks: `dir.y == 0` → `+Y` endpoint; box component `== 0` → `+half_extent`). `RelativePose(T)` — frame-of-A precompute `rot_rel = conj(rot_a)·rot_b`, `pos_rel = conj(rot_a)·(pos_b − pos_a)` (uses `conjugate()`, never `inverse()`) + `supportB` mapping B's core support into A's frame.
- 2026-07-17 21:10 — E1: `tests/narrowphase_test.zig` — `support functions return extremal points` (point/segment/box on cardinal + oblique dirs, through two non-trivial relative rotations with analytic values) and `support tie-breaks are fixed` (exact `eql`, byte-stable). Keyed to `config.Real`. `root.zig` gains the narrowphase import + comptime test pins NOW (§13 lazy-analysis guard — an unpinned module's tests are silently skipped); the `Real` public re-exports remain E4 per the brief (in-scope early touch of a listed file, not a frozen-section deviation). Green: `zig build`, `test-forge-3d` debug+ReleaseSafe (31/31, the 2 new tests confirmed collected), `lint`, `fmt`, and `-Dphysics_f64=true` debug+ReleaseSafe.
- 2026-07-17 21:31 — E1 review STOP (verdict Claude.ai): the "at least one non-trivial relative rotation" criterion was unmet for the SEGMENT core — `rp_rot` exercised the box, `rp_pose` the point (with `rot_rel = identity`). Added a segment-under-rotation case (`rp_seg`, B rotated +90° about +Z; `supportB(seg, +X) = (0.9,0,0)`, `−X = (−0.9,0,0)` — analytic) to `support functions return extremal points`, exercising the real capsule GJK path (inverse-rotate dir → sign-of-local-Y endpoint → map back). Re-validated green: `test-forge-3d` debug+ReleaseSafe (31/31), `lint`, `fmt`, and `-Dphysics_f64=true` debug+ReleaseSafe. **E1 verdict: GO.**
- 2026-07-17 21:50 — E2: `Simplex(T)` added to `narrowphase.zig` (388 lines at E2, under the 500 guideline). `Vertex` triplet `(w, support_a, support_b)`; `Result` (closest + count + surviving `indices[4]` + `bary[4]`); `closestOriginPoint`/`Segment`/`Triangle`/`Tetra` — Ericson RTCD §5.1 Voronoi-region case analyses specialized to P = origin, fixed region order (first match / strict-`<` min wins → deterministic). Every division guarded: segment `|ab|²<=0`; triangle edges via squared-edge-length denoms (`>0`), face `!(denom_sum>0)` → `minOverEdges3`; tetra interior `!(|det|>0)` → `minOverFaces4`. Interior barycentric via signed-volume ratios. Tests: `closest point on segment/triangle/tetrahedron regions` (segment A/B/edge; triangle 3 vertex + 3 edge + face; tetra vertex/edge/face/interior — analytic closest + exact feature set + bary-sum-1 + 1/4 interior weights), `simplex solver handles degenerate inputs` (dup segment/triangle vertex, collinear triangle, coplanar tetra — finite, ≤-expected feature, bary-sum-1), `simplex feature reconstructs closest from barycentrics` (uses `Vertex`, Σ bary·w = closest). Green: `test-forge-3d` debug+ReleaseSafe (34/34), `lint`, `fmt`, `-Dphysics_f64=true` debug+ReleaseSafe, full `zig build`. Note: `minOverEdges3` is a float safety net not hit by exact-arithmetic tests (an exactly-collinear triangle routes through the edge branch, which IS exercised); it is analyzed/compiled and built on the tested `closestOriginSegment` primitive. `minOverFaces4` IS exercised (coplanar-tetra test). **E2 verdict: GO** (two non-blocking observations noted for the E4 review: `minOverEdges3` unexercised — a legitimate float safety net; `pointOutsidePlane` with a coplanar ref → interior branch → `minOverFaces4`, correct by construction).
- 2026-07-17 23:40 — E3: `gjk` loop + `GjkResult(T)` added to `narrowphase.zig` (**576 lines** — over the 500 Review guideline; conscious brief-driven overage, the brief mandates the whole GJK stack — support shapes + simplex solver + loop + result — in this single file, mirroring M1.1.1 `broadphase.zig` at 686). `pub const max_gjk_iterations = 32`; named `rel_tolerance` (relative squared-distance progress test) + `deep_eps_sq` (absolute A-frame origin-reached threshold), both T-dependent (f32/f64). Loop: seed dir from `pos_rel` (fixed `unit_x` fallback if degenerate), Minkowski support `support_A(d) − support_B(−d)`, E2 Voronoi reduction carrying barycentrics, **never normalizes the search direction** (squared distances, one `@sqrt`). Deep iff the terminal simplex encloses the origin (tetra interior `res.count==4` OR `closest·closest <= deep_eps_sq`); otherwise `.separated` iff `dist² > (r_a+r_b)²` strictly else `.shallow`. Closest points reconstructed from barycentrics in A's frame then mapped to world; distance is the rigid-invariant `|closest_a − closest_b|`. `.deep` carries the terminal simplex (EPA seed), distance 0, no depth/normal. No `unreachable` (the simplex dispatcher's `else` arm is the tetrahedron). Tests: `gjk separated pairs report analytic distance` (6 combos ss/sb/sc/bb/bc/cc, analytic distances 3/4/5/3/4/5, canonical AND under an oblique global rigid transform — rotation invariance; separation == distance; each closest point on its core), `gjk shallow pairs are detected` (overlap + exact-touch `dist == r_a+r_b == 5` → shallow), `gjk deep pairs enclose the origin` (6 combos incl. coincident points, point-in-box, point-on-segment, box overlap, segment-through-box, crossing segments + a rotated bb; origin-enclosure re-verified via the solver), `gjk is deterministic and iteration-bounded` (bit-identical two runs; parallel-box-faces adversarial → distance 1, terminates; tiny-rotation near-parallel → sane). Green: `test-forge-3d` debug+ReleaseSafe (38/38), `lint`, `fmt`, `-Dphysics_f64=true` debug+ReleaseSafe, full `zig build`. **E3 verdict: GO** (two non-blocking review notes carried forward: `deep_eps_sq` absolute-threshold scale sensitivity, and the 576-line file overage). Operational note: a stale empty `.git/index.lock` (left by a VS Code git refresh, no `git` CLI running) blocked the first E3 commit — removed, commits replayed, no diff impact.
- 2026-07-18 — E4 (integration + close): `shape.zig` `supportShape(Shape) SupportShape(Real)` (sphere→point+r / capsule→segment(half_height)+r / box→box(half_extents)+0; `else => unreachable` per the ShapeStore invariant, mirror of `worldAabb`); `body_manager.zig` `rotation(id) ?Quatr` getter (symmetric to `position`) + `gjkPair(store, a, b) ?GjkResult` (resolves both bodies' pose + support shape, null on any stale handle); `root.zig` re-exports `SupportShape`/`RelativePose`/`Simplex`/`GjkResult`/`max_gjk_iterations` at `Real` + a `Real`-bound `gjk` wrapper, header refreshed. Tests: `body_manager_test` `rotation getter round-trips and rejects stale handles`; `narrowphase_test` `broadphase pairs filtered by gjkPair` (BodyManager + `Broadphase(Real)`, `computePairs` → `gjkPair` per candidate; a constructed fat-AABB false positive → `.separated`, a genuine overlap → `.deep`, stale pair → null). `CLAUDE.md` §3.4 updated (current-state, Tags row `v0.11.2-narrowphase-gjk`, M1.1.2 open-decision, Last updated). Audits: FR tool-words on the branch diff = clean; drift grep (`narrowphase`/`gjk`/new symbols) = no stale refs, confined to forge_3d. Green: `test-forge-3d` + full `zig build test` debug+ReleaseSafe (40/40 forge; full suite exit 0), `lint`, `fmt` (CI paths), `-Dphysics_f64=true` debug+ReleaseSafe, `zig build`. **E4 verdict: STOP** — post-delivery review (Claude.ai + a second Codex opinion) found the `.deep`/`.shallow`/`.separated` classification rested on ABSOLUTE distance thresholds and an EXACT tetra-degeneracy test rather than robust geometric proofs. Milestone reopened for a one-pass correction cycle (below).
- 2026-07-18 — Correction cycle (bug fixes Fix 1–2; Fix 3–5 in § Recorded deviations). **Fix 1** (`gjk`): anti-cycling guard — after the support `w` and before adding it, `break` if `w` duplicates a simplex vertex within `dup_rel_sq` × simplex-magnitude² (`duplicateSupport`); kills at the source the degenerate tetrahedra the float progress test let through. **Fix 2** (`closestOriginTetra`): degeneracy is now RELATIVE — `|bary_det|² ≤ deg_rel² · (max squared edge)³` (`maxEdgeSq`) instead of the exact `!(|bary_det| > 0)`, so a near-flat tetra falls back to `minOverFaces4` (count ≤ 3, never a spurious `.deep`). Green after Fix 1–4: `test-forge-3d` 40/40. **Fix 5** tests added (`gjk near-contact pairs are not deep`; `gjk shallow pairs are detected` widened to 6 combos). Full re-validation: `test-forge-3d` debug+ReleaseSafe (41/41), full `zig build test` (exit 0), `lint`, `fmt`, `-Dphysics_f64=true` debug+ReleaseSafe, `zig build`. `narrowphase.zig` now **639 lines**.
- 2026-07-18 — Second correction cycle (targeted **Fix 3b only**, Guy-directed). The first cycle's Fix 3 eliminated the O(1)-scale false `.deep` (sweep 4000 → 0, verified) but its new deep early-out stayed RELATIVE to the Minkowski-vertex magnitude (`closest·closest ≤ rel_deep²·maxVertexMag²`), which reproduced the same class of bug at scale (509/2000 false `.deep` on boxes of half-extents 50–500 at ~1 cm contact — the character-on-a-large-ground case). **Fix 3b** (see § Recorded deviations): `.deep` now rests on geometric ENCLOSURE (`res.count == 4`, a non-degenerate origin-containing tetra, no distance threshold) + a numerical-NOISE floor for the degenerate-Minkowski cases (`degenerateOriginReached`: `closest·closest ≤ mach_eps²·maxSupportMagSq`, `mach_eps` = machine-epsilon-calibrated, scaled by the ABSOLUTE support magnitude, not shape size). `rel_deep` + `originEnclosed` removed; `maxVertexMagSq` retained for the unchanged anti-cycling test; new `maxSupportMagSq`. Verified via an O(1)+large-scale sweep probe (0 false `.deep` at O(1) AND half-extents 50–500 ~1 cm; deep preserved incl. large box overlap) at f32 and f64, probe then removed. Tests: `gjk near-contact pairs are not deep` gained a large-oriented-box near-contact case (→ `.separated`); `gjk deep pairs enclose the origin` gained a half-extents-50 franc-overlap case (→ `.deep`). Fixes 1/2/4/5 untouched. Re-validated green: `test-forge-3d` debug+ReleaseSafe f32 (41/41) + f64, full `zig build test` (exit 0), `lint`, `fmt`, `zig build`. `narrowphase.zig` now **674 lines**.
- 2026-07-18 — Third correction cycle (two threshold recalibrations **P1, P2**, from a Codex review, Guy-directed). Guiding principle enforced: no `.deep`/`.shallow`/`.separated` decision may depend on a GEOMETRIC quantity (`r_sum`, shape size, `w`-vertex magnitude); every remaining threshold is `k·floatEps(T)·coordScale` (coordScale = absolute support magnitude), absorbing only float rounding noise. **P1** (contact margin): the old `.separated iff dist² > (r_sum·(1+contact_rel))²` grew the margin as `contact_rel·r_sum` (0.1 m at `r_sum`=1000 → two r=500 spheres 5 cm apart mis-classified `.shallow`, a collision margin beyond the core radius, out of scope §32). Now `.separated iff dist − r_sum > conv_k·floatEps(T)·coord_scale` (`coord_scale = @sqrt(maxSupportMagSq)`, additive on the already-computed `dist`); `contact_rel` removed. **P2** (noise floor): `mach_eps = 1e-6` (f32) was 8.4× the f32 machine epsilon, so `mach_eps²·maxSupportMagSq` was a visible geometric threshold (~8.7e-5 in distance at half-extent 50 → a point 2e-5 outside a box-50 mis-classified `.deep`). Now `mach_eps = noise_k·std.math.floatEps(T)`, `noise_k`≈2 (`degenerateOriginReached` form unchanged). Both `noise_k`/`conv_k` named ≈2 via `std.math.floatEps`, no magic literals. Verified by a diagnostic sweep probe at f32 AND f64 (r=500 gap 0.05 → `.separated`, tangent → `.shallow`; box-50 point 2e-5 → `.separated`, 1e-5 → `.deep`; all degenerate deeps incl. half-height-50 crossed segments + box-50 overlap stay `.deep`), probe then removed. Fixes 1/2/3b/5 untouched. Adversarial review (4-lens workflow: geometric-dependency, scale-sweep, test-coverage, calibration) run on the committed diff. Re-validated green: `test-forge-3d` debug+ReleaseSafe f32 (42/42) + f64, full `zig build test` (exit 0), `lint`, `fmt`, `zig build`. `narrowphase.zig` now **687 lines**.

## Recorded deviations

- 2026-07-18 (third post-delivery correction cycle, threshold recalibrations **P1 + P2** from a Codex review, Guy-directed — one pass on the existing diff, no new numbered gate). Refinements of two already-tracked realizations: **P1 refines Fix 4** (the contact margin), **P2 refines Fix 3b** (the noise floor). **Guiding principle** (now enforced and documented in-code): no `.deep`/`.shallow`/`.separated` decision may depend on a GEOMETRIC quantity — `r_sum`, shape size, or the magnitude of the `w`-simplex vertices; `.deep` is geometric enclosure (`res.count == 4`); every remaining threshold is of the form `k · floatEps(T) · coordScale`, where `coordScale` is the cores' coordinate scale (the ABSOLUTE support magnitude, `@sqrt(maxSupportMagSq)`), and absorbs ONLY float rounding noise. **P1 (contact margin, refines Fix 4):** Fix 4's `.separated iff dist² > (r_sum·(1 + contact_rel))²` made the margin grow as `contact_rel·r_sum` — at `r_sum` = 1000 the slack is 0.1 m, so two r=500 spheres genuinely 5 cm apart (centres at 1000.05) were mis-classified `.shallow`. That is precisely a *collision margin beyond the core radius*, explicitly out of scope (Out of scope, and brief §32 = `engine-physics-forge.md §1.5`). The margin must absorb the GJK *convergence* error on `dist` (a function of the coordinate scale, not the radius). Fix: remove `contact_rel`; `.separated iff dist − r_sum > contact_margin`, `contact_margin = conv_k · std.math.floatEps(T) · coord_scale`, `coord_scale = @sqrt(maxSupportMagSq(terminal simplex))`, an additive comparison on the already-computed `dist`; `conv_k` = named constant ≈ 2. **P2 (noise floor, refines Fix 3b):** Fix 3b's `mach_eps = 1e-6` (f32) is 8.4× the f32 machine epsilon (1.19e-7), so the floor `mach_eps² · maxSupportMagSq` was itself a *visible geometric threshold* (~8.7e-5 in distance for a box of half-extent 50) — a point 2e-5 outside an oriented box-50 was mis-classified `.deep`. Fix: recalibrate to the real rounding noise with a MINIMAL accumulation margin — `mach_eps = noise_k · std.math.floatEps(T)`, `noise_k` = named constant ≈ 2 (f32 ≈ 2.4e-7, f64 ≈ 4.4e-16); `degenerateOriginReached` unchanged in form (`closest·closest ≤ mach_eps² · maxSupportMagSq`), only the constant changes. Both constants expressed via `std.math.floatEps(T)`, never an f32/f64 magic literal. **Fundamental-limit note:** under f32 rounding at large scale the degenerate deep/shallow boundary is bounded by f32 precision (≈ `floatEps·coordScale` in distance — ~1.5e-5 at half-extent 50); the floor tracks that rounding noise, it is not a geometric margin, and the boundary is pushed back ~9 orders in f64. Verified by a diagnostic sweep probe (measuring status + distance across gap sweeps) at f32 AND f64, then removed. Non-regression tests: `gjk contact margin is absolute not radius-proportional` (r=500 gap 0.05 → `.separated` — fails under the old margin; exact tangency → `.shallow`; rotated variant); `gjk near-contact pairs are not deep` gains a noise-level case (box-50 point 2e-5 → `.separated`); `gjk deep pairs enclose the origin` gains a half-height-50 crossed-segment degenerate deep (rests on the noise floor, most sensitive to the recalibration). Fixes 1, 2, 3b, 5 left unchanged. An adversarial 4-lens review workflow (geometric-dependency / scale-sweep / test-coverage / calibration, each finding independently verified) was run on the committed diff. Guy-directed; PR/merge/tag unchanged.
- 2026-07-18 (second post-delivery correction cycle, targeted **Fix 3 only**, Guy-directed — one pass on the existing diff, no new numbered gate). **Fix 3b (deep by geometric enclosure + a numerical-NOISE floor, not a vertex-relative threshold):** the first cycle's Fix 3 replaced the absolute `deep_eps_sq` with a threshold RELATIVE to the Minkowski-vertex magnitude (`closest·closest ≤ rel_deep²·maxVertexMag²`). That still reproduced the original bug class for LARGE shapes — an origin near a large *distant* face has a large vertex magnitude, so the vertex-relative threshold grew with shape size and mistook proximity for enclosure (reproduced numerically: 509/2000 false `.deep` on boxes of half-extents 50–500 at ~1 cm from contact — the character-on-a-large-ground case, phantom penetration). **Root cause:** `.deep` ("cores intersect") is an ABSOLUTE geometric property, not one relative to shape size; the only legitimate role of a distance threshold here is to absorb the floating-point NOISE on intrinsically-degenerate Minkowski configs (coincident/collinear cores that cannot be tetrahedralized), never to provide a geometric tolerance. **Fix 3b:** (1) franc `.deep` = `res.count == 4` — a non-degenerate origin-containing tetrahedron (guaranteed by Fix 2's relative degeneracy guard), with NO distance threshold on that path; (2) the distance early-out (renamed `originEnclosed` → `degenerateOriginReached`) becomes a numerical-noise test — `closest·closest ≤ mach_eps² · coordScale²`, where `coordScale²` = max over the simplex vertices of `max(support_a·support_a, support_b·support_b)` (the ABSOLUTE support magnitude = the coordinate scale of the `w = support_a − support_b` subtraction, hence the scale of its rounding error), and `mach_eps` is a named T-dependent machine-epsilon constant (1e-6 f32 / 1e-13 f64) carrying an accumulation margin — NOT a geometric tolerance; (3) `rel_deep` removed as the cause of the residual; `maxVertexMagSq` kept for the (unchanged) anti-cycling duplicate test; new `maxSupportMagSq` for the noise test. Verified by an O(1)+large-scale sweep probe (0 false `.deep` at O(1) AND at half-extents 50–500 ~1 cm contact; deep preserved — coincident, point-in-box, box overlap, AND large box overlap) at f32 and f64, probe removed after. Non-regression tests: `gjk near-contact pairs are not deep` gains a large oriented-box near-contact case (half-extents 50, ~1 cm → `.separated`); `gjk deep pairs enclose the origin` gains a half-extents-50 franc-overlap case (→ `.deep`, proving the hardening does not weaken enclosure at scale). Fixes 1, 2, 4, 5 left unchanged. Guy-directed; PR/merge/tag unchanged.
- 2026-07-18 (post-delivery correction cycle, Claude.ai + Codex review, Guy-validated — one pass on the existing diff, no new numbered gate). **Fix 3 (deep by enclosure + relative `deep_eps`) [SUPERSEDED by Fix 3b above for the deep-threshold half]:** the absolute A-frame origin-reached threshold (`deep_eps_sq` 1e-8/1e-12) is removed; `.deep` is now `res.count == 4` (a non-degenerate tetra enclosing the origin, robust via Fix 2) OR `closest·closest ≤ rel_deep² · maxVertexMag²` — relative to the current simplex magnitude, so proximity is no longer mistaken for intersection (a `5e-5` point-point gap is no longer a false `.deep`). When all simplex vertices sit at the origin the threshold is 0, so `.deep` then needs `closest` exactly at the origin. **Fix 4 (shallow/separated contact margin):** GJK distance is approximate, so `.separated` is declared only when `dist² > (r_sum·(1 + contact_rel))²` (named T-dependent `contact_rel`); a tangency can no longer tip one ULP into `.separated`, honoring the frozen "touch = shallow" convention. A realization honoring frozen intent (Guy-validated), like the `insert(gpa, …)` unmanaged-first precedent. **Fix 5 + 25↔70 resolution:** the Scope (line 25, "all 6 combinations in all 3 regimes") and the detailed shallow criterion (line 70, ss+sb-only, no rotation) contradicted; Guy validated aligning line 70 on line 25 (the Scope prevails). Line 70 edited (frozen-section, Claude.ai-authorized); the shallow test now covers all 6 combos under a non-trivial global rotation, plus a `gjk near-contact pairs are not deep` non-regression test (oriented box at ~1 cm → not `.deep`; two point cores at `5e-5`, `r_sum ≥ 1` → `.shallow`). The bb-shallow combo uses a rounded box (`roundedBoxShape`, box core + convex radius) — forge_3d's `supportShape` yields radius-0 boxes that can only be separated or deep, so this exercises the narrowphase's radius-agnostic shallow path for the box core.
- (Earlier, E1–E4: none — the FROZEN SECTION was implemented verbatim. In-scope realizations, in the Execution log: `gjk` takes an explicit `comptime T` + full world poses while `BodyManager.gjkPair(store, a, b)` is the frozen `BodyId` adapter; `root.zig` re-exports the narrowphase surface at `Real`; the `root.zig` test-pin landed at E1 per §13 while the `Real` re-exports landed at E4.)

## Blockers encountered

- None. (Operational, not a design blocker: a stale empty `.git/index.lock` left by a VS Code git refresh blocked the first E3 commit; no `git` CLI was running, so it was removed and the commits replayed — Execution log 2026-07-17.)

## Closing notes

- **What worked:** The gate-by-gate E1→E4 decomposition with a Claude.ai review between each gate — the E1 STOP (segment core never exercised under a non-trivial relative rotation) was caught before it compounded. The frozen frame-of-A + cores-plus-inflation model produced clean, deterministic GJK: every analytic distance matched to `1e-3`, and the E2 solver's guarded divisions made all degenerate inputs NaN-safe. The broadphase→narrowphase integration demonstrably rejects a constructed fat-AABB false positive (`.separated`) while still reporting a genuine overlap as `.deep`. The post-delivery review round (Claude.ai + Codex) is what caught the absolute-threshold classification fragility before merge.
- **What deviated from the original spec:** Three post-delivery correction cycles — see Recorded deviations. First cycle: Fix 3 (relative deep + deep-by-enclosure), Fix 4 (contact margin), Fix 5 + the 25↔70 line-70 frozen-section alignment. Second cycle (Fix 3 only): **Fix 3b** — the deep threshold was still relative to the Minkowski-vertex magnitude and reproduced the bug at large scale; `.deep` moved to geometric enclosure + a numerical-noise floor. Third cycle (**P1 + P2**, Codex review): the two remaining thresholds still carried a geometric dependency — P1's contact margin scaled with `r_sum` (a collision margin beyond the core radius, out of scope), P2's noise floor was calibrated at 8× machine epsilon (a visible geometric threshold). Both are now pure float-noise thresholds `k·floatEps(T)·coordScale`. All Guy-directed/validated, one pass on the existing diff each.
- **What to flag explicitly in review:** (1) **No classification decision depends on a geometric quantity.** `.deep` is geometric ENCLOSURE — a non-degenerate origin-containing tetrahedron (`res.count == 4`, degeneracy guarded relative to the tetra's own edges via `deg_rel`), NO distance threshold. The two remaining classification thresholds are pure float-noise, both `k·floatEps(T)·coordScale` with `coordScale = @sqrt(maxSupportMagSq)` (absolute support magnitude): `degenerateOriginReached` (the deep noise floor, `mach_eps = noise_k·floatEps`) and the shallow/separated contact margin (`conv_k·floatEps·coord_scale`, additive on `dist`). `noise_k`/`conv_k` ≈ 2 (a couple of ULPs). No `r_sum`-proportional margin and no shape-size- or `w`-magnitude-relative threshold survive (P1 removed `contact_rel`, P2 removed the 8×-eps `mach_eps` literal). The search-path tolerances `rel_tolerance`/`dup_rel_sq` are not classification decisions. (2) **f32 fundamental limit:** at large coordinate scale the degenerate deep/shallow boundary is bounded by f32 precision (≈ `floatEps·coordScale`, ~1.5e-5 at half-extent 50); the floor tracks that rounding noise (it is not a geometric margin) and the boundary is ~9 orders tighter in f64. (3) `narrowphase.zig` is 687 lines (> 500 Review guideline) — brief-mandated single-file GJK stack (mirror of `broadphase.zig` at 686). (4) `minOverEdges3` (degenerate-triangle fallback) is a float safety net not runtime-exercised — an exactly-collinear triangle routes through the edge branch (which IS exercised); the helper is analysed and built on the tested `closestOriginSegment`. (5) `.deep` can be reported with a < 4-vertex terminal simplex for low-dimensional Minkowski configs (coincident points, point-on-segment, crossing segments); EPA (M1.1.3) must expand such seeds to a tetrahedron. (6) The bb-shallow test uses a rounded box (box core + convex radius) — narrowphase-only, since forge_3d boxes are radius-0.
- **Final measurements:** 42 forge_3d tests green in debug + ReleaseSafe, at f32 AND `-Dphysics_f64=true` (local); full `zig build test` green (macOS cosmetic runner noise only, exit 0). `narrowphase.zig` 687 lines. GJK converges within the 32-iteration bound on every tested pair, including exactly-parallel and near-parallel box faces. No benchmarks at this gate (C1.1 perf lands with stepping M1.1.5+; fast-path speedups are M1.1.4).
- **Residual risks / tech debt left intentionally:** (a) `minOverEdges3` unexercised safety net (flag 4). (b) Low-dim `.deep` seeds need EPA tetra-expansion (flag 5) — an M1.1.3 input contract, not debt here. (c) No EPA/manifold (M1.1.3), no fast paths (M1.1.4), no depth/normal — out of scope by design. (d) The classification scale-sensitivity risk is fully RESOLVED across three cycles: the E3 absolute-`deep_eps` → Fix 3 (vertex-relative) → Fix 3b (geometric enclosure + noise floor) for the deep threshold; then P1 (absolute contact margin, not `r_sum`-proportional) + P2 (noise floor recalibrated to ~machine epsilon, not 8×). No classification threshold now carries a geometric dependency; all are `k·floatEps(T)·coordScale`. The one irreducible limit is f32 precision at large coordinate scale (flag 2) — a fundamental float limit, not debt, and ~9 orders looser than f64.
