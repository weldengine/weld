component Acc {
  out: int = 0
  out2: int = 0
}

rule run(entity: Entity)
  when entity has Acc
{
  let who = "weld"
  let n = 7
  let msg = "hi {who}, n={n + 1}!"
  entity.get_mut(Acc).out = msg.len()
  entity.get_mut(Acc).out2 = "{1.5}|{true}|\{x}".len()
}
