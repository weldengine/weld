component Counter {
  value: int = 0
}

resource Trigger {
  armed: bool = false
}

rule maybe_increment(entity: Entity)
  when entity has Counter and resource Trigger changed
{
  entity.get_mut(Counter).value += 1
}
