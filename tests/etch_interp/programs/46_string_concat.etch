component Acc {
  out: int = 0
  out2: int = 0
}

rule run(entity: Entity)
  when entity has Acc
{
  let prefix = "ab"
  entity.get_mut(Acc).out = ("ab" + "cd").len()
  entity.get_mut(Acc).out2 = (prefix + "cd" + "e").len()
}
