// E1582 CooldownInvalid — a negative cooldown literal is rejected (the
// positive-Duration rule; a DURATION_LIT lexeme is unsigned, so the
// unary-minus check covers numeric and duration forms alike).

ability Dash {
  cooldown: -1.5
}
