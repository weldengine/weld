// E0001 ParseError — `dialogue` is out of scope until its E4 slice lands;
// the lexer flags it as `error_unknown_keyword`, which the parser raises
// with an "UnsupportedConstructInS3" message. (Replaces the former `quest`
// fixture: `quest` graduated to a real keyword with its M0.8 E4 slice.)

dialogue MerchantGreeting {
    speaker "merchant" {
        line: "Welcome!"
    }
}

component Filler {
    v: int = 0
}
