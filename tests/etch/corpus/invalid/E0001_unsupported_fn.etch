// E0001 ParseError — `fn` top-level is out of S3 scope and the lexer
// flags it as `error_unknown_keyword`, which the parser raises with a
// "UnsupportedConstructInS3" message.

fn add(a: int, b: int) -> int {
    a + b
}

component Filler {
    v: int = 0
}
